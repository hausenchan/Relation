# 招聘雷达需求梳理

> 更新时间：2026-05-05
> 模块定位：监控竞品公司在 Boss 直聘的候选人流向，辅助判断对手招聘动向与人才市场信号。
> 代码位置：`server/boss-watcher/` + `client/src/pages/RecruitRadar*.js`

---

## 一、产品定位

**一句话**：盯着 Boss 直聘上那几家重点公司的候选人池每天变化，有人进、有人走、状态变了，就告诉我。

**用途**：
- 判断对手在挖谁、在"收简历"的岗位有哪些
- 发现自家在招岗位下，竞品员工投递或被推荐的信号（挖人机会）
- 作为人才情报的自动化入口，减少"每天登录 Boss 翻一遍"的人力

**不是什么**：
- 不是 ATS（不替代招聘系统）
- 不是完整的人才库（人才沉淀在 persons 表，这里只是发现层）
- 不是群发工具（所有推送只给内部少数人）

**敏感度**：高。涉及"挖人意图"，推送内容与事件数据都必须限内部人员可见，前端水印 + 数据加密（见 `敏感字段加密清单模板.md`）。

---

## 二、当前实现状态

### 2.1 已完成

**后端（`server/boss-watcher/`）**
- `auth.js` — Boss Cookie 存取（AES-256-CBC 加密到 `session.json`），Cookie 有效性校验
- `sync-positions.js` — 拉取"我在招"的岗位列表
- `fetch-candidates.js` — 按岗位拉取推荐候选人，过滤出目标公司命中的人，写入快照
- `diff.js` — 今日快照 vs 最近一次历史快照，产出 `new` / `gone` / `status_change` 三类事件
- `dingtalk.js` — 钉钉企业应用工作通知（markdown）推送，accessToken 缓存
- `scheduler.js` — cron `9:00` / `14:00` 定时抓取；异步执行 + 任务日志 + 状态持久化；进程重启后把 running 标记为 failed
- `routes.js` — 目标公司 CRUD / 事件列表（公司/岗位/类型筛选）/ 手动触发（异步，返回 jobId）/ 任务状态查询 / 统计 `/stats` / 钉钉测试
- `db.js` — 5 张表：`boss_watch_target` / `boss_watch_position` / `boss_watch_snapshot` / `boss_watch_event` / `boss_watch_job_log`
- 60 天外的快照、事件、任务日志每日凌晨 3:30 清理

**前端**
- `RecruitRadar.js` — 概览卡（监控公司/岗位/今日/累计事件）+ 事件列表 + 筛选（公司/岗位/类型）+ 运行中任务 Alert（3 秒轮询）+ 手动触发 + 状态变化 prev→new 展示 + PC/移动双布局 + Watermark
- `RecruitRadarConfig.js` — Boss Cookie 配置、钉钉配置 + 测试、岗位同步、目标公司管理

### 2.2 数据流

```
定时 / 手动
   ↓
createJob → runJob（异步）
   ↓
fetchAndFilter   每个在招岗位 → 拉推荐候选人 → 命中目标公司的写快照
   ↓
runDiff          today snapshot vs 最近一次历史 snapshot → 生成 event
   ↓
saveEvents       写入 boss_watch_event
   ↓
sendEventNotice  有事件则推钉钉工作通知
   ↓
updateJob        任务日志打完（success / failed + error）
```

---

## 三、已知问题与风险

### 3.1 功能性缺口
1. **事件是死的**：用户看完一条事件，没法标记"已跟进 / 忽略 / 已联系"，下次刷新还是同样的噪音。
2. **候选人无画像**：事件只存了 name / title / city / status，缺学历、工龄、期望薪资、上一家公司等。需要点进候选人详情看全貌。
3. **无 Boss 深链**：事件里没有 Boss 候选人主页链接，看到感兴趣的人还得自己回 Boss 搜。
4. **不入库**：发现一个重点候选人，没法一键"加入人才库"（对接 persons 表），线索流失。
5. **去重粗糙**：同一候选人如果在多个岗位匹配，会产生多条事件；短时间内 status 反复跳会刷屏。
6. **抓取频率/范围无配置**：9:00 / 14:00 写死；pageSize、并发、城市过滤都是硬编码。
7. **目标公司 ID 获取体验差**：用户要自己去 Boss 找 `encXXX`，理想是给公司名搜索。
8. **账号失效无主动预警**：Cookie 过期只能等下一次抓取失败，应在失效瞬间钉钉告警。
9. **无手动重跑指定日期**：diff 依赖历史快照，中间漏抓一天后目前只能等下次自然跑。

### 3.2 安全与合规
1. **Cookie 加密密钥硬编码在 `auth.js`**（`ENCRYPT_KEY = 'boss-watcher-key-2026-relation!'`），跟 `.db` 一起拖走即可解密。应收敛到统一的 `master.key`（参考 `敏感字段加密清单模板.md`）。
2. **事件表中 `candidate_name` / `candidate_title` / `company_name` 明文**，按加密清单应与 persons 同级加密。
3. **快照 `candidates_json` 大段明文**，同上。
4. **日志有可能打印 Cookie / 候选人名**（`console.log` 多处），需检查并脱敏。
5. **钉钉消息内容含明文候选人名**，属于"通道出站即明文"，需评估是否接受。
6. **访问控制缺失**：前端 Watermark 存在，但后端接口未见 role 校验，任何登录用户都能看所有事件，应限特定角色（老板 / 招聘负责人）。

### 3.3 健壮性
1. **Boss 接口字段兼容靠 `||` 堆砌**（`item.encryptGeekId || item.geekId || item.uid`），风控或改版时没有明确告警，只会静默返回空。
2. **抓取完全串行**：N 个岗位 × 每岗位翻页，高峰期可能跑十几分钟，长时间占用 running。
3. **无限流与重试**：Boss 风控返回非 0 时 `break`，但不区分"空结果"和"被风控"，下一轮会继续撞。
4. **diff 基线用"最近一次历史快照"**，如果上一次是 10 天前，会把中间 10 天积压的变化全部算成今天的 new/gone，噪音爆炸。需要对基线做"最多 N 天"的保护。

---

## 四、需求清单（按优先级）

### P0 — 立即做（已在眼前，改动集中，价值立竿见影）
- [ ] **事件状态机**：`boss_watch_event` 增加 `status` 字段（`new` / `viewed` / `followed` / `ignored`）+ `handled_by` + `handled_at`，前端列表加按钮 + 默认过滤掉已忽略。
- [ ] **事件详情抽屉**：点击事件行弹出抽屉，展示完整 `detail_json`、同一候选人的历史事件串、Boss 候选人链接（若能拿到 `encryptGeekId` 构造）。
- [ ] **diff 基线保护**：如果最近历史快照距今 > 3 天，只做"进入"事件，不做"消失"（避免长时间未跑后爆量）。
- [ ] **抓取日期回填**：`/trigger` 支持指定 `date`，用于补跑；`runDiff(today, baseline)` 参数透传。
- [ ] **Boss 账号失效钉钉告警**：抓取开始前探活 Cookie，失效直接推一条"Cookie 已失效，请重新配置"的钉钉，并把本次 job 标记 skipped。

### P1 — 短期做（价值高、改动稍大）
- [ ] **候选人一键入库**：事件抽屉里"加入人才库"按钮 → 在 `persons` 表建一条 `person_category=talent` 记录，回链 event；已入库的候选人后续事件自动关联 person。
- [ ] **钉钉消息优化**：按"公司维度"聚合（而非事件维度）；每家公司列最多 N 条关键变动；文末附"查看详情"链接跳前端过滤页；超过阈值用链接代替全量展开。
- [ ] **目标公司添加体验**：前端加"搜索公司名"输入框 → 后端代调 Boss 公司搜索接口 → 选中自动填 `boss_company_id`，免手动找 encXXX。
- [ ] **敏感字段加密落地**：按清单加密 `boss_watch_event` / `boss_watch_snapshot` 中的候选人与公司字段，并把 `auth.js` 切到统一 `master.key`。
- [ ] **后端访问控制**：`/api/boss-watcher/*` 全部加 role 中间件，仅 `boss` / `招聘负责人` 可访问。

### P2 — 中期做（体验与稳健性）
- [ ] **抓取配置化**：管理页暴露 cron 表达式、pageSize、每岗位最大页数、并发度、城市白名单，写入独立 `boss_watch_config` 表。
- [ ] **抓取并发优化**：岗位级可控并发（2-3 并发），总耗时压到原来 1/3；单岗位级仍串行避免风控。
- [ ] **同一候选人合并视图**：同一 `geek_id` 的事件按时间聚合，列表默认一行展示最新动态 + 折叠历史。
- [ ] **审计日志**：谁在什么时候查看了哪条事件，写 `boss_watch_access_log`，便于事后追查。
- [ ] **数据导出**：支持按时间范围导出事件 Excel（解密后），给招聘周会用。

### P3 — 远期 / 可选
- [ ] **接入其它渠道**：脉脉、LinkedIn（视合规）。
- [ ] **按关键词告警**：命中"带薪资期望 > X"或"title 含关键词"的候选人单独高亮 + 单独推送。
- [ ] **人才池重合度分析**：和本司 `persons` 对比，自动识别"我们曾经联系过但没入职"的老候选人再次活跃。
- [ ] **趋势图**：某公司近 30 天"出去"、"变活跃"的候选人趋势，作为竞争对手"动荡指数"。

---

## 五、建议先做的三件事（给下一个工作日）

> 按"改动小、收益明显、不阻塞后续"的顺序排。

1. **事件状态 + 详情抽屉（P0 前两项合并）**
   - 后端：`ALTER TABLE boss_watch_event ADD COLUMN status TEXT DEFAULT 'new'`，加一个 `PATCH /events/:id/status` 接口。
   - 前端：列表加"忽略 / 已跟进"按钮，默认过滤 `status != 'ignored'`；点行弹抽屉展示完整信息。
   - 预计半天。

2. **diff 基线保护 + Cookie 失效告警（P0 后两项）**
   - `diff.js` 在 baseline 距今 > 3 天时，只产 `new`、不产 `gone`。
   - `scheduler.runJob` 起始探活 Cookie，失效直接推钉钉 + job 标 `skipped`。
   - 预计 1-2 小时。

3. **候选人一键入库（P1 第一项）**
   - 事件抽屉"加入人才库"按钮，复用现有 persons POST 接口，带默认字段回填。
   - 预计半天到一天（取决于 persons 表必填字段）。

做完这三件之后，雷达从"自动化信息流"升级为"可跟进的工作台"，后续钉钉优化与加密落地是相对独立的下一批。

---

## 六、关键决策（2026-05-05 已确认）

### 6.1 事件保留期
- **维持 60 天自动清理**。已入库到 `persons` 的候选人独立保存，不受清理影响。
- **影响**：`scheduler.cleanOldData` 不动；事件入库后只在 `persons` 上引用 `event_id` / `geek_id`，原始 event 即便被清理也不影响人才库本身。

### 6.2 可见范围
- **仅四位老板**：`ceo` / `coo` / `cto` / `cmo`。
- **影响**：
  - 后端：`server/boss-watcher/routes.js` 全量加 role 中间件（参考现有 `executiveRoles` / `ADMIN_ROLES` 的检查方式，`server/index.js:4813` 与 `client/src/AuthContext.js` 已有这四个角色定义）。
  - 前端菜单：`client/src/App.js` 招聘雷达入口仅对这四个角色显示。
  - 老板之外（包括 admin）默认无权看，避免广义"管理员"误开权限。
- **路由层是硬门**：`requireExec(['ceo','coo','cto','cmo'])` 中间件，所有 `/api/boss-watcher/*` 都过。

### 6.3 钉钉接收人
- **当前阶段单人**：仍用现有 `receiverUserId` 字段。
- **下一阶段需支持四人**：每位老板有独立 `receiverUserId`，按 6.5 的岗位分发规则各推各的（不要四个人都收全部）。
- **演进策略**：先做"按岗位分发"的逻辑层，钉钉配置层在 P1 改成数组（`receivers: [{role, userId}]`），不要急着上群机器人。

### 6.4 加密策略
- **事件层不加密**（候选人名、岗位、状态都明文存）。理由：60 天清理 + 仅四位老板可见 + 钉钉本身已是出站明文，加密性价比低且阻碍筛选/搜索。
- **入库到 `persons` 即加密**：一旦点"加入人才库"，就走 `persons` 的加密管道（按 `敏感字段加密清单模板.md` 的 P2 批次）。
- **`auth.js` 的 Cookie 加密密钥**仍需收敛到统一 `master.key`（这是基础设施问题，与雷达数据加密分开做）。

### 6.5 钉钉推送的岗位分发规则

按 `boss_watch_position.title`（即"我在招"的岗位标题）做关键词归类，事件按归类推给对应老板：

| 归类 | 接收老板 | 关键词（命中即归类，不区分大小写） |
|---|---|---|
| 产品 / 运营 | COO | `产品`、`运营`、`增长`、`策划`、`PM`、`产品经理`、`用户研究` |
| 研发 / 技术 | CTO | `开发`、`工程师`、`研发`、`架构`、`算法`、`数据`、`测试`、`运维`、`前端`、`后端`、`全栈`、`SRE`、`DevOps`、`AI`、`机器学习` |
| 商务 / 销售 | CMO | `商务`、`销售`、`BD`、`市场`、`品牌`、`渠道`、`客户成功` |
| 其他 | CEO | 上述都不命中 |

**实现要点**：
- 关键词表写到 `server/boss-watcher/dispatch-rules.js`，方便后续调整。
- 单条事件只发给一位老板，不做兜底广播（避免噪音）。
- 命中规则在 diff 完后、push 钉钉前应用：按岗位分桶 → 每位老板单独构造一份 markdown → 单独 `sendWorkNotice`。
- 配置页加"分发规则预览"按钮，列出当前每位老板会收到哪些岗位的事件，便于核对。
- 钉钉配置 schema 升级为：
  ```json
  { "appKey": "...", "appSecret": "...", "agentId": "...",
    "receivers": { "ceo": "userId1", "coo": "userId2", "cto": "userId3", "cmo": "userId4" } }
  ```
  老字段 `receiverUserId` 做一次性兼容（读取时 fallback 到 CEO），下次配置时强制四个都填。

### 6.6 候选人入库后的归属
- 默认归到**当前操作人**（点"加入人才库"按钮的那位老板）。
- 入库时记录 `created_by_role`，便于后续按角色聚合。

---

## 七、调整后的 P0 任务（按确认结果重排）

1. **后端访问控制**（原 P1，提至 P0 第 1 项，因为 6.2 已硬性要求）
   - `server/boss-watcher/routes.js` 全部接口加 `requireExec` 中间件，仅 `ceo/coo/cto/cmo` 可访问。
   - 前端菜单与路由守卫同步收窄。
   - 预计 1 小时。

2. **岗位分发规则 + 钉钉配置升级**（6.5 落地）
   - 新建 `server/boss-watcher/dispatch-rules.js`，导出 `classifyPosition(title) -> 'ceo'|'coo'|'cto'|'cmo'`。
   - `dingtalk.js` 的 `sendEventNotice` 按分类分桶，分别 `sendWorkNotice`。
   - `dingtalk-config.json` schema 升级为 `receivers` map，旧 `receiverUserId` fallback 到 ceo。
   - 配置页 4 个 userId 输入框 + "分发规则预览"。
   - 预计 1 天。

3. **事件状态机 + 详情抽屉**（原 P0 不变）
   - 见前文第五节第 1 项，预计半天。

4. **diff 基线保护 + Cookie 失效告警**（原 P0 不变）
   - 见前文第五节第 2 项，预计 1-2 小时。
   - Cookie 失效告警按 6.5 推给 CEO（兜底）。

5. **候选人一键入库**（原 P1，保留 P1）
   - 默认归当前操作人（6.6）。
   - 走 `persons` 加密管道（6.4）。
   - 预计半天到一天。

**建议下一步**：先做 #1 + #2，是产品形态变化（权限边界 + 推送对象），改完后续每个迭代都基于新的边界，避免回头再迁数据。
