# Relation 业务中台 UI 规范

最后更新：2026-07-30

本文档定义 Relation 业务列表页和管理页的统一视觉与交互基线，适用于工作台、媒体管理、人脉管理、
互动记录、商机、公司研究、策略、需求、产品资产、主体管理、产品模版，以及后续新增的同类中台页面。
在线表格编辑区继续遵守石墨式表格体验要求；普通文档编辑区继续遵守文档中心既有编辑体验。

## 1. 设计目标

- 业务中台优先服务高频录入、检索、比较和审核，整体气质应安静、紧凑、清晰、可靠。
- 页面之间必须像同一套产品，不因模块不同出现完全不同的字体、卡片、筛选条、表格密度和操作按钮。
- 视觉强调应留给业务状态、异常、待办和主操作；避免用大面积装饰性色块争夺注意力。
- 移动端不是压缩桌面表格，而是提供可扫描的业务卡片和收敛后的筛选/操作入口。

## 2. 适用页面结构

业务列表页默认使用以下结构：

1. 页面治理栏：页面标题、数量、关键状态说明、刷新、列设置、主操作。
2. 指标概览区：可选，用于展示核心数量或状态分布。
3. 筛选工具条：搜索、枚举筛选、日期范围、重置/展开等筛选动作。
4. 数据区：桌面优先表格，移动端优先卡片列表。
5. 详情/编辑区：桌面优先抽屉，复杂录入可用弹窗；移动端全屏抽屉优先。

页面不得新增营销式 hero、装饰性渐变背景、嵌套卡片或与业务无关的说明性大段文案。

## 3. 字体与层级

统一使用系统默认中文字体栈，避免页面级自定义字体。字号不得随视口宽度缩放。

| 场景 | 字号 | 字重 | 行高 | 颜色 |
| --- | --- | --- | --- | --- |
| 页面标题 | 16px | 600 | 24px | #1f2937 |
| 区块标题 | 15px | 600 | 22px | #1f2937 |
| 表头 | 13px | 600 | 20px | #344054 |
| 表格主文本 | 13px | 400/500 | 20px | #1f2937 |
| 主字段链接 | 13px | 600 | 20px | #1677ff |
| 次要信息 | 12px | 400 | 18px | #667085 |
| 辅助/空值 | 12px | 400 | 18px | #98a2b3 |
| 移动卡片标题 | 15px | 600 | 22px | #1f2937 |

除数据大屏或移动端大标题外，业务列表页不使用 20px 以上标题文字。统计数字桌面最大 24px，移动端最大
26px。

## 4. 色彩

### 4.1 基础色

- 主色：#4f46e5，用于当前页签、主按钮、关键选中态。
- 链接色：#1677ff，用于文本链接、详情入口。
- 页面背景：#f6f8fb 或现有全局浅灰背景。
- 容器背景：#ffffff。
- 表头背景：#f8fafc。
- 悬浮行背景：#f8fbff。
- 主边框：#e5e7eb。
- 弱边框：#eef0f3。

### 4.2 状态色

- 成功/启用/已完成：#16a34a。
- 进行中/测试中/待对接：#1677ff 或 #0891b2。
- 警告/中等/处理中：#f59e0b。
- 危险/重要/阻塞/删除：#ef4444。
- 信息/共享/普通分类：#4f46e5 或 #1677ff。
- 空值/停用/未知：#98a2b3。

同一语义在所有页面必须使用同一色系。不得在同一页面中同时大面积使用紫、蓝、青、绿、粉、红、橙
渐变。

## 5. 间距与尺寸

- 页面内容区桌面 padding：16px 20px；移动端：12px。
- 页面治理栏下间距：12px。
- 指标区与筛选区间距：12px。
- 筛选区与表格间距：12px。
- 筛选控件高度：32px。
- 主按钮高度：32px。
- 图标按钮尺寸：32px，表格行内紧凑图标按钮可为 28px。
- 表格桌面默认行高：48px；信息量特别大的页面可在 44px-56px 范围内调整，但同页必须一致。
- 卡片圆角：8px；不再新增 12px 以上的大圆角业务容器。
- 重复卡片间距：10px-12px。

## 6. 页面治理栏

治理栏负责承载页面身份和主操作，默认单行：

- 左侧：页面名、记录数、必要的状态说明。
- 右侧：刷新、导入/导出、列设置、主操作。
- 页面已经由顶部工作区页签表达位置时，治理栏不重复堆叠大标题和说明。
- 主操作只保留一个最重要动作，如“新增商机”“添加人脉”“新增产品资产”。
- 次要动作用普通按钮或图标按钮，不和主操作争夺视觉权重。

## 7. 指标概览区

指标卡用于辅助筛选和概览，不作为装饰。默认使用白底轻量卡：

- 背景：#ffffff。
- 边框：1px solid #e5e7eb。
- 圆角：8px。
- 内边距：14px 16px。
- 标题：12px，#667085。
- 数字：22px-24px，600/700。
- 图标：28px-32px 浅色底，颜色跟语义一致。

只有经营总览、正式数据大屏或明确需要情绪化展示的页面才允许使用大面积渐变指标卡。工作台、商机、
策略、需求、产品资产等日常操作页默认不得新增高饱和渐变统计卡；存量改造时应逐步替换为轻量卡。

## 8. 筛选工具条

筛选条统一为白底或无边框工具带，控件从左到右按“搜索 -> 核心枚举 -> 日期 -> 负责人/归属 -> 重置/
更多”排列：

- 搜索框默认宽度 220px-280px，placeholder 明确搜索范围。
- 枚举 Select 默认宽度 120px-180px。
- 人员/公司/人脉选择必须支持 `showSearch`，并使用稳定 `label` 搜索，不得依赖 React children 字符串。
- 多选筛选使用 `maxTagCount="responsive"`，避免撑高工具条。
- 桌面筛选条可换行，但行间距固定 8px；不得因个别控件长文本导致页面横向溢出。
- 移动端筛选收入口径统一：主列表展示“搜索 + 筛选”或仅“筛选”按钮，复杂条件进入全屏抽屉，底部
  固定“重置 / 完成”。

## 9. 表格

桌面业务列表默认使用统一表格质感：

- 表头背景 #f8fafc，表头文字 13px/600。
- 正文文字 13px，主字段 600，次字段 12px。
- 行 hover 使用 #f8fbff。
- 边框只使用横向弱分隔，避免过重网格。
- 空值统一显示 `-`，颜色 #98a2b3。
- 主字段链接左对齐，可点击打开详情；不要整列用大号蓝字。
- 重要次级信息显示在主字段下方，最多两行，超出省略。
- 操作列固定在右侧时需控制阴影强度，不得遮挡正文；优先保持 120px-160px。
- 删除入口使用危险色；低频操作可收入 `···` 菜单。
- 表格列宽必须有最小/最大约束，长文本使用省略和 Tooltip，不得撑开页面。

同一页面不得混用 AntD 默认 Table、ResizableTable 和自定义裸表格造成视觉差异；确需混用时必须套用
统一 class。

## 10. 标签与状态

- Tag 高度默认 22px，圆角 4px-6px，字号 12px。
- 表格内紧凑标签可使用 20px 行高、11px-12px 字号。
- 状态标签必须表达业务状态，不得把所有分类都做成高饱和色块。
- 多标签连续出现时，间距 4px，不能撑高表格行；超过数量使用 `+N` 或 Tooltip。

## 11. 按钮与操作

- 主按钮：`type="primary"`，32px 高，带 lucide/AntD 图标时优先使用图标+短文本。
- 次按钮：默认按钮，32px 高。
- 表格行操作：文字链接或 28px 图标按钮，顺序统一为“详情 / 预览 / 编辑 / 删除”。
- 删除必须二次确认；只读用户不展示或禁用写操作，服务端仍需独立鉴权。
- 不得用多个高亮主按钮并排表达同一层级动作。

## 12. 移动端列表

移动端不直接压缩桌面表格：

- 每条记录为独立白底卡片，圆角 8px，边框 #e5e7eb，内边距 14px。
- 顶部：主标题 + 日期/状态 + `···` 操作。
- 第二层：公司、创建人、负责人、城市等核心元信息。
- 第三层：状态/分类/重要程度 Tag。
- 第四层：描述、结果、下一步等摘要，最多两行。
- 整卡可点击打开详情；列表卡禁止误选文字。
- 编辑、删除等操作进入底部操作面板，删除仍需二次确认。

## 13. 详情与编辑

- 桌面详情优先用右侧 Drawer，宽度按内容复杂度使用 520px-720px。
- 移动端详情/编辑使用全屏 Drawer，顶部固定标题，底部固定主操作。
- 表单 label 和输入控件对齐，复杂富文本字段保持同一编辑器。
- 详情页信息分组使用轻量分隔和小标题，不在 Drawer 内嵌套多层 Card。

## 14. 禁止项

- 禁止新增大面积高饱和渐变统计卡作为普通业务页默认样式。
- 禁止业务列表页使用营销页式 hero、插画背景、装饰性光斑或过度阴影。
- 禁止每页随意 inline `fontSize`、`borderRadius`、`boxShadow` 作为主要视觉方案；临时兼容应逐步收敛到统一 class。
- 禁止一个页面内出现两套明显不同的表格行高、表头、按钮和 Tag 风格。
- 禁止筛选控件因长标签把页面撑出横向滚动。
- 禁止用前端隐藏替代后端权限控制。

## 15. 改造优先级

第一批先统一视觉冲突最大的日常业务页：

1. 商机 `/leads`
2. 策略 `/strategies`
3. 需求 `/dev-tasks`
4. 产品资产 `/product-assets`

第二批统一高频数据录入和资产页：

1. 工作台 `/`
2. 媒体管理 `/media-management`
3. 人脉管理 `/persons`
4. 互动记录 `/interactions`
5. 公司研究 `/companies`

第三批统一低频资产配置页：

1. 主体管理 `/company-subjects`
2. 产品模版 `/product-templates`

每批改造必须保留现有业务列顺序、权限、筛选语义、移动端核心操作和性能门禁。视觉统一不应夹带业务
逻辑重构，除非现有结构阻碍可用性或一致性。
