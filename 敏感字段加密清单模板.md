# 商业机密加密方案（Relation 项目）

> 场景：防止 `data.db` 文件被拿走、离职员工留存副本、VPS 被入侵后数据库被直接读取。
> 技术栈：Node.js + Express + SQLite（better-sqlite3）。
> 威胁模型：保护的是**商业机密**（客户、人才、战略内容），不是 PII 合规。

---

## 一、需要加密的模块和字段

按模块梳理，只列"真正敏感、泄漏会伤业务"的字段。其他字段（状态、时间、ID、分类标签）保持明文——否则任何筛选、排序、预警都会被破坏。

### 1. 人脉管理（persons / 人才库 / 客户管理）

| 字段 | 是否加密 | 理由 |
|---|---|---|
| `name`（姓名） | ✅ 加密 | 客户/人才名单是核心资产 |
| `phone`（手机） | ✅ 加密 | 核心联系方式 |
| `wechat` / `email` | ✅ 加密 | 同上 |
| `current_company` / `current_title` | ✅ 加密 | 暴露"你们在挖谁"、"客户是谁" |
| `target_title`（目标职位） | ✅ 加密 | 暴露招聘意图 |
| `skills` / `notes` / `remark` | ✅ 加密 | 人才评估、客户偏好等私密判断 |
| `salary_expectation` / `current_salary` | ✅ 加密 | 薪资信息敏感 |
| `address` / `location` | ✅ 加密（若有） |  |
| `importance`（high/vip） | ❌ 明文 | 需要筛选"重点客户" |
| `person_category` / `relation_types` | ❌ 明文 | 需要筛选 |
| `created_at` / `updated_at` | ❌ 明文 | 需要排序 |

### 2. 互动记录（interactions）

| 字段 | 是否加密 | 理由 |
|---|---|---|
| `content` / `result` / `notes`（拜访内容、会议记录） | ✅ 加密 | 真正的机密——谈了什么、承诺了什么 |
| `next_action`（下一步动作） | ✅ 加密 | 暴露策略 |
| `participants`（参与人） | ✅ 加密（若含外部人名） |  |
| `type`（电话/拜访/会议） | ❌ 明文 | 统计/筛选用 |
| `date` | ❌ 明文 | 排序、"距今天数"计算 |
| `person_id` | ❌ 明文 | 关联查询 |

### 3. 线索池（leads / opportunities）

| 字段 | 是否加密 | 理由 |
|---|---|---|
| `title`（线索标题，如"字节穿山甲游戏预算"） | ✅ 加密 | 暴露商业机会 |
| `description`（线索描述） | ✅ 加密 | 同上 |
| `evaluation_note`（评估结论） | ✅ 加密 | 内部判断 |
| `company_id` / `contact_id` | ❌ 明文 | 外键 |
| `source_type` / `priority` / `status` | ❌ 明文 | 筛选用 |

### 4. 跟进记录 / 任务（follow_up_tasks / tasks）

| 字段 | 是否加密 | 理由 |
|---|---|---|
| `title` / `description` | ✅ 加密 | 可能含客户名、金额、承诺 |
| `done_note` / `result`（完成备注） | ✅ 加密 | 同上 |
| `status` / `priority` / `date` / `assigned_to` | ❌ 明文 | 筛选、红点、排序都要用 |

### 5. 公司经营（executive_reports）—— **加密优先级最高**

整张表就是战略月会、经营周会、重点客户、高级人才。**所有富文本字段全部加密**。

| 字段 | 是否加密 |
|---|---|
| `weekly_results` / `key_judgment` / `decision_needed` / `next_week_actions` | ✅ 加密 |
| `key_issues` / `decisions` | ✅ 加密 |
| `strategic_direction` / `key_focus` / `monthly_summary` | ✅ 加密 |
| `attendees`（参会人员） | ❌ 明文（仅 user_id 数组） |
| `report_type` / `meeting_date` / `year` / `month` / `week` | ❌ 明文 |

> "高级人才"、"重点客户"、"竞争公司动态"本身不是独立表，而是 `persons` / `companies` / `company_dynamics` 的视图。加密在源表做（见上面 1、6）。

### 6. 公司研究（companies / company_dynamics）

| 字段 | 是否加密 | 理由 |
|---|---|---|
| `companies.name` | ⚠️ 看情况 | 如果"研究中的公司名单"本身是机密（尤其 competitor 分类），加密 |
| `companies.notes` / `analysis` | ✅ 加密 | 内部分析判断 |
| `company_dynamics.content`（动向内容） | ✅ 加密 | 情报内容 |
| `company_dynamics.impact_analysis`（影响分析） | ✅ 加密 | 内部判断 |
| `companies.category` / `industry` | ❌ 明文 | 筛选用 |

### 7. 周报（weekly_reports）

| 字段 | 是否加密 |
|---|---|
| `summary` / `achievements` / `next_plan` / `next_focus` / `problems` / `support_needed` / `boss_comment` | ✅ 加密 |
| `user_id` / `week` / `status` / `submit_time` | ❌ 明文 |

### 8. 目标管理（goals）

| 字段 | 是否加密 |
|---|---|
| `description` / `current_progress` / `progress_note` | ✅ 加密 |
| `title`（"Q2预算商务接入目标"） | ⚠️ 看情况，若涉及具体数字/客户名则加密 |
| `target_value`（"接入5家新预算方"） | ✅ 加密 |

### 9. 出差 / 送礼

| 字段 | 是否加密 |
|---|---|
| `trips.purpose`（出差目的） / `trips.notes` | ✅ 加密（含客户名、谈判内容） |
| `gifts.recipient`（收礼人） / `gift_plans.reason` | ✅ 加密 |
| 费用、审批状态、时间 | ❌ 明文 |

### 10. 用户表（users）

| 字段 | 处理 |
|---|---|
| `password` | 继续用 bcrypt 哈希（已是业界做法，不是"加密"） |
| `phone` / `email` | ✅ 加密（内部员工 PII） |
| `name` / `username` / `role` | ❌ 明文（登录、权限要用） |

---

## 二、技术方案

### 2.1 核心思路

**只做应用层字段级加密**，不要做整库加密：
- 整库加密（如 SQLCipher）密码写在代码里等于没加——拿到文件就拿到密码
- 字段级加密 + 密钥独立存放：拿到 `.db` 文件 ≠ 拿到密钥 ≠ 能解密

### 2.2 算法

- **对称加密：AES-256-GCM**
  - Node.js 内置 `crypto` 模块支持，不需要装包
  - GCM 自带完整性校验，防止密文被篡改
- **密码哈希：继续用 bcrypt / argon2**（不改）

### 2.3 密钥存放（最关键的一步）

SQLite 部署模式下，最务实的方案：

**方案 A（推荐，零成本）：环境变量 + 独立密钥文件**
```
/etc/relation/master.key       # 32 字节随机，chmod 600，只有 node 进程用户可读
                                # 绝不提交 git，.gitignore 已排除
server 启动时读入内存
```
- 优点：部署简单、密钥与 `.db` 不在同一备份里
- 要求：密钥文件不进 git、不进 Docker 镜像、不进备份包
- 风险：服务器被完全攻陷则密钥也丢——但此时攻击者已能直接从运行中的 node 进程读内存，任何方案都挡不住。能挡住"DB 文件被单独拷走"就已经覆盖 90% 的泄漏场景

**方案 B（进阶）：上云 KMS**
- 如果后续上阿里云/腾讯云：用云 KMS 保管主密钥（KEK），本地缓存数据密钥（DEK）
- 成本很低（KMS 调用几毛钱/月），但运维复杂度上升
- **当前阶段不必急**，先落地 A 方案

**方案 C（不推荐）**：密钥写在 `.env` 并提交仓库 → 等于没加密。

### 2.4 密文格式

每个加密字段存为一串 base64 或 hex，内部结构：

```
version(1B) | iv(12B) | ciphertext(N) | auth_tag(16B)
```

- `version`：未来换算法/轮换密钥时用（先固定 0x01）
- `iv`：每次加密随机生成，GCM 的 IV **绝对不能重复**
- `auth_tag`：GCM 认证标签

### 2.5 查询问题

- **模糊搜索客户名/人才名**：加密后无法 `LIKE '%张%'`
  - 方案：加一个**小写归一化的 HMAC 索引列** `name_idx = HMAC-SHA256(index_key, 规范化(name))`，用于**等值查找**（登录、手机号查重）
  - 模糊搜索：前端搜索时应用层解密后在内存里过滤（数据量小时可行，百万级数据要单独方案）
  - 你当前场景数据量不大（几百到几千条），**应用层过滤完全够用**
- **排序**：排序字段（时间、importance、status）本来就不加密，没影响
- **统计计数**：不受影响
- **JOIN**：外键（ID）不加密，不受影响

### 2.6 实施结构

```
server/
├── lib/
│   ├── crypto.js          # encrypt(plain) / decrypt(cipher) / hmac(v) 三个函数
│   └── encryptedFields.js # 配置：每张表哪些字段加密
├── db/
│   └── migrate-encrypt.js # 一次性回填脚本
└── index.js               # 写入前加密、读出后解密（统一在 DAO 层）
```

**关键点**：加解密**只在 DAO/service 层做一次**，不要散落在每个路由里。建议写个薄薄的包装：

```js
// 读：SELECT 出来后遍历字段解密
// 写：INSERT/UPDATE 前遍历字段加密
// 按 encryptedFields.js 的配置自动处理
```

---

## 三、迁移顺序（按风险从高到低）

| 批次 | 模块 | 优先级 | 说明 |
|---|---|---|---|
| P0 | 封装 crypto.js + 密钥生成/部署流程 | 最高 | 基础设施 |
| P1 | `executive_reports` | 最高 | 战略月会/经营周会，泄漏后果最严重，且记录少，迁移最快 |
| P2 | `persons`（含 talents/clients） | 高 | 客户+人才名单 |
| P3 | `interactions` | 高 | 拜访记录 |
| P4 | `companies` + `company_dynamics` | 中 | 竞品情报 |
| P5 | `leads` / `follow_up_tasks` / `tasks` | 中 | 线索和跟进 |
| P6 | `weekly_reports` / `goals` | 中 | 周报、目标 |
| P7 | `trips` / `gifts` / `users.phone` | 低 | 查漏补缺 |

### 单表标准迁移步骤

1. 加字段：敏感字段原地改为存密文（或新增 `xxx_enc`，双写过渡）
2. 写 `migrate-encrypt-xxx.js` 一次性脚本：`SELECT *` → 加密 → `UPDATE`
3. 停服执行（你的场景几秒到几分钟搞定），或先双写再切读
4. 上线后抽样解密检查
5. 之后的新写入全走加密通道

---

## 四、必须注意的坑

1. **备份文件**：`/server/data.db`、`data-server.db` 这些本身就是明文。迁移完成后**立刻删除旧备份**，否则等于没加密
2. **Git 历史**：检查 `.gitignore` 是否已排除 `*.db`、`master.key`。你的 server.log 也别提交
3. **日志**：Express 的请求日志、异常栈很容易把明文打出来——加密字段在日志里要脱敏或不打
4. **uploads/ 目录**：如果有客户上传的合同/简历，这些文件本身也要考虑加密存储或至少做访问控制
5. **IV 不能重复**：每次加密都 `crypto.randomBytes(12)`，不要用固定值、不要用时间戳
6. **密钥轮换**：版本号字段留着，一年内大概率用不上，但留个口子
7. **开发环境**：本地开发用独立的 `dev.key`，不要用生产密钥
8. **协同编辑场景**：经营周报多人编辑时，先解密到内存 → 编辑 → 重新加密写回，注意并发冲突（`last_edited_by` / `last_edited_at` 做乐观锁）

---

## 五、下一步

你确认上面的**字段选择和优先级**后，我可以直接产出：

1. `server/lib/crypto.js`（AES-256-GCM 封装 + HMAC 索引 + 密钥加载）
2. `server/lib/encryptedFields.js`（字段映射配置）
3. `scripts/gen-master-key.js`（密钥生成脚本 + 部署说明）
4. `scripts/migrate-encrypt-p1-executive.js`（P1 批次迁移脚本，作为样板）
5. 一个 DAO 层示范，展示如何把加解密透明化

需要重点确认的几件事：
- [ ] `companies.name` 是否加密？（影响面较大，竞品列表看不到公司名会影响筛选）——公司名也会加密，但前端不影响展示
- [ ] `persons.name` 加密后，前端人脉列表展示时是否可以接受"全部解密加载"？（数据量 < 5000 条完全 OK）——同样，后端全部加密，前端全部解密显示
- [ ] 密钥部署方式：先走方案 A（服务器本地文件）还是直接上云 KMS？——方案A
- [ ] 是否有历史 `.db` 备份分发出去过？需要评估泄漏面——没有发出去过
