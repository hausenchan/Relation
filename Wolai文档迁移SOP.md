# Wolai 文档迁移 SOP

> 版本：v1.0  
> 日期：2026-07-03  
> 适用范围：Wolai 文档迁移为统一蒸馏库输入

---

## 1. 文档目的

本 SOP 用于指导团队将 Wolai 中的历史文档稳定迁移到统一蒸馏库，作为后续知识蒸馏、Skill 调优和评测的数据输入。

本次迁移的目标不是优先复刻 Wolai 的完整协同体验，而是优先保证以下信息稳定落地：

- 文档标题
- 文档正文
- 标题层级
- 表格内容
- 图片与附件引用
- 标签
- 创建人 / 更新时间
- 原始链接
- 文档所属目录或空间路径

---

## 2. 适用原则

### 2.1 本次迁移优先级

优先级从高到低：

1. `Wolai 原生结构化导出`
2. `Chrome 自动采集`
3. `PDF / 截图 / OCR 兜底`

### 2.2 不建议的做法

不建议直接：

- 把 PDF 当主输入源
- 只复制粘贴正文
- 不留原始链接和来源信息
- 不做试迁移直接全量跑

原因：

- 层级和表格结构容易丢失
- 后续无法回放和审计
- 难以定位知识来源
- 容易把低价值内容和高价值内容一起搬进来

---

## 3. 角色分工

### 3.1 业务负责人

负责：

- 确定迁移范围
- 确定优先级
- 识别高价值文档
- 协调 Wolai 权限

### 3.2 迁移执行人

负责：

- 导出或触发采集
- 记录批次
- 检查导出结果
- 提交问题清单

### 3.3 研发 / 数据同学

负责：

- 导入统一蒸馏库
- 处理字段映射
- 跑标准化和切 chunk
- 做失败重试和日志

### 3.4 业务验收人

负责：

- 核对内容完整性
- 核对表格和附件是否可用
- 确认文档是否适合进入蒸馏流程

---

## 4. 迁移前准备

### 4.1 确认迁移目标

先明确本次迁移属于哪一类：

1. 仅迁入统一蒸馏库，作为 AI 输入
2. 同时迁入统一蒸馏库，并筛选一部分进组织中台文档中心

建议前期按第 1 类执行。

### 4.2 盘点文档范围

按以下维度整理清单：

- Wolai 空间
- 文件夹
- 页面数量
- 文档类型
- 负责人
- 是否涉及敏感信息
- 是否仍在活跃维护

建议先做一个迁移清单表，字段至少包含：

- `workspace_name`
- `folder_path`
- `doc_title`
- `doc_url`
- `doc_type`
- `owner_name`
- `priority_level`
- `migrate_batch_no`
- `status`

### 4.3 文档分类建议

迁移前先分为：

1. `规则类`
2. `案例类`
3. `流程 / SOP 类`
4. `会议纪要类`
5. `复盘 / 周报类`
6. `行业资料类`
7. `低价值存档类`

建议前期先迁移前 5 类中的高价值内容。

### 4.4 账号与权限准备

需准备：

- 一个可访问目标 Wolai 空间的迁移账号
- 对应空间 / 文件夹 / 页面访问权限
- 若使用 Chrome 自动采集，需要该账号能稳定登录

---

## 5. 迁移方式选择

### 5.1 方式 A：Wolai 原生导出

适用条件：

- Wolai 当前账号界面支持页面导出或空间导出
- 可导出 Markdown / HTML / 富文本结构

推荐格式优先级：

1. `Markdown + 附件`
2. `HTML + 附件`
3. `PDF`

执行要点：

- 每次导出保留原目录结构
- 不要只拿正文，附件和图片也要保留
- 同一批次输出统一命名

### 5.2 方式 B：Chrome 自动采集

适用条件：

- Wolai 原生批量导出能力不足
- 页面可在浏览器中稳定访问
- 可接受通过登录态进行受控采集

执行要点：

- 从目录树逐篇打开
- 抓标题、正文、表格、图片、附件链接
- 记录页面 URL 和目录路径
- 保存原始 HTML / 文本快照

### 5.3 方式 C：PDF / OCR 兜底

适用条件：

- 页面导出和自动采集都不可行
- 文档量较小
- 内容结构要求不高

注意：

- 该方式仅作为补救
- 不建议作为主链路

---

## 6. 推荐迁移路径

建议采用“两阶段迁移”。

### 6.1 第一阶段：试迁移

先选：

- 1 个 Wolai 空间
- 1 到 2 个文件夹
- 100 到 300 篇文档

试迁移目标：

- 验证导出或采集是否稳定
- 验证表格和附件保留情况
- 验证导入统一蒸馏库后能否顺利切 chunk
- 验证蒸馏输出质量

### 6.2 第二阶段：批量迁移

试迁移通过后，再按批次扩展。

建议批次粒度：

- 每批次 200 到 1000 篇文档
- 每批次一个明确的空间或目录范围

---

## 7. 具体执行步骤

### 7.1 建立迁移批次

为每轮迁移生成：

- `batch_no`
- `scope_desc`
- `source_account`
- `operator_name`
- `planned_doc_count`
- `start_time`

### 7.2 导出或采集原始文档

每篇文档至少保留：

- 原始标题
- 原始 URL
- 原始目录路径
- 原始正文快照
- 图片 / 附件引用
- 作者信息
- 更新时间

### 7.3 原始数据入库

先写入统一蒸馏库原始层：

- `ai_data_sources`
- `ai_source_sync_jobs`
- `ai_raw_records`

说明：

- 不要跳过原始层直接只留清洗后的文本
- 原始层是后续回放、重跑、问题追踪的基础

### 7.4 标准化处理

原始数据进入标准化层：

- `ai_source_items`
- `ai_source_chunks`

标准化时需要完成：

- 标题和目录路径整理
- 文档分类
- 正文切块
- 表格结构抽取
- 图片与附件建立引用

### 7.5 质量校验

至少检查：

1. 文档数是否一致
2. 标题是否一致
3. 正文是否为空
4. 表格是否丢失
5. 图片 / 附件是否可访问
6. URL 是否留存
7. 更新时间是否留存

### 7.6 发布到蒸馏可用范围

校验通过后，将符合要求的内容标记为可进入蒸馏流程。

---

## 8. 验收标准

### 8.1 基础验收

- 迁移成功率 >= 95%
- 标题完整率 >= 99%
- 正文非空率 >= 98%
- 原始 URL 保留率 = 100%
- 更新时间保留率 >= 95%

### 8.2 结构验收

- 标题层级基本正确
- 常见表格可读
- 图片和附件引用不丢
- 长文档切块可用

### 8.3 蒸馏验收

随机抽取样本文档，验证是否能稳定产出：

- 规则卡片
- 案例卡片
- 流程步骤
- few-shot 示例

---

## 9. 常见问题处理

### 9.1 页面能看但抓不到完整正文

处理建议：

- 优先抓渲染后 DOM
- 记录失败文档清单
- 对少量失败文档走人工补采

### 9.2 表格结构丢失

处理建议：

- 原始层同时留 HTML
- 标准化阶段专门处理表格节点
- 重要表格单独抽结构化 JSON

### 9.3 图片或附件失效

处理建议：

- 下载到内部对象存储或本地归档区
- 不只保留外链
- 记录下载状态和失败原因

### 9.4 文档有重复版本

处理建议：

- 按标题 + URL + 更新时间识别版本
- 原始层全留
- 标准化层只标记当前有效版本

### 9.5 敏感内容不宜进入蒸馏

处理建议：

- 在入库前打敏感标签
- 必要时脱敏
- 不符合要求的内容不进入正式蒸馏层

---

## 10. 迁移后建议动作

迁移不是终点，后面建议接着做：

1. 建立 Wolai 来源配置
2. 建立增量同步机制
3. 对高价值文档做人工标签补录
4. 沉淀首批规则卡片 / 案例卡片
5. 将试迁移结果用于 1 到 2 个核心 Skill 调优

---

## 11. 推荐输出物

每轮迁移结束后，建议至少产出：

1. 迁移批次报告
2. 失败文档清单
3. 结构异常清单
4. 敏感文档清单
5. 可进入蒸馏的文档清单

---

## 12. 关联文档

- [统一蒸馏库设计方案.md](/Users/chenhaozan/Documents/AI/Relation/统一蒸馏库设计方案.md)
- [Wolai到统一蒸馏库字段映射表.md](/Users/chenhaozan/Documents/AI/Relation/Wolai到统一蒸馏库字段映射表.md)
- [Chrome自动采集Wolai脚本设计方案.md](/Users/chenhaozan/Documents/AI/Relation/Chrome自动采集Wolai脚本设计方案.md)
- [sql/ai_distill_mysql.sql](/Users/chenhaozan/Documents/AI/Relation/sql/ai_distill_mysql.sql)
