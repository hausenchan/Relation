# 产品模版与提版代理 PRD

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 产品模块 | 产品模版、产品资产提版、提版 IP 代理 |
| 页面 | `/product-templates`、`/product-assets` |
| 文档状态 | 已实现版本 PRD |
| 最后更新 | 2026-07-30 |
| 关联文档 | `资产管理模块PRD.md`、`产品模版提版代理测试用例.md` |

## 2. 背景与问题

产品资产提版依赖固定的产品模版项目和多个外部域名。不同主体、API、埋点、CDN 及短剧服务可能需要
使用不同的 IP 出口，单纯设置进程级 `HTTP_PROXY` 无法在同一任务内按域名区分代理，也存在以下风险：

- 代理配置散落在脚本或服务器环境中，无法统一维护、停用和审计。
- 提版任务可能因缺少出口而直连外部服务，无法满足网络隔离要求。
- 代理账号密码可能进入接口、日志、任务历史或脚本参数，造成敏感信息泄露。
- 代理被编辑或删除后，已创建但尚未完成的任务行为不稳定。
- 产品模版和代理缺少统一入口，运营人员无法快速确认当前提版配置。

本模块将产品模版与代理纳入同一产品管理入口，并在提版创建时完成域名匹配和代理快照，在任务期间使用
任务级域名路由器，任务终止后关闭临时转发器。

## 3. 产品目标

### 3.1 目标

1. 在 `/product-templates` 页面提供“模版 / 代理”两个 Tab。
2. 支持 HTTP、HTTPS IP 代理配置的新增、查看、编辑、删除、启用和停用。
3. 提版创建前按域名主机名自动选择可用代理，缺少匹配代理时阻断任务创建。
4. 支持同一个提版任务内多个域名使用不同代理。
5. 代理只在当前提版任务期间生效，不改变服务器全局代理环境。
6. 全链路保护账号、密码和代理连接信息，进度与历史只展示安全摘要。
7. 代理配置变化不影响已经创建的提版任务，任务使用创建时的加密配置快照。

### 3.2 非目标

- 不提供代理供应商采购、余额、健康分、自动续费或线路测速平台。
- 不代理浏览器、服务器其他接口或除提版 `upload.js` 外的子进程。
- 不支持 SOCKS、FTP 或其他非 HTTP/HTTPS 代理协议。
- 不在本模块保存真实支付宝 Token、身份密钥明文或第三方服务凭据。
- 不通过前端隐藏替代服务端权限校验。

## 4. 用户与权限

### 4.1 用户角色

| 用户 | 查看模版 | 管理模版 | 查看代理 | 管理代理 | 发起提版 |
| --- | --- | --- | --- | --- | --- |
| 管理员 | 是 | 是 | 是 | 是 | 是 |
| CEO/COO/CTO/CMO | 按菜单权限 | 按写权限 | 按菜单权限 | 按写权限 | 按产品资产写权限 |
| 普通可写用户 | 按菜单权限 | 按 `canWrite` | 按菜单权限 | 按 `canWrite` | 按产品资产写权限 |
| `readonly` | 是 | 否 | 是 | 否 | 否 |
| `guest` | 按菜单/模块读权限 | 否 | 按菜单/模块读权限 | 否 | 否 |

### 4.2 权限规则

- 代理 API 复用产品模版菜单权限，满足 `/product-templates` 或兼容的 `/product-assets` 菜单入口后才可读。
- 写接口必须独立经过服务端 `canWrite`，不能依赖前端按钮隐藏。
- 未登录统一返回 `401`；无菜单权限返回 `403`。
- `readonly`、`guest` 直接调用 POST、PUT、DELETE 必须返回 `403`。
- 提版接口继续使用产品资产菜单、数据范围和写权限校验。
- 页面写操作通过当前会话的 `canWrite('product_assets')` 控制展示，但这只是交互优化，不是安全边界。

## 5. 核心业务流程

### 5.1 代理配置流程

1. 用户进入产品模版页面，切换到“代理”Tab。
2. 系统加载代理列表，默认按优先级升序、更新时间倒序、ID 倒序排列。
3. 用户可按名称、状态、域名后缀筛选。
4. 创建代理时填写协议、地址、端口、可选账号密码、域名后缀、优先级、启用状态和备注。
5. 编辑普通字段时，账号和密码留空表示保持原值；勾选“清除账号/清除密码”才会清除。
6. 删除前二次确认。删除不会改写已有提版任务的快照。

### 5.2 提版流程

1. 用户从产品资产发起提版。
2. 服务端校验产品资产、关联主体、模版、模版版本、APPID、身份密钥和域名。
3. 服务端读取启用中的代理配置，按目标域名逐一匹配。
4. 所有目标域名均匹配成功且存在启用的 `*` 默认代理后，创建提版任务和提版记录。
5. 服务端保存加密代理快照和不含凭据的匹配摘要，接口返回 `202` 和安全摘要。
6. 任务启动本地临时 HTTP 转发器，转发器只监听 `127.0.0.1` 随机端口。
7. `upload.js` 子进程获得任务级 `HTTP_PROXY`、`HTTPS_PROXY`、`ALL_PROXY` 环境变量。
8. 转发器按目标 Host 选择快照中的上游代理，支持普通 HTTP 请求和 HTTPS `CONNECT` 隧道。
9. 上传成功、失败、重试结束、取消或任务异常时，在 `finally` 中关闭转发器并销毁连接。
10. 进度和历史只显示目标域名、匹配后缀、代理名称等安全信息。

### 5.3 缺代理处理

当任一目标域名没有匹配到启用代理，或没有启用的 `*` 默认代理时：

- 提版创建接口立即返回 `400`。
- 不创建 task，不创建 release record，不启动 `upload.js`。
- 不允许通过直连路径继续执行。
- 错误中展示缺失域名和“代理 Tab”配置引导，不展示账号、密码或完整连接串。

## 6. 产品模版页面

### 6.1 Tab 结构

页面地址：`/product-templates`

| Tab | URL 状态 | 内容 |
| --- | --- | --- |
| 模版 | 无 `tab` 或 `tab=template` | 产品模版列表、筛选、详情、版本、预览、编辑 |
| 代理 | `?tab=proxy` | 代理列表、筛选、详情、创建、编辑、删除、启停 |

直接访问 `/product-templates?tab=proxy` 必须保持代理 Tab。

### 6.2 模版 Tab

保留原产品模版能力：

- 按名称、模版类型、预算类型、平台和状态筛选。
- 查看模版版本快照和项目目录。
- 可写用户可创建、编辑、预览和删除模版。
- 只读用户保留查看、详情和刷新，不展示写操作。

### 6.3 代理 Tab 列表

列表字段：

- 代理名称。
- 出口地址，展示协议、主机/IP 和端口，不展示账号密码。
- 域名后缀标签。
- 优先级。
- 认证状态，仅显示“已配置/无”，不显示账号明文。
- 启用状态。
- 更新时间。
- 详情、编辑、删除操作。

筛选项：

- 代理名称：模糊匹配。
- 域名后缀：模糊匹配。
- 状态：启用、停用。

### 6.4 代理表单

| 字段 | 必填 | 校验与交互 |
| --- | --- | --- |
| 代理名称 | 是 | 1-100 个字符 |
| 协议 | 是 | `HTTP` 或 `HTTPS` |
| 代理主机/IP | 是 | 域名、IPv4 或 IPv6，不允许路径、空格、账号信息 |
| 端口 | 是 | 1-65535 整数 |
| 账号 | 否 | 最多 200 个字符；编辑时留空保持原值 |
| 密码 | 否 | 最多 500 个字符；编辑时留空保持原值 |
| 域名后缀 | 是 | 支持多个值、`example.com`、`*.example.com`、`*` |
| 优先级 | 否 | 0-100000 整数，默认 100，数值越小越优先 |
| 状态 | 是 | 启用或停用，默认启用 |
| 备注 | 否 | 最多 2000 个字符 |

编辑状态下额外提供“清除账号”和“清除密码”。列表、详情、错误提示和成功提示不得回显密码。

### 6.5 代理详情

详情展示：名称、协议、主机/IP、端口、账号掩码、密码是否已配置、域名后缀、优先级、状态、备注和更新时间。

详情不得返回或渲染：

- 密码明文。
- 完整代理 URL。
- `Proxy-Authorization`。
- 加密快照原文。

## 7. 域名匹配规则

### 7.1 输入规范化

- 目标域名取 URL 的 hostname，不使用路径、查询参数或端口参与匹配。
- 主机名统一转小写。
- 代理后缀统一转小写。
- `*.example.com` 和 `.example.com` 规范化为 `example.com`。
- `*` 为默认规则，匹配任何有效目标主机名。
- 域名后缀按完整标签边界匹配，`example.com` 不匹配 `example.com.evil.test`。
- 主体域名仍必须满足提版域名校验：HTTPS、非本机、非内网、无账号密码。

### 7.2 选择顺序

候选代理按以下顺序排序，取第一条：

1. 最长匹配后缀优先，`api.example.com` 优先于 `example.com`，具体后缀优先于 `*`。
2. 后缀长度相同时，优先级数值小者优先。
3. 优先级相同时，`updated_at` 较新的优先。
4. 更新时间相同时，ID 较大的优先。
5. 停用代理不参与匹配。

### 7.3 必须匹配的域名

普通模版必须匹配：

- API 域名。
- 埋点域名。
- CDN 域名。

短剧模版额外必须匹配短剧域名。普通模版即使主体配置了短剧域名，也不把短剧域名纳入本次任务。

所有任务都必须存在启用的 `*` 默认代理，即使具体域名已有更精确代理。

## 8. 数据模型

### 8.1 `product_release_proxies`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | integer | 主键 |
| `name` | text | 代理名称 |
| `protocol` | varchar | `http` 或 `https` |
| `host` | varchar | 主机/IP |
| `port` | integer | 端口 |
| `username` | encrypted text | 加密账号，可为空 |
| `password` | encrypted text | 加密密码，可为空 |
| `domain_suffixes_json` | longtext | 规范化后的域名后缀数组 |
| `priority` | integer | 优先级 |
| `status` | varchar | `enabled` 或 `disabled` |
| `remark` | longtext | 备注 |
| `created_by` | integer | 创建人 |
| `created_at` | datetime | 创建时间 |
| `updated_at` | datetime | 更新时间 |

账号密码使用现有字段级 AES-GCM 机制，数据库原始值不得出现明文。

### 8.2 提版任务扩展字段

`product_asset_release_tasks` 增加：

- `proxy_snapshot`：加密的代理配置快照，包含任务执行所需的完整代理参数。
- `proxy_summary`：不含凭据的 JSON 摘要，包含域名、主机名、匹配后缀、代理 ID 和代理名称。

`product_asset_release_records` 增加 `proxy_summary`，用于历史展示。

快照保存后，代理配置被修改或删除不影响该任务；新任务读取新配置。

## 9. API 设计

所有 API 均位于 `/api` 下，需登录并执行菜单权限校验。

### 9.1 代理 API

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/api/product-release-proxies` | 查询代理列表 |
| GET | `/api/product-release-proxies/:id` | 查询代理详情 |
| POST | `/api/product-release-proxies` | 创建代理 |
| PUT | `/api/product-release-proxies/:id` | 更新代理 |
| DELETE | `/api/product-release-proxies/:id` | 删除代理 |

列表查询参数：`name`、`status`、`domain_suffix`。

创建请求示例：

```json
{
  "name": "默认出口",
  "protocol": "http",
  "host": "proxy.example.net",
  "port": 8080,
  "username": "optional-user",
  "password": "optional-password",
  "domain_suffixes": ["*"],
  "priority": 100,
  "status": "enabled",
  "remark": "默认出口"
}
```

示例仅使用占位值，不代表真实凭据。创建成功返回代理 ID；列表和详情只返回 `username_mask`、`has_password`
和 `has_auth` 等安全字段。

更新规则：

- 未提交账号或密码：保持原值。
- 提交 `clear_username=true`：清除账号。
- 提交 `clear_password=true`：清除密码。
- 其他字段按部分更新处理，并重新校验最终值。

### 9.2 提版 API 扩展

创建提版：`POST /api/product-assets/:id/releases`

成功返回 `202`：

```json
{
  "id": 123,
  "status": "pending",
  "validated": true,
  "proxy_summary": [
    {
      "field": "api_domain",
      "domain": "https://api.example.com",
      "hostname": "api.example.com",
      "proxy_id": 1,
      "proxy_name": "默认出口",
      "matched_suffix": "*"
    }
  ]
}
```

任务查询 `GET /api/product-asset-release-tasks/:id` 和历史查询
`GET /api/product-assets/:id/releases` 只返回 `proxy_summary`，不返回 `proxy_snapshot`。

### 9.3 错误约定

| HTTP 状态 | 场景 |
| --- | --- |
| 400 | 字段不合法、域名未匹配、缺少默认代理、提版前置条件不满足 |
| 401 | 未登录 |
| 403 | 无菜单权限或只读账号写操作 |
| 404 | 代理、产品资产或提版任务不存在 |
| 409 | 同一 APPID 已有排队中或执行中的提版任务 |
| 502 | 任务转发器没有找到目标代理或上游代理失败 |

缺代理错误至少包含缺失域名和代理 Tab 配置引导，不包含账号密码或完整代理 URL。

## 10. 任务级代理路由器

### 10.1 设计

服务器进程不设置全局 `HTTP_PROXY`、`HTTPS_PROXY` 或 `ALL_PROXY`。每个提版任务独立创建：

- 监听地址：`127.0.0.1`。
- 端口：操作系统分配的临时端口。
- 路由数据：任务创建时的代理快照。
- 注入范围：`upload.js` 子进程环境。
- `NO_PROXY`/`no_proxy`：任务子进程中清空，避免绕过域名匹配。

### 10.2 HTTP 请求

`upload.js` 访问 HTTP 目标时，子进程将请求发往本地转发器。转发器读取绝对 URL 或 Host，选择快照中的
上游代理，并以代理请求方式转发，同时注入上游代理所需的 Basic 认证头。

### 10.3 HTTPS CONNECT

`upload.js` 访问 HTTPS 目标时，转发器接收 `CONNECT host:port`，按目标 hostname 选择上游代理，再向上游
代理建立 CONNECT 隧道。只有上游返回 `200` 后才向子进程建立成功隧道。

### 10.4 关闭与资源释放

以下情况统一进入清理逻辑：

- 成功。
- 第一次失败后重试。
- 最终失败。
- 任务取消。
- 任务启动、代理启动或脚本执行异常。
- 服务重启后的任务恢复处理。

清理动作包括关闭本地 HTTP 服务器、销毁活动 socket、清理子进程环境引用和记录安全的关闭日志。关闭后端口
不得继续接受请求。

## 11. 安全与隐私

### 11.1 凭据保护

- 账号密码写入数据库前使用字段级 AES-GCM 加密。
- 列表、详情、任务、历史、错误响应不返回密码明文。
- 任务日志不记录代理 URL、账号密码、`Proxy-Authorization` 或子进程完整环境。
- 测试、文档和示例只使用占位凭据。
- 任务快照允许服务端在执行时解密，但接口序列化必须主动移除快照。

### 11.2 网络隔离

- 提版任务必须有匹配代理，不允许缺代理直连。
- 临时转发器只监听回环地址，外部网络不能直接接入。
- 任务之间不共享转发端口或路由快照。
- 服务器其他请求不继承任务代理环境。
- `NO_PROXY` 清空，避免脚本通过环境变量绕过任务代理。

### 11.3 审计

可记录：操作人、配置 ID、操作类型、状态变化、域名数量、代理名称和任务 ID。

不得记录：密码、完整认证头、完整代理连接串、加密快照、身份密钥内容和第三方 Token。

## 12. 非功能要求

- 代理列表查询不得在返回行循环中产生 N+1 数据库查询。
- 代理列表和提版创建接口遵守现有 API 性能门禁，主要只读接口 p95 目标不超过 300ms。
- 页面继续使用一级路由懒加载，生产构建首屏 JavaScript gzip 不超过 400KB，单个异步 chunk 不超过 500KB。
- 数据库迁移必须幂等，支持旧提版表新增字段。
- MySQL 为运行时数据库，测试使用隔离库，不得连接生产业务库。
- 前端窄屏下表单、表格、详情和提版进度不得出现不可操作的溢出或重叠。

## 13. 验收标准

### 13.1 功能验收

- `/product-templates` 可稳定切换模版和代理 Tab，刷新后代理 Tab 状态可恢复。
- 代理支持创建、查询、编辑、删除、启停和密码重新配置。
- `example.com`、`*.example.com`、`*` 规则和最长后缀优先级正确。
- 同长度规则按优先级、更新时间、ID 正确选择。
- 普通模版匹配三类域名，短剧模版匹配四类域名。
- 缺少具体代理或默认代理时提版返回 `400`，不创建任务。
- HTTP 普通请求、HTTPS CONNECT、多域名不同代理均可路由。
- 成功、失败、重试和取消后本地转发器关闭。

### 13.2 安全验收

- 列表和详情无 `password` 明文。
- 数据库账号密码为密文。
- 任务和历史无 `proxy_snapshot` 明文和密码。
- 日志无密码、认证头和完整代理连接串。
- readonly/guest 无法通过直接 API 写入。
- 代理不匹配时无直连路径。

### 13.3 测试验收

测试用例详见 `产品模版提版代理测试用例.md`，至少覆盖：

- 代理字段校验、CRUD、脱敏和权限。
- 域名规范化、边界、通配符、最长后缀和排序。
- HTTP 转发、HTTPS CONNECT、多域名、多代理、认证和关闭端口。
- 提版成功、失败、快照、历史摘要和缺代理阻断。
- 前端 Tab、筛选、CRUD、只读态、提版摘要和错误展示。
- `node --check`、后端全量测试、前端全量测试、隔离 MySQL 启动迁移、生产构建和性能门禁。

## 14. 当前实现与后续规划

### 14.1 当前已实现

- 产品模版页面“模版 / 代理”双 Tab。
- `product_release_proxies` 表及五个代理 API。
- HTTP/HTTPS 代理配置、加密账号密码和安全序列化。
- 按域名匹配的任务级临时转发器。
- 提版任务代理快照、摘要、生命周期清理和前端展示。
- 单元测试、隔离提版集成测试、前端回归测试和交接文档。

### 14.2 后续规划

- 增加代理连通性检测和可选的管理员手动测试按钮，不自动访问业务域名。
- 增加代理配置变更历史和审计详情，但继续禁止记录凭据。
- 为任务增加结构化路由统计：域名、请求数量、失败数量和上游响应耗时。
- 在专用测试环境验证真实 HTTPS 上游代理证书和真实上传链路。
- 评估任务队列、服务重启恢复和跨进程任务执行，不改变当前任务快照不变量。
