# 资产管理模块 PRD

## 1. 背景

运营侧目前需要维护多类长期资产，包括产品应用、预算状态、媒体状态、公司主体、账号等。现阶段最明确的需求是管理“产品资产”：记录不同预算线下的应用名称、公司主体、上线状态、投放状态，以及应用是否被上游核减过、核减原因分析和对应处理策略。

如果仅把“产品资产”放在业务流转下，后续新增预算状态、媒体状态等能力时，菜单结构会逐渐分散。因此建议新增一级菜单“资产管理”，将产品、预算、媒体等长期维护对象统一归口。

## 2. 模块定位

资产管理模块用于沉淀运营资产台账，帮助运营人员快速查询、维护和复盘不同资产的当前状态、历史变化和关键问题。

当前已实现“主体管理”“产品资产”“产品模版”和“媒体管理”。主体管理作为公司主体主数据，产品资产中的公司主体必须关联主体管理中的主体记录，避免纯文本带来的错别字、同名主体和筛选不稳定问题。媒体管理用于维护媒体接入台账，并为每条媒体记录关联一篇完整的文档中心文档。

## 3. 菜单结构

一级菜单：资产管理

子菜单：

- 主体管理
- 产品资产
- 产品模版
- 媒体管理
- 预算状态（后续）
- 账号资产（后续，可选）

当前已上线：

- 资产管理
  - 主体管理
  - 产品资产
  - 产品模版
  - 媒体管理

建议路由：

- `/company-subjects`
- `/product-assets`
- `/product-templates`
- `/media-management`

建议模块 key：

- `company_subjects`
- `product_assets`
- `media_assets`

## 4. 第一阶段范围

当前范围包含主体管理、产品资产管理和媒体管理：

- 主体列表
- 新增主体
- 编辑主体
- 主体详情
- 主体证照附件上传
- 从主体跳转产品资产并自动按主体筛选
- 产品资产列表
- 新增产品资产
- 编辑产品资产
- 产品资产详情
- 产品资产 CSV 导入
- 产品资产导入模板下载
- 核减记录管理
- 核减原因分析
- 关联现有策略模块
- 基础筛选和统计
- 媒体台账的新增、编辑、查看和删除
- 媒体全字段搜索、枚举筛选和预算多选筛选
- 每条媒体关联文档的块编辑、目录、附件、共享、版本记录、页面编辑记录和历史恢复

第一阶段暂不做：

- 自动同步第三方平台数据
- 预算消耗自动计算
- 复杂经营看板
- 审批流
- 自动预警
- 多版本策略评估模型

## 5. 核心对象

### 5.1 主体管理

主体管理用于维护集团及其公司主体主数据。产品资产必须关联主体记录。

建议字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| 集团名字 | 文本 | 否 | 主体所属集团或业务集团名称 |
| 公司主体 | 文本 | 是 | 法律主体或经营主体名称，建议唯一 |
| 小程序个数 | 数字 | 否 | 该主体名下小程序数量，可手填 |
| 法人 | 文本 | 否 | 法定代表人姓名 |
| 法人电话 | 文本 | 否 | 法人联系电话 |
| 邮箱 | 文本 | 否 | 主体常用联系邮箱 |
| 备注 | 长文本 | 否 | 其他说明 |
| 创建人 | 用户 | 自动 | 系统记录 |
| 创建时间 | 时间 | 自动 | 系统记录 |
| 更新时间 | 时间 | 自动 | 系统记录 |

主体附件建议按类型管理，允许每个类型上传一个或多个文件：

| 附件类型 | 说明 |
| --- | --- |
| 法人身份证正反面照片 | 身份证正面、反面照片或扫描件 |
| 营业执照 | 营业执照图片或 PDF |
| 增值电信业务经营许可证 | ICP / 增值电信业务经营许可证 |
| 网络文化经营许可证 | 网络文化经营许可证 |
| 广播电视节目制作许可证 | 广播电视节目制作经营许可证 |
| 其他 | 其他主体相关资料 |

说明：

- “法人”“法人电话”“邮箱”属于主体基础字段，不作为附件类型。
- 证照资料属于附件，需记录附件类型、文件名、上传人、上传时间。
- 主体删除前需要检查是否已被产品资产引用。建议第一版不允许删除已被引用主体，或仅支持停用。

### 5.2 产品资产

产品资产是主对象，表示一个运营侧长期维护的应用或产品。

建议字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| 集团名字 | 文本 / 自动带出 | 否 | 从关联主体带出，列表冗余展示 |
| 应用名称 | 文本 | 是 | 产品或应用名称 |
| 预算类型 | 枚举 | 是 | 支小、抖小、微小等 |
| 公司主体 | 关联主体 | 是 | 必须选择主体管理中的主体记录 |
| 主体 ID | 关联 ID | 是 | `company_subjects.id`，用于精确筛选 |
| 平台 | 枚举 | 否 | Android、iOS、H5、小程序、快应用等 |
| APPID | 文本 | 否 | 小程序 AppID、应用 AppID 等 |
| 应用标识 | 文本 | 否 | 包名、小程序 ID 或其他平台标识 |
| 上线状态 | 枚举 | 是 | 未上线、已上线可用、已上线不可用、投放中、暂停投放、已下线 |
| 运营负责人 | 用户 | 否 | 当前负责维护的人 |
| 备注 | 文本 | 否 | 补充说明 |
| 创建人 | 用户 | 自动 | 系统记录 |
| 创建时间 | 时间 | 自动 | 系统记录 |
| 更新时间 | 时间 | 自动 | 系统记录 |

预算类型建议：

- 支小
- 抖小
- 微小
- 快应用
- H5
- 其他

上线状态建议：

- 未上线
- 已上线可用
- 已上线不可用
- 投放中
- 暂停投放
- 已下线

产品资产与主体关系：

- 新增 / 编辑产品资产时，“公司主体”使用下拉选择，来源为主体管理记录。
- 选择主体后自动带出“集团名字”和“公司主体名称”用于展示。
- 产品资产保存主体关联 ID，避免只靠文本匹配。
- 从主体管理点击某一主体时，跳转产品资产页面并自动携带主体 ID 筛选。

### 5.3 产品资产导入

产品资产支持 CSV 批量导入，并提供模板下载。

导入能力：

- 支持 `.csv` 文件。
- 页面提供“下载导入模板”。
- 上传后校验必填字段、枚举值和主体匹配结果。
- 导入前可先预览解析结果，错误行需提示原因。
- 导入成功后刷新产品资产列表。

模板建议字段：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| 集团名字 | 否 | 用于辅助匹配主体；主体重名时建议填写 |
| 公司主体 | 是 | 必须能匹配主体管理中的主体记录 |
| 应用名称 | 是 | 产品或应用名称 |
| APPID | 否 | 小程序或应用 APPID |
| 预算类型 | 是 | 支小、抖小、微小、快应用、H5、其他 |
| 平台 | 否 | Android、iOS、H5、小程序、快应用、其他 |
| 应用标识 | 否 | 包名、小程序 ID 等 |
| 上线状态 | 是 | 未上线、已上线可用、已上线不可用、投放中、暂停投放、已下线 |
| 运营负责人 | 否 | 可填写用户名或显示名，系统匹配用户 |
| 备注 | 否 | 补充说明 |

主体匹配规则：

- 优先用“公司主体”精确匹配主体记录。
- 如存在重名主体，则使用“集团名字 + 公司主体”联合匹配。
- 找不到主体时，该行导入失败，不自动创建主体，避免主数据污染。

### 5.4 核减记录

核减记录挂在产品资产下。一个产品资产可以有多条核减记录。

建议字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| 所属产品资产 | 关联 | 是 | 关联产品资产 |
| 核减日期 | 日期 | 是 | 发生核减的日期 |
| 核减方 / 上游 | 文本 | 否 | 平台、渠道、客户等 |
| 核减前预算 | 数字 | 否 | 核减前预算值 |
| 核减后预算 | 数字 | 否 | 核减后预算值 |
| 核减金额 | 数字 | 否 | 可由前后预算计算，也可手填 |
| 核减比例 | 数字 | 否 | 可由前后预算计算，也可手填 |
| 处罚对象 | 枚举 | 否 | 主体、应用、广告位 |
| 核减原因分类 | 枚举 | 是 | 标准化原因 |
| 原因分析 | 长文本 | 否 | 详细分析 |
| 影响范围 | 长文本 | 否 | 对投放、收入、资源等影响 |
| 当前状态 | 枚举 | 是 | 待分析、处理中、已恢复等 |
| 负责人 | 用户 | 否 | 当前跟进人 |
| 附件 | 文件 | 否 | 截图、通知、邮件等 |
| 创建人 | 用户 | 自动 | 系统记录 |
| 创建时间 | 时间 | 自动 | 系统记录 |
| 更新时间 | 时间 | 自动 | 系统记录 |

核减原因分类建议：

- 数据质量问题
- ROI 不达标
- 消耗异常
- 转化异常
- 上游政策调整
- 预算整体收缩
- 产品 / 素材问题
- 结算 / 合规问题
- 竞争挤压
- 其他

核减状态建议：

- 待分析
- 已分析
- 处理中
- 持续观察
- 已恢复
- 已关闭

处罚对象建议：

- 主体
- 应用
- 广告位

### 5.5 关联策略

产品资产模块不单独建设“应对策略”表，也不在资产模块内重复维护策略内容。

策略统一复用现有“策略”模块。核减记录只负责描述问题，策略模块负责承接解决方案、执行过程和复盘结果。

策略模块需要扩展来源关联能力：

- 策略可以不关联任何来源，独立新增
- 策略可以关联商机 / 线索
- 策略可以关联产品资产下的一条核减记录

实现口径：

- `strategies.source_type = 'lead'` 表示关联线索
- `strategies.source_type = 'asset_reduction'` 表示关联核减记录
- `strategies.source_id` 存对应来源记录 ID

在核减记录下可以查看已关联策略，并可以快捷跳转到策略模块新增一条关联当前核减记录的策略。

策略执行状态建议：

- 待执行
- 执行中
- 已完成
- 无效关闭

效果判断建议：

- 有效
- 部分有效
- 无效
- 待观察

### 5.6 媒体管理

媒体管理用于维护媒体接入、版本、预算、对接进度和特殊配置台账。CID 使用文本存储，必须为 1-20 位数字并保留前导零。

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| CID | 数字文本 | 是 | 1-20 位数字，保留前导零，唯一 |
| 媒体 | 文本 | 是 | 媒体名称，同时作为关联文档标题 |
| 重要程度 | 单选 | 是 | 重点、中等、一般 |
| 类目 | 单选 | 是 | 电商、工具、资讯、音视频、出行服务、返利、生活服务、网赚、小说、校园、游戏、支付宝小程序、其他 |
| YYZ版本 | 单选 | 是 | 支付宝H5、API（H5+回调）、CPD-API、SDK-数据版+UI版、SDK-数据版、SDK-UI版 |
| 域名 | 文本 | 否 | 媒体域名 |
| 版本号 | 文本 | 否 | 最新业务版本号 |
| 最新媒体发版时间 | 日期 | 否 | `YYYY-MM-DD` |
| 最新支持功能 | 长文本 | 否 | 最新版本能力说明 |
| 展示样式 | 单选 | 是 | 单预算独立、平铺、YYZ聚合、YYZ聚合-单预算独立、YYZ聚合-平铺 |
| 预算 | 多选 | 否 | H5、微小、支小、淘小、许愿星、CPA-普通、CPA-拉新拉活、CPA-微小、自研APP |
| UV量级 | 文本 | 否 | 媒体流量量级 |
| 对接进度 | 单选 | 是 | 待对接、对接中、测试中、搁浅、跑量中、无量、下线 |
| 负责人 | 用户单选 | 否 | 选择有效系统用户；负责人自动加入关联文档共享人 |
| 上线时间 | 日期 | 否 | `YYYY-MM-DD` |
| 是否有鉴黄API功能 | 可空单选 | 否 | 是、已告知但暂不对接、已沟通待反馈 |
| APPID-SDK UI版 | 文本 | 否 | 最多 32 个字符 |
| 任务配置要求 | 长文本 | 否 | 配置要求说明 |
| 特殊入口信息 | 长文本 | 否 | 特殊入口说明 |
| 其他特殊记录 | 长文本 | 否 | 其他补充记录 |

列表行为：

- 搜索框匹配全部媒体字段、负责人名称和关联文档正文。
- 重要程度、类目、YYZ版本、展示样式、对接进度、负责人、鉴黄API支持单选筛选；预算支持多选且按同时包含匹配。
- 桌面列表支持横向滚动和列宽拖动；双击任意记录打开约 2/3 屏宽的详情抽屉。
- 详情上半部分展示全部媒体字段，下半部分直接复用文档中心编辑器。

文档关联与权限：

- 新建媒体时自动创建关联文档，固定归档到
  `国内项目 / 产运 / 落地 / YYZ / 媒体对接`，文档业务字段为
  `domain=domestic_project`、`department_key=OPS`、`doc_type=IMP`，编号使用
  `D{序号}-DOMESTIC-OPS-IMP-{年份}`。
- 媒体关联文档使用文档中心的普通默认图标，不使用媒体播放箭头；收藏后在文档列表中也与
  其他普通文档保持一致。
- 已有关联文档在服务启动时幂等迁移到上述目录和编号，并清理旧的 `GEN/ALL/MEDIA` 元数据及
  `media` 图标；正文、附件、共享、收藏和历史记录不变。
- 媒体文档身份以 `media_assets.document_id` 关系判断，不再依赖 `doc_type=MEDIA`；媒体名称与
  文档标题保持同步，文档中心不得绕过媒体管理单独改名。
- 关联文档提供块编辑、目录、附件、共享、版本记录、页面编辑记录和历史恢复等完整能力。
- 通过媒体管理入口权限校验的用户统一读取全部未删除媒体记录及其关联文档、正文、附件和历史，
  不再按关联文档的创建人或共享范围缩减列表；因此不同授权用户在相同筛选条件下看到的记录集合
  必须一致。除 `readonly/guest` 外，入口用户默认可编辑媒体信息和关联文档，无需逐篇手工共享；
  `readonly/guest` 保持只读。
- 所有有效入口用户动态显示在关联文档共享弹窗的“个人”和“有权限成员”中，并标记为媒体管理菜单
  系统授权。系统授权不可在单篇文档中移除，也不写入普通共享表；菜单取消或账号停用后立即消失。
  用户原本存在的手工个人共享独立保存，不因菜单授权增加或取消而被覆盖。菜单入口判定和动态成员
  名单必须读取数据库当前权限，不能因服务多进程各自持有运行时缓存而延迟生效。
- 删除媒体仅允许 CEO、COO、CTO、CMO，或 `role=leader` 且在系统小组中实际负责“流量商务”
  商务小组的用户。普通管理员、媒体创建人、共享协作者、预算商务组长和其他部门组长均不展示
  删除入口，服务端删除接口同步返回 `403`。
- 删除媒体时软删除关联文档，使正文、历史和附件停止展示但保留审计数据。

## 6. 页面设计

### 6.1 产品资产列表

用于运营人员日常查看和维护产品资产。

顶部统计卡片：

- 全部产品资产数
- 已上线数量
- 投放中数量
- 被核减产品数
- 处理中核减数

列表字段：

- 产品资产 ID
- 集团名字
- 应用名称
- APPID
- 预算类型
- 公司主体
- 平台
- 上线状态
- 运营负责人
- 是否被核减过
- 最近核减日期
- 最近核减状态
- 更新时间
- 操作

筛选项：

- 预算类型
- 集团名字
- 公司主体
- APPID
- 平台
- 上线状态
- 是否被核减过
- 运营负责人
- 最近核减状态
- 创建 / 更新时间范围

操作：

- 新增产品资产
- 导入产品资产
- 下载导入模板
- 编辑
- 查看详情
- 新增核减记录

### 6.2 主体管理列表

用于维护公司主体主数据。

列表字段：

- 集团名字
- 公司主体
- 小程序个数
- 法人
- 法人电话
- 邮箱
- 附件数量
- 关联产品数
- 更新时间
- 操作

筛选项：

- 集团名字
- 公司主体
- 法人
- 邮箱

操作：

- 新增主体
- 编辑主体
- 查看详情
- 上传附件
- 查看关联产品

点击主体行或“查看关联产品”时，跳转到产品资产页面，并自动按该主体筛选。

### 6.3 主体详情

主体详情展示：

- 集团名字
- 公司主体
- 小程序个数
- 法人
- 法人电话
- 邮箱
- 备注
- 关联产品资产列表入口
- 附件列表

附件列表展示：

- 附件类型
- 文件名
- 上传人
- 上传时间
- 下载 / 查看
- 删除

### 6.4 产品资产详情

详情页建议使用 Tab：

- 基本信息
- 核减记录
- 关联策略

基本信息展示：

- 集团名字
- 应用名称
- 预算类型
- 公司主体
- APPID
- 平台
- 应用标识
- 上线状态
- 运营负责人
- 备注
- 创建信息

核减记录展示：

- 按核减日期倒序
- 支持新增、编辑、查看详情
- 每条核减记录展示处罚对象、原因分类、状态、负责人、核减金额 / 比例

关联策略展示：

- 可按核减记录聚合展示
- 展示策略标题、执行状态、效果判断、负责人
- 支持跳转策略详情
- 支持从核减记录快捷新增关联策略

### 6.5 核减记录列表（第二阶段可独立）

如果核减事件数量较多，可以在第二阶段独立成一个列表入口。

列表字段：

- 应用名称
- 集团名字
- 预算类型
- 公司主体
- 核减日期
- 处罚对象
- 核减原因分类
- 核减金额 / 比例
- 当前状态
- 负责人
- 策略数量
- 最新效果判断

## 7. 业务流程

### 7.1 新增产品资产

1. 运营人员进入资产管理 - 产品资产
2. 点击新增产品资产
3. 选择公司主体，系统自动带出集团名字和主体名称
4. 填写应用名称、APPID、预算类型、上线状态等信息
5. 提交后进入产品资产列表

### 7.2 导入产品资产

1. 运营人员进入资产管理 - 产品资产
2. 点击下载导入模板
3. 按模板填写集团名字、公司主体、应用名称、APPID、预算类型等信息
4. 点击导入并上传 CSV
5. 系统解析文件，校验主体是否存在、枚举值是否合法、必填项是否完整
6. 用户确认后批量写入产品资产
7. 导入完成后刷新列表，并展示成功 / 失败数量

### 7.3 新增主体

1. 运营人员进入资产管理 - 主体管理
2. 点击新增主体
3. 填写集团名字、公司主体、小程序个数、法人、法人电话、邮箱等基础信息
4. 提交后进入主体列表或主体详情

### 7.4 上传主体资料

1. 进入主体详情
2. 在附件区域选择附件类型
3. 上传对应证照或其他资料
4. 系统记录文件、附件类型、上传人和上传时间

### 7.5 从主体查看产品

1. 在主体管理列表点击某一主体
2. 跳转到产品资产页面
3. 产品资产页面自动按 `company_subject_id` 筛选
4. 列表展示该主体下所有产品资产

### 7.6 记录核减事件

1. 在产品资产详情或列表中点击新增核减记录
2. 填写核减日期、处罚对象、核减方、核减前后预算、原因分类、原因分析
3. 保存后，该产品资产标记为“被核减过”
4. 最近核减日期和最近核减状态自动更新

### 7.7 关联策略

1. 在核减记录下点击新增关联策略
2. 跳转到策略模块，并自动带入来源类型 `asset_reduction` 和核减记录 ID
3. 在策略模块内填写策略标题、维度、负责人、策略描述等信息
4. 策略执行和复盘继续沿用现有策略模块能力
5. 核减记录中展示已关联策略数量和策略列表

### 7.8 核减状态流

建议状态流：

待分析 -> 已分析 -> 处理中 -> 持续观察 -> 已恢复 / 已关闭

说明：

- 待分析：刚记录，还没有明确原因
- 已分析：已完成原因分析
- 处理中：已有策略并正在执行
- 持续观察：策略已执行，但效果需要观察
- 已恢复：预算或状态恢复
- 已关闭：问题结束但未恢复，或不再继续处理

## 8. 权限设计

复用现有用户和角色体系。

建议权限：

| 角色 | 权限 |
| --- | --- |
| 普通运营 | 查看组内产品资产；新增、编辑自己负责的产品资产和核减记录 |
| 组长 / 负责人 | 查看和维护团队范围内的产品资产 |
| 管理员 | 查看和维护全部数据 |
| 只读用户 | 仅查看 |

如果已有跨团队权限模块，后续可以将 `product_assets` 纳入跨团队授权范围。

主体管理权限建议：

| 角色 | 权限 |
| --- | --- |
| 普通运营 | 查看主体；可上传与自己业务相关的主体资料 |
| 组长 / 负责人 | 查看主体；维护团队范围内主体资料 |
| 管理员 | 新增、编辑、删除 / 停用主体，管理全部附件 |
| 只读用户 | 仅查看 |

## 9. 数据表建议

### 9.1 company_subjects

```sql
CREATE TABLE company_subjects (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  group_name TEXT,
  company_entity TEXT NOT NULL,
  mini_program_count INTEGER DEFAULT 0,
  legal_person TEXT,
  legal_person_phone TEXT,
  email TEXT,
  remark TEXT,
  status TEXT DEFAULT 'active',
  created_by INTEGER,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 9.2 company_subject_attachments

```sql
CREATE TABLE company_subject_attachments (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  subject_id INTEGER NOT NULL,
  attachment_type TEXT NOT NULL,
  file_name TEXT NOT NULL,
  file_path TEXT NOT NULL,
  file_size INTEGER,
  mime_type TEXT,
  uploaded_by INTEGER,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

附件类型建议：

```text
legal_person_id_card_front
legal_person_id_card_back
business_license
icp_license
network_culture_license
radio_tv_program_license
other
```

### 9.3 product_assets

```sql
CREATE TABLE product_assets (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  group_name TEXT,
  company_subject_id INTEGER NOT NULL,
  app_name TEXT NOT NULL,
  appid TEXT,
  budget_type TEXT NOT NULL,
  company_entity TEXT NOT NULL,
  platform TEXT,
  app_identifier TEXT,
  launch_status TEXT NOT NULL DEFAULT 'not_launched',
  owner_id INTEGER,
  remark TEXT,
  created_by INTEGER,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

说明：

- `company_subject_id` 是产品资产和主体管理的主关联字段。
- `group_name`、`company_entity` 可作为冗余展示字段，便于列表和导入结果快照展示。
- 主体名称变化时，产品资产展示可选择实时 join 主体表，或批量同步冗余字段。

### 9.4 product_asset_reductions

```sql
CREATE TABLE product_asset_reductions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  asset_id INTEGER NOT NULL,
  reduction_date DATE NOT NULL,
  upstream TEXT,
  before_budget REAL,
  after_budget REAL,
  reduction_amount REAL,
  reduction_ratio REAL,
  punishment_object TEXT,
  reason_type TEXT NOT NULL,
  reason_analysis TEXT,
  impact_scope TEXT,
  status TEXT NOT NULL DEFAULT 'pending_analysis',
  owner_id INTEGER,
  created_by INTEGER,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 9.5 media_assets

```sql
CREATE TABLE media_assets (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  cid TEXT NOT NULL UNIQUE,
  media_name TEXT NOT NULL,
  importance TEXT NOT NULL DEFAULT 'general',
  category TEXT NOT NULL,
  yyz_version TEXT NOT NULL,
  domain_name TEXT,
  version_number TEXT,
  latest_release_date DATE,
  latest_features TEXT,
  display_style TEXT NOT NULL,
  budget_types TEXT,
  uv_scale TEXT,
  integration_progress TEXT NOT NULL DEFAULT 'pending',
  owner_id INTEGER,
  launch_date DATE,
  porn_api_status TEXT,
  sdk_ui_appid TEXT,
  task_config_requirements TEXT,
  special_entry_info TEXT,
  other_notes TEXT,
  document_id INTEGER NOT NULL UNIQUE,
  created_by INTEGER NOT NULL,
  updated_by INTEGER NOT NULL,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

敏感自由文本字段走统一 AES-GCM 字段加密；枚举、日期、外键、CID 和筛选字段保持可查询。

### 9.6 strategies 关联扩展

不新增产品资产专用策略表，复用现有 `strategies` 表的来源字段：

```sql
-- 关联线索
source_type = 'lead'
source_id = leads.id

-- 关联核减记录
source_type = 'asset_reduction'
source_id = product_asset_reductions.id
```

策略列表、策略详情和核减记录详情都需要能展示该关联关系。

## 10. 后续扩展

### 10.1 预算状态

用于管理不同预算线、预算账户或预算包的状态。

可能字段：

- 预算名称
- 预算类型
- 预算状态
- 归属主体
- 负责人
- 可用额度
- 当前消耗
- 状态说明

### 10.2 资产看板

后续可增加资产管理看板：

- 各预算类型产品数量
- 各状态产品数量
- 月度核减趋势
- 核减原因分布
- 策略有效率
- 高风险产品列表

## 11. 验收标准

第一版验收标准：

- 可以从一级菜单“资产管理”进入“产品资产”
- 可以新增、编辑、查看产品资产
- 产品资产列表支持基础筛选
- 产品资产详情展示基本信息、核减记录、关联策略
- 可以为产品资产新增多条核减记录
- 可以从核减记录快捷新增一条关联策略
- 策略模块可以选择关联线索或核减记录
- 列表中可以看到产品是否被核减过、最近核减日期和最近核减状态
- 权限遵循现有用户体系，普通用户不能越权编辑不属于自己的数据
- 可以从一级菜单“资产管理”进入“主体管理”
- 可以新增、编辑、查看主体
- 主体详情支持上传和查看证照附件
- 产品资产的公司主体必须关联主体管理记录
- 从主体管理点击主体可跳转产品资产，并自动筛选该主体产品
- 产品资产支持集团名字、公司主体、APPID 字段展示
- 产品资产支持 CSV 导入和模板下载
- 导入时能校验主体匹配、必填项和枚举值
- 可以从“资产管理”进入“媒体管理”
- 可以新增、编辑、查看和删除有权限的媒体记录，CID 前导零不会丢失
- 搜索能命中媒体全部字段及关联文档正文，所有单选字段和预算多选均可筛选
- 双击列表记录打开约 2/3 屏宽详情，详情展示全部媒体字段
- 媒体详情下半部分支持文档块、目录、附件、共享、版本记录、页面编辑记录和历史恢复
- 相同筛选条件下，所有通过媒体管理入口权限校验的用户看到相同记录数；访客仍需同时具备菜单和
  `product_assets` 模块读权限。可写角色无需单独共享即可编辑媒体和关联文档，`readonly/guest`
  只读；入口用户在关联文档“个人”和权限成员中动态显示，权限变更跨服务进程立即生效，取消菜单后
  系统授权立即撤销且不影响手工共享。
