# 文档中心在线表格原生基础版 PRD

## 1. 背景与决策

文档中心已经上线 `document_kind=spreadsheet` 在线表格形态，并具备自研工作簿、网格编辑、保存、
历史、权限、Excel 导入导出和协作基础。此前曾评估通过 Univer 承载完整类石墨表格能力，但实际
接入后存在明显包体和上线验证成本：

- Univer 异步 chunk gzip 超过当前前端性能门禁。
- 线上真实文档保存回读、排序、筛选、冻结、导入导出和只读权限仍需完整验收。
- 当前没有独立测试或灰度分支，直接全量切换风险较高。

2026-07-29 决策：近期在线表格改回原生自研路线，先完成基础 Excel / 石墨表格常用能力，保证线上
轻量、稳定、可控，并与 Relation 文档权限、历史和协作底座深度结合。Univer 作为后续备选技术路线
保留研究结论，但不作为本期交付依赖。

2026-07-29 补充确认：本期保留 Univer 依赖和线上预览开关，但默认关闭；正式在线表格入口继续走
原生编辑器。原生基础版 UI 仍要求以石墨表格为像素级参考对象，重点覆盖表格工作区，不复刻石墨
品牌、Logo、水印或受保护素材。

## 2. 目标

### 2.1 产品目标

- 在线表格可以承担预算、台账、名单、排期、经营数据和批量数据维护。
- 用户在文档中心创建或打开在线表格后，可以像使用轻量 Excel 一样完成录入、粘贴、整理、筛选和
  基础计算。
- 在线表格继续复用 Relation 的标题、文件夹、编号、分享、权限、收藏、历史、自动保存、多人更新
  和附件治理外壳。

### 2.2 工程目标

- 不引入重型表格运行时，生产构建继续满足首屏 JS gzip `400KB` 和单异步 chunk gzip `500KB` 门禁。
- 保留 Univer 依赖和预览开关时，默认正式入口不得加载 Univer；常规生产构建和验收需以关闭预览
  的入口为准。若构建工具因动态 import 仍打入 Univer 大 chunk，需要在实现期继续拆入口或形成
  单独预览构建策略。
- 继续使用现有 `relation_spreadsheet_workbook_v1` 工作簿格式，避免短期内迁移数据格式。
- 复杂逻辑放在 `client/src/utils/spreadsheetWorkbook.js`，交互放在
  `client/src/components/SpreadsheetDocumentEditor.js`，避免继续膨胀 `Documents.js`。
- 服务端继续通过 `server/lib/spreadsheetWorkbookFile.js`、`server/lib/spreadsheetOperations.js` 和文档
  保存接口做权限、格式、大小、历史和检索文本校验。

## 3. 需求范围

### 3.1 一期必须支持

1. 基础录入
   - 点击单元格选中，双击、直接输入或公式栏输入内容。
   - `Enter`、`Tab`、方向键提交并移动选区。
   - 支持清空单元格、连续输入自动保存、保存中再次输入不丢内容。

2. 复制粘贴
   - 支持单个单元格复制粘贴。
   - 支持从 Excel、石墨、WPS、飞书表格等复制 TSV / HTML 表格并从当前单元格开始批量粘贴。
   - 复制选区时同时写入纯文本 TSV 和 HTML table，便于粘贴回 Office 或其他在线表格。

3. 行列增删
   - 支持在选中行/列前后插入行/列。
   - 支持删除选中行/列。
   - 插入或删除后必须迁移本工作簿公式引用、合并区域、筛选范围、冻结状态、行高列宽和选区。

4. 简单公式和常用函数
   - 单元格以 `=` 开头时作为公式原文保存，展示值由安全求值器计算。
   - 禁止使用 `eval` 或执行任意 JS。
   - 一期函数至少支持：`SUM`、`AVERAGE`、`COUNT`、`COUNTA`、`MAX`、`MIN`、`IF`、`AND`、`OR`、
     `ROUND`、`ABS`、`TODAY`、`NOW`、`LEN`、`LEFT`、`RIGHT`、`MID`、`CONCAT`、`VLOOKUP`、
     `XLOOKUP`、`SUMIF`、`COUNTIF`。
   - 支持基础四则运算、括号、单元格引用、区域引用和跨 Sheet 精确引用。
   - 支持常见错误码展示：`#REF!`、`#VALUE!`、`#DIV/0!`、`#NAME?`、`#CYCLE!`。

5. 排序、普通筛选、冻结、合并
   - 支持按当前列升序、降序排序。
   - 支持普通筛选：按列值勾选、全选、清空筛选。
   - 工具栏冻结入口使用石墨式下拉菜单，按当前活动单元格动态提供“冻结至当前行（N 行）”、
     “冻结至当前列（N 列）”、“冻结至当前行和列（N 行 | N 列）”和“取消冻结”。单独冻结行或列
     只更新对应维度并保留另一维，支持先后操作组合冻结；未冻结时“取消冻结”置灰。
   - 冻结状态按 Sheet 保存，最多冻结前 100 行、50 列；操作进入自动保存、页面历史和撤销重做。
   - 支持合并单元格和取消合并；合并区域内编辑、复制、删除、排序需要有明确限制或提示。

6. 基础格式
   - 支持字体、字号、加粗、斜体、下划线、文字颜色、填充色。
   - 支持水平对齐、垂直对齐、自动换行。
   - 支持单元格边框、行高、列宽。
   - 格式改动必须进入自动保存、历史、页面编辑记录和撤销重做。

7. Excel 简单导入导出
   - 可编辑用户可以导入 `.xlsx` / `.xlsm`。
   - 可见用户可以导出 `.xlsx`。
   - 一期导入导出至少保留 Sheet、单元格原值、公式、基础样式、合并、冻结、行高列宽。
   - `.xlsm` 只保留公式和值及可解析的基础样式，不处理、不执行、不导出 VBA 宏。
   - 不支持的复杂能力必须明确降级，不得静默写坏工作簿。

8. Relation 文档底座结合
   - 复用文档中心权限：只读用户不能写入单元格、格式、行列、Sheet 或导入。
   - 复用自动保存、手动保存、关闭 Tab 前保存、历史版本、页面编辑记录、恢复历史和多人更新。
   - 复用在线表格 SSE / presence 和 5 秒轮询兜底。
   - 本地 dirty 时继续使用三方合并；同一单元格冲突保留本地并提示。

### 3.2 当前仍不做

- 不做数据透视表。
- 不做图表。
- 不做复杂条件筛选和多条件高级筛选。
- 不做完整 Excel 函数全集、数组公式、动态数组、宏和外部数据源。
- 不做附件、图片、批注等浮动对象插入。
- 不处理 `.xlsm` 的 VBA 宏，不执行宏、不保存宏、不导出宏。
- 不把 Univer 或其他重型表格运行时接入正式默认入口；已有 Univer 预览能力保留但默认关闭。

单元格锁定、条件格式和数据验证已纳入本轮增强范围，不再属于排除项；具体交互、数据结构、权限和
验收口径见第 12 节。

## 4. 页面与交互

### 4.1 布局

在线表格继续在文档中心详情页内打开：

- 顶部保留 Relation 文档标题、编号、文件夹、分享、收藏、历史和页面菜单。
- 正文区域为表格工作区，包含：
  - 轻量菜单栏。
  - 工具栏。
  - 公式栏。
  - 网格区。
  - Sheet 标签区。

桌面非嵌入场景采用全高紧凑布局：

- 多文档 Tab 压缩为固定紧凑行；文档标题、自动保存状态、文件夹路径及页面/分享/历史/收藏入口合并
  为一条治理栏，不再重复渲染大号标题、业务标签组和创建人信息。
- 表格工作区取消卡片式外边框和圆角；菜单栏、工具栏、公式栏、网格与 Sheet 区连续铺满剩余空间。
- 网格是工作区中唯一纵向伸缩和滚动区域；底部 Sheet 与缩放区固定占用布局空间，在常见桌面短屏上
  也必须完整可见，不能被网格内容推到视口之外。
- 普通文档、嵌入式文档和移动端不启用该紧凑壳，继续使用各自现有布局。

### 4.2 石墨像素级 UI 要求

原生基础版仍以石墨表格工作区为像素级参考对象，验收对象限定在表格编辑区域，不要求复刻
Relation 顶部文档治理外壳，也不得使用石墨品牌 Logo、商标、水印或受保护素材。

必须对齐的关键尺寸和状态：

- 菜单栏：高度、字号、字重、文字间距、左右内边距、hover 背景和底部分隔线。
- 工具栏：高度、图标尺寸、按钮热区、分组分隔线、禁用态、hover 态、选中态和整体密度。
- 公式栏：名称框宽度、`fx` 标识、输入框高度、边框、背景、字号和上下分隔线。
- 网格：默认行高、默认列宽、列头高度、行号宽度、字体大小、网格线颜色、背景色和滚动条位置。
- 选区：活动单元格边框、区域选中填充、填充柄、行列头高亮、复制虚线和远端选区样式。
- Sheet 区：高度、新增按钮、Sheet 标签宽度、激活态、hover 态、底部分隔线和左右滚动。
- 弹层：右键菜单、筛选面板、颜色面板、字体/字号菜单、冻结菜单等必须保持表格软件质感，不能
  出现后台表单式大弹窗。
- 当前紧凑基线为菜单栏约 `32px`、工具栏约 `34px`、公式栏约 `29px`、名称框约 `74px`、Sheet 区
  约 `34px`；Sheet 激活态使用平面标签和底部强调线，不使用大面积主色胶囊按钮。

验收方式：

- 使用石墨截图建立视觉基线。
- 用 Playwright 分别在 `1440px`、`1920px` 和一个移动宽度截图回归。
- 对菜单栏高度、工具栏高度、公式栏高度、默认行高、默认列宽、行号宽度、列头高度、字体大小、
  网格线、选区边框和 Sheet 标签高度做像素级检查。

### 4.3 工具栏

一期工具栏至少包含：

- 撤销、重做。
- 字体、字号。
- 加粗、斜体、下划线。
- 文字颜色、填充色、边框。
- 水平对齐、垂直对齐、自动换行。
- 格式刷：单击使用一次，双击连续使用，再次点击或按 `Esc` 退出。
- 合并、取消合并。
- 排序升序、排序降序、筛选。
- 冻结。
- 导入、导出。

按钮尽量使用图标，悬浮显示 Tooltip。工具栏高度、单元格行高、列头、行号和输入区域需保持紧凑，
默认行高建议 `24px`，默认列宽建议 `96px`，默认字号 `13px`。

格式刷只复制来源单元格的完整基础样式，不复制值、公式、合并关系、行高或列宽。来源为多格选区时，
按来源二维样式图案循环平铺到目标选区；来源单元格没有的样式需清除目标对应旧样式。每次目标应用
作为一个撤销步骤，连续模式可依次应用多个目标，只读状态下入口置灰。

冻结按钮采用“网格图标 + 下拉箭头”的紧凑样式，菜单数字随活动单元格更新。选择“冻结至当前行”
更新行冻结并保留已有列冻结，选择“冻结至当前列”更新列冻结并保留已有行冻结，选择“冻结至当前行
和列”同时设置两维；只读用户不可打开冻结菜单。

### 4.4 单元格编辑

- 单击选中，双击进入编辑。
- 直接输入替换当前单元格。
- 公式栏与当前单元格双向同步。
- 编辑公式时保留以 `=` 开头的公式原文。
- 只读状态下禁止进入编辑态，工具栏写操作置灰。

### 4.5 选区与菜单

- 支持单元格、连续区域、整行、整列选择。
- 点击行号选择当前 Sheet 整行，点击列字母选择整列；对应表头和全部可见选区显示连续浅蓝背景与
  蓝色边界。整行整列选择不修改工作簿，但复制、清空、格式及协作选区继续复用现有范围能力；拖动
  行高列宽手柄不得误触发整行整列选择。
- 右键菜单支持复制、粘贴、清空、插入行列、删除行列、合并、取消合并、排序、筛选、冻结。
- 本轮右键菜单扩展为剪切、复制、粘贴、插入复制的单元格、删除、排序、合并、设置单元格格式、
  锁定单元格、条件格式和数据验证；菜单分组、子菜单、禁用态及操作语义见第 12 节。
- 移动端一期只做查看和轻量编辑；不做复杂筛选、格式、行列操作和完整右键菜单。必要操作可收敛为
  少量底部操作面板。

## 5. 数据模型

继续使用现有工作簿格式：

```json
{
  "format": "relation_spreadsheet_workbook_v1",
  "activeSheetId": "sheet_1",
  "sheets": [
    {
      "id": "sheet_1",
      "name": "工作表1",
      "rowCount": 1000,
      "columnCount": 26,
      "cells": {},
      "rowHeights": {},
      "columnWidths": {},
      "mergedCells": [],
      "filters": [],
      "frozen": null,
      "protectedRanges": [],
      "conditionalFormats": [],
      "dataValidations": []
    }
  ],
  "styles": {},
  "definedNames": {}
}
```

### 5.1 单元格

单元格最小结构：

```json
{
  "v": "原始值或公式",
  "style": {}
}
```

约束：

- 公式原文始终保存在 `v`，不得用计算结果覆盖。
- 展示值可在前端派生，不直接写入工作簿。
- 单格内容大小继续受服务端限制，避免大正文塞入单元格。

### 5.2 样式

一期支持的样式字段：

- `fontFamily`
- `fontSize`
- `bold`
- `italic`
- `underline`
- `color`
- `backgroundColor`
- `horizontalAlign`
- `verticalAlign`
- `wrap`
- `border`
- `numberFormat`

未知样式字段保存前必须清理或兼容保留，不能导致旧工作簿打不开。

## 6. 保存、历史与协作

- 单元格、格式、行列、Sheet、冻结、合并、排序和筛选变化均必须标记 dirty。
- 字体、字号、粗体、斜体、下划线、文字颜色、填充色、边框、对齐、换行和数字格式的每次确认均
  作为一个独立工作簿事务进入撤销栈；一次操作作用于连续区域、整行或整列时只产生一个撤销步骤。
- `Ctrl/Cmd+Z`、`Ctrl/Cmd+Shift+Z`、`Ctrl+Y` 与工具栏撤销/重做按钮必须使用同一工作簿历史；格式
  操作不得因为公式栏、网格焦点或自动保存而落入普通文档历史或提示“没有可撤回的操作”。
- 自动保存继续防抖；关闭 Tab、切换文档、打开历史、恢复历史前必须等待当前保存完成。
- 页面编辑记录不能只比较 `content_text`，格式、行高、列宽、冻结、合并等变化也要生成记录。
- 在线表格 operation API 继续优先用于小批量变更；超过限制或未知结构变化回退到文档级保存。
- 多人协作冲突继续按远端基线、本地工作簿、远端最新工作簿做三方合并。

## 7. 权限与安全

- 未登录返回 `401`。
- 无文档可见权限返回 `403` 或 `404`，与现有文档接口保持一致。
- `readonly`、`guest` 可查看和导出，不可编辑、导入、排序、筛选、冻结、合并或修改格式。
- 前端禁用只负责体验，服务端保存、导入和 operation 接口必须独立拒绝越权。
- 系统操作日志不得记录完整工作簿、单元格批量 before/after、公式批量内容或附件临时路径。
- 所有用户输入参与 SQL 时必须走参数化查询；动态列名只能来自白名单。

## 8. 性能要求

- 生产构建首屏 JS gzip 不超过 `400KB`。
- 单个异步 chunk gzip 不超过 `500KB`。
- 打开 10 万行、500 列、1 万非空单元格的工作簿时，首屏网格可交互时间目标不超过 `1000ms`。
- 网格必须继续使用双向虚拟滚动，不能按全表渲染 DOM。
- 滚动处理必须在事件回调内同步读取 `scrollTop` / `scrollLeft`，不得在异步 state updater 里访问
  React 事件对象。
- 大范围粘贴、排序和导入需要分批处理或显示进度，不能长时间阻塞主线程无反馈。

## 9. 验收标准

1. 新建在线表格后可以录入、保存、刷新回读。
2. 从 Excel / 石墨复制 3 行 3 列内容粘贴到当前单元格，位置和内容正确。
3. 复制 Relation 表格选区后可以粘贴到 Excel，并保持矩阵结构。
4. 插入和删除行列后，公式引用、合并区域、冻结和选区保持正确。
5. `SUM`、`AVERAGE`、`COUNT`、`IF`、`VLOOKUP`、`XLOOKUP`、`SUMIF`、`COUNTIF`、跨 Sheet 引用和
   除零错误展示正确。
6. 按某列升序、降序排序后，关联行数据不串行。
7. 普通筛选可以按列值勾选，并可清除恢复。
8. 冻结首行、首列和自定义冻结后，滚动时冻结区域稳定。
9. 合并和取消合并可以保存、刷新回读。
10. 字体、字号、颜色、对齐、边框、行高列宽可以保存、刷新回读。
11. Excel 导入导出保留基础数据、公式、样式、合并、冻结和 Sheet。
12. `.xlsm` 导入不执行宏，导出不保留宏，并向用户明确提示宏不处理。
13. 只读用户无法编辑、导入或调用写接口，但可以导出。
14. 两个用户改不同单元格可以自动合并；同一单元格冲突保留本地并提示。
15. 打开历史前会先保存当前草稿；恢复历史后重新生成版本。
16. 默认关闭 Univer 预览时，正式入口使用原生编辑器；预览开关可以保留但不得默认影响用户。
17. 生产构建和性能门禁通过；正式默认入口不得加载 Univer 大 chunk。
18. 对单格、连续区域、整行或整列设置填充色、粗体、斜体等格式后，撤销一次恢复操作前的完整样式，
    重做一次再次应用；值、公式和未选中单元格不受影响。
19. 整列或普通区域升序、降序均保留标题，空白单元格始终位于有效数据之后；降序不能把有效数据
    变成空白或仅显示空白选区，关联行、公式、样式和验证规则不得串位。
20. 选中公式单元格时公式栏展示以 `=` 开头的公式原文，不展示计算值；同 Sheet 引用的单元格或区域
    使用与公式引用标记一致的彩色虚线框，普通文本中的伪坐标不得被识别为引用。
21. 当前声明的运算符、函数、引用和错误码通过第 12.3 节公式矩阵；依赖单元格变化、跨 Sheet 引用、
    插入删除行列、Sheet 重命名、排序、撤销重做和刷新回读后，公式原文和计算结果均正确。
22. 从石墨复制的数据若剪贴板包含公式元数据，粘贴后必须保留公式原文、引用和基础格式；若来源只
    提供计算值，必须明确提示“源内容未提供公式，仅粘贴结果值”，不得推测或伪造公式。
23. 在选区内右键不改变选区，在选区外右键先选中目标单元格；右键菜单及二级菜单始终落在可视区，
    关闭后恢复网格焦点，不能留下紫色或其他异常持久焦点框。
24. 剪切、复制、粘贴、插入复制的单元格、删除、排序、合并和设置单元格格式均可通过右键完成，
    每次成功写操作均可撤销、自动保存、刷新回读并生成页面编辑记录。
25. 锁定区域后无解锁权限的编辑者无法通过输入、粘贴、清空、排序、行列操作、导入或直接 API 修改；
    有管理权限者可解锁，查看和复制不受影响。
26. 条件格式可创建、编辑、停用、调整优先级和删除，规则随目标范围、历史恢复及多人更新持久化；
    条件样式只影响显示，不覆盖原始值、公式或手工样式。
27. 数据验证可配置列表、数字、日期时间、文本长度、复选框、评分、进度、身份/联系方式、温度及
    自定义公式；直接输入、粘贴和批量操作遵守同一拒绝或警告策略，规则可撤销和历史恢复。
28. 连续选择多个单元格时只显示一个范围外框，活动格保持原背景，其他格使用浅蓝底色，右下角填充柄
    可见；不得出现首格独立粗框叠在范围框内的双重选中效果。
29. 选择单列执行升降序后，当前已用数据区内的其他列按整行同步移动；日期、名称、公式、样式和验证
    仍对应原记录，标题保持原位，一次撤销恢复完整排序前数据。
30. 连续选区底栏显示总和，并可展开平均、最大、最小、计数和数值计数；公式按计算值参与，切换指标
    不修改工作簿、不触发保存或历史。

## 10. 分期计划

### 第一期：可用性补齐

- 修复不能输入、输入丢失和快速保存误判。
- 补齐复制粘贴、基础行列增删、排序、普通筛选、冻结、合并。
- 补齐基础样式、行高列宽和保存回读。
- 保留线上 Univer 预览入口和依赖但默认关闭；如现有动态 import 导致常规构建仍包含 Univer 大 chunk，
  本期需调整入口拆分或构建策略，恢复正式默认入口包体门禁。
- 建立石墨像素级 UI 基线和截图回归。

### 第二期：公式与 Excel 往返增强

- 扩展常用函数和错误传播。
- 完善插入删除行列后的跨 Sheet 公式迁移。
- 完善 Excel 样式、冻结、合并和多 Sheet 往返。

### 第三期：协作、历史和大表体验

- 优化 operation 差异粒度和冲突提示。
- 优化大范围粘贴、排序和导入的进度反馈。
- 补齐多人 presence、远端选区和历史恢复的视觉稳定性。

### 本轮增强交付顺序

1. 阻断缺陷：格式撤销/重做、降序空白、公式栏原文和公式引用高亮、工具栏异常焦点框。
2. 核心右键菜单：剪切、复制、粘贴、插入复制的单元格、删除、排序、合并、设置单元格格式。
3. 数据治理菜单：锁定单元格、条件格式、数据验证；三项必须有真实数据模型、保存、权限和历史，
   不得先放置无效果的菜单占位。

## 11. 已确认范围

1. 本期保留 Univer 依赖和线上预览开关，但默认关闭；正式默认入口继续使用原生编辑器。
2. 原生基础版 UI 仍要求像素级对齐石墨表格工作区；Relation 文档治理外壳保持自身产品风格。
3. 常用函数除基础清单外，增加 `VLOOKUP`、`XLOOKUP`、`SUMIF`、`COUNTIF`。
4. 移动端本期只做查看和轻量编辑，不做复杂筛选、格式和行列操作。
5. Excel 导入导出以 `.xlsx` 为主；`.xlsm` 只保留公式和值及可解析基础样式，不处理宏。

## 12. 2026-07 表格编辑、公式与右键菜单增强

### 12.1 格式撤销与重做

- 填充色、文字颜色、粗体、斜体、下划线、字体、字号、边框、水平/垂直对齐、自动换行和数字格式
  都属于工作簿写操作。执行前必须记录选择范围及完整 `before`，确认后记录完整 `after`。
- 单次工具栏点击、颜色面板确认或格式弹窗确认只生成一个事务；颜色悬浮预览、打开/关闭弹层、移动
  选区不得写入撤销栈。取消弹窗时工作簿和撤销栈均不变化。
- 撤销格式时只恢复本次命中的样式字段和范围，不清空单元格值、公式、批注式元数据或相邻样式；重做
  精确恢复相同结果。自动保存成功、SSE 更新或切换网格焦点后，本地尚未撤销的有效事务不得丢失。
- 多人协作下撤销只反向应用当前用户自己的最近事务；若目标位置已被远端修改，必须进入现有冲突合并
  或明确提示，不能用旧整本快照静默覆盖协作者的新内容。

### 12.2 排序正确性与菜单视觉

- 升序和降序共用同一目标范围、标题识别、类型归一和稳定排序实现，仅方向不同。整列选择时只处理
  当前已用数据区，不把声明但未使用的 1000 行空白参与有效数据排序。
- 数字按数值、日期时间按时间、文本按 `zh-CN` 自然顺序、布尔值按固定顺序比较；公式按当前计算值
  排序，但移动时保留公式原文、样式、数字格式、验证和保护元数据。
- 空白值在升序和降序中都放在非空数据之后；错误值保持稳定且不造成其他行丢失。相等值保持原先
  相对顺序。首行被识别或明确指定为标题时始终保留。
- 连续多列区域排序时整行区域一起移动；选择单列或点击列头排序时，该列只作为排序键，系统自动把
  排序范围横向扩展到当前 Sheet 已用数据区的全部列，日期、名称及其他关联字段必须随同一行一起
  移动。自定义排序需允许选择范围、是否包含标题、关键列和升/降序，并在执行前展示影响范围。
- 排序作为一个原子事务进入撤销、自动保存和页面历史；失败或取消时工作簿、更新时间和历史不变化。
- 鼠标点击排序、筛选、冻结、合并、颜色、字体等工具按钮后不得残留紫色焦点框。鼠标态使用轻量
  灰/浅蓝 hover 和 active；键盘用户仅通过 `:focus-visible` 获得清晰蓝色焦点环，不能删除可访问性
  焦点。弹层关闭后焦点回到网格或原触发器，所有同类菜单统一执行。

### 12.3 公式兼容与测试矩阵

- 选择含公式的单元格时，单元格展示计算值，公式栏始终展示原始公式，例如 `=P4/T4`。公式原文不得
  被格式化结果 `2.09` 覆盖；刷新、历史恢复、复制粘贴、排序和 Excel 往返后仍保持。
- 同 Sheet 公式引用按出现顺序分配稳定颜色，公式栏中的引用文本与网格中的单格/区域使用同色虚线
  边框；活动公式单元格继续使用实线主选区。绝对/混合引用、区域引用及重复引用需正确合并展示。
- 跨 Sheet 引用在切换到被引用 Sheet 时可继续展示对应范围；当前 Sheet 不可见的引用不得错误框选
  同名坐标。双引号字符串中的 `A1`、Sheet 名中的数字和名称前缀相似项不得高亮。
- 从石墨、Excel、WPS、飞书粘贴时，读取浏览器可见的标准 HTML/文本及来源提供的公式元数据；公式
  元数据存在时保留原文和相对位置迁移。源站未向浏览器暴露公式时只能粘贴值并明确提示，Excel 文件
  导入继续作为完整保留公式的兜底路径。
- 首轮自动化和真实文档回归至少覆盖：
  - 运算符：`+ - * / ^ &`、一元正负、括号和 `= <> < <= > >=`。
  - 引用：`A1`、`$A$1`、`A$1`、`$A1`、区域、中文/空格/单引号 Sheet 名及跨 Sheet 引用。
  - 聚合：`SUM`、`AVERAGE`、`MIN`、`MAX`、`COUNT`、`COUNTA`。
  - 逻辑：`IF`、`AND`、`OR`；数学：`ROUND`、`ABS`。
  - 文本：`LEN`、`LEFT`、`RIGHT`、`MID`、`CONCAT`。
  - 条件与查找：`SUMIF`、`COUNTIF`、`VLOOKUP`、`XLOOKUP`。
  - 日期时间：`TODAY`、`NOW`，按工作簿 `zh-CN / Asia/Shanghai` 口径计算和展示。
  - 错误：`#DIV/0!`、`#NAME?`、`#REF!`、`#VALUE!`、`#CYCLE!`，错误传播和循环链路一致。
- 修改上游值、连续依赖、跨 Sheet 依赖、插入删除行列、重命名 Sheet、排序、撤销重做、导入导出和
  刷新回读均要触发正确重算。公式解析和计算继续禁止 `eval`，长度、深度和展开单元格数量沿用上限。
- “支持公式”只指 PRD 明确列出的语法和函数；未支持函数必须稳定显示 `#NAME?` 或明确降级提示，
  不得返回看似正确的旧缓存值。

### 12.4 右键菜单通用交互

- 在已有选区内右键保持当前选区；在选区外右键先切换为目标单元格，再打开菜单。菜单锚定点击位置，
  自动向左/上翻转以留在网格可视区，二级菜单同样防溢出，不随横纵滚动漂移到屏幕外。
- 菜单使用紧凑表格软件样式，一级项高度、字号、图标、快捷键、分隔线和箭头统一；不使用大圆角卡片
  或紫色焦点框。无权限、无剪贴板内容或当前选择不适用时禁用并通过 Tooltip 说明原因。
- 只读用户保留复制，其他写操作隐藏或禁用。关闭菜单后选区不丢失，`Esc` 关闭，键盘上下键、左右键、
  Enter 和快捷键可完整操作。
- 菜单顺序和首期语义：
  1. `剪切`：写入内部结构化剪贴板和标准 TSV/HTML，显示复制虚线；仅在目标粘贴成功后清空源区域，
     `Esc` 取消剪切且不改数据。
  2. `复制`：保留值、公式、基础样式、数字格式、合并信息及可迁移规则，并同时提供标准 TSV/HTML。
  3. `粘贴`：优先使用 Relation 结构化数据，其次 HTML table，最后 TSV；矩阵从选区左上角落位。
  4. `插入复制的单元格`：提供“右移”和“下移”，先扩出等尺寸空位再粘贴；公式、合并、行高列宽
     和规则引用按结构变化迁移，整次操作原子提交。
  5. `清除`：至少提供清除内容、清除格式、清除全部；不改变行列结构。
  6. `插入`：按选择形态提供插入单元格并右移/下移、插入整行、插入整列。
  7. `删除`：提供删除行、删除列、删除单元格并左移、删除单元格并上移；与“清除”语义严格区分。
  8. `排序`：提供升序、降序、自定义排序，执行第 12.2 节规则。
  9. `合并单元格/取消合并`：仅矩形多格选区可用；非左上角存在内容时二次确认，取消不修改数据。
  10. `设置单元格格式`、`锁定单元格`、`条件格式`、`数据验证`执行下述专项规则。
- 任一写操作必须形成单一撤销事务；超过 operation API 大小或数量限制时回退整本文档保存，但仍需
  保持原子性。失败不得部分移动、清空或写入单元格。

### 12.5 设置单元格格式

- 使用居中模态框，左侧分类、右侧实时示例和参数，底部明确“确定/取消”；打开时读取活动单元格，
  多选范围存在混合格式时显示“多种格式”，不得擅自覆盖未修改项。
- 分类至少包括：常规、文本、数值、百分比、货币、会计专用、日期、时间、分数、科学计数、特殊、
  自定义。数值类支持小数位、千分位和负数样式；日期时间按工作簿时区；自定义格式需限制长度和语法。
- 数字格式只改变显示，不改变原始值或公式。确定后作为一个事务应用到完整选区并可撤销、保存、历史
  恢复和 Excel 往返；取消不产生 dirty。

### 12.6 锁定单元格

- Sheet 使用稳定 ID 的 `protectedRanges` 保存矩形范围、创建人、可编辑成员、说明和启用状态；禁止仅
  在 DOM 上禁用输入而没有持久化和服务端校验。
- 文档所有者、管理员或具备共享管理权限的用户可锁定、调整成员和解锁；普通编辑者只能修改明确
  授权给自己的锁定范围。只读用户不能创建锁定规则。
- 锁定范围仍可查看、选择和复制，但输入、公式栏、粘贴、剪切落位、清空、格式、排序、合并、行列
  增删、Excel 导入和 operation/整本保存都必须执行相同权限校验。
- 与锁定范围相交的结构操作需整体拒绝并指出冲突范围，不能部分执行。锁定、解锁和成员变化进入自动
  保存、页面历史和审计摘要，不在普通日志中记录单元格正文。

### 12.7 条件格式

- Sheet 使用 `conditionalFormats` 保存稳定规则 ID、目标范围、规则类型、比较条件、阈值、展示样式、
  优先级、是否“满足后停止”和启用状态。
- 首批规则覆盖：大于/小于/介于/等于、文本包含、日期条件、空白/非空、重复/唯一；展示至少支持
  文字颜色和填充色。数据条、色阶和图标集可在模型兼容前提下后续增加。
- 条件样式为派生显示层，不覆盖单元格手工样式、原始值或公式。值或公式结果变化后立即重算；规则
  冲突按优先级和“满足后停止”确定结果。
- 可创建、编辑、复制、停用、调整优先级和删除规则。规则范围随行列插入删除迁移，排序仅移动数据，
  不意外改变规则范围；操作可撤销、保存、协作合并和历史恢复。

### 12.8 数据验证

- Sheet 使用 `dataValidations` 保存稳定规则 ID、目标范围、类型、条件、允许空值、输入提示、无效数据
  处理方式和启用状态。同一单元格命中多条规则时按优先级执行并在配置阶段提示冲突。
- 首批类型按石墨菜单覆盖：列表、数字、日期时间、文本内容/长度、复选框、评分、进度、身份证、
  手机号、固定电话、电子邮箱、温度和自定义公式。
- 无效数据支持“拒绝输入”和“允许但警告”两种模式；直接输入、公式栏、粘贴、填充、批量导入和 API
  写入必须使用同一校验结果。既有不合法数据在新增规则时提示数量，由用户选择保留警告或取消配置。
- 列表型在单元格内提供紧凑下拉；复选框、评分和进度采用适合类型的编辑控件，但底层值必须可导出、
  搜索和参与明确支持的公式。身份证和联系方式只做格式校验，不宣称真实性认证。
- 规则创建、修改、删除和范围迁移可撤销、自动保存、协作合并和历史恢复；服务端校验结构、大小、
  权限和锁定范围，客户端负责即时交互与公式型规则计算。

### 12.9 连续选区与底栏统计

- 连续多格选区使用一个完整蓝色外框，不再给活动格叠加独立粗框；活动格保持原始背景，范围内其他格
  使用统一浅蓝选区底色，右下角显示填充柄。网格线继续可见，外框不能因虚拟滚动或冻结区域断裂。
- 选中两个及以上单元格且范围内存在内容时，Sheet 底栏右侧默认显示“总和”；点击后展开总和、平均、
  最大、最小、计数和数值计数，并允许选择任一指标作为底栏主指标。
- 总和、平均、最大和最小只统计数值及可解析数值字符串，公式使用当前计算结果；计数统计非空结果，
  数值计数只统计数值。空白、日期文本和错误值不得被错误计入数值统计。
- 选区统计是纯视图计算，不修改工作簿、不产生 dirty、撤销步骤、自动保存或页面历史；整行整列等
  大范围选择必须遍历稀疏非空单元格，不能按声明行列面积创建巨大矩阵。
