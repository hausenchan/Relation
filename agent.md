# Agent 入口

本项目的自动开发上下文使用标准文件名 [`AGENTS.md`](./AGENTS.md)。

每次开始任务时按顺序读取：

1. `AGENTS.md`：长期功能地图、耦合关系、开发和提交约束。
2. `handoff.md`：当前未完成进度、验证状态和下一步。
3. 对应模块 PRD 与实际代码。

长期规则不要重复写入本文件；临时进度不要写入 `AGENTS.md`。
