# 文档中心 TAPD 需求导入 PRD

## 1. 背景

目前产品需求主要沉淀在 TAPD，组织中台文档中心已经具备文档、富文本、图片、附件和第三方内容导入能力。为了减少需求资料散落在不同系统中的查找成本，需要支持从 TAPD 批量导入需求到文档中心，并尽量保持 TAPD 原需求中的文字、图片、表格、附件和格式样式。

测试阶段优先支持“按某个迭代导入需求”，避免一次性全量导入带来数据量、格式兼容和重复文档问题。

## 2. 目标

1. 支持从 TAPD 指定项目空间中选择某个迭代，批量导入该迭代下的需求。
2. 支持导入需求标题、正文、图片、附件、状态、负责人、优先级、迭代、模块、原始链接等信息。
3. 尽量保持 TAPD 需求正文的富文本格式，包括标题、段落、列表、表格、加粗、颜色、链接、图片等。
4. 图片和附件导入到文档中心可控存储中，避免依赖 TAPD 临时链接或登录态链接。
5. 支持重复导入策略，便于后续同一迭代增量更新。
6. API 密钥和凭证只保存在服务器配置中，不进入代码仓库。

## 3. 非目标

1. 第一版不做 TAPD 与文档中心的实时双向同步。
2. 第一版不反向写回 TAPD。
3. 第一版不做所有 TAPD 字段的完整映射，只保留文档中心检索和追溯所需的核心字段。
4. 第一版不保证像素级 100% 一致，但需要做到内容完整、格式高保真、图片附件可访问。

## 4. 用户角色

- 产品负责人：希望把一个迭代的需求沉淀到文档中心，方便组织内统一查看。
- 研发/测试/运营：希望在文档中心按目录、关键字、项目、迭代检索需求。
- 管理员：负责配置 TAPD API 凭证、导入范围和目标目录。

## 5. 入口设计

文档中心导入入口新增：

- `导入 -> TAPD 需求导入`

弹窗分为四步：

1. 连接配置
   - TAPD API 账号/密码或开放应用凭证。
   - 服务器侧读取已配置凭证时，前端只显示“已配置”，不回显密钥。

2. 选择范围
   - 选择 TAPD 项目空间 `workspace_id`。
   - 加载项目下迭代列表。
   - 选择某个迭代 `iteration_id`。
   - 可选状态筛选：全部、规划中、实现中、已完成等。
   - 可选测试数量：只导入前 3 条、5 条、10 条或全部。

3. 导入位置
   - 选择文档中心目录空间/部门。
   - 选择目标文件夹。
   - 可选：按 TAPD 项目名称、迭代名称自动创建子目录。

4. 导入策略
   - 重复需求识别字段：`tapd_workspace_id + tapd_story_id`。
   - 已存在时：
     - 跳过。
     - 覆盖更新正文。
     - 创建新版本。
   - 默认推荐：覆盖更新正文，并保留文档 ID 和历史版本。

## 6. 导入流程

1. 前端提交导入配置。
2. 后端验证 TAPD 凭证是否可用。
3. 后端读取项目空间下的迭代列表。
4. 用户选择迭代后，后端按 `workspace_id + iteration_id` 分页拉取需求列表。
5. 对每条需求读取详情：
   - 标题。
   - 描述/富文本正文。
   - 状态。
   - 负责人。
   - 优先级。
   - 模块。
   - 迭代。
   - 创建人、创建时间、更新时间。
   - 原 TAPD URL。
6. 解析需求正文 HTML。
7. 提取正文中的图片 URL，并由服务端下载。
8. 将图片上传/保存到文档中心附件体系，替换正文图片地址。
9. 拉取或识别 TAPD 附件，并保存为文档中心附件 block。
10. 将正文转为文档中心 blocks。
11. 写入文档中心：
    - 新需求创建新文档。
    - 已存在需求按重复策略处理。
12. 返回导入结果：
    - 成功数。
    - 跳过数。
    - 失败数。
    - 失败原因列表。

## 7. TAPD API 设计

### 7.1 凭证

TAPD API 凭证由服务器环境变量或安全配置保存：

```text
TAPD_API_BASE_URL=https://api.tapd.cn
TAPD_API_USER=xxxx
TAPD_API_PASSWORD=xxxx
```

要求：

- 不写入 Git 仓库。
- 不在前端回显。
- 不在日志中打印。
- 推荐使用只读权限账号。

### 7.2 项目空间

需要支持读取或配置：

- `workspace_id`
- 项目名称
- 项目状态

如果 TAPD API 无法直接列出公司全部项目，第一版允许管理员手动填写 `workspace_id`。

### 7.3 迭代

需要支持按项目空间读取迭代列表：

- `iteration_id`
- 迭代名称
- 开始时间
- 结束时间
- 状态

前端展示为下拉选择。测试阶段必须先选择一个迭代，不支持默认全量。

### 7.4 需求

按迭代分页拉取需求：

- `workspace_id`
- `iteration_id`
- `page`
- `limit`
- 可选 `status`
- 可选 `modified_after`

需要注意 TAPD API 分页限制，后端应循环分页直到没有更多数据。

### 7.5 图片与附件

图片处理优先级：

1. 优先从需求正文 HTML 中解析 `<img>`。
2. 服务端携带 TAPD API 凭证或必要 cookie 下载图片。
3. 下载成功后保存为文档中心图片附件。
4. 替换正文图片 URL 为文档中心可访问 URL。
5. 下载失败时保留原 URL，并在导入结果中给出警告。

附件处理优先级：

1. 优先调用 TAPD 附件接口获取附件列表。
2. 下载附件并创建文档中心附件记录。
3. 如果 API 无法获取附件，保留附件链接和原始文件名。

## 8. 文档中心落库设计

### 8.1 文档标题

默认：

```text
[TAPD] 需求标题
```

可选配置：

```text
需求标题
```

### 8.2 文档正文结构

导入后的文档建议结构：

1. 顶部信息区
   - 来源：TAPD
   - 状态
   - 负责人
   - 优先级
   - 迭代
   - 模块
   - 原始链接
   - TAPD 需求 ID

2. 需求正文
   - 从 TAPD 描述转为文档中心 blocks。
   - 保留标题、段落、列表、表格、图片、链接等。

3. 附件区
   - 附件 block 列表。

### 8.3 来源元数据

文档需要保存来源字段，便于后续增量导入和去重：

```json
{
  "source_system": "tapd",
  "tapd_workspace_id": "123456",
  "tapd_story_id": "987654",
  "tapd_iteration_id": "456789",
  "tapd_url": "https://www.tapd.cn/xxx",
  "tapd_updated_at": "2026-07-15 10:00:00"
}
```

## 9. 重复导入策略

用以下组合判断是否为同一需求：

```text
source_system = tapd
tapd_workspace_id
tapd_story_id
```

策略：

- 跳过：已有同源需求时不更新。
- 覆盖更新：更新正文、元数据、图片附件引用。
- 新建版本：保留原文档，创建新的历史版本记录。

第一版默认使用“覆盖更新 + 保留历史版本”。

## 10. 格式保真策略

### 10.1 富文本

支持转换：

- 标题。
- 段落。
- 加粗、斜体、下划线。
- 字体颜色、背景色。
- 有序列表、无序列表。
- 链接。
- 引用。
- 表格。
- 图片。

### 10.2 原始 HTML 备份

每篇导入文档保留 TAPD 原始正文 HTML：

```json
{
  "original_html": "<div>...</div>"
}
```

用途：

- 后续格式兼容优化。
- 与文档中心渲染结果对照。
- 必要时提供“查看原始 TAPD 样式”。

### 10.3 兜底方案

如果 API 返回的正文不完整，后续可以增加“浏览器抓取兜底”：

- 使用已登录服务账号访问 TAPD 需求页面。
- 抓取页面正文 DOM。
- 提取最终渲染后的图片和附件链接。

第一版先不实现浏览器兜底，只预留扩展点。

## 11. 权限与安全

1. 只有管理员、CXO 或配置了导入权限的角色可以使用 TAPD 导入。
2. TAPD 凭证仅服务器保存。
3. 导入日志不输出密钥。
4. 图片和附件下载需要限制大小，复用当前文档中心附件大小限制。
5. 需要对导入 HTML 做安全清洗，去除 script、事件属性、危险链接。
6. 导入结果中仅展示必要错误信息，不暴露 TAPD 凭证和内部请求头。

## 12. 日志与可观测

导入任务记录：

- 任务 ID。
- 操作人。
- workspace_id。
- iteration_id。
- 目标文件夹。
- 导入开始/结束时间。
- 总需求数。
- 成功数。
- 跳过数。
- 失败数。
- 失败明细。

单条需求记录：

- tapd_story_id。
- 文档 ID。
- 导入状态。
- 错误信息。
- 图片下载成功/失败数。
- 附件下载成功/失败数。

## 13. 第一版验收标准

1. 管理员可以打开 `导入 -> TAPD 需求导入`。
2. 可以配置或使用服务器上的 TAPD API 凭证。
3. 可以选择或填写 `workspace_id`。
4. 可以加载该项目空间的迭代列表。
5. 可以选择某个迭代并只导入该迭代需求。
6. 测试阶段支持只导入前 3/5/10 条。
7. 导入后文档中心生成对应文档。
8. 文档标题与 TAPD 需求标题一致。
9. 文档正文文字内容完整。
10. 正文图片可以在文档中心正常展示。
11. 表格、列表、链接等基础格式尽量保留。
12. 附件可以作为文档中心附件展示或下载。
13. 重复导入同一需求时不会创建重复文档，按配置策略处理。
14. 导入失败时给出可读错误，不导致整个系统异常。

## 14. 测试计划

### 14.1 样例需求

至少准备 3 类 TAPD 需求：

1. 纯文字需求。
2. 带图片需求。
3. 带表格、附件、复杂富文本需求。

### 14.2 测试步骤

1. 配置 TAPD API 凭证。
2. 填写或选择一个 `workspace_id`。
3. 拉取迭代列表。
4. 选择一个测试迭代。
5. 设置只导入前 3 条。
6. 执行导入。
7. 对比 TAPD 原页面与文档中心文档：
   - 标题。
   - 正文。
   - 图片。
   - 表格。
   - 附件。
   - 链接。
8. 再次执行同一批导入，验证重复策略。

## 15. 分期计划

### 第一期：按迭代导入 MVP

- API 凭证配置。
- workspace_id 手动输入或选择。
- 迭代列表读取。
- 按迭代分页读取需求。
- 正文 HTML 转 blocks。
- 图片下载并替换。
- 附件导入。
- 重复导入去重。
- 导入结果展示。

### 第二期：增强保真和批量能力

- 支持多迭代批量导入。
- 支持按状态、负责人、更新时间增量导入。
- 增强表格、颜色、嵌套列表兼容。
- 支持原始 TAPD 样式预览。

### 第三期：同步任务

- 支持定时同步某个迭代或项目。
- 支持导入任务历史。
- 支持变更对比。

## 16. 待确认事项

1. TAPD API 凭证类型：账号密码，还是开放应用凭证。
2. 是否可以通过 API 获取公司下全部项目空间。
3. 迭代接口返回字段和需求接口中迭代筛选字段的准确名称。
4. TAPD 正文图片是否需要额外 cookie 或签名才能下载。
5. TAPD 附件是否有独立下载接口。
6. 导入目标目录规则：按项目/迭代建目录，还是全部导到用户选择目录。
7. 重复导入默认策略是否确定为“覆盖更新 + 保留历史版本”。
