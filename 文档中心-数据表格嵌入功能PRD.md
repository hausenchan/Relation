# 文档中心数据表格-嵌入功能 PRD

## 背景

文档中心已支持在文档中插入基础的数据表格-嵌入块，但当前能力偏简单：排序会直接改写数据行，筛选只有单个文本条件，缺少类似 Wolai/Notion 数据库表格的字段类型、视图配置、复合排序、筛选、分组等能力。用户希望在文档内直接完成轻量数据透视表式的查看和管理，并且从 Wolai MCP 导入同类表格文档时，尽量保持原内容、字段类型、选项、格式和视图效果一致。

## 目标

1. 在文档编辑器中，通过 `+ -> 数据表格 - 嵌入` 插入可编辑的数据表格。
2. 支持表格名称、字段名称、字段类型、字段颜色、选项配置、单元格内容编辑。
3. 支持按字段配置复合排序、筛选和分组，显示结果不破坏原始行数据。
4. 支持 Wolai MCP 导入数据库/嵌入表格类文档，保留表格标题、字段、行数据、字段类型、选项颜色、列宽和可识别的视图规则。
5. 数据全部保存在文档 block 的 `meta` 中，不新增独立业务数据表，降低迁移风险。

## 范围

### 本期实现

- 插入默认表格：包含 `标题`、`标签`、`字段名` 三列和若干空行。
- 表格名称可直接编辑。
- 字段菜单支持：
  - 字段改名。
  - 字段类型切换：标题、文本、数字、单选、多选、勾选框、日期、成员、网址链接、邮箱、电话。
  - 字段颜色和标题颜色设置。
  - 单选/多选/成员类字段的选项维护，包括名称和颜色。
  - 在左侧/右侧添加字段、删除字段。
  - 按当前字段添加排序或筛选。
- 单元格编辑支持：
  - 文本/标题/数字/链接/邮箱/电话为富文本行内编辑。
  - 单选、多选、成员使用标签选择器。
  - 勾选框使用 checkbox。
  - 日期使用日期选择器。
- 视图工具栏支持：
  - 视图配置：查看当前表格统计、清空排序/筛选/分组。
  - 排序：新增多条排序规则，按规则顺序排序。
  - 筛选：新增多条筛选规则，支持包含任一条件、为空、不为空。
  - 分组：选择字段分组，可勾选展示哪些分组。
- 分组展示时，每个分组内继续遵循当前排序规则。
- Wolai MCP 导入兼容：
  - 数据库/表格块导入为 `database-embed`。
  - 映射 Wolai 字段类型到本系统字段类型。
  - 保留选项名称、颜色、行数据和列宽。
  - 尽力识别 Wolai 视图中的排序、筛选、分组配置。

### 暂不实现

- 多视图持久化和视图重命名的完整管理。
- 跨文档引用同一张数据表。
- 复杂公式、汇总统计、权限到单列/单行级别。
- 与 Wolai 实时双向同步。

## 数据模型

数据表格-嵌入 block 使用 `meta` 保存：

```json
{
  "tableName": "数据表格-嵌入样例",
  "columns": ["执行事项", "状态", "日期"],
  "rows": [["事项 A", "待完成", "2026-07-15"]],
  "fieldTypes": ["title", "select", "date"],
  "tagOptions": {
    "1": [
      { "name": "待完成", "color": "#f3f4f6" },
      { "name": "进行中", "color": "#dbeafe" },
      { "name": "已完成", "color": "#dcfce7" }
    ]
  },
  "sorts": [
    { "columnIndex": 1, "direction": "asc" },
    { "columnIndex": 2, "direction": "desc" }
  ],
  "filters": [
    { "columnIndex": 1, "operator": "contains_any", "values": ["待完成"] }
  ],
  "group": {
    "columnIndex": 1,
    "visibleValues": ["待完成", "进行中"],
    "collapsed": {}
  },
  "columnWidths": [220, 200, 200],
  "columnColors": {},
  "titleColorColumns": {}
}
```

## 字段类型

- `title`：标题字段，视觉加粗。
- `text`：普通文本。
- `number`：数字，排序按数值。
- `select`：单选，按选项顺序排序。
- `multi_select`：多选，按选项顺序排序并支持多值。
- `checkbox`：勾选框。
- `date`：日期，排序按时间。
- `person`：成员，可多选标签。
- `url`：网址链接。
- `email`：邮箱。
- `phone`：电话。

## 排序规则

- 支持多条排序规则，按列表顺序依次比较。
- 文本类：A -> Z、Z -> A。
- 数字/日期类：1 -> 9、9 -> 1。
- 选项类：按选项正序、按选项倒序。
- 排序只影响展示顺序，不直接改写 `rows` 的存储顺序。

## 筛选规则

- 支持多条筛选规则。
- 选项类字段支持 `包含任一条件`。
- 通用字段支持 `包含任一条件`、`为空`、`不为空`。
- 多条筛选规则之间为 AND 关系。
- 筛选后继续叠加排序规则。

## 分组规则

- 默认无分组。
- 选择字段后按该字段的值分组展示。
- 选项类字段按选项配置顺序展示分组。
- 支持选择要显示的分组值。
- 每个分组内部继续应用现有排序规则。

## Wolai MCP 导入兼容

导入时按以下优先级构建表格：

1. 优先读取 Wolai 数据库/表格节点中的 schema、columns、fields、properties。
2. 读取 rows、records、items、children 中的行数据。
3. 将 Wolai 属性类型映射到本系统字段类型。
4. 将 select/multi_select/status 的选项映射为 `tagOptions`，保留颜色。
5. 读取 columnWidths、view、sort/filter/group 等可识别配置。
6. 如果 Wolai 返回普通 HTML table，则保留为普通表格；如果带数据库 schema，则导入为 `database-embed`。

## 验收标准

1. 插入数据表格-嵌入后，默认列、默认状态选项和新增行可正常使用。
2. 表格名称、字段名、字段类型、选项名称/颜色可编辑并保存。
3. 日期字段单元格可用日期选择器赋值。
4. 状态字段可单选赋值为待完成、进行中、已完成等选项。
5. 右上角排序可按状态、日期等字段添加多条规则并生效。
6. 筛选状态为待完成后，只显示符合条件的记录，并保持排序规则。
7. 分组选择状态后，按状态值分组展示；选择部分状态时只显示对应分组。
8. Wolai MCP 导入数据库/嵌入表格文档后，不出现 JSON 原文泄漏，表格字段、内容、选项和基础格式可正确展示。
9. 构建与静态检查通过。
