# 产品模版提版代理测试用例

## 1. 测试范围

覆盖产品模版“模版 / 代理”Tab、代理配置 CRUD、权限、域名匹配、任务级 HTTP/HTTPS 转发器、提版任务生命周期、
敏感信息保护、数据库迁移和前端回归。

测试不执行真实支付宝上传、真实代理供应商调用或生产数据库操作；外部上传统一使用隔离 fake `upload.js`，
网络转发使用本地临时 HTTP 服务和上游代理服务。

## 2. 测试环境和数据

| 项目 | 配置 |
| --- | --- |
| 后端 | Node.js，`DB_CLIENT=mysql` |
| 数据库 | 随机命名隔离 MySQL 测试库；单元测试可使用临时 SQLite |
| 密钥 | `/tmp` 随机 AES/HMAC 测试密钥 |
| 账号 | 管理员、普通可写用户、readonly、guest、无菜单权限用户、未登录用户 |
| 代理 | HTTP 无认证、HTTP 带认证、HTTPS 上游代理、具体域名代理、`*` 默认代理、停用代理 |
| 目标域名 | `api.example.com`、`analytics.example.com`、`cdn.example.com`、`drama.example.com` |
| 外部脚本 | fake `upload.js`，可记录参数、环境变量、成功、失败和重试次数 |

## 3. 用例清单

### A. 代理配置 CRUD 与字段校验

| ID | 用例 | 操作 | 预期 |
| --- | --- | --- | --- |
| PROXY-001 | 新增完整 HTTP 代理 | 填写名称、HTTP、主机、端口、账号密码、域名后缀、优先级、启用状态并保存 | 保存成功，列表展示脱敏账号和代理信息 |
| PROXY-002 | 新增 HTTPS 代理 | 协议选择 HTTPS，其余字段合法 | 保存成功，协议正确保存 |
| PROXY-003 | 缺少名称 | 名称为空提交 | `400`，不写入记录 |
| PROXY-004 | 缺少主机 | 主机为空提交 | `400`，不写入记录 |
| PROXY-005 | 非法主机 | 输入带路径、空格、账号密码或非法字符的主机 | `400`，不写入记录 |
| PROXY-006 | 非法协议 | 提交 `socks5` 或其他协议 | `400`，只允许 HTTP/HTTPS |
| PROXY-007 | 非法端口 | 提交 0、65536、小数、负数或非数字 | `400`，不写入记录 |
| PROXY-008 | 合法端口边界 | 提交 1 和 65535 | 保存成功 |
| PROXY-009 | 域名后缀规范化 | 提交 `*.example.com`、`.example.com`、重复值 | 保存为去重后的 `example.com` |
| PROXY-010 | 非法域名后缀 | 提交 URL 路径、端口、查询参数或空后缀 | `400`，不写入记录 |
| PROXY-011 | 默认代理 | 域名后缀填写 `*` | 保存成功，匹配所有未被更具体规则覆盖的域名 |
| PROXY-012 | 优先级和状态边界 | 提交负数、超大优先级和非法状态 | `400`；合法值保存成功 |
| PROXY-013 | 长度限制 | 名称、账号、密码、备注分别超过限制 | `400`，错误字段明确 |
| PROXY-014 | 详情脱敏 | GET 列表和详情 | 不返回 `password` 明文；只返回 `has_password`、账号掩码 |
| PROXY-015 | 原始数据库加密 | 查询数据库原始 `username/password` | 值为加密密文，不包含明文凭据 |
| PROXY-016 | 编辑普通字段 | 只修改备注、优先级或状态 | 其他字段和原密码保持不变 |
| PROXY-017 | 修改密码 | 编辑时输入新密码 | 新密码可用于后续代理认证，接口不回显 |
| PROXY-018 | 清除密码 | 勾选清除密码后保存 | `has_password=false`，后续连接不发送认证 |
| PROXY-019 | 空密码兼容 | 编辑密码留空且不勾选清除 | 原密码保持不变 |
| PROXY-020 | 删除代理 | 删除未运行任务引用的代理 | 删除成功，列表不再展示 |
| PROXY-021 | 删除不存在代理 | 删除非法 ID | `404` |
| PROXY-022 | 查询筛选 | 按名称、状态、域名后缀筛选 | 返回结果准确，不泄露密码 |

### B. 权限与安全

| ID | 用例 | 操作 | 预期 |
| --- | --- | --- | --- |
| AUTH-001 | 未登录读取 | 不带 Token 请求代理列表 | `401` |
| AUTH-002 | 无菜单权限读取 | 登录但无 `/product-templates`、`/product-assets` 菜单 | `403` |
| AUTH-003 | readonly 读取 | readonly 请求列表和详情 | 成功读取，不能新增、编辑、删除 |
| AUTH-004 | guest 读取 | guest 具备模块读权限 | 可读取；无写权限时写操作 `403` |
| AUTH-005 | 普通写用户 | 普通可写用户执行 CRUD | 服务端允许范围内正常写入 |
| AUTH-006 | 前端只读态 | readonly 打开代理 Tab | 不显示新增、编辑、删除入口 |
| AUTH-007 | 伪造前端写入 | readonly 直接调用 POST/PUT/DELETE | 服务端独立返回 `403` |
| AUTH-008 | 提版权限 | 无产品资产菜单或无写权限调用提版 | 服务端拒绝，不创建任务 |
| AUTH-009 | 日志脱敏 | 代理认证失败、脚本失败、转发失败 | 日志不出现密码、Proxy-Authorization、完整代理 URL |
| AUTH-010 | 快照脱敏 | 查询任务和提版历史 | 返回域名、代理名称等摘要，不返回代理密码或完整快照 |

### C. 域名匹配

| ID | 用例 | 操作 | 预期 |
| --- | --- | --- | --- |
| MATCH-001 | 精确域名 | 代理后缀 `api.example.com` 匹配 API 域名 | 匹配成功 |
| MATCH-002 | 子域名 | 代理后缀 `example.com` 匹配 `api.example.com` | 匹配成功 |
| MATCH-003 | 边界防误匹配 | `example.com` 不匹配 `example.com.evil.test` | 只能由 `*` 或其他合法规则匹配 |
| MATCH-004 | 最长后缀 | 同时配置 `*`、`example.com`、`api.example.com` | 选择 `api.example.com` |
| MATCH-005 | 同长度优先级 | 同后缀配置多个代理 | 选择优先级数值更小的代理 |
| MATCH-006 | 同优先级更新时间 | 优先级相同，更新时间不同 | 选择更新时间更新的代理 |
| MATCH-007 | 完全相同更新时间 | 时间相同但 ID 不同 | 选择 ID 更大的代理 |
| MATCH-008 | 停用代理 | 最优匹配代理停用 | 跳过停用代理，选择次优启用代理 |
| MATCH-009 | 缺少默认代理 | 目标域名有具体匹配但没有启用 `*` | 提版创建 `400`，不创建任务 |
| MATCH-010 | 缺少目标域名代理 | `cdn.example.com` 无任何匹配 | 提版创建 `400`，错误指出具体域名 |
| MATCH-011 | 相同主机去重 | API 和 CDN 使用同一主机 | 代理摘要只记录一个主机匹配，任务仍完整执行 |
| MATCH-012 | 普通模版域名 | 普通模版主体有短剧域名但未选择短剧模版 | 短剧域名不参与必选匹配 |
| MATCH-013 | 短剧模版域名 | 选择短剧模版 | 短剧域名必须参与匹配 |
| MATCH-014 | 域名协议校验 | 主体域名为 HTTP、内网或非法域名 | 提版前置校验 `400` |

### D. 任务级代理转发器

| ID | 用例 | 操作 | 预期 |
| --- | --- | --- | --- |
| ROUTER-001 | HTTP 转发 | 通过本地代理访问 HTTP 目标 | 请求到达正确上游代理和目标服务 |
| ROUTER-002 | HTTPS CONNECT | 通过本地代理建立 CONNECT 隧道 | 返回 200 隧道并可完成后续请求 |
| ROUTER-003 | 多域名多代理 | 两个域名分别配置不同上游代理 | 每个域名到达对应上游代理 |
| ROUTER-004 | 代理认证 | 上游代理要求账号密码 | 发送正确 Basic Proxy-Authorization |
| ROUTER-005 | 无匹配目标 | 访问未配置目标 | 返回 `502`，不直连目标 |
| ROUTER-006 | NO_PROXY 覆盖 | 环境中原有 NO_PROXY | 任务环境清空 NO_PROXY，不绕过任务代理 |
| ROUTER-007 | 环境隔离 | 启动任务代理 | 只向 upload 子进程注入 HTTP_PROXY/HTTPS_PROXY/ALL_PROXY，不修改服务进程环境 |
| ROUTER-008 | 任务结束关闭 | 提版成功后探测本地转发端口 | 端口关闭，不能继续转发 |
| ROUTER-009 | 失败关闭 | upload 脚本失败后探测本地端口 | 端口关闭，任务标记失败 |
| ROUTER-010 | 上游错误 | 上游代理拒绝或断开 | 返回可识别错误，不泄露凭据，连接资源释放 |
| ROUTER-011 | 非法 CONNECT | CONNECT 目标端口非法 | 返回 `502`，不建立连接 |
| ROUTER-012 | 并发请求 | 同一任务多个域名并发访问 | 路由互不串线，任务结束可完整关闭所有连接 |

### E. 提版任务生命周期

| ID | 用例 | 操作 | 预期 |
| --- | --- | --- | --- |
| RELEASE-001 | 创建前置校验 | 缺主体、模版、版本、身份密钥或域名 | `400/503`，不创建任务 |
| RELEASE-002 | 缺代理阻断 | 代理不完整 | `400`，不创建任务、不启动 upload |
| RELEASE-003 | 任务代理摘要 | 代理匹配后创建任务 | `202` 返回摘要，任务详情继续返回摘要 |
| RELEASE-004 | 快照加密 | 创建任务后读取原始数据库 | 快照密文不含代理密码明文 |
| RELEASE-005 | 配置删除后执行 | 创建任务后删除代理 | 任务仍使用创建时快照完成执行 |
| RELEASE-006 | 配置修改后执行 | 创建任务后修改代理密码/地址 | 当前任务不受影响，新任务使用新配置 |
| RELEASE-007 | 成功提版 | fake upload 返回 0 | 任务 success，记录 launched，代理关闭 |
| RELEASE-008 | 脚本失败 | fake upload 返回非 0 | 任务 failed，错误原因可读，代理关闭 |
| RELEASE-009 | 可重试失败 | 第一次失败、第二次成功 | 执行次数为 2，最终 success，代理只在任务期间有效 |
| RELEASE-010 | 不可重试错误 | 代理快照缺失或脚本不存在 | 不无意义重试，最终 failed |
| RELEASE-011 | 同 AppID 并发 | 同 AppID 同时创建两个任务 | 第二个返回 `409` |
| RELEASE-012 | 取消排队任务 | pending 状态取消 | 任务 cancelled，不启动 upload |
| RELEASE-013 | 重启恢复 | 服务在 pending/running 时重启 | 任务标记 interrupted/failed，不遗留可用代理端口 |
| RELEASE-014 | 普通模版 | 普通模版含三类域名 | 仅三类域名传入 upload，短剧模板标记为 0 |
| RELEASE-015 | 短剧模版 | 短剧模版含四类域名 | 四类域名均传入 upload，短剧模板标记为 1 |
| RELEASE-016 | 历史摘要 | 查看提版历史 | 展示域名和代理名称，不展示密钥、账号密码和完整连接串 |

### F. 前端交互

| ID | 用例 | 操作 | 预期 |
| --- | --- | --- | --- |
| UI-001 | 默认 Tab | 打开 `/product-templates` | 默认显示“模版”Tab |
| UI-002 | 代理 Tab | 点击“代理” | 加载代理列表，URL 保留 `tab=proxy` |
| UI-003 | 刷新代理 Tab | 直接打开 `?tab=proxy` | 仍显示代理 Tab |
| UI-004 | 新增表单 | 点击新增代理并填写合法数据 | 调用 POST，成功刷新列表 |
| UI-005 | 编辑表单 | 编辑代理并留空密码 | 保留原密码；勾选清除密码才清除 |
| UI-006 | 删除确认 | 点击删除 | 先二次确认，确认后调用 DELETE |
| UI-007 | 筛选 | 输入名称、状态、域名后缀 | 请求参数准确，列表结果刷新 |
| UI-008 | 只读用户 | readonly 打开代理 Tab | 可查看，不显示写操作 |
| UI-009 | 提版摘要 | 创建提版任务 | 进度弹窗展示代理数量、域名和代理名称 |
| UI-010 | 提版缺代理 | 服务端返回缺代理错误 | 页面展示明确错误，不打开错误详情或误报成功 |
| UI-011 | 提版历史 | 打开产品详情历史 | 展示安全代理摘要 |
| UI-012 | 移动端 | 在窄屏打开 Tab、表格、表单和进度弹窗 | 无横向溢出，控件可操作 |

### G. 数据库、性能和交付

| ID | 用例 | 操作 | 预期 |
| --- | --- | --- | --- |
| DB-001 | MySQL 空库启动 | `DB_CLIENT=mysql` 启动隔离空库 | 代理表和提版字段自动创建，服务正常监听 |
| DB-002 | 旧库升级 | 在已有产品表数据库启动 | 增量字段幂等补齐，不破坏历史提版记录 |
| DB-003 | 重复启动 | 同一数据库连续启动两次 | 迁移无重复错误，服务正常启动 |
| DB-004 | 长文本 | 写入长备注、快照和摘要 | MySQL LONGTEXT 可正常保存和读取 |
| DB-005 | 事务完整性 | 代理解析失败时检查数据库 | 不产生半成品 task/record |
| DB-006 | SQL 注入 | 名称、后缀、备注输入 SQL 特殊字符 | 参数化处理，不执行注入内容 |
| PERF-001 | 代理列表 | 加载大量代理配置 | 无 N+1 查询，响应满足 API 性能门禁 |
| PERF-002 | 前端构建 | 运行前端构建和性能门禁 | 首屏不超过 400KB gzip，异步 chunk 不超过 500KB |
| DELIV-001 | 语法检查 | `node --check` 相关服务端文件 | 全部通过 |
| DELIV-002 | 全量测试 | 后端、前端全量测试 | 无新增失败；既有警告需区分记录 |
| DELIV-003 | 文档交接 | 检查 PRD 和 handoff | 包含配置、匹配、生命周期、验证结果和遗留风险 |

## 4. 自动化映射与执行结果

| 用例范围 | 自动化位置 | 当前结果 |
| --- | --- | --- |
| PROXY-003~013、MATCH-001~014 | `server/lib/productReleaseProxy.test.js`，并复用 `productTemplateRelease.test.js` 的主体域名校验 | 通过；代理单元测试 10/10 |
| PROXY-001、PROXY-014~020、AUTH-001~003、AUTH-007、AUTH-009~010、MATCH-009~013、RELEASE-002、RELEASE-003、RELEASE-007~008、RELEASE-014~016、DB-004~006 | `server/lib/productReleaseDomainIntegration.test.js` | 通过；集成测试 1/1，使用临时 SQLite 和子进程密钥隔离 |
| ROUTER-001~009、ROUTER-011~012 | `server/lib/productReleaseProxy.test.js` | 通过；HTTP、认证头、多域名、HTTPS CONNECT、无匹配、环境隔离和关闭端口均覆盖 |
| UI-001~010 | `client/src/pages/ProductTemplates.test.js`、`client/src/pages/ProductAssets.test.js` | 通过；7/7 |
| PERF-002、DELIV-001~003 | 生产构建、性能门禁、`node --check`、后端/前端全量测试 | 通过 |

以下用例保留为上线前人工或后续专用环境回归：guest 账号完整读写矩阵、删除不存在代理和复杂筛选组合的 API 矩阵、任务取消竞态、服务重启恢复、HTTPS 上游代理真实 TLS 证书、真实代理供应商和真实支付宝上传，以及移动端真机视觉检查。它们不阻塞当前隔离链路，但不能以本地 fake upload 结果替代。

本轮执行结果：后端 `node --test server/lib/*.test.js` 共 141 条，137 条通过、4 条按环境开关跳过、0 条失败；前端全量 42 个套件、237 条测试通过。

## 5. 通过标准

- P0/P1 用例全部通过；P2 用例不得出现数据泄露、直连绕过或任务状态错误。
- 代理密码、身份密钥、Token 不出现在接口响应、任务快照明文、日志、测试输出和文档示例中。
- 缺少代理时不创建提版任务，不启动 upload，不产生可用直连路径。
- 提版成功、失败、重试、取消和服务重启后均不遗留任务级代理监听端口。
- MySQL 隔离库启动、迁移幂等、前后端全量测试和生产构建全部通过。
