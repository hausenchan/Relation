# 经营周会模块 PRD

## 1. 文档信息

- 模块名称：经营周会
- 所属入口：目标计划
- 文档版本：V1.4
- 编写日期：2026-07-16
- 最近更新：2026-07-17
- 目标用户：CEO、COO、CTO、CMO 及被授权参与经营周会的核心管理人员

## 2. 背景

公司每周需要围绕经营情况召开经营周会。当前经营周会记录更偏固定表单，无法满足多人分区填写、会前汇总、AI 生成会议提纲、会后沉淀决策和后续扩展的需求。

经营周会内容通常包含高敏经营信息，尤其涉及业务数据、判断、决策和后续动作。现有字段级加密使用服务端密钥文件，能够防止数据库单独泄露，但无法防止拿到服务器密钥文件或服务器权限的运维人员查看内容。因此，新模块需要在产品设计上同时满足“好用”和“更高安全等级”两个目标。

## 3. 产品目标

1. 在目标计划下新增“经营周会”模块。
2. 支持按周创建经营周会，列表展示周时间范围和准备状态。
3. 每周记录固定分为“准备”和“会议”两大模块。
4. 准备模块支持多个负责人分别填写自己的准备内容，并按身份做严格可见性隔离。
5. 会议模块包含 AI 会议提纲和会议结论，对该周全部参会人员共享。
6. 每周记录有独立详情页，可在“准备 / 会议”之间切换。
7. 提供年度汇总页，集中查看全年 AI 会议提纲和会议结论。
8. 复用文档中心的块编辑能力，支持富文本、列表、附件、图片等结构化内容。
9. 所有必填准备内容完成后，支持调用 AI 生成会议提纲。
10. AI 生成的会议提纲必须避免披露业务毛利、毛利率、利润等敏感数据。
11. 开会后支持填写会议结论，重点沉淀决策和后续动作。
12. 模板、负责人、准备块、问题项均可扩展，后续增加或减少人员不需要大改代码。
13. 权限上同时满足菜单权限、敏感模块权限、周会参与身份和记录级内容权限。
14. 新增端到端加密能力，让运维即使拿到数据库、服务器文件和旧服务端密钥，也无法解密新高敏内容。
15. 支持公司级密钥恢复：运维可以独立恢复系统、数据库和附件密文，老板或两名 CXO 负责恢复解密能力。
16. 登录密码、安全密码、换设备、忘记密码、人员离职和密钥轮换均不得造成授权历史数据永久丢失。
17. 将平台运维、账号管理、敏感授权、内容解密和安全审计拆分为不同权限，取消超级管理员对高敏内容的隐式读取权。
18. 为经营周会及后续人脉、公司研究、文档中心高敏内容提供统一的 SL1 / SL2 / SL3 安全等级和加密基础设施。

## 4. 非目标

本期不做以下能力：

- 不做复杂会议排期和日历邀请。
- 不做会议纪要自动语音转写。
- 不做经营指标看板。
- 不做跨公司或外部人员协作。
- 不做所有历史加密数据一次性强制迁移到新加密方案。
- 不直接把业务按钮运行时做成 Codex 任务；经营周会 AI 分析走模型 API，Codex 主要用于研发、调试、自动化维护。
- 不承诺任何单一人员在没有 CXO 参与的情况下恢复 SL2 / SL3 明文；这是防止运维单独解密的必要安全边界。
- 不把“拥有服务器 root、数据库 root 或超级管理员账号”等同于“拥有业务数据解密权”。

## 5. 用户角色

### 5.1 管理、运维与安全角色

系统不再使用“一个超级管理员同时拥有全部系统权限和全部业务数据权限”的模型，拆分为以下角色：

#### 5.1.1 平台运维管理员 `platform_operator`

负责：

- 部署已签名的应用版本。
- 启停服务、监控、扩容和故障处理。
- 备份和恢复 MySQL、OSS 密文及密钥元数据。
- 查看不包含业务明文的技术日志和运行指标。

限制：

- 不能查看 SL2 / SL3 明文。
- 不能读取用户私钥、两套公司恢复私钥或明文 DEK。
- 不能把自己升级为 CXO、敏感数据管理员或数据授权人。
- 不能创建可绕过双人审批的超级管理员。
- 默认没有生产数据库交互式查询权限；紧急 root 权限必须走双人审批、限时授权和全程审计。

#### 5.1.2 账号管理员 `account_admin`

负责创建普通账号、停用离职账号、重置登录密码和配置普通菜单权限。

限制：

- 重置登录密码只恢复登录能力，不恢复 SL2 / SL3 解密能力。
- 不能单独授予 CXO 身份、SL3 权限或发起公司密钥恢复。
- 不能读取任何用户的安全密码和明文私钥。

#### 5.1.3 数据安全管理员 `security_admin`

由老板或明确指定的 CXO 担任，负责：

- 配置敏感模块人员。
- 审批 SL2 / SL3 数据授权、撤权和降级。
- 发起公司恢复流程；仅老板本人通过老板硬件恢复通道时可以单独完成，其他人员必须走两名 CXO门限通道。
- 配置经营周会模板和高敏模块安全等级。

#### 5.1.4 安全审计员 `security_auditor`

- 查看账号变更、授权、解密、导出、恢复、密钥轮换和紧急运维审计。
- 不能修改业务数据、权限和密钥。
- 审计日志写入独立、不可修改或具备哈希链校验的存储。

#### 5.1.5 紧急恢复角色 `break_glass`

- 平时禁用，不用于日常登录。
- 启用需要老板或两名 CXO 批准，并强制 MFA、限时、指定用途和全程录像 / 审计。
- 只能恢复服务和密文，不能绕过公司恢复密钥直接读取 SL2 / SL3 明文。

共同原则：任何管理角色都不因为“管理员”身份自动获得经营周会记录密钥。

### 5.2 CXO

包括 CEO、COO、CTO、CMO，以及后续明确配置为 CXO 身份的人员。

能力：

- 查看全部经营周会记录。
- 查看同一周全部人员的准备内容，包括其他 CXO 和后续指定参与人的准备内容。
- 填写、保存和提交自己及其他周会准备人员的准备内容。
- CXO 默认可以编辑该周全部准备块，包括其他 CXO 和指定参与人的准备内容。
- 查看该周 AI 会议提纲和会议结论。
- 生成或重新生成 AI 会议提纲。
- 填写和维护会议结论。
- 查看全年 AI 会议提纲和会议结论汇总。
- 参与敏感授权、公司密钥恢复和紧急运维审批。
- 在安全密码遗失、人员更换或灾难恢复时，按老板硬件通道或两名 CXO门限通道恢复授权数据。

### 5.3 指定参与人

指由 CXO 或管理员指定参加某一周或某类经营周会，但不属于 CXO 的成员。

能力：

- 查看自己被指定参与的周会记录。
- 只查看、填写、保存和提交自己的准备内容。
- 不能查看任何 CXO 的准备内容。
- 不能查看其他指定参与人的准备内容。
- 查看自己参与周会的 AI 会议提纲和会议结论。
- 默认只读会议结论；获得 `can_edit_decision` 显式授权后可参与协作编辑。
- 在年度汇总页查看自己实际参与周次的 AI 会议提纲和会议结论。

### 5.4 准备内容负责人

准备内容负责人既可以是 CXO，也可以是指定参与人。每位负责人拥有独立准备块：

- 可以使用经营周会默认准备模板。
- 可以使用已有的其他准备模板。
- 可以绑定个人专属模板。
- 同一人员在不同周会中可以使用不同模板。
- 周会创建后保存模板快照，后续修改模板不影响已创建周会。

当前阶段只启用四个 CXO 准备块，且准备内容只允许 CXO 查看。扩展指定参与人后，仍按“CXO 看全部、指定参与人只看自己”的规则执行。

## 6. 业务流程

### 6.1 新建经营周会

1. 用户进入 `目标计划 -> 经营周会`。
2. 点击“新建经营周会”。
3. 选择周时间范围，例如 `2026-07-13 ~ 2026-07-19`。
4. 选择模板，默认使用“经营周会默认模板”。
5. 选择本周参与人；当前阶段默认只有四个 CXO，后续可以追加指定参与人。
6. 为参与人选择准备模板，可使用默认模板、已有模板或个人模板。
7. 系统根据参与人和模板生成本周准备块，并保存模板快照。
8. 保存后进入该周独立经营周会详情页。

### 6.2 准备模块

1. 各负责人进入本周经营周会的“准备”页签。
2. 系统只返回当前用户有权查看的准备块。
3. CXO 可以查看全部准备块；指定参与人只能看到自己的准备块。
4. 负责人找到自己负责的准备块。
5. 在文档编辑区填写内容。
6. 点击“提交本块”。
7. 系统记录提交人、提交时间和状态。
8. 全部必填准备块提交后，状态变为“准备已完成”。

### 6.3 AI 生成会议提纲

1. 全部必填准备块提交后，“生成会议提纲”按钮可用。
2. 点击按钮后，前端本地解密内容。
3. 前端本地做敏感信息脱敏。
4. 将脱敏后的结构化内容提交给 AI。
5. AI 返回固定结构的会议提纲。
6. 系统再次扫描提纲中的敏感信息。
7. 扫描通过后保存加密后的会议提纲。

### 6.4 会后填写结论

1. 会议结束后，授权人员进入“会议结论”区域。
2. 记录最终决策、负责人、完成时间和后续动作。
3. 保存后加密存储。

### 6.5 年度汇总

1. 用户进入“经营周会 -> 年度汇总”。
2. 选择年份，默认当前年份。
3. 系统按周展示该年度 AI 会议提纲和会议结论。
4. CXO 可以查看该年度全部周会的会议内容。
5. 指定参与人只能查看自己实际参与周次的会议内容。
6. 年度汇总不展示任何人的准备内容，避免准备内容权限被聚合页绕过。
7. 汇总页在浏览器逐条解密有权访问的会议记录，服务端不生成包含明文的年度汇总副本。

## 7. 页面结构

### 7.1 列表页

路径建议：`/executive/operational` 或 `/operational-meetings`

展示字段：

- 周时间范围
- 会议状态
- 准备完成进度
- AI 提纲状态
- 会议结论状态
- 创建人
- 更新时间

状态建议：

- 草稿
- 准备中
- 准备已完成
- 提纲已生成
- 已开会
- 已归档

列表操作：

- 新建
- 查看
- 编辑
- 生成提纲
- 归档
- 删除

### 7.2 每周独立详情页

每一周经营周会使用独立页面，建议路径：`/operational-meetings/:id`。

页面顶部展示周时间范围、参与人、准备完成进度和会议状态，并固定提供两个一级页签：

- `准备`
- `会议`

不再以临时抽屉作为主要承载方式，用户刷新、复制链接和返回时都能稳定定位到具体周次和页签。

#### 7.2.1 准备页签

顶部显示：

- 周时间范围
- 当前状态
- 已完成准备块数量
- 必填块数量
- 最近更新时间

CXO 下方显示所有参与人的准备块；指定参与人只显示自己的准备块。当前阶段默认显示四个 CXO 准备块：

- 海外项目组（陈锦标）
- 国内项目组（陈豪赞）
- 商务信息（林璐韵）
- 产研落地（贺敏）

每个折叠块展示：

- 准备块标题
- 负责人
- 状态：未填写 / 填写中 / 已提交 / 已锁定
- 提交人
- 提交时间
- 文档块编辑区
- 保存按钮
- 提交按钮

默认问题项：

1. 本周核心结果
2. 一个最重要的判断
3. 需要会上决策的问题
4. 下周建议动作

说明：

- 问题项通过模板配置生成，不写死在代码中。
- 内容编辑区复用文档中心 blocks 数据结构。
- 第一版可以复用文档中心已有编辑能力，后续再抽公共组件。

#### 7.2.2 会议页签

会议页签对本周全部 CXO 和指定参与人可见，包含两个区域：

- AI 会议提纲
- 会议结论

##### AI 会议提纲区

按钮：

- 生成会议提纲，仅具备 `generate_agenda` 权限的 CXO 显示
- 重新生成，仅具备 `generate_agenda` 权限的 CXO 显示
- 复制
- 保存为正式提纲

生成条件：

- 所有必填准备块均已提交。
- 当前用户是具备 `generate_agenda` 权限的 CXO。
- 当前用户已解锁安全密钥。

会议提纲固定包含：

一、本次会议目标

二、本周经营总览

三、本次重点讨论和决策议题

四、建议会议议程

五、下周动作建议

六、请大家会前准备

安全要求：

- 不输出毛利数据信息。
- 不输出毛利率、利润、利润率、GM、gross profit 等敏感指标。
- 不输出可反推出毛利的明细组合。

##### 会议结论区

支持填写：

- 决策事项
- 负责人
- 截止时间
- 后续动作
- 风险和待确认问题

第一版可用文档块编辑区或结构化列表，建议结构化列表更利于后续追踪。

### 7.3 年度汇总页

建议路径：`/operational-meetings/annual?year=2026`。

页面包含：

- 年份选择器
- 全年周会数量
- 已生成提纲数量
- 已填写结论数量
- 按周倒序或正序切换
- 关键词搜索
- “仅看有结论”筛选

页面默认在同一页展开各周的 AI 会议提纲和会议结论，允许按周折叠，但不要求用户进入每周详情才能查看。为避免一次解密全年内容导致页面卡顿，可以按可视区域逐周解密和渲染；关键词搜索在浏览器完成已授权内容解密后执行，服务端不接触或索引会议明文。

每周汇总项展示：

- 周时间范围
- 会议状态
- AI 会议提纲六段内容
- 会议结论和决策事项
- 负责人、截止时间和动作状态
- 跳转到该周独立详情页的入口

年度汇总页不展示准备内容，不提供跨周读取个人准备块的接口。

## 8. 模板设计

经营周会必须模板驱动，避免硬编码。

### 8.1 模板字段

模板包含：

- 模板名称
- 模板状态
- 默认是否启用
- 适用模块 key
- 创建人
- 更新时间

### 8.2 准备模板块字段

每个准备模板块包含：

- 块标题
- 负责人用户
- 负责人角色
- 是否必填
- 排序
- 默认折叠状态
- 默认问题列表
- 默认文档 blocks
- 是否允许负责人外的人编辑
- 适用参与身份：`cxo` / `designated` / `all`
- 可见性策略：固定为 `cxo_and_owner`
- 是否为个人专属模板

### 8.3 模板管理能力

系统管理中新增“经营周会模板”配置，支持：

- 新增模板
- 编辑模板
- 启用 / 停用模板
- 新增准备模板块
- 删除准备模板块
- 调整准备模板块排序
- 修改负责人
- 修改默认问题项
- 设置必填 / 非必填
- 复制现有模板给指定参与人
- 为指定参与人创建个人模板
- 设置模板适用身份

## 9. 数据模型

### 9.1 operational_meeting_templates

经营周会模板表。

字段建议：

- `id`
- `name`
- `module_key`
- `is_default`
- `status`
- `created_by`
- `created_at`
- `updated_at`

### 9.2 operational_meeting_template_sections

准备模板块表。

字段建议：

- `id`
- `template_id`
- `section_key`
- `title`
- `owner_user_id`
- `owner_role`
- `is_required`
- `sort_order`
- `default_collapsed`
- `default_questions_json`
- `default_blocks_json`
- `editable_by_owner_only`
- `created_at`
- `updated_at`

### 9.3 operational_meetings

经营周会主表。

字段建议：

- `id`
- `template_id`
- `week_start`
- `week_end`
- `title`
- `security_level`：固定为 `SL3`，不允许普通用户降低
- `status`
- `preparation_status`
- `agenda_status`
- `decision_status`
- `created_by`
- `updated_by`
- `created_at`
- `updated_at`
- `archived_at`

说明：

- 周时间范围用 `week_start` 和 `week_end` 独立存储，便于查询和展示。
- 同一周默认只允许一个未删除经营周会，避免重复。

### 9.4 operational_meeting_sections

经营周会实际准备块表。

字段建议：

- `id`
- `meeting_id`
- `template_section_id`
- `section_key`
- `title`
- `owner_user_id`
- `owner_type`：`cxo` / `designated`
- `template_snapshot_json`
- `visibility_scope`：固定为 `cxo_and_owner`
- `security_level`：继承周会，固定为 `SL3`
- `status`
- `content_ciphertext`
- `content_text_ciphertext`
- `crypto_version`
- `submitted_by`
- `submitted_at`
- `locked_at`
- `created_at`
- `updated_at`

状态：

- `draft`
- `submitted`
- `locked`

### 9.5 operational_meeting_participants

每周经营周会参与人表，是周会内容权限的主要依据。

字段建议：

- `id`
- `meeting_id`
- `user_id`
- `participant_type`：`cxo` / `designated`
- `preparation_section_id`
- `preparation_template_id`
- `can_generate_agenda`
- `can_edit_decision`：指定参与人默认 `false`，按需单独授权
- `status`：`active` / `removed`
- `created_by`
- `created_at`
- `updated_at`

约束：

- 同一周会同一用户只能有一条有效参与记录。
- CXO 默认加入每周周会，也允许按配置调整。
- 指定参与人必须明确加入具体周会，不能仅凭敏感模块权限看到所有周会。
- `can_generate_agenda` 只允许对 CXO 开启，指定参与人固定为 `false`，避免其浏览器为生成提纲而取得其他人的准备明文。
- 移除参与人后，需要同步执行记录密钥撤销和必要的重加密。

### 9.6 operational_meeting_agendas

AI 会议提纲表。

字段建议：

- `id`
- `meeting_id`
- `agenda_ciphertext`
- `agenda_text_ciphertext`
- `security_level`：固定为 `SL3`
- `source_hash`
- `model_provider`
- `model_name`
- `prompt_version`
- `safety_scan_status`
- `generated_by`
- `generated_at`
- `created_at`
- `updated_at`

### 9.7 operational_meeting_decisions

会议结论 / 决策表。

字段建议：

- `id`
- `meeting_id`
- `decision_ciphertext`
- `decision_text_ciphertext`
- `security_level`：固定为 `SL3`
- `owner_user_id`
- `due_date`
- `status`
- `created_by`
- `created_at`
- `updated_at`

### 9.8 sensitive_module_members

敏感模块授权人员表。

字段建议：

- `id`
- `module_key`
- `user_id`
- `permission_level`
- `created_by`
- `created_at`

`module_key` 示例：

- `operational_meeting`
- `executive_reports`
- `weekly_reports`

### 9.9 crypto_user_keys

用户加密密钥表。

字段建议：

- `id`
- `user_id`
- `public_key_jwk`
- `encrypted_private_key_jwk`
- `kdf_algorithm`
- `kdf_params_json`
- `key_version`
- `is_current`
- `status`
- `created_at`
- `rotated_at`

说明：

- 公钥可以明文存在数据库。
- 私钥必须先在浏览器中用用户安全密码加密，再保存到数据库。
- 服务端永远不能拿到明文私钥。
- 同一用户允许保留多个历史密钥版本，唯一约束为 `(user_id, key_version)`，不能只保留一行并覆盖旧密钥。
- 正常修改安全密码时只重新包裹同一份私钥，不生成新公私钥，不改变 `key_version`。
- 发生密钥泄漏、私钥损坏或恢复操作时才创建新密钥版本；旧版本在相关记录完成重新授权前保留为只读恢复状态。

### 9.10 crypto_record_keys

记录级内容密钥授权表。

字段建议：

- `id`
- `record_type`
- `record_id`
- `recipient_type`：`user` / `owner_recovery` / `cxo_threshold_recovery`
- `recipient_id`：用户 ID 或对应恢复公钥版本 ID
- `encrypted_dek`
- `recipient_key_version`
- `dek_version`
- `grant_status`
- `granted_by`
- `created_at`
- `revoked_at`

说明：

- 每份经营周会内容生成随机 DEK。
- DEK 分别用授权用户公钥加密后保存。
- 用户打开内容时，只能用自己的私钥解开属于自己的 `encrypted_dek`。

记录密钥分发规则：

- 准备块：分发给全部有效 CXO，以及该准备块负责人本人。
- CXO 自己的准备块：只需分发给全部有效 CXO，不额外扩大范围。
- 指定参与人的准备块：分发给全部有效 CXO 和该指定参与人本人，不能分发给其他指定参与人。
- AI 会议提纲：分发给该周全部有效 CXO 和全部有效指定参与人。
- 会议结论：分发给该周全部有效 CXO 和全部有效指定参与人。
- 年度汇总：不创建新的年度明文或年度共用 DEK，复用每周会议提纲和会议结论的记录级授权。
- 所有 SL2 / SL3 记录额外生成两份恢复 encrypted DEK：一份使用老板恢复公钥，一份使用 CXO 门限恢复公钥。

### 9.11 company_recovery_keys

公司恢复公钥及版本元数据表。每个版本分别保存老板恢复通道和 CXO 门限通道的公钥，服务端不保存可直接使用的恢复私钥。

字段建议：

- `id`
- `key_version`
- `recovery_channel`：`owner` / `cxo_threshold`
- `public_key_jwk`
- `threshold`：CXO 通道默认 `2`
- `share_count`：CXO 通道默认 `3`
- `custody_policy_version`
- `status`：`active` / `rotating` / `retired`
- `activated_by`
- `activated_at`
- `rotated_at`

### 9.12 crypto_recovery_requests

密钥恢复申请表。

字段建议：

- `id`
- `request_type`：`forgot_security_password` / `disaster_restore` / `user_key_loss` / `key_rotation`
- `target_user_id`
- `scope_type`：`record` / `module` / `all_authorized_records`
- `scope_id`
- `reason`
- `status`：`pending` / `approved` / `executing` / `completed` / `rejected` / `expired`
- `recovery_channel`：`owner` / `cxo_threshold`
- `required_approvals`：老板通道为 `1`，CXO 通道默认 `2`
- `requested_by`
- `expires_at`
- `completed_at`

### 9.13 crypto_recovery_approvals

恢复审批记录表。

字段建议：

- `id`
- `request_id`
- `approver_user_id`
- `decision`：`approved` / `rejected`
- `mfa_verified_at`
- `signature`
- `comment`
- `created_at`

约束：

- 同一审批人只能审批一次。
- 申请人不能同时作为两个审批名额。
- 老板通道必须验证老板专属硬件恢复凭证；CXO 通道至少两名有效 CXO审批后才能进入恢复执行阶段。
- 审批在短时间窗口内有效，默认 30 分钟，超时必须重新申请。

### 9.14 security_events

高敏安全事件表，用于记录密钥和权限操作，不记录业务明文。

字段建议：

- `id`
- `event_type`
- `actor_user_id`
- `subject_user_id`
- `resource_type`
- `resource_id`
- `security_level`
- `result`
- `previous_event_hash`
- `event_hash`
- `created_at`

事件同时异步写入独立审计存储，避免运维通过修改业务数据库删除痕迹。

## 10. 权限设计

### 10.0 权限模型概览

经营周会不能只依靠“是否在敏感模块名单”判断内容可见性。最终权限由四层共同决定：

1. 菜单权限：能否看到经营周会入口。
2. 敏感模块权限：能否进入经营周会模块。
3. 周会参与身份：是否为 CXO，或是否被指定参加该周周会。
4. 记录级内容权限：当前内容属于准备块、AI 提纲还是会议结论，以及用户是否持有对应记录 DEK。

### 10.1 两道门

进入经营周会模块必须同时满足两道基础权限：

1. 菜单权限：用户拥有经营周会菜单 key。
2. 敏感模块权限：用户在 `operational_meeting` 的敏感授权名单内。

即使用户有菜单权限，只要不是敏感授权人员，也不能看到菜单、不能访问页面、不能调用后端接口。通过两道基础权限后，仍需根据每周参与身份和内容类型继续做行级、记录级校验。

### 10.2 前端权限

前端处理：

- 目标计划菜单下新增“经营周会”。
- 只有同时满足菜单权限和敏感模块权限时才展示入口。
- 如果用户直接访问路由，展示无权限页面。
- 每周详情页只能渲染后端明确返回的可见准备块，不能先返回全部再由前端隐藏。
- 指定参与人看不到其他人的准备块标题、负责人、状态、更新时间和密文标识。
- 年度汇总只展示当前用户有权访问的会议提纲和会议结论。

### 10.3 后端权限

后端所有经营周会 API 必须挂统一中间件：

- 已登录。
- 具备菜单权限。
- 具备敏感模块权限。
- 写操作需具备写权限。
- 查询周会详情时按参与身份过滤准备块。
- 查询会议页签时校验当前用户是该周有效参与人。
- 查询年度汇总时逐周过滤参与权限。

不能只依赖前端隐藏。

### 10.4 操作权限

建议权限分级：

- `view_meeting`：查看自己有权参与周次的 AI 会议提纲和会议结论。
- `view_all_preparations`：查看该周全部准备内容，仅 CXO 可拥有。
- `view_own_preparation`：查看自己的准备内容。
- `edit_own`：编辑自己负责的准备块。
- `edit_all`：编辑该周全部准备块，CXO 身份默认拥有。
- `generate_agenda`：汇总全部准备内容并生成 AI 提纲，只允许 CXO 获得。
- `edit_decision`：编辑会议结论。
- `manage`：管理模板、授权和归档。

### 10.5 内容可见性矩阵

| 内容 | CXO | 本周指定参与人 | 非本周参与人 | 管理角色但非参与人 |
| --- | --- | --- | --- | --- |
| CXO 的准备内容 | 可查看全部 | 不可查看 | 不可查看 | 不可查看明文 |
| 指定参与人的准备内容 | 可查看全部 | 仅本人可查看自己的 | 不可查看 | 不可查看明文 |
| AI 会议提纲 | 可查看 | 可查看 | 不可查看 | 不可查看明文 |
| 会议结论 | 可查看 | 可查看 | 不可查看 | 不可查看明文 |
| 年度会议汇总 | 全年全部周次 | 仅自己参与周次 | 不可查看 | 不可查看明文 |

### 10.6 当前阶段与扩展阶段

当前阶段：

- 准备模块只有四个 CXO 准备块。
- 只有 CXO 能看到准备内容。
- 会议模块也仅对 CXO 开放，因为尚未配置其他指定参与人。

扩展阶段：

- 可以为某周新增指定参与人。
- 指定参与人获得自己的准备块和模板。
- CXO 自动获得该指定参与人准备块的查看权限。
- 指定参与人只获得自己准备块的查看、编辑权限。
- 该周 AI 会议提纲和会议结论同时授权给 CXO 和全部有效指定参与人。

### 10.7 防越权要求

- 后端不得向指定参与人返回其他准备块的密文、标题、状态和负责人信息。
- 修改 `sectionId`、`meetingId` 或年度汇总查询参数不能绕过权限。
- 下载附件、预览附件、历史版本和审计详情必须复用准备块或会议记录的可见性规则。
- AI 生成时只有具备 `generate_agenda` 权限的 CXO 可以汇总全部准备内容；指定参与人不得取得该权限，也不得取得其他准备块的明文或 DEK。
- 普通指定参与人的浏览器不能取得其他准备块的 encrypted DEK，因此即使截获接口响应也无法解密。

### 10.8 管理权与解密权分离

- 平台运维管理员可以部署、备份和恢复，但不能获得任何用户 encrypted DEK 或两套公司恢复私钥。
- 账号管理员可以重置登录密码，但不能替用户重置安全密码、覆盖用户公钥或读取历史密文。
- 数据安全管理员可以发起授权和恢复流程；SL3 授权和密钥替换必须双人审批，恢复则使用“老板硬件凭证”或“两名 CXO门限”任一通道。
- CXO 身份、敏感模块身份和记录密钥授权是三个独立状态；修改账号角色不能自动补发历史记录密钥。
- 后端不得因为用户是 `admin`、数据库管理员或运维角色而绕过记录级权限。
- 普通管理员不能自行创建同级或更高权限账号；管理角色变更必须由数据安全管理员审批。

### 10.9 安全等级策略

经营周会固定为 `SL3`。统一安全等级定义如下：

| 等级 | 名称 | 适用内容 | 加密与权限要求 |
| --- | --- | --- | --- |
| SL1 | 内部 | 普通任务、基础资料、低敏文档 | 登录、RBAC、服务端静态加密 |
| SL2 | 机密 | 关键人脉、公司情报、机密文档 | 浏览器端加密、显式授权、公司恢复密钥 |
| SL3 | 核心机密 | 经营周会、重大决策、核心财务和战略 | SL2 + 双人审批、MFA、可信发布、限制 AI / 导出 |

规则：

- 父级文件夹或业务记录的安全等级可以向下继承，子内容不能低于父级等级。
- 附件、历史版本、全文摘要和搜索索引必须继承正文安全等级。
- SL2 / SL3 降级需要数据安全管理员发起并由另一名 CXO 批准。
- 服务端只允许对 SL1 建立明文全文索引；SL2 / SL3 搜索在浏览器解密后执行，或使用经过独立评审的加密索引。

### 10.10 首次初始化与管理员创建

- 系统不得在代码或启动脚本中自动创建默认管理员、默认密码或固定 JWT 密钥。
- 全新数据库启动后进入“待所有者接管”状态，只开放健康检查和一次性初始化接口，不开放业务接口。
- 一次性初始化凭证由老板控制的发布 / 安全流程生成，默认 15 分钟过期、只能使用一次，不写入日志和数据库明文。
- 老板使用初始化凭证创建 `owner` 身份、绑定 MFA / Passkey，并完成两套公司恢复密钥仪式。
- 首次接管成功后初始化接口永久关闭；再次开启必须使用线下恢复流程，不能通过修改普通配置开启。
- 平台运维账号由老板或账号管理员在接管后创建，默认没有任何业务模块、敏感授权和记录密钥。
- 后续创建 `platform_operator`、`account_admin`、`security_admin` 或 CXO 身份需要符合对应审批策略，不能由普通管理员任意填写角色字符串完成提权。
- 即使运维直接向数据库插入 `admin` 或 `cxo` 记录，也不会自动获得 encrypted DEK、公司恢复私钥或历史 SL2 / SL3 明文。

## 11. 新加密方案

### 11.1 目标

新加密方案要满足：

- 运维拿到数据库，看不到明文。
- 运维拿到服务器密钥文件，看不到新高敏内容。
- 运维有服务器文件权限，也不能直接解密新高敏内容。
- 不影响旧 `enc:v1:` 数据读取。
- 后续可逐步迁移已有高敏数据。
- 运维可以使用代码、MySQL 备份和 OSS 备份恢复完整系统，但恢复完成后仍只能看到密文。
- 老板或任意两名有效 CXO 可以恢复解密能力，且恢复过程不能把公司恢复私钥交给运维或业务服务器。
- 用户修改登录密码或安全密码后，前后历史数据仍可正常访问。
- 单个用户忘记安全密码、丢失电脑或私钥损坏时，不得导致公司数据永久丢失。

### 11.2 方案原则

核心原则：

**可用于解密的明文密钥不能放在服务器上，SL2 / SL3 解密不能发生在普通业务服务器上。**

只要服务器能解密，拥有服务器权限的人理论上就能看到明文。因此经营周会内容采用端到端加密：

- 浏览器本地加密。
- 浏览器本地解密。
- 服务端只保存密文。
- 服务端只保存被授权用户公钥和两套公司恢复公钥加密后的内容密钥。

### 11.3 密钥结构

每个用户拥有：

- 公钥：存服务端。
- 私钥：浏览器生成后，用用户安全密码加密，再存服务端。
- 安全密码：只在用户脑中和浏览器运行时存在，不传服务端。
- 用户密钥版本：正常改安全密码不变更版本；发生泄漏、损坏或恢复时才生成新版本。

每份记录拥有：

- DEK：随机内容密钥，用于加密会议内容。
- encrypted DEK：用每个授权人的公钥分别加密一份。

公司拥有两套互相独立的恢复密钥：

- 老板恢复公钥：用于包裹一份恢复 DEK，可以保存在服务端。
- 老板恢复私钥：只存在于老板专属硬件安全密钥和异地备用硬件中，老板本人可独立完成恢复。
- CXO 门限恢复公钥：用于包裹另一份恢复 DEK，可以保存在服务端。
- CXO 门限恢复私钥：生成后拆分为 3 份恢复分片，任意 2 名有效 CXO共同操作才能临时恢复。
- 恢复材料：分别存放在老板硬件凭证、不同 CXO 的硬件介质和异地保险柜 / 安全托管中。

两套恢复私钥只用于密钥恢复和重新授权，不用于日常查看业务内容。

### 11.4 加密流程

新建经营周会内容时：

1. 浏览器生成随机 DEK。
2. 浏览器使用 DEK 加密内容 blocks。
3. 浏览器根据内容类型计算授权接收人：准备块使用“全部 CXO + 本人”，会议内容使用“该周全部参与人”。
4. 浏览器读取这些授权人员的公钥。
5. 浏览器用每个授权人员公钥加密 DEK。
6. 浏览器分别使用老板恢复公钥和 CXO 门限恢复公钥，各加密一份 DEK。
7. 服务端保存内容密文、用户 encrypted DEK 和两份恢复 encrypted DEK。

查看经营周会内容时：

1. 用户输入安全密码。
2. 浏览器用安全密码解开用户私钥。
3. 浏览器获取该用户对应的 encrypted DEK。
4. 浏览器用私钥解开 DEK。
5. 浏览器用 DEK 解密内容。

服务端全程不接触明文内容、明文私钥和明文 DEK。

CXO 门限恢复过程中的私钥合成、DEK 解包和重新包裹必须在受信任的 CXO 客户端中完成，不上传服务端。

### 11.5 授权新增人员

给新人员授权时，必须先判断内容类型：

- 新增指定参与人时，只给其自己的准备块和该周会议内容补充授权。
- 不能给其补充其他人员准备块的授权。
- 新增 CXO 时，可以按公司规则给历史准备内容和会议内容补充授权。

授权流程：

1. 已授权用户打开记录并本地解密 DEK。
2. 浏览器获取新人员公钥。
3. 浏览器用新人员公钥加密 DEK。
4. 服务端新增一条 `crypto_record_keys`。

SL3 额外要求：

- 新增 CXO、扩大历史范围或批量授权必须由两名 CXO审批。
- 普通周会参与人加入具体周次时，可以由具备周会管理权的 CXO 单人操作，但授权范围只能是该周会议内容及其本人准备块。
- 授权操作只补充 encrypted DEK，不应把明文内容发送到服务端。

### 11.6 撤销人员权限

撤销后需要重加密：

1. 已授权用户本地解密内容。
2. 浏览器生成新 DEK。
3. 浏览器用新 DEK 重新加密内容。
4. 浏览器只给剩余授权人重新分发 encrypted DEK。
5. 原 encrypted DEK 标记为 revoked。

说明：

- 如果被撤销人员曾经在撤销前看过内容，无法阻止其已经截图或离线保存。
- 撤销主要保证撤销后无法继续访问更新后的内容。
- 撤销指定参与人后，需要撤销其自己的准备块和该周会议内容密钥，但不能影响 CXO 的持续访问。
- 如果指定参与人仍需保留自己的历史准备内容，可只撤销会议后续版本权限；具体策略由管理员操作时明确选择。

### 11.7 旧数据兼容

现有 `enc:v1:` 继续保留：

- 旧数据仍用服务端密钥读取。
- 新经营周会内容默认使用 `enc:v2:`。
- 新加密模块需要支持识别加密版本。
- 后续对已有高敏模块做迁移时，采用逐步迁移，不一次性强制迁移。

### 11.8 安全密码找回

安全密码本身不能由服务端找回，因为服务端从未持有安全密码。系统恢复的是“数据访问能力”，不是找回原密码。

正式恢复流程：

1. 用户先通过登录密码重置和 MFA 恢复账号身份。
2. 用户在可信浏览器生成新的用户公私钥，私钥使用新安全密码加密。
3. 用户提交“忘记安全密码”恢复申请，选择恢复范围和原因。
4. 用户选择恢复通道：老板通道由老板完成 MFA 和硬件凭证验证；CXO 通道由两名有效 CXO分别完成 MFA 并批准申请。
5. 老板通道在可信客户端使用老板硬件恢复私钥；CXO 通道由两名保管人输入各自恢复分片，客户端临时合成 CXO 门限恢复私钥。
6. 客户端读取对应恢复通道的 encrypted DEK，解开相关记录 DEK。
7. 客户端使用用户新公钥重新加密 DEK，并新增用户记录密钥授权。
8. 客户端立即清除内存中的恢复私钥、分片和明文 DEK。
9. 系统记录恢复范围、审批人、执行设备、结果和哈希审计，不记录任何明文。

恢复默认只恢复该用户原本有权访问的记录，不能借恢复流程扩大权限。

### 11.9 年度汇总加密原则

- 年度汇总服务端只返回周次、状态、记录 ID、密文和当前用户对应的 encrypted DEK。
- 浏览器逐条解密 AI 会议提纲和会议结论后组合展示。
- 服务端不保存年度汇总明文，不创建“所有周会共用密钥”。
- CXO 的年度页可解密全年记录；指定参与人只会收到自己参与周次的记录和 DEK。
- 年度汇总的关键词搜索在浏览器解密后本地执行，服务端不建立会议内容明文索引。

### 11.10 登录密码与安全密码关系

| 场景 | 处理方式 | 对历史数据影响 |
| --- | --- | --- |
| 用户修改登录密码 | 校验旧密码，更新密码哈希和 `password_version`，注销全部旧会话 | 无影响 |
| 管理员重置登录密码 | 完成身份核验和 MFA，只重置登录密码 | 不自动获得解密能力 |
| 用户知道旧安全密码并修改 | 解开原私钥后，使用新安全密码重新加密同一私钥 | 前后数据全部可读 |
| 用户忘记安全密码 | 生成新密钥，通过老板硬件通道或两名 CXO门限通道补发原授权记录 DEK | 原授权范围全部恢复 |
| 用户更换电脑 / 浏览器 | 下载 encrypted private key，输入安全密码解锁 | 无影响 |
| 用户私钥疑似泄漏 | 生成新密钥版本，重新包裹授权记录 DEK，吊销旧版本 | 完成迁移后无影响 |
| 用户离职 | 停用账号、失效会话、撤销授权，SL3 内容按策略轮换 DEK | 在职用户不受影响 |

密码安全要求：

- 登录密码和安全密码用途不同，不允许系统自动设成相同值，也不能相互替代。
- 登录密码服务端只保存强哈希，推荐 Argon2id；兼容期可保留 bcrypt，并在用户下次登录时升级哈希。
- 安全密码用于本地包裹用户私钥，推荐 Argon2id 内存困难 KDF；KDF 算法、盐、内存、迭代次数和版本随 encrypted private key 保存。
- 安全密码建议至少 12 位或 4 个随机词组，不能只依赖 8 位最低要求。
- SL3 用户必须启用 MFA / Passkey；重置登录密码、替换用户公钥、轮换密钥和恢复操作均要求再次验证。
- 登录凭证泄漏后，攻击者不能仅凭登录权限覆盖用户公钥；正常覆盖必须提供旧私钥签名，无法提供时只能进入公司恢复审批流程。
- 登录接口需要限速、失败锁定、异常设备提醒和会话管理；离职或改密码后全部旧 token 立即失效。

### 11.11 用户密钥轮换

- 正常安全密码修改不得生成新 RSA 密钥，只重新加密原私钥。
- 主动轮换时生成新 `key_version`，旧版本不得立即删除。
- 系统统计旧版本仍关联的记录数量，全部重新包裹完成后才允许退役旧版本。
- 轮换期间用户密钥环可以同时解锁新旧版本，保证前后数据连续可读。
- 发现私钥泄漏时，除重新包裹 DEK 外，还需对高风险记录生成新 DEK 并重新加密内容，防止旧私钥和旧 encrypted DEK 组合继续解密。

### 11.12 公司恢复密钥保管

采用“老板独立恢复 + CXO `2-of-3` 门限恢复”双通道：

- 老板通道：老板恢复私钥写入两枚不可导出的硬件安全密钥，一枚日常封存、一枚异地备用；任何一枚都只能在老板完成身份验证后使用。
- CXO 分片 A：一名长期任职 CXO 保管。
- CXO 分片 B：另一名 CXO 保管。
- CXO 分片 C：第三名 CXO、公司保险柜、法务托管或独立安全托管保管。

要求：

- 老板硬件凭证和三份 CXO 分片不能存放在同一云账号、同一密码管理器或同一办公地点。
- 运维、开发外包和普通系统管理员不得保管恢复分片。
- 每季度执行一次不读取业务明文的分片可用性校验。
- 每年至少执行一次完整灾难恢复演练。
- 老板硬件凭证丢失、CXO 分片保管人变更或任一恢复材料疑似泄漏时，必须轮换公司恢复密钥，并逐步重新包裹所有 SL2 / SL3 DEK。

首次密钥仪式：

1. 在干净、受控、断开远程管理的可信设备上生成老板恢复密钥和 CXO 门限恢复密钥。
2. 将老板恢复私钥写入两枚硬件安全设备，设置独立 PIN 并分别封存。
3. 将 CXO 门限恢复私钥拆成 3 份，打印或写入加密离线介质，分别交付保管人。
4. 只把两套恢复公钥和公开指纹写入系统。
5. 使用一条测试记录分别验证老板通道和 CXO 通道都能恢复，然后销毁测试明文和生成设备上的临时私钥。
6. 三名保管人签署保管清单，记录介质编号、存放地点、最后校验时间和替补人规则，但不记录分片内容。

### 11.13 灾难恢复边界

运维可以独立完成：

- 部署已签名代码和基础设施。
- 恢复 MySQL 全量备份和增量日志。
- 恢复 OSS 密文附件。
- 恢复用户 encrypted private key、记录 encrypted DEK、两套公司恢复公钥和审批审计数据。
- 验证记录数量、附件数量、密文完整性和哈希一致性。

运维不能独立完成：

- 解密 SL2 / SL3 正文和附件。
- 生成、导出或恢复任一公司恢复私钥。
- 替老板或 CXO批准密钥恢复。
- 通过创建超级管理员绕过 encrypted DEK 授权。

恢复完成后，授权用户可以直接使用原安全密码解锁原私钥；只有原安全密码同时遗失时，才需要老板本人或两名 CXO执行公司恢复流程。

### 11.14 可信发布与运行代码

端到端加密无法单独防御“运维修改前端代码并等待用户输入安全密码”的攻击，因此 SL3 必须同时满足：

- 生产构建由老板控制的 CI/CD 身份生成，运维不能直接上传任意前端文件。
- 发布制品和容器镜像必须签名，生产环境只运行白名单签名和固定 digest 的版本。
- 主分支合并和生产发布至少需要一名代码审批人和一名业务 / 安全审批人。
- 生产服务器默认禁止长期 SSH root；紧急 shell 访问走 `break_glass` 双人审批、限时授权和会话审计。
- 浏览器端设置严格 CSP，禁止未授权脚本、内联脚本和第三方脚本加载。
- 高风险场景可进一步采用签名桌面客户端或企业浏览器扩展承载 SL3 解密逻辑。

### 11.15 附件加密与预览

经营周会附件、图片、PDF 和导入文件全部继承 `SL3`，不能继续以可猜测 URL 或公开静态目录提供。

上传流程：

1. 浏览器为每个附件生成独立文件 DEK。
2. 浏览器分块加密附件，OSS 只保存密文文件。
3. 文件 DEK 按正文相同的授权人规则包裹，并额外分别包裹给老板恢复公钥和 CXO 门限恢复公钥。
4. 数据库存储密文对象 key、文件哈希、加密算法、DEK 版本和授权记录，不存明文文件。

预览 / 下载流程：

1. 后端先校验记录级权限，再返回短时有效的密文下载地址。
2. 浏览器下载密文并在本地解密预览。
3. SL3 下载默认记录审计，可按策略增加水印、禁止批量下载或要求再次 MFA。
4. `/oss/*`、`/uploads/*` 等公共路径不得直接访问高敏附件。

### 11.16 搜索与索引

- SL1 可以使用服务端明文索引和全文搜索。
- SL2 / SL3 标题、正文、摘要和附件提取文本均不得进入服务端明文索引。
- 第一阶段在浏览器下载有权访问的密文，解密后建立本地临时索引。
- 本地索引存入 IndexedDB 时也必须使用用户设备密钥加密，退出登录或设备撤权后清除。
- 后续如引入盲索引或可信执行环境，需要独立安全评审并记录其可能泄露的关键词频率和访问模式。

## 12. AI 分析方案

### 12.1 AI 底层选择

经营周会“生成会议提纲”建议使用：

- OpenAI Responses API
- GPT-5.6 系列模型
- 结构化 JSON 输出

不建议把用户点击按钮时直接运行 Codex agent，原因：

- Codex 更适合代码仓库、开发工具、自动修复、代码审查等工程任务。
- 经营周会提纲属于业务文本分析，直接调用模型 API 更稳定、延迟更低、边界更清楚。
- Codex 可用于研发阶段实现、测试和维护该模块。

### 12.2 AI 调用安全链路

SL3 默认安全链路：

1. 浏览器本地解密经营周会内容。
2. 浏览器本地脱敏。
3. 浏览器通过老板 / CXO 控制的独立 AI 网关提交最小化、脱敏后的内容。
4. AI 返回会议提纲。
5. 浏览器本地再次扫描敏感信息。
6. 浏览器本地加密提纲。
7. 服务端保存密文。

说明：

- 普通业务服务不代理 SL3 AI 明文请求，避免业务服务器运维查看输入。
- AI 网关部署在独立云账号或独立安全域，由老板 / CXO 控制访问策略和日志策略。
- AI 网关不得记录请求正文、响应正文和调试抓包，只记录调用人、模型、时间、token 数和请求哈希。
- 调用外部模型意味着模型提供商会处理脱敏后的文本，必须使用企业级数据政策、关闭训练用途并明确留存策略。
- 如果公司无法接受任何外部模型接触内容，则 SL3 AI 必须关闭，或改为本地模型 / 可信执行环境。
- 浏览器直连模型仅作为过渡方案，API Key 不能长期明文保存在浏览器或普通用户设备中。

### 12.3 脱敏规则

AI 输入前必须移除或替换：

- 毛利
- 毛利率
- 利润
- 利润率
- GM
- gross profit
- gross margin
- 分成后利润
- 可反推毛利的收入、成本、分成比例组合

脱敏策略：

- 命中敏感字段名和相邻数字时整体替换为 `[已脱敏经营指标]`。
- 命中表格列名为敏感字段时，删除该列。
- 命中同一句中同时出现收入、成本、分成、利润等组合时，整体替换。
- 输出前再次扫描，命中则阻断保存。

### 12.4 Prompt 约束

系统提示词要求：

- 只基于用户提供的脱敏内容生成会议提纲。
- 不输出毛利、毛利率、利润、利润率、GM、gross profit 等敏感信息。
- 不猜测、推导或补全被脱敏数据。
- 输出固定六段结构。
- 语言简洁，突出经营判断、待决策问题和下周动作。

### 12.5 输出结构

AI 返回 JSON：

```json
{
  "meeting_goal": "",
  "weekly_overview": "",
  "key_topics": "",
  "agenda": "",
  "next_actions": "",
  "preparation": ""
}
```

前端渲染为：

一、本次会议目标

二、本周经营总览

三、本次重点讨论和决策议题

四、建议会议议程

五、下周动作建议

六、请大家会前准备

## 13. API 设计

### 13.1 经营周会

- `GET /api/operational-meetings`
- `POST /api/operational-meetings`
- `GET /api/operational-meetings/:id`
- `PUT /api/operational-meetings/:id`
- `DELETE /api/operational-meetings/:id`
- `POST /api/operational-meetings/:id/archive`
- `GET /api/operational-meetings/:id/participants`
- `PUT /api/operational-meetings/:id/participants`

### 13.2 准备内容

- `GET /api/operational-meetings/:id/preparations`
- `GET /api/operational-meetings/:id/preparations/mine`
- `PUT /api/operational-meeting-sections/:sectionId`
- `POST /api/operational-meeting-sections/:sectionId/submit`
- `POST /api/operational-meeting-sections/:sectionId/unsubmit`

说明：

- CXO 调用 preparations 返回全部准备块。
- 指定参与人调用 preparations 或 preparations/mine 都只能返回本人准备块。
- 无权限准备块必须返回 404 或 403，不能返回密文后依赖前端隐藏。

### 13.3 AI 提纲

- `POST /api/operational-meetings/:id/agenda/generate`
- `GET /api/operational-meetings/:id/agenda`
- `PUT /api/operational-meetings/:id/agenda`

说明：

- 如果采用浏览器直连 AI，则后端 `generate` 接口只负责生成 nonce、记录审计和保存密文结果。
- 如果采用服务端代理 AI，则请求体只能包含脱敏后的内容。
- `generate` 接口只允许具备 `generate_agenda` 权限的 CXO 调用，指定参与人只能读取生成后的提纲。

### 13.4 会议结论

- `GET /api/operational-meetings/:id/decisions`
- `POST /api/operational-meetings/:id/decisions`
- `PUT /api/operational-meeting-decisions/:decisionId`
- `DELETE /api/operational-meeting-decisions/:decisionId`

### 13.5 模板

- `GET /api/operational-meeting-templates`
- `POST /api/operational-meeting-templates`
- `PUT /api/operational-meeting-templates/:id`
- `DELETE /api/operational-meeting-templates/:id`
- `POST /api/operational-meeting-templates/:id/enable`

### 13.6 敏感模块授权

- `GET /api/admin/sensitive-modules`
- `GET /api/admin/sensitive-modules/:moduleKey/members`
- `PUT /api/admin/sensitive-modules/:moduleKey/members`

### 13.7 年度汇总

- `GET /api/operational-meetings/annual-summary?year=2026`

返回内容按周组织，只包含：

- 周时间范围和会议状态
- AI 会议提纲密文及当前用户记录密钥
- 会议结论密文及当前用户记录密钥
- 决策事项的非明文状态字段

不返回任何准备块内容或准备块密文。

### 13.8 用户密钥与安全密码

- `GET /api/crypto/user-keys`
- `POST /api/crypto/user-keys`
- `PUT /api/crypto/user-keys/:keyVersion/rewrap`
- `POST /api/crypto/user-keys/rotate`
- `POST /api/crypto/user-keys/:keyVersion/retire`

说明：

- `rewrap` 只更新 encrypted private key，必须由旧私钥签名证明当前用户确实解锁了原密钥。
- `rotate` 创建新密钥版本，不能覆盖旧版本。
- 任何密钥替换都要求再次 MFA，并写入安全审计。

### 13.9 公司密钥恢复

- `GET /api/crypto/company-recovery-key`
- `POST /api/crypto/recovery-requests`
- `GET /api/crypto/recovery-requests/:id`
- `POST /api/crypto/recovery-requests/:id/approve`
- `POST /api/crypto/recovery-requests/:id/reject`
- `POST /api/crypto/recovery-requests/:id/commit-rewrapped-keys`
- `POST /api/crypto/company-recovery-key/rotate`

说明：

- 服务端只协调申请、审批、范围校验和保存重新包裹后的 encrypted DEK。
- 老板恢复私钥、CXO 门限恢复私钥、恢复分片和明文 DEK 不得出现在任何请求体中。
- `commit-rewrapped-keys` 必须校验恢复通道、老板硬件凭证或 CXO审批数量、申请有效期、目标用户原权限范围和客户端签名。

### 13.10 安全等级与双人审批

- `GET /api/security/classifications/:resourceType/:resourceId`
- `PUT /api/security/classifications/:resourceType/:resourceId`
- `POST /api/security/approval-requests`
- `POST /api/security/approval-requests/:id/approve`

经营周会返回的 `security_level` 固定为 `SL3`，普通更新接口不能修改。

## 14. 审计日志

必须记录以下操作：

- 新建经营周会
- 新增或移除周会参与人
- 为参与人分配或更换准备模板
- 修改周时间范围
- 编辑准备块
- 提交准备块
- 生成 AI 提纲
- 重新生成 AI 提纲
- 保存会议结论
- 修改敏感模块授权
- 新增或撤销加密授权
- 模板修改
- 查看年度汇总
- 登录密码修改和管理员重置
- 安全密码重新包裹私钥
- 用户密钥创建、轮换、退役和异常覆盖尝试
- 公司恢复申请、审批、执行和失败
- SL2 / SL3 授权、撤权、降级和批量导出
- SL3 附件预览、下载和分享
- 紧急运维权限申请、批准、登录和退出
- 生产版本签名、发布和回滚

审计日志不得记录明文内容，只记录：

- 操作类型
- 操作人
- 记录 ID
- 时间
- 状态变化
- hash 或摘要标识

审计可靠性要求：

- 使用 `previous_event_hash + 当前事件` 生成哈希链。
- 同步或异步复制到独立云账号的 WORM / 不可修改存储。
- 业务数据库日志被删除或修改时，审计系统能够检测哈希断链。
- 平台运维不能删除独立审计副本。

## 15. 验收标准

### 15.1 功能验收

1. 目标计划菜单下可以看到“经营周会”入口。
2. 用户可以新建经营周会并选择周时间范围。
3. 列表正确展示周时间范围和状态。
4. 每周详情使用独立页面，并显示“准备 / 会议”两个一级页签。
5. 当前阶段新建后自动生成默认四个 CXO 准备块。
6. 扩展后可以新增指定参与人，并为其使用默认、已有或个人准备模板。
7. 每个准备块可独立编辑、保存、提交。
8. 准备块内容支持文档中心基础块编辑能力。
9. 所有必填准备块提交后，生成会议提纲按钮可用。
10. AI 生成结果包含固定六段结构。
11. AI 生成结果不包含毛利、毛利率、利润、GM 等敏感信息。
12. 会后可以填写会议结论。
13. 模板中新增或删除准备块后，新建周会按新模板生成。
14. 年度汇总页可以一次查看全年 AI 会议提纲和会议结论，并可跳转到具体周次。

### 15.2 权限验收

1. 没有菜单权限的用户看不到经营周会入口。
2. 有菜单权限但没有敏感模块授权的用户仍看不到经营周会入口。
3. 直接访问路由时，无敏感授权用户被拦截。
4. 直接调用后端 API 时，无敏感授权用户返回 403。
5. CXO 可以编辑该周任意准备块；非 CXO 参与人只能编辑自己的准备块。
6. 敏感模块授权变更后，前后端权限即时生效。
7. 当前阶段非 CXO 即使拥有菜单和敏感模块权限，也不能查看四个 CXO 的准备内容。
8. CXO 可以查看同周全部准备内容。
9. 指定参与人只能看到自己的准备内容，不能看到 CXO 或其他指定参与人的准备内容和元数据。
10. CXO 和该周指定参与人都能查看 AI 会议提纲和会议结论。
11. 非该周参与人不能查看该周 AI 会议提纲和会议结论。
12. CXO 年度汇总可查看全年会议内容；指定参与人只可查看自己参与周次。
13. 年度汇总接口不返回准备内容。
14. 指定参与人不能调用 AI 提纲生成接口，也不能通过生成流程取得其他人的准备明文或 DEK。

### 15.3 加密验收

1. 新经营周会内容不以明文保存到数据库。
2. 新经营周会内容不使用旧服务端密钥 `enc:v1:` 作为唯一保护。
3. 删除服务器旧密钥后，新经营周会内容仍可由授权用户在浏览器端解密。
4. 未授权用户即使拿到数据库内容，也无法解密经营周会内容。
5. 服务端日志不出现经营周会明文。
6. 旧 `enc:v1:` 数据仍可正常读取。
7. 指定参与人的准备块 DEK 只分发给本人和 CXO。
8. AI 会议提纲和会议结论 DEK 分发给该周全部有效参与人。
9. 指定参与人无法取得其他准备块的 encrypted DEK。

### 15.4 AI 安全验收

1. AI 输入前会进行本地脱敏。
2. 脱敏后的内容不包含毛利相关字段和数字。
3. AI 输出前或保存前会二次扫描敏感信息。
4. 命中敏感信息时，阻断保存并提示用户。
5. AI 输出结构稳定，可直接渲染到六段提纲。

### 15.5 密钥与密码恢复验收

1. 修改登录密码后全部旧 token 立即失效，历史 SL3 数据仍可解密。
2. 知道旧安全密码时，修改安全密码只重新包裹原私钥，历史和新数据都可解密。
3. 忘记安全密码时，单个管理员、单个非老板 CXO 和平台运维都不能完成恢复。
4. 老板使用专属硬件恢复凭证，或者两名 CXO审批并提供满足门限的恢复分片后，可以恢复用户原授权范围内的历史记录。
5. 恢复过程不能把公司恢复私钥、恢复分片和明文 DEK发送给服务端。
6. 用户密钥轮换期间，新旧数据都可正常查看；旧版本仅在关联记录清零后允许退役。
7. 修改账号角色、创建超级管理员或直接修改数据库不能自动获得历史 encrypted DEK。
8. 登录接口具备限速和锁定策略，SL3 用户未启用 MFA 时不能查看或创建真实 SL3 内容。
9. 系统不存在默认管理员账号、默认密码或代码内置的生产 JWT 密钥。

### 15.6 运维与灾难恢复验收

1. 使用代码、MySQL 备份和 OSS 备份可以在全新环境恢复系统。
2. 运维恢复后可以验证账号、记录、附件和密钥元数据完整，但不能读取 SL2 / SL3 明文。
3. 原授权用户使用原安全密码可以在恢复环境直接解密历史数据。
4. 原安全密码遗失时，老板本人或两名 CXO可以通过公司恢复流程恢复访问。
5. 数据库、OSS、备份文件和服务器文件中均不存在老板恢复私钥、CXO 门限恢复私钥和明文恢复分片。
6. 未签名或 digest 不在白名单的前端 / 容器制品不能发布到生产。
7. 紧急 root 权限必须双人审批、自动过期并产生独立审计记录。
8. 完整恢复演练满足 `RPO <= 5 分钟`、`RTO <= 2 小时` 的目标，若基础设施暂不支持，应记录实际结果和改进计划。

### 15.7 文档安全等级交互验收

1. 普通知识库和普通文档默认 SL1，不因“仅创建人可见”或锁图标被误判为 SL3。
2. 文档自动继承文件夹最低等级，不能保存为低于文件夹的等级。
3. 文档可以单独升级；移动或复制后最终等级始终取模块、目标文件夹和文档等级中的最高值。
4. 文档标题区域始终显示盾牌图标和完整等级文字，不能只依赖颜色。
5. SL2 / SL3 创建和授权前会校验用户公钥及两套公司恢复公钥状态。
6. 附件、历史版本、缩略图、OCR、AI输出、导出文件和搜索索引不能低于正文等级。
7. SL1 升级 SL2 / SL3 时，正文和大附件支持浏览器分块加密、断点续传和失败重试。
8. 全部密文及密钥校验成功后才原子切换，失败时保留原稳定版本，不出现半加密状态。
9. SL2 升级 SL3 且授权范围不变时，不重复上传正文和大附件。
10. SL2 / SL3 降级必须说明风险、填写原因并完成规定审批；固定 SL3 模块不能降级。
11. 未授权用户不能通过列表、搜索、移动、复制、导入、附件地址或修改请求参数取得高敏内容和元数据。
12. 修改数据库中的 `security_level`、账号角色或文件夹字段不能绕过 encrypted DEK 和记录级授权。
13. 文件夹批量升级会展示文档数、附件数、总容量、密钥未就绪人员和迁移进度。
14. 浏览器关闭或网络中断后，迁移任务可以从已校验分块继续，不重复处理已完成对象。

## 16. 分期落地

### 16.1 第一期：功能闭环

完成：

- 菜单入口。
- 经营周会列表。
- 新建周会。
- 默认模板。
- 四个 CXO 准备块。
- 文档块编辑区复用。
- 准备块提交状态。
- 会议结论。
- 基础 AI 生成提纲。
- 菜单权限和敏感模块权限。

说明：

- 第一期开发环境可以使用模拟数据验证功能，但生产环境不得用服务端可解密方案承载真实经营周会内容。
- 真实 SL3 内容上线前必须完成第二期端到端加密。

### 16.2 第二期：端到端加密

完成：

- 用户安全密码。
- 用户公私钥生成。
- 私钥本地加密保存。
- 记录级 DEK。
- DEK 分发给授权用户。
- 浏览器本地加密和解密。
- 新经营周会内容使用 `enc:v2`。
- SL3 附件浏览器端加密和本地预览。
- 用户安全密码正常修改只重新包裹私钥。

### 16.3 第三期：公司恢复与运维隔离

完成：

- 公司恢复公私钥、老板硬件恢复通道和 CXO `2-of-3` 分片。
- 双人恢复申请和审批。
- 忘记安全密码后的无损授权恢复。
- 用户密钥版本管理和轮换。
- 管理角色拆分，取消超级管理员隐式数据访问。
- 签名发布、紧急运维审批和独立审计存储。
- MySQL、OSS、密钥元数据的灾难恢复演练。

### 16.4 第四期：AI 安全增强

完成：

- 浏览器本地脱敏规则完善。
- AI 输出敏感信息扫描。
- 结构化 JSON 输出校验。
- AI 生成版本管理。
- 提纲重新生成对比。
- 独立 CXO 可控 AI 网关。
- SL3 AI 请求无正文日志和最小化审计。

### 16.5 第五期：历史数据与跨模块迁移

完成：

- 识别已有高敏模块。
- 支持旧 `enc:v1` 和新 `enc:v2` 并存。
- 用户打开旧数据后可重新加密为 `enc:v2`。
- 逐步迁移周报、经营报表、人脉关键字段等高敏内容。
- 为人脉、公司研究和文档中心增加 SL1 / SL2 / SL3 分类。
- 将高敏附件迁移为客户端加密的 OSS 密文对象。
- 迁移完成后逐步降低旧服务端主密钥的业务范围。

## 17. 风险与已确认决策

### 17.1 安全密码丢失

风险：

- 用户忘记安全密码后，无法解密自己的私钥。

已确认方案：

- 使用老板恢复公钥和 CXO 门限恢复公钥，为每份 SL2 / SL3 DEK 增加两份独立恢复副本。
- 老板恢复私钥只存在于老板硬件设备，CXO 门限恢复私钥采用 `2-of-3` 分片，两者都不存服务器。
- 忘记安全密码后，由老板本人或两名 CXO在可信客户端恢复原授权范围。

剩余风险：

- 如果老板两枚硬件恢复凭证均丢失、CXO 门限分片不足两份，且所有授权用户私钥也不可用，对应数据将无法恢复。
- 因此必须执行分片异地保管、季度校验和年度完整恢复演练。

### 17.2 AI 调用路径

风险：

- 如果 AI 请求经过服务端，运维理论上可以看到脱敏后的 AI 输入。

建议：

- SL3 正式环境使用 CXO 控制的独立 AI 网关。
- 普通业务服务端代理只允许用于开发测试或 SL1 数据，不能承载真实 SL3 输入。

### 17.3 文档中心编辑器复用

风险：

- 文档中心编辑器目前集中在 `Documents.js` 中，直接复用可能需要组件拆分。

建议：

- 第一期抽出通用 block editor 组件。
- 保持 blocks 数据结构一致，减少迁移成本。

### 17.4 旧数据迁移

风险：

- 旧 `enc:v1` 数据依赖服务器密钥，不能直接废弃。

建议：

- 保持旧读写兼容。
- 新数据默认使用 `enc:v2`。
- 历史数据按模块逐步迁移。

### 17.5 运行代码被恶意替换

风险：

- 拥有生产服务器 root 的人员可以替换前端代码，等待用户输入安全密码或查看明文。

控制：

- 运维只部署老板控制 CI 生成的签名制品。
- 生产环境校验镜像签名和 digest。
- 禁止长期 root，紧急权限必须双人审批和限时。
- 高风险阶段可使用独立签名客户端承载 SL3 解密。

残余风险：

- 如果同一人员同时控制云账号根权限、CI 签名身份、生产服务器和审计系统，则无法仅靠应用代码保证安全，必须通过组织和账号分权消除这一组合权限。

## 18. 推荐实现原则

1. 模板驱动，不硬编码四个负责人。
2. 后端权限强校验，不依赖前端隐藏。
3. 明文不进数据库、不进日志、不进审计。
4. 服务端不保存明文私钥、不保存明文 DEK。
5. AI 只处理脱敏文本。
6. 旧加密兼容读取，新高敏内容使用新加密版本。
7. 第一版先把业务流程做顺，安全能力尽早纳入架构，不要后补。
8. 所有 SL2 / SL3 数据和附件必须同时包裹给授权用户、老板恢复公钥和 CXO 门限恢复公钥。
9. 登录权、管理权、授权权和解密权相互独立，任何单个管理员都不能同时拥有全部能力。
10. 不创建默认用户名、默认密码或代码内置 JWT 密钥；首次初始化由老板使用一次性初始化凭证完成。
11. SL3 批量授权、密钥轮换和紧急运维必须双人审批；密钥恢复支持老板硬件通道或两名 CXO门限通道。
12. 生产只运行签名制品，备份只有通过实际恢复演练才视为有效。

## 19. 跨模块统一安全架构

### 19.1 模块默认安全等级

| 模块 / 内容 | 默认等级 | 说明 |
| --- | --- | --- |
| 经营周会准备、AI 提纲、会议结论 | SL3 | 固定，不允许普通用户降低 |
| 旧经营周报、战略月会 | SL3 | 逐步从服务端主密钥迁移到端到端加密 |
| 人脉基础资料 | SL1 | 姓名、公司、职位等按现有业务权限管理 |
| 人脉机密备注、判断、简历和附件 | SL2；老板私密内容可设 SL3 | 单独设置“机密资料区”，避免整个人脉记录失去普通搜索能力 |
| 公司基础资料 | SL1 | 公司名称、行业、公开网站等 |
| 公司研究判断、竞品情报、财务判断和核心附件 | SL2；重大决策资料可设 SL3 | 记录级显式授权 |
| 文档中心普通文档 | SL1 | 继续支持服务端全文搜索 |
| 文档中心机密文件夹 / 文档 | SL2 / SL3 | 文件夹等级向下继承，正文、标题、版本和附件统一加密 |

### 19.2 安全等级定义与控制要求

说明：`SL1` 仍属于公司内部信息，不等于可以公开到互联网。安全等级越高，泄露影响、授权要求和技术控制越严格。

| 等级 | 名称 | 泄露影响 | 默认可见范围 | 存储与搜索 | AI、导出与分享 |
| --- | --- | --- | --- | --- | --- |
| SL1 | 内部 | 对个人或公司影响较低，可通过常规管理纠正 | 按现有菜单、团队、创建人和共享权限 | 可使用服务端静态加密、服务端全文搜索 | 可按普通权限使用 AI 和导出，禁止公开分享时仍需登录 |
| SL2 | 机密 | 可能造成客户、人才、合同、商业判断或竞争损失 | 创建人、明确授权人和业务负责人 | 必须端到端加密；服务端无明文；搜索在浏览器本地执行 | AI 必须脱敏并走受控网关；导出、下载和分享记录审计 |
| SL3 | 核心机密 | 可能影响公司经营安全、控制权、重大决策、核心财务或关键个人安全 | 老板、CXO 和逐条明确指定的参与人 | 必须端到端加密、两套公司恢复 encrypted DEK、可信发布和 MFA；服务端不建立明文索引 | 默认禁止外部分享；AI、批量导出、降级和扩大授权需要额外审批 |

### 19.3 具体信息分类表

| 业务领域 | 具体信息 | 默认等级 | 定级说明 / 升级条件 |
| --- | --- | --- | --- |
| 经营周会 | 每位 CXO / 指定参与人的准备内容 | SL3 | 固定 SL3，包含经营判断、问题和待决策事项 |
| 经营周会 | AI 会议提纲、会议结论、决策和下周动作 | SL3 | 固定 SL3，AI 生成内容继承输入最高等级 |
| 经营周会 | 周时间范围、参会人、提交状态、会议状态 | SL3 | 元数据也可能暴露组织安排和经营节奏，与周会记录整体保护 |
| 战略经营 | 战略方向、年度目标、预算分配、重大调整、董事会 / CXO 决策 | SL3 | 未正式公开前固定 SL3 |
| 财务经营 | 毛利、毛利率、利润、利润率、现金流、成本结构、分成比例、未公开收入和预算 | SL3 | 可推导核心盈利能力的数据也按 SL3 |
| 重大事项 | 并购、融资、股权、重大合同、诉讼、危机处置、裁员和核心高管任免 | SL3 | 对公司控制权、声誉或人员有重大影响 |
| 人脉管理 | 老板个人私密人脉、私密评价、关键关系判断和特殊维护策略 | SL3 | 由老板标记，只有老板及逐条授权人员可见 |
| 人脉管理 | 姓名、公司、公开职位、公开行业信息 | SL1 | 不包含私人联系方式和内部评价时为 SL1 |
| 人脉管理 | 手机、微信、邮箱、住址、生日、私人关系、互动记录 | SL2 | 属于个人信息和关系资产，不得默认全公司可见 |
| 人才管理 | 简历、工作经历、薪资、求职意向、面试评价、能力判断和背调资料 | SL2 | 涉及候选人隐私；核心高管候选人或敏感任免可升级为 SL3 |
| 人脉附件 | 简历、合同、证件、聊天截图、录音、拜访资料 | SL2 | 自动继承所属人脉记录；含证件、重大承诺或老板私密内容时升级为 SL3 |
| 员工管理 | 普通周报、绩效反馈、工作评价、薪资和人事沟通 | SL2 | 仅本人、直属管理者和明确授权 HR 可见；高管薪酬、任免和裁员方案升级为 SL3 |
| 账号身份 | 用户名、显示名、部门、岗位和普通角色信息 | SL1 | 仅公司内部使用，不对公网公开 |
| 账号安全 | 密码哈希、MFA 凭证、Passkey、会话 token、重置 token 和设备恢复凭证 | SL3-凭证 | 不返回前端、不进入日志，按专用认证安全要求存储和轮换 |
| 公司基础资料 | 公司名称、行业、官网、公开产品、公开工商信息 | SL1 | 来源公开且不包含内部判断 |
| 公司研究 | 内部评价、合作判断、竞争分析、关系网络、风险分析和跟进策略 | SL2 | 属于公司内部商业判断 |
| 公司研究 | 客户报价、合同条件、渠道政策、结算信息、非公开收入和业务数据 | SL2 | 涉及核心盈利、重大客户或可推导毛利时升级为 SL3 |
| 竞品研究 | 公开新闻、公开产品资料和公开市场信息 | SL1 | 仅保存公开事实时为 SL1 |
| 竞品研究 | 内部推演、情报来源、竞争策略、商业机会和未公开数据 | SL2 | 涉及战略决策或重大竞争行动时升级为 SL3 |
| 线索与商机 | 普通线索来源、负责人、公开联系方式和一般跟进状态 | SL1 | 不包含敏感报价和内部策略时为 SL1 |
| 线索与商机 | 客户需求、预算、报价、谈判底线、竞争对手、成交概率和内部策略 | SL2 | 重大客户、核心预算或影响公司战略时升级为 SL3 |
| 文档中心 | 一般制度、工作流程、公开培训材料、普通项目记录 | SL1 | 允许公司内部按权限搜索和共享 |
| 文档中心 | 客户方案、合同草稿、产品规划、内部复盘、项目风险和未公开运营资料 | SL2 | 文件夹或文档标记为机密，标题、正文、版本和附件统一为 SL2 |
| 文档中心 | 战略、重大决策、核心财务、董事会材料、经营周会和老板私密文档 | SL3 | 文件夹和文档固定使用 SL3 控制 |
| 文档附件 | 图片、PDF、Word、Excel、音视频和导入原文件 | 继承父级 | 附件不能低于所属文档或业务记录等级 |
| AI 数据 | Prompt、上下文、上传文件、模型输出和人工修订结果 | 继承最高输入等级 | 多个来源混合时取最高等级；不能因脱敏自动降低，降级需重新评估 |
| 搜索数据 | 标题、摘要、提取文本、关键词索引和向量 | 继承原文 | SL2 / SL3 不得进入服务端明文全文索引或普通向量库 |
| 操作日志 | 普通登录、页面访问和非敏感操作元数据 | SL1 | 日志不得包含业务正文、密码、token 和 DEK |
| 安全审计 | SL3 授权、恢复、导出、密钥轮换和紧急 root 审批记录 | SL2 | 审计内容不含业务明文；涉及恢复材料位置或安全架构细节时升级为 SL3 |
| 系统配置 | 普通功能开关、非敏感模板和展示配置 | SL1 | 不包含密钥和账号凭证 |
| 系统凭证 | JWT 签名密钥、数据库密码、OSS 密钥、AI API Key、部署签名密钥 | SL3-凭证 | 不作为普通业务记录保存；进入专用 Secret Manager，按最小权限和轮换策略管理 |
| 恢复材料 | 用户明文私钥、老板恢复私钥、CXO 门限分片、明文 DEK、安全密码 | SL3-密钥 | 禁止写入 MySQL、OSS、日志、PRD、代码仓库和普通密码配置；只在可信客户端内存或硬件设备中短暂存在 |

`SL3-凭证` 和 `SL3-密钥` 是 SL3 的特殊子类，不是新的安全等级。它们额外要求不得作为普通业务字段保存、展示、搜索或导出。

### 19.4 定级与继承规则

1. 信息同时符合多个等级时，取最高等级。
2. 同一记录中混有 SL1 和 SL2 / SL3 内容时，优先拆分为“基础资料区”和“机密资料区”；无法拆分时整条记录按最高等级处理。
3. 标题、摘要、评论、历史版本、附件、导出文件、AI 输入输出、搜索索引和缓存继承正文等级。
4. 父文件夹、父业务记录或模板设定等级后，子内容只能保持或升级，不能自动降低。
5. 用户可以把自己有权管理的内容升级；SL2 降为 SL1、SL3 降为 SL2 / SL1 必须由数据安全管理员发起并由另一名 CXO批准。
6. “已经公开”必须有可验证的公开来源；内部口头传播、群聊转发或部分人员知道不视为公开。
7. 无法判断时默认按更高一级保存，后续再由数据负责人审核降级。
8. 数据安全等级只决定最低保护要求，具体可见人员仍由业务权限、参与身份和记录 encrypted DEK共同决定。

### 19.5 文档中心安全等级交互

#### 19.5.1 等级计算与字段

用户不需要为每篇文档重复手工定级。文档最终有效等级取以下三项中的最高值：

```text
有效安全等级 = max(模块固定等级, 文件夹最低等级, 文档单独等级)
```

文件夹字段建议：

- `minimum_security_level`：`SL1` / `SL2` / `SL3`
- `security_level_locked`：是否为模块固定等级，固定后普通用户不能修改
- `classification_policy_version`

文档字段建议：

- `security_level`：文档单独设置的等级
- `effective_security_level`：按继承规则计算后的实际等级
- `security_level_source`：`module` / `folder` / `document` / `policy`
- `classification_reason`
- `classified_by`
- `classified_at`
- `crypto_status`：`plain` / `pending` / `encrypting` / `ready` / `failed` / `pending_downgrade`
- `crypto_version`

规则：

- 文档可以高于文件夹等级，但不能低于文件夹最低等级。
- SL3 文档移动到 SL1 文件夹后仍保持 SL3。
- 经营周会、董事会等固定 SL3 目录不显示可降低选项。
- 当前页面的“锁 / 仅创建人可见”表示访问权限，不等于安全等级；安全等级使用独立盾牌图标和文字标识。

#### 19.5.2 新建文档

新建文档弹窗增加“安全等级”分段选择：

```text
安全等级
[ SL1 内部 ] [ SL2 机密 ] [ SL3 核心机密 ]

当前文件夹最低等级：SL2
可见人员：创建人、项目负责人、已选择成员
```

交互要求：

- 默认选中文件夹继承等级，用户通常无需操作。
- 低于文件夹最低等级的选项置灰，并提示等级来源。
- 选择 SL2 / SL3 后显示“可见人员”选择器和加密授权状态。
- 被授权人员未设置用户公钥时显示“安全密钥未就绪”，不能完成对该人员的加密授权。
- SL3 创建或首次查看要求再次 MFA；扩大授权范围按审批策略执行。
- 页面不得使用颜色作为唯一识别方式，必须同时显示 `SL1 内部`、`SL2 机密`、`SL3 核心机密` 文本。

#### 19.5.3 文档详情页

文档标题右侧常驻显示安全等级，例如：

```text
项目经营复盘    [盾牌 SL2 机密]
```

视觉建议：

- SL1：中性灰，显示 `SL1 内部`
- SL2：金色 / 琥珀色，显示 `SL2 机密`
- SL3：红色，显示 `SL3 核心机密`

点击安全等级标签打开右侧“安全设置”抽屉，展示：

- 当前有效等级和文档单独等级。
- 等级来源及所在文件夹最低等级。
- 加密状态和加密版本。
- 可见人员、未就绪密钥人员和公司恢复密钥状态。
- 最近调整人、调整时间和定级原因。
- `调整等级`、`管理可见人员`、`查看安全日志` 操作。

无等级管理权限的用户只能查看当前等级和自己的访问状态，不能看到完整授权人员名单。

#### 19.5.4 文件夹安全设置

文件夹右键菜单增加带盾牌图标的“安全设置”：

- 设置最低安全等级。
- 查看等级继承规则。
- 查看高于文件夹等级的例外文档。
- 批量升级现有文档。

提高文件夹等级时，系统统计并展示：

- 受影响文档数量。
- 历史版本数量。
- 附件数量和总容量。
- 未配置用户公钥的授权人员。
- 预计需要迁移的数据量。

提高文件夹等级会使所有低于新最低等级的内容进入加密迁移。降低文件夹等级只影响以后新建内容，不自动降低已有文档。

#### 19.5.5 列表、目录树与筛选

- 文档列表增加“安全等级”和“加密状态”列。
- 支持按 `SL1 / SL2 / SL3`、`已加密 / 迁移中 / 异常` 筛选。
- 目录树在名称右侧显示紧凑 `S2`、`S3` 标识；SL1 默认不显示，减少视觉干扰。
- 用户无权查看时，后端不返回标题、摘要、密文、附件对象 key 和授权人员名单。
- 仅有访问权限但没有等级管理权限的用户不能看到降级、批量授权和安全策略操作。

#### 19.5.6 操作权限

- 文档创建人可以将自己管理的文档升级到更高等级。
- 文件夹管理员可以设置文件夹最低等级，但不能单独降低现有 SL2 / SL3 文档。
- 数据安全管理员发起 SL2 / SL3 降级、SL3 扩大授权和批量导出。
- SL3 降级或扩大历史授权需要另一名 CXO批准。
- 普通共享用户不能修改安全等级和授权接收人。
- 平台运维不能修改安全等级、可见人员、encrypted DEK 或公司恢复授权。

### 19.6 等级变更与大文件迁移

#### 19.6.1 SL1 升级为 SL2 / SL3

1. 展示正文、历史版本、附件数量、总容量和授权人员。
2. 检查所有接收人用户公钥、老板恢复公钥和 CXO 门限恢复公钥。
3. 浏览器压缩可压缩文本后，按块加密正文和历史版本。
4. 浏览器对附件执行 `4MB ~ 8MB` 分块流式加密和断点续传。
5. 密文上传完成后校验对象 hash、块数量、记录密钥和恢复密钥。
6. 全部校验成功后原子切换文档到新密文版本。
7. 删除在线明文正文、摘要、搜索索引、公开附件 URL 和临时文件。

迁移失败时保持原版本可用，不允许出现正文已删除但密文未完成的半加密状态。

#### 19.6.2 SL2 升级为 SL3

- 加密格式和授权人员不变时，不重新上传正文和附件，只更新安全策略、审批、AI和导出限制。
- 如果需要缩小授权范围，必须轮换 Document Key；是否重加密历史大附件按“软撤权 / 彻底撤权”策略决定。
- 固定 SL3 模块升级后不允许普通用户再发起降级。

#### 19.6.3 SL2 / SL3 降级

- 明确提示：降级到 SL1 后，普通业务服务器和具备生产运行权限的人员理论上可以处理明文。
- 必须填写降级原因、目标等级、影响范围和保留策略。
- 数据安全管理员发起，另一名 CXO批准后才执行。
- SL3 固定模块不允许降级。
- 降级完成后仍保留不可修改的安全审计记录。

#### 19.6.4 迁移任务体验

- 大文档迁移进入全局任务中心，展示文档数、附件数、已处理容量、当前速度和失败项。
- 加密发生在浏览器或签名可信客户端，不在普通业务服务器解密处理。
- 每个完成分块写入检查点；浏览器关闭或网络中断后可以从检查点继续。
- 用户可以最小化进度面板继续使用其他页面，但执行设备必须保持在线。
- 迁移中默认禁止同时编辑受影响文档；只读访问使用迁移前稳定版本。
- 任务失败支持重试单个文档或附件，不重新处理已校验成功的对象。

#### 19.6.5 移动、复制和导入

- 移动到更高等级文件夹时，提示并自动进入升级流程。
- 移动到更低等级文件夹时保留原等级，不自动降级。
- 复制文档时保留原等级；目标文件夹等级更高时取更高等级。
- Word、PDF、Wolai 和其他导入文档先继承目标文件夹等级，再开始上传和解析。
- 附件、历史版本、缩略图、OCR、AI结果和搜索索引全部继承文档有效等级。

### 19.7 自动定级提醒

- 浏览器本地扫描毛利、利润、现金流、预算、并购、股权、裁员、简历、薪资、证件、合同、报价和谈判底线等高敏特征。
- 命中规则后建议升级到 SL2 或 SL3，并说明触发的分类类别，不把原文发送给服务端分类。
- 自动规则只能建议升级，不能自动降级。
- 用户拒绝高风险升级时必须填写原因；命中模块固定 SL3 规则时不能继续以 SL1 / SL2 保存。
- AI 辅助分类只能作为建议，最终有效等级仍受模块和文件夹最低等级约束。

### 19.8 统一加密服务边界

#### 19.8.1 文档与附件密钥层级

为避免文档含大量图片、附件时为每位用户重复生成大量记录密钥，文档中心采用文档级密钥层级：

1. 每份 SL2 / SL3 文档生成一个随机 `Document Key`。
2. 文本块使用 Document Key 加密，每个块使用唯一 nonce，并把文档 ID、块 ID 和版本作为 AAD。
3. 每个附件生成独立 `File Key`，附件密文只保存一份。
4. File Key 使用 Document Key 包裹，文档历史版本只引用附件对象，不重复保存附件文件。
5. Document Key 分别包裹给授权用户、老板恢复公钥和 CXO 门限恢复公钥。
6. 新增授权用户时只增加一份 wrapped Document Key，不重新上传正文、图片和附件。
7. 普通权限变更只重新包裹 Document Key；只有彻底撤权或密钥泄漏时才重新加密历史内容和大附件。

#### 19.8.2 存储与版本成本控制

- 文本和可压缩 JSON 必须先压缩再加密；密文不能再次有效压缩。
- 图片、PDF、Word、Excel、音视频直接以二进制密文保存，禁止转 Base64，避免约 33% 容量膨胀。
- 文档正文按块保存，编辑时只产生发生变化的加密块。
- 历史版本保存加密块引用清单，不复制未变化的正文块和附件。
- 同一附件在同一文档的多个版本中复用同一个密文对象；跨文档去重默认关闭，避免泄露“两个文档包含同一文件”的关系。
- 图片上传时额外生成一张常用尺寸的加密缩略图；列表只下载缩略图，打开原图时再读取原始密文。
- OCR、向量和AI分析按需执行，不对全部 SL2 / SL3 附件自动预计算。
- 旧版本根据安全和合规策略进入 OSS 低频存储；法律保留或审计要求的版本不得自动删除。

#### 19.8.3 前后端职责

前端共享以下安全组件：

- 用户密钥管理器：创建、解锁、重新包裹和轮换用户密钥。
- 记录加密器：生成 DEK、加密正文、给用户及两套公司恢复公钥包裹 DEK。
- 附件流式加密器：支持大文件分块加密、上传、下载和本地预览。
- 权限接收人解析器：根据模块、记录类型和安全等级计算准确授权范围。
- 恢复客户端：使用老板硬件恢复私钥，或在两名 CXO审批后使用门限分片重新包裹 DEK。
- 本地搜索器：对已解密的 SL2 / SL3 内容建立临时加密索引。

后端只负责：

- 身份认证、业务权限和审批状态校验。
- 保存密文、encrypted DEK、公钥、密钥版本和审计元数据。
- 提供密文上传下载和短期授权地址。
- 校验提交的记录密钥接收人不能超出业务允许范围。

### 19.9 旧数据迁移

1. 盘点 `enc:v1`、明文正文、明文附件和公开附件 URL。
2. 由数据负责人确定记录安全等级和授权人员。
3. 确认所有接收人用户公钥、老板恢复公钥及 CXO 门限恢复公钥已经就绪。
4. 在受控迁移窗口读取旧数据并立即转换为 `enc:v2` 密文和记录级 encrypted DEK。
5. 校验新密文可由授权用户及公司恢复流程解密后，再删除在线库中的旧明文 / `enc:v1` 副本。
6. 旧备份在保留期内仍可能被旧主密钥解密，必须隔离、限制访问并按合规周期销毁。
7. 每批迁移记录数量、成功数、失败数、哈希和审批人，不记录业务明文。

## 20. 灾难恢复与业务连续性

### 20.1 必备恢复资产

- 基础设施即代码和部署脚本。
- 已签名应用镜像、前端制品和版本清单。
- MySQL 全量备份、binlog / 时间点恢复材料。
- OSS 密文附件及对象清单。
- 用户 encrypted private key 和全部密钥版本。
- 用户 encrypted DEK、两套恢复 encrypted DEK、两套公司恢复公钥及版本。
- 权限、审批、审计、文件哈希和数据完整性清单。
- DNS、证书、域名、云账号和第三方服务恢复手册。

备份副本必须位于独立备份账号或不可修改存储，生产运维账号不能单独删除全部备份。

### 20.2 新运维恢复流程

1. 新运维使用基础设施代码创建全新运行环境。
2. 部署经过签名校验的固定版本应用。
3. 恢复 MySQL 和 OSS 密文，不导入老板恢复私钥或 CXO 门限恢复私钥。
4. 执行自动一致性检查：表数量、记录数量、附件数量、对象 hash、密钥引用和审计链。
5. 系统进入“已恢复、待业务验证”状态，运维只能看到技术检查结果和密文元数据。
6. 一名原授权 CXO 使用原登录密码和安全密码验证随机抽样的历史内容。
7. 如果原用户密钥可用，系统直接恢复正常，无需公司恢复私钥。
8. 如果用户密钥不可用，老板本人或两名 CXO 发起公司恢复流程并重新授权。
9. 验证搜索、附件预览、年度汇总和权限隔离后，系统恢复对外服务。
10. 生成恢复报告，由运维、业务负责人和安全审计员共同确认。

### 20.3 可靠性目标

- 目标 `RPO <= 5 分钟`：最多丢失 5 分钟内新写入的数据。
- 目标 `RTO <= 2 小时`：重大故障后两小时内恢复核心读写能力。
- MySQL 每日全量备份、持续 binlog、跨账号备份。
- OSS 开启版本控制或不可变备份，防止误删和勒索破坏。
- 每季度执行技术恢复演练，每年至少执行一次包含 CXO 密钥恢复的完整演练。
- 每次演练验证历史、新增、附件、密钥轮换前后和权限变更前后的数据。

系统不使用“可靠性 100%”作为不可验证承诺，而以 RPO、RTO、备份成功率、恢复演练成功率和数据完整性校验作为正式验收指标。

## 21. 已确认架构决策

1. 运维可以独立恢复系统、数据库、OSS 附件密文和全部密钥元数据。
2. 运维不能独立解密 SL2 / SL3 内容，也不能通过超级管理员账号绕过记录级密钥。
3. 原授权用户可以用原安全密码直接恢复解密能力。
4. 安全密码遗失或用户私钥损坏时，由老板硬件恢复通道，或者两名 CXO门限恢复通道恢复原授权范围。
5. 老板恢复私钥及其硬件备份、CXO 门限恢复私钥的 `2-of-3` 分片均不存放在代码、服务器、MySQL、OSS 或普通密码配置中。
6. 经营周会固定为 SL3，其他模块按 SL1 / SL2 / SL3 逐步迁移。
7. 平台运维、账号管理、数据授权、安全审计和解密能力必须分权。
