# 经营周会模块 PRD

## 1. 文档信息

- 模块名称：经营周会
- 所属入口：目标计划
- 文档版本：V1.0
- 编写日期：2026-07-16
- 目标用户：CEO、COO、CTO、CMO 及被授权参与经营周会的核心管理人员

## 2. 背景

公司每周需要围绕经营情况召开经营周会。当前经营周会记录更偏固定表单，无法满足多人分区填写、会前汇总、AI 生成会议提纲、会后沉淀决策和后续扩展的需求。

经营周会内容通常包含高敏经营信息，尤其涉及业务数据、判断、决策和后续动作。现有字段级加密使用服务端密钥文件，能够防止数据库单独泄露，但无法防止拿到服务器密钥文件或服务器权限的运维人员查看内容。因此，新模块需要在产品设计上同时满足“好用”和“更高安全等级”两个目标。

## 3. 产品目标

1. 在目标计划下新增“经营周会”模块。
2. 支持按周创建经营周会，列表展示周时间范围和填写状态。
3. 支持多个负责人分别填写自己的简报内容。
4. 复用文档中心的块编辑能力，支持富文本、列表、附件、图片等结构化内容。
5. 所有必填简报完成后，支持调用 AI 生成会议提纲。
6. AI 生成的会议提纲必须避免披露业务毛利、毛利率、利润等敏感数据。
7. 开会后支持填写会议结论，重点沉淀决策和后续动作。
8. 模板、负责人、填写块、问题项均可扩展，后续增加或减少人员不需要大改代码。
9. 权限上同时满足菜单权限和敏感模块权限两道门。
10. 新增端到端加密能力，让运维即使拿到数据库、服务器文件和旧服务端密钥，也无法解密新高敏内容。

## 4. 非目标

本期不做以下能力：

- 不做复杂会议排期和日历邀请。
- 不做会议纪要自动语音转写。
- 不做经营指标看板。
- 不做跨公司或外部人员协作。
- 不做所有历史加密数据一次性强制迁移到新加密方案。
- 不直接把业务按钮运行时做成 Codex 任务；经营周会 AI 分析走模型 API，Codex 主要用于研发、调试、自动化维护。

## 5. 用户角色

### 5.1 系统管理员

负责：

- 配置菜单权限。
- 配置敏感模块可见人员。
- 配置经营周会模板。
- 配置 AI 模型连接。

限制：

- 系统管理员可以配置模块，但如果不是敏感模块授权人员，不能查看经营周会内容明文。
- 在新加密方案下，系统管理员和运维都不能通过数据库或服务器密钥解密经营周会内容。

### 5.2 经营周会授权人员

通常为 CEO、COO、CTO、CMO 或老板指定人员。

能力：

- 查看经营周会列表。
- 查看、填写、编辑被授权的周会内容。
- 提交自己负责的简报块。
- 在全部必填块提交后生成 AI 会议提纲。
- 填写会议结论。

### 5.3 简报负责人

例如：

- 海外项目组（陈锦标）
- 国内项目组（陈豪赞）
- 商务信息（林璐韵）
- 产研落地（贺敏）

能力：

- 填写自己负责的简报块。
- 提交自己的简报。
- 查看权限范围内的周会内容。

## 6. 业务流程

### 6.1 新建经营周会

1. 用户进入 `目标计划 -> 经营周会`。
2. 点击“新建经营周会”。
3. 选择周时间范围，例如 `2026-07-13 ~ 2026-07-19`。
4. 选择模板，默认使用“经营周会默认模板”。
5. 系统根据模板生成本周简报块。
6. 保存后进入该周经营周会详情。

### 6.2 会前填写

1. 各负责人进入本周经营周会。
2. 找到自己负责的填写块。
3. 在文档编辑区填写内容。
4. 点击“提交本块”。
5. 系统记录提交人、提交时间和状态。
6. 全部必填块提交后，状态变为“简报已完成”。

### 6.3 AI 生成会议提纲

1. 全部必填块提交后，“生成会议提纲”按钮可用。
2. 点击按钮后，前端本地解密内容。
3. 前端本地做敏感信息脱敏。
4. 将脱敏后的结构化内容提交给 AI。
5. AI 返回固定结构的会议提纲。
6. 系统再次扫描提纲中的敏感信息。
7. 扫描通过后保存加密后的会议提纲。

### 6.4 会后填写结论

1. 会议结束后，授权人员进入“会议结论”区域。
2. 记录最终决策、负责人、完成时间和后续动作。
3. 保存后加密存储。

## 7. 页面结构

### 7.1 列表页

路径建议：`/executive/operational` 或 `/operational-meetings`

展示字段：

- 周时间范围
- 会议状态
- 简报完成进度
- AI 提纲状态
- 会议结论状态
- 创建人
- 更新时间

状态建议：

- 草稿
- 填写中
- 简报已完成
- 提纲已生成
- 已开会
- 已归档

列表操作：

- 新建
- 查看
- 编辑
- 生成提纲
- 归档
- 删除

### 7.2 详情页

详情页建议分为三个区域：

- 简报填写
- AI 会议提纲
- 会议结论

#### 7.2.1 简报填写区

顶部显示：

- 周时间范围
- 当前状态
- 已完成填写块数量
- 必填块数量
- 最近更新时间

下方显示模板生成的折叠块：

- 海外项目组（陈锦标）
- 国内项目组（陈豪赞）
- 商务信息（林璐韵）
- 产研落地（贺敏）

每个折叠块展示：

- 填写块标题
- 负责人
- 状态：未填写 / 填写中 / 已提交 / 已锁定
- 提交人
- 提交时间
- 文档块编辑区
- 保存按钮
- 提交按钮

默认问题项：

1. 本周核心结果
2. 一个最重要的判断
3. 需要会上决策的问题
4. 下周建议动作

说明：

- 问题项通过模板配置生成，不写死在代码中。
- 内容编辑区复用文档中心 blocks 数据结构。
- 第一版可以复用文档中心已有编辑能力，后续再抽公共组件。

#### 7.2.2 AI 会议提纲区

按钮：

- 生成会议提纲
- 重新生成
- 复制
- 保存为正式提纲

生成条件：

- 所有必填简报块均已提交。
- 当前用户有敏感模块访问权限。
- 当前用户已解锁安全密钥。

会议提纲固定包含：

一、本次会议目标

二、本周经营总览

三、本次重点讨论和决策议题

四、建议会议议程

五、下周动作建议

六、请大家会前准备

安全要求：

- 不输出毛利数据信息。
- 不输出毛利率、利润、利润率、GM、gross profit 等敏感指标。
- 不输出可反推出毛利的明细组合。

#### 7.2.3 会议结论区

支持填写：

- 决策事项
- 负责人
- 截止时间
- 后续动作
- 风险和待确认问题

第一版可用文档块编辑区或结构化列表，建议结构化列表更利于后续追踪。

## 8. 模板设计

经营周会必须模板驱动，避免硬编码。

### 8.1 模板字段

模板包含：

- 模板名称
- 模板状态
- 默认是否启用
- 适用模块 key
- 创建人
- 更新时间

### 8.2 模板填写块字段

每个填写块包含：

- 块标题
- 负责人用户
- 负责人角色
- 是否必填
- 排序
- 默认折叠状态
- 默认问题列表
- 默认文档 blocks
- 是否允许负责人外的人编辑

### 8.3 模板管理能力

系统管理中新增“经营周会模板”配置，支持：

- 新增模板
- 编辑模板
- 启用 / 停用模板
- 新增填写块
- 删除填写块
- 调整填写块排序
- 修改负责人
- 修改默认问题项
- 设置必填 / 非必填

## 9. 数据模型

### 9.1 operational_meeting_templates

经营周会模板表。

字段建议：

- `id`
- `name`
- `module_key`
- `is_default`
- `status`
- `created_by`
- `created_at`
- `updated_at`

### 9.2 operational_meeting_template_sections

模板填写块表。

字段建议：

- `id`
- `template_id`
- `section_key`
- `title`
- `owner_user_id`
- `owner_role`
- `is_required`
- `sort_order`
- `default_collapsed`
- `default_questions_json`
- `default_blocks_json`
- `editable_by_owner_only`
- `created_at`
- `updated_at`

### 9.3 operational_meetings

经营周会主表。

字段建议：

- `id`
- `template_id`
- `week_start`
- `week_end`
- `title`
- `status`
- `brief_status`
- `agenda_status`
- `decision_status`
- `created_by`
- `updated_by`
- `created_at`
- `updated_at`
- `archived_at`

说明：

- 周时间范围用 `week_start` 和 `week_end` 独立存储，便于查询和展示。
- 同一周默认只允许一个未删除经营周会，避免重复。

### 9.4 operational_meeting_sections

经营周会实际填写块表。

字段建议：

- `id`
- `meeting_id`
- `template_section_id`
- `section_key`
- `title`
- `owner_user_id`
- `status`
- `content_ciphertext`
- `content_text_ciphertext`
- `crypto_version`
- `submitted_by`
- `submitted_at`
- `locked_at`
- `created_at`
- `updated_at`

状态：

- `draft`
- `submitted`
- `locked`

### 9.5 operational_meeting_agendas

AI 会议提纲表。

字段建议：

- `id`
- `meeting_id`
- `agenda_ciphertext`
- `agenda_text_ciphertext`
- `source_hash`
- `model_provider`
- `model_name`
- `prompt_version`
- `safety_scan_status`
- `generated_by`
- `generated_at`
- `created_at`
- `updated_at`

### 9.6 operational_meeting_decisions

会议结论 / 决策表。

字段建议：

- `id`
- `meeting_id`
- `decision_ciphertext`
- `decision_text_ciphertext`
- `owner_user_id`
- `due_date`
- `status`
- `created_by`
- `created_at`
- `updated_at`

### 9.7 sensitive_module_members

敏感模块授权人员表。

字段建议：

- `id`
- `module_key`
- `user_id`
- `permission_level`
- `created_by`
- `created_at`

`module_key` 示例：

- `operational_meeting`
- `executive_reports`
- `weekly_reports`

### 9.8 crypto_user_keys

用户加密密钥表。

字段建议：

- `id`
- `user_id`
- `public_key_jwk`
- `encrypted_private_key_jwk`
- `kdf_algorithm`
- `kdf_params_json`
- `key_version`
- `status`
- `created_at`
- `rotated_at`

说明：

- 公钥可以明文存在数据库。
- 私钥必须先在浏览器中用用户安全密码加密，再保存到数据库。
- 服务端永远不能拿到明文私钥。

### 9.9 crypto_record_keys

记录级内容密钥授权表。

字段建议：

- `id`
- `record_type`
- `record_id`
- `user_id`
- `encrypted_dek`
- `key_version`
- `grant_status`
- `granted_by`
- `created_at`
- `revoked_at`

说明：

- 每份经营周会内容生成随机 DEK。
- DEK 分别用授权用户公钥加密后保存。
- 用户打开内容时，只能用自己的私钥解开属于自己的 `encrypted_dek`。

## 10. 权限设计

### 10.1 两道门

经营周会模块必须同时满足两道权限：

1. 菜单权限：用户拥有经营周会菜单 key。
2. 敏感模块权限：用户在 `operational_meeting` 的敏感授权名单内。

即使用户有菜单权限，只要不是敏感授权人员，也不能看到菜单、不能访问页面、不能调用后端接口。

### 10.2 前端权限

前端处理：

- 目标计划菜单下新增“经营周会”。
- 只有同时满足菜单权限和敏感模块权限时才展示入口。
- 如果用户直接访问路由，展示无权限页面。

### 10.3 后端权限

后端所有经营周会 API 必须挂统一中间件：

- 已登录。
- 具备菜单权限。
- 具备敏感模块权限。
- 写操作需具备写权限。

不能只依赖前端隐藏。

### 10.4 操作权限

建议权限分级：

- `view`：查看。
- `edit_own`：编辑自己负责的填写块。
- `edit_all`：编辑全部填写块。
- `generate_agenda`：生成 AI 提纲。
- `manage`：管理模板、授权和归档。

## 11. 新加密方案

### 11.1 目标

新加密方案要满足：

- 运维拿到数据库，看不到明文。
- 运维拿到服务器密钥文件，看不到新高敏内容。
- 运维有服务器文件权限，也不能直接解密新高敏内容。
- 不影响旧 `enc:v1:` 数据读取。
- 后续可逐步迁移已有高敏数据。

### 11.2 方案原则

核心原则：

**密钥不能放在服务器上，解密不能发生在服务器上。**

只要服务器能解密，拥有服务器权限的人理论上就能看到明文。因此经营周会内容采用端到端加密：

- 浏览器本地加密。
- 浏览器本地解密。
- 服务端只保存密文。
- 服务端只保存被授权用户公钥加密后的内容密钥。

### 11.3 密钥结构

每个用户拥有：

- 公钥：存服务端。
- 私钥：浏览器生成后，用用户安全密码加密，再存服务端。
- 安全密码：只在用户脑中和浏览器运行时存在，不传服务端。

每份记录拥有：

- DEK：随机内容密钥，用于加密会议内容。
- encrypted DEK：用每个授权人的公钥分别加密一份。

### 11.4 加密流程

新建经营周会内容时：

1. 浏览器生成随机 DEK。
2. 浏览器使用 DEK 加密内容 blocks。
3. 浏览器读取授权人员公钥。
4. 浏览器用每个授权人员公钥加密 DEK。
5. 服务端保存内容密文和多份 encrypted DEK。

查看经营周会内容时：

1. 用户输入安全密码。
2. 浏览器用安全密码解开用户私钥。
3. 浏览器获取该用户对应的 encrypted DEK。
4. 浏览器用私钥解开 DEK。
5. 浏览器用 DEK 解密内容。

服务端全程不接触明文内容、明文私钥和明文 DEK。

### 11.5 授权新增人员

给新人员授权时：

1. 已授权用户打开记录并本地解密 DEK。
2. 浏览器获取新人员公钥。
3. 浏览器用新人员公钥加密 DEK。
4. 服务端新增一条 `crypto_record_keys`。

### 11.6 撤销人员权限

撤销后需要重加密：

1. 已授权用户本地解密内容。
2. 浏览器生成新 DEK。
3. 浏览器用新 DEK 重新加密内容。
4. 浏览器只给剩余授权人重新分发 encrypted DEK。
5. 原 encrypted DEK 标记为 revoked。

说明：

- 如果被撤销人员曾经在撤销前看过内容，无法阻止其已经截图或离线保存。
- 撤销主要保证撤销后无法继续访问更新后的内容。

### 11.7 旧数据兼容

现有 `enc:v1:` 继续保留：

- 旧数据仍用服务端密钥读取。
- 新经营周会内容默认使用 `enc:v2:`。
- 新加密模块需要支持识别加密版本。
- 后续对已有高敏模块做迁移时，采用逐步迁移，不一次性强制迁移。

### 11.8 安全密码找回

安全密码不能由服务端找回，因为服务端不知道安全密码。

第一版建议：

- 安全密码丢失后，用户无法解密历史授权内容。
- 可由其他已授权 CXO 打开内容后，为该用户新公钥重新授权。

后续可选：

- 多 CXO 门限恢复。
- 离线恢复码。
- 企业硬件密钥。

## 12. AI 分析方案

### 12.1 AI 底层选择

经营周会“生成会议提纲”建议使用：

- OpenAI Responses API
- GPT-5.6 系列模型
- 结构化 JSON 输出

不建议把用户点击按钮时直接运行 Codex agent，原因：

- Codex 更适合代码仓库、开发工具、自动修复、代码审查等工程任务。
- 经营周会提纲属于业务文本分析，直接调用模型 API 更稳定、延迟更低、边界更清楚。
- Codex 可用于研发阶段实现、测试和维护该模块。

### 12.2 AI 调用安全链路

默认安全链路：

1. 浏览器本地解密经营周会内容。
2. 浏览器本地脱敏。
3. 脱敏后的内容调用 AI。
4. AI 返回会议提纲。
5. 浏览器本地再次扫描敏感信息。
6. 浏览器本地加密提纲。
7. 服务端保存密文。

说明：

- 如果 AI 请求经过业务服务端，服务端会短暂接触脱敏后的文本。
- 如果要求运维连脱敏文本都看不到，则 AI 请求应由浏览器直连 OpenAI 或 CXO 可控 AI 网关。

### 12.3 脱敏规则

AI 输入前必须移除或替换：

- 毛利
- 毛利率
- 利润
- 利润率
- GM
- gross profit
- gross margin
- 分成后利润
- 可反推毛利的收入、成本、分成比例组合

脱敏策略：

- 命中敏感字段名和相邻数字时整体替换为 `[已脱敏经营指标]`。
- 命中表格列名为敏感字段时，删除该列。
- 命中同一句中同时出现收入、成本、分成、利润等组合时，整体替换。
- 输出前再次扫描，命中则阻断保存。

### 12.4 Prompt 约束

系统提示词要求：

- 只基于用户提供的脱敏内容生成会议提纲。
- 不输出毛利、毛利率、利润、利润率、GM、gross profit 等敏感信息。
- 不猜测、推导或补全被脱敏数据。
- 输出固定六段结构。
- 语言简洁，突出经营判断、待决策问题和下周动作。

### 12.5 输出结构

AI 返回 JSON：

```json
{
  "meeting_goal": "",
  "weekly_overview": "",
  "key_topics": "",
  "agenda": "",
  "next_actions": "",
  "preparation": ""
}
```

前端渲染为：

一、本次会议目标

二、本周经营总览

三、本次重点讨论和决策议题

四、建议会议议程

五、下周动作建议

六、请大家会前准备

## 13. API 设计

### 13.1 经营周会

- `GET /api/operational-meetings`
- `POST /api/operational-meetings`
- `GET /api/operational-meetings/:id`
- `PUT /api/operational-meetings/:id`
- `DELETE /api/operational-meetings/:id`
- `POST /api/operational-meetings/:id/archive`

### 13.2 填写块

- `GET /api/operational-meetings/:id/sections`
- `PUT /api/operational-meeting-sections/:sectionId`
- `POST /api/operational-meeting-sections/:sectionId/submit`
- `POST /api/operational-meeting-sections/:sectionId/unsubmit`

### 13.3 AI 提纲

- `POST /api/operational-meetings/:id/agenda/generate`
- `GET /api/operational-meetings/:id/agenda`
- `PUT /api/operational-meetings/:id/agenda`

说明：

- 如果采用浏览器直连 AI，则后端 `generate` 接口只负责生成 nonce、记录审计和保存密文结果。
- 如果采用服务端代理 AI，则请求体只能包含脱敏后的内容。

### 13.4 会议结论

- `GET /api/operational-meetings/:id/decisions`
- `POST /api/operational-meetings/:id/decisions`
- `PUT /api/operational-meeting-decisions/:decisionId`
- `DELETE /api/operational-meeting-decisions/:decisionId`

### 13.5 模板

- `GET /api/operational-meeting-templates`
- `POST /api/operational-meeting-templates`
- `PUT /api/operational-meeting-templates/:id`
- `DELETE /api/operational-meeting-templates/:id`
- `POST /api/operational-meeting-templates/:id/enable`

### 13.6 敏感模块授权

- `GET /api/admin/sensitive-modules`
- `GET /api/admin/sensitive-modules/:moduleKey/members`
- `PUT /api/admin/sensitive-modules/:moduleKey/members`

## 14. 审计日志

必须记录以下操作：

- 新建经营周会
- 修改周时间范围
- 编辑填写块
- 提交填写块
- 生成 AI 提纲
- 重新生成 AI 提纲
- 保存会议结论
- 修改敏感模块授权
- 新增或撤销加密授权
- 模板修改

审计日志不得记录明文内容，只记录：

- 操作类型
- 操作人
- 记录 ID
- 时间
- 状态变化
- hash 或摘要标识

## 15. 验收标准

### 15.1 功能验收

1. 目标计划菜单下可以看到“经营周会”入口。
2. 用户可以新建经营周会并选择周时间范围。
3. 列表正确展示周时间范围和状态。
4. 新建后自动生成默认四个填写块。
5. 每个填写块可独立编辑、保存、提交。
6. 填写块内容支持文档中心基础块编辑能力。
7. 所有必填块提交后，生成会议提纲按钮可用。
8. AI 生成结果包含固定六段结构。
9. AI 生成结果不包含毛利、毛利率、利润、GM 等敏感信息。
10. 会后可以填写会议结论。
11. 模板中新增或删除填写块后，新建周会按新模板生成。

### 15.2 权限验收

1. 没有菜单权限的用户看不到经营周会入口。
2. 有菜单权限但没有敏感模块授权的用户仍看不到经营周会入口。
3. 直接访问路由时，无敏感授权用户被拦截。
4. 直接调用后端 API 时，无敏感授权用户返回 403。
5. 非负责人不能编辑仅负责人可编辑的填写块。
6. 敏感模块授权变更后，前后端权限即时生效。

### 15.3 加密验收

1. 新经营周会内容不以明文保存到数据库。
2. 新经营周会内容不使用旧服务端密钥 `enc:v1:` 作为唯一保护。
3. 删除服务器旧密钥后，新经营周会内容仍可由授权用户在浏览器端解密。
4. 未授权用户即使拿到数据库内容，也无法解密经营周会内容。
5. 服务端日志不出现经营周会明文。
6. 旧 `enc:v1:` 数据仍可正常读取。

### 15.4 AI 安全验收

1. AI 输入前会进行本地脱敏。
2. 脱敏后的内容不包含毛利相关字段和数字。
3. AI 输出前或保存前会二次扫描敏感信息。
4. 命中敏感信息时，阻断保存并提示用户。
5. AI 输出结构稳定，可直接渲染到六段提纲。

## 16. 分期落地

### 16.1 第一期：功能闭环

完成：

- 菜单入口。
- 经营周会列表。
- 新建周会。
- 默认模板。
- 四个填写块。
- 文档块编辑区复用。
- 填写块提交状态。
- 会议结论。
- 基础 AI 生成提纲。
- 菜单权限和敏感模块权限。

说明：

- 第一期可先沿用服务端加密保存，但必须为 `crypto_version` 和后续 `enc:v2` 预留字段。
- 高敏内容上线前建议直接进入第二期加密方案。

### 16.2 第二期：端到端加密

完成：

- 用户安全密码。
- 用户公私钥生成。
- 私钥本地加密保存。
- 记录级 DEK。
- DEK 分发给授权用户。
- 浏览器本地加密和解密。
- 新经营周会内容使用 `enc:v2`。

### 16.3 第三期：AI 安全增强

完成：

- 浏览器本地脱敏规则完善。
- AI 输出敏感信息扫描。
- 结构化 JSON 输出校验。
- AI 生成版本管理。
- 提纲重新生成对比。

### 16.4 第四期：历史数据迁移

完成：

- 识别已有高敏模块。
- 支持旧 `enc:v1` 和新 `enc:v2` 并存。
- 用户打开旧数据后可重新加密为 `enc:v2`。
- 逐步迁移周报、经营报表、人脉关键字段等高敏内容。

## 17. 风险与待确认

### 17.1 安全密码丢失

风险：

- 用户忘记安全密码后，无法解密自己的私钥。

建议：

- 第一版通过其他已授权 CXO 重新授权恢复。
- 后续增加恢复码或多 CXO 门限恢复。

### 17.2 AI 调用路径

风险：

- 如果 AI 请求经过服务端，运维理论上可以看到脱敏后的 AI 输入。

建议：

- 默认服务端只处理脱敏内容。
- 高安全模式下浏览器直连 OpenAI 或 CXO 控制的 AI 网关。

### 17.3 文档中心编辑器复用

风险：

- 文档中心编辑器目前集中在 `Documents.js` 中，直接复用可能需要组件拆分。

建议：

- 第一期抽出通用 block editor 组件。
- 保持 blocks 数据结构一致，减少迁移成本。

### 17.4 旧数据迁移

风险：

- 旧 `enc:v1` 数据依赖服务器密钥，不能直接废弃。

建议：

- 保持旧读写兼容。
- 新数据默认使用 `enc:v2`。
- 历史数据按模块逐步迁移。

## 18. 推荐实现原则

1. 模板驱动，不硬编码四个负责人。
2. 后端权限强校验，不依赖前端隐藏。
3. 明文不进数据库、不进日志、不进审计。
4. 服务端不保存明文私钥、不保存明文 DEK。
5. AI 只处理脱敏文本。
6. 旧加密兼容读取，新高敏内容使用新加密版本。
7. 第一版先把业务流程做顺，安全能力尽早纳入架构，不要后补。
