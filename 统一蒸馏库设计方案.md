# 统一蒸馏库设计方案

> 版本：v1.0  
> 日期：2026-07-03  
> 适用范围：IAA 广告业务 AI 蒸馏 / Skill / Eval 前期 MVP

---

## 1. 设计目标

围绕当前已确认的两类数据来源，建设一套可持续演进的统一蒸馏库：

1. `组织中台数据库`
   - 已包含文档中心、公司研究、策略结果、任务反馈等内部知识与业务记录
2. `selectedDB`
   - 位于 `mid-max.midongtech.com` 背后的业务数据源
   - 存放广告经营相关历史结构化数据

目标不是再造一个“大而全”的数据仓库，而是建设一个专门服务 AI 的中间层数据库，让蒸馏、Skill 调优、评测和最小调用入口都基于一套稳定、标准、可审计的数据层运行。

---

## 2. 核心结论

建议采用 `双源库 + 统一蒸馏库` 的方案，而不是二选一。

即：

- `组织中台数据库` 继续作为内部文档和业务记录源库
- `selectedDB` 继续作为业务事实源库
- 新增 `统一蒸馏库（MySQL）` 作为 AI 服务层数据库

不建议的两种做法：

1. **不建议让 Skill 直接长期依赖 selectedDB 在线查询**
   - 口径不稳定
   - 运行时耦合重
   - 成本和故障面都高
   - 不利于审核、回放和评测

2. **不建议一开始就建设一个覆盖所有业务的超大统一仓库**
   - MVP 周期内投入过大
   - 会拖慢蒸馏和 Skill 调优
   - 很多字段在前期并不会被真正使用

正确做法是：

- 保留源系统
- 只同步当前蒸馏和 Skill 真正需要的数据子集
- 在统一蒸馏库中做标准化、融合、审核、发布和回放

---

## 3. 三层数据库角色分工

### 3.1 组织中台数据库

定位：`内部知识与业务动作源库`

主要提供：

- 文档中心文档
- 公司研究内容
- 复盘、周报、会议纪要
- 策略记录、任务记录、反馈记录
- 用户、部门、权限、共享关系

不负责：

- AI 评测闭环
- Skill 版本管理
- 蒸馏候选资产管理
- 外部数据标准化沉淀

### 3.2 selectedDB

定位：`业务事实源库`

主要提供：

- 广告经营历史数据
- 预算、收入、展示、点击、填充、请求等指标
- 主体 / 产品 / 媒体 / 渠道表现
- 小时级 / 天级 / 周级历史趋势
- 可能的实验、策略配置、投放结果相关事实

不建议直接承担：

- 文档蒸馏
- Skill 运行时实时依赖
- 评测回放主存储

### 3.3 统一蒸馏库（MySQL）

定位：`AI 服务层数据库`

负责承接：

- 来源接入配置
- 原始快照留存
- 标准化事实层
- 文本切片与来源映射
- 蒸馏候选资产与正式资产
- Skill 定义、版本、绑定关系
- 运行日志、反馈、评测

一句话概括：

`源库继续各司其职，统一蒸馏库负责为 AI 提供可用、可控、可回放的数据底座。`

---

## 4. 推荐分层模型

统一蒸馏库建议按 5 层组织。

### 4.1 接入层

负责记录“数据从哪来、怎么来、谁负责、多久同步一次”。

典型表：

- `ai_data_sources`
- `ai_source_sync_jobs`
- `ai_external_field_mappings`

### 4.2 原始层

负责保存原始快照，便于追溯、重放和问题排查。

典型表：

- `ai_raw_records`

说明：

- 文档类来源可保存文档快照或正文快照
- selectedDB 类来源可保存接口返回、导出报表、标准化前 JSON
- 该层不追求查询性能，重点是留痕和回放

### 4.3 标准化层

负责将不同来源统一成 AI 能理解的稳定结构。

典型表：

- `ai_source_items`
- `ai_source_chunks`
- `ai_metric_dictionary`
- `ai_dimension_dictionary`
- `ai_fact_metric_values`
- `ai_strategy_change_logs`

说明：

- 文档被切成标准 chunk
- 业务数据被整理成统一事实表
- 口径、维度、指标都在这一层统一

### 4.4 蒸馏资产层

负责沉淀候选规则、案例、模式、诊断步骤等资产，并完成审核发布。

典型表：

- `ai_knowledge_assets`
- `ai_knowledge_versions`
- `ai_knowledge_links`

说明：

- “候选资产”和“正式资产”都在此管理
- 每个资产都要能追溯到来源文档、来源 chunk、来源数据样本或来源报表

### 4.5 Skill / Eval 层

负责 AI 运行和验证闭环。

典型表：

- `ai_skill_defs`
- `ai_skill_versions`
- `ai_skill_asset_bindings`
- `ai_skill_runs`
- `ai_eval_cases`
- `ai_eval_runs`
- `ai_feedback_records`

---

## 5. 为什么不用“直接读取 selectedDB”

短期看直接读很快，长期看问题会很多。

### 5.1 运行时不稳定

- selectedDB 面向业务系统，不一定面向 AI 调用
- 查询口径一旦变更，Skill 输出可能直接漂移
- 一旦外部网络、权限、接口限流异常，Skill 就不可用

### 5.2 无法沉淀“已验证认知”

Skill 真正需要的不是“再查一次原始数据”，而是：

- 哪种模式算异常
- 哪种结论在公司历史里被验证过
- 哪种建议在某业务线有效、在另一条业务线无效

这些都需要进入统一蒸馏库，形成正式知识资产。

### 5.3 不利于评测与回放

评测时要保证输入稳定。

如果 Skill 每次都实时读 selectedDB：

- 同一用例今天和明天可能跑出不同结果
- 很难判断是 Prompt 变了、知识变了还是源数据变了

所以更合理的做法是：

- selectedDB 先同步到内部标准化层
- Skill、评测、回归都基于统一蒸馏库运行

---

## 6. 为什么也不建议一开始就做“超大统一库”

因为前期目标不是 BI，不是全量数据搬家，而是：

- 先把蒸馏准
- 先把 Skill 调准
- 先把评测跑顺

所以统一蒸馏库前期只同步以下必需数据即可：

### 6.1 从组织中台同步

- 文档中心中的经营类文档
- 公司研究文档
- 复盘文档
- 策略结果与任务反馈
- 用户与权限映射

### 6.2 从 selectedDB 同步

- 当前 1 到 2 个核心 Skill 需要的历史指标
- 主体 / 产品 / 媒体 / 渠道维度主数据
- 策略变更或实验结果数据
- 可直接支撑异常诊断和策略建议的数据子集

结论是：

`先做“够用的统一蒸馏库”，而不是“完美的大仓库”。`

---

## 7. 前期 MVP 推荐数据范围

### 7.1 文档类

- 文档标题
- 文档正文快照
- 创建人 / 更新时间
- 文档标签
- 来源模块
- 共享范围

### 7.2 经营事实类

- 日期
- 主体
- 产品
- 媒体
- 渠道
- 请求量
- 填充量
- 展示量
- 点击量
- 收入
- eCPM
- CTR
- 填充率
- 策略版本 / 实验标识（如果有）

### 7.3 蒸馏资产类

- 规则卡片
- 案例卡片
- 数据模式卡片
- 诊断步骤模板
- few-shot 示例

### 7.4 Skill / Eval 类

- Skill 定义与版本
- 绑定资产
- 运行记录
- 评测样本
- 评测结果
- 人工反馈

---

## 8. MySQL 作为正式方案的原因

建议统一蒸馏库直接使用 `MySQL 8.0+`，原因如下：

1. 更适合后续部署到服务器
2. 团队普遍更熟悉
3. 支持 JSON、事务、索引、外键、审计字段
4. 后续和线上业务系统对齐更顺
5. 比 SQLite 更适合多人协作和定时同步

### 8.1 建议约束

- 字符集：`utf8mb4`
- 存储引擎：`InnoDB`
- 时间字段统一：`DATETIME(3)`
- 主键统一：`BIGINT UNSIGNED`
- 枚举类字段先使用 `VARCHAR`，不急着用 `ENUM`
- 复杂结构统一存 `JSON`
- 所有业务表统一保留：
  - `created_at`
  - `updated_at`
  - `created_by`
  - `updated_by`

---

## 9. 同步策略设计

### 9.1 同步原则

1. **源库只读**
2. **统一蒸馏库可写**
3. **按来源类型走不同同步链路**
4. **先留原始快照，再做标准化**
5. **失败可重试，成功可追溯**

### 9.2 组织中台同步链路

```text
组织中台文档/记录 -> ai_data_sources -> ai_raw_records -> ai_source_items -> ai_source_chunks -> ai_knowledge_assets
```

### 9.3 selectedDB 同步链路

```text
selectedDB接口/报表 -> ai_data_sources -> ai_raw_records -> ai_fact_metric_values / ai_strategy_change_logs -> ai_knowledge_assets
```

### 9.4 推荐同步频率

- 文档类：5 到 15 分钟增量同步
- 日级经营事实：每日定时 + 手动补跑
- 小时级经营事实：每小时同步
- 策略变更日志：按小时或按事件同步
- 评测样本和反馈：实时写入

---

## 10. 数据库表分组建议

### 10.1 接入与同步

- `ai_data_sources`
- `ai_source_sync_jobs`
- `ai_external_field_mappings`
- `ai_raw_records`

### 10.2 标准化来源与文本

- `ai_source_items`
- `ai_source_chunks`
- `ai_metric_dictionary`
- `ai_dimension_dictionary`
- `ai_fact_metric_values`
- `ai_strategy_change_logs`

### 10.3 蒸馏资产

- `ai_knowledge_assets`
- `ai_knowledge_versions`
- `ai_knowledge_links`

### 10.4 Skill

- `ai_skill_defs`
- `ai_skill_versions`
- `ai_skill_asset_bindings`
- `ai_skill_runs`

### 10.5 Eval 与反馈

- `ai_eval_cases`
- `ai_eval_runs`
- `ai_feedback_records`

---

## 11. 表设计原则

### 11.1 文档类不要直接依赖原始业务表结构

原因：

- 原系统字段未来可能调整
- AI 只需要和蒸馏相关的那一部分

所以统一蒸馏库里应该存“AI 视角下的标准来源项”，而不是把原表一比一照搬。

### 11.2 经营数据优先用“长表”承接指标

推荐使用 `ai_fact_metric_values` 这种长表：

- 一个指标一行
- 指标代码统一
- 维度按统一字段 + `extra_dimensions_json` 承接

优点：

- 易扩展
- 适合不同业务线共用
- 更利于后续蒸馏成“数据模式卡片”

### 11.3 知识资产必须版本化

原因：

- 规则会变
- 案例会补充
- Prompt 依赖会变化

所以：

- 资产主表记录当前状态
- 版本表记录每次发布内容
- 运行和评测绑定版本号

### 11.4 Skill 运行记录必须和证据关联

建议每次运行至少留：

- 输入参数
- 输出结果
- 使用的 Skill 版本
- 使用的知识版本或资产列表
- 命中的来源证据
- 耗时、Token、成本

---

## 12. 推荐实施顺序

### 第 1 步：先建统一蒸馏库

落 MySQL 库和表，不先碰复杂前端。

### 第 2 步：先打通两条同步链路

1. 组织中台文档同步
2. selectedDB 经营事实同步

### 第 3 步：先做两类标准化

1. 文本 chunk 标准化
2. 历史指标标准化

### 第 4 步：先沉淀 3 类资产

1. 规则卡片
2. 案例卡片
3. 数据模式卡片

### 第 5 步：先服务 1 到 2 个 Skill

例如：

- 收入异常诊断
- 预算分配建议

### 第 6 步：最后再接复杂 HMI

等产物和评测都稳定后，再做更完整的组织中台入口和多人使用界面。

---

## 13. 最后建议

如果现在目标是两周内做出能验证价值的 MVP，最优路径不是：

- 直接让模型去查 selectedDB
- 或者先做一个很重的大仓库

而是：

1. 用 MySQL 建一个专门的统一蒸馏库
2. 从组织中台和 selectedDB 各同步一部分高价值数据
3. 先把标准化、蒸馏、Skill、Eval 跑通
4. 等输出稳定后，再决定是否进一步扩成更完整的数据中台

---

## 14. 配套文件

- MySQL DDL：[`sql/ai_distill_mysql.sql`](/Users/chenhaozan/Documents/AI/Relation/sql/ai_distill_mysql.sql)
- 主 PRD：[`IAA广告业务AI基建PRD.md`](/Users/chenhaozan/Documents/AI/Relation/IAA广告业务AI基建PRD.md)
- selectedDB 接入方案：[`selectedDB接入统一蒸馏库技术方案.md`](/Users/chenhaozan/Documents/AI/Relation/selectedDB接入统一蒸馏库技术方案.md)
- selectedDB 字段映射：[`selectedDB到统一蒸馏库字段映射表.md`](/Users/chenhaozan/Documents/AI/Relation/selectedDB到统一蒸馏库字段映射表.md)
