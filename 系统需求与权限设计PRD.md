# 系统需求与权限设计 PRD

## 1. 文档目的

本文档用于统一梳理当前系统的业务需求、模块边界和权限设计口径，作为后续产品迭代、研发实现、测试验收和权限问题排查的统一依据。

本文档基于当前代码实现梳理，覆盖：

- 系统角色与组织模型
- 菜单、模块、读写权限
- 各业务模块核心需求
- 数据可见性规则
- 任务、目标、线索、策略、需求、资产、商务协作等关键链路
- 当前实现中的权限差异和待统一点

## 2. 系统定位

系统是一个面向组织内部的经营、商务、人脉、任务、目标和资产协同平台。

核心目标：

- 让个人可以管理自己的任务、人脉、互动、线索和周报。
- 让小组成员和组长围绕同一小组进行任务和业务协同。
- 让总监查看和管理其辖区小组的数据。
- 让老板角色查看公司级经营视图和全量关键业务数据。
- 让管理员维护组织结构、账号、菜单权限和跨团队授权。

## 3. 用户角色

当前系统角色包括：

- `admin`：系统管理员，默认拥有最高权限。
- `ceo` / `coo` / `cto` / `cmo`：老板/高管角色，经营模块和任务视角中拥有全量可见能力。
- `sales_director`：商务总监，可查看和管理其管辖小组的数据。
- `leader`：小组组长，可查看和管理其所在或负责小组的数据。
- `member`：普通成员，默认查看自己和所属小组范围内的部分业务数据。
- `readonly`：只读账号，前端可读但后端写操作应受限。
- `guest`：访客账号，可通过模块权限配置进行有限访问。

当前系统还存在 `executive_role` 字段，用于在用户主角色不是 `ceo/coo/cto/cmo` 时补充老板身份。

## 4. 组织模型

### 4.1 小组

小组由 `teams` 维护，包含：

- 小组名称
- 所属部门
- 小组组长

用户与小组关系由两层组成：

- `users.team_id`：主小组。
- `user_teams`：用户可归属的多个小组。

需求口径：

- 普通成员和组长的数据边界应优先按小组归属判断。
- 同一小组内成员可在任务等协同模块中互相看到小组范围内的数据。
- 跨组可见需要显式授权或共享。

### 4.2 总监管辖小组

商务总监通过 `director_teams` 绑定可管辖的小组。

需求口径：

- 总监可查看其管辖小组成员的数据。
- 总监也可查看自己作为组长负责的小组数据。

### 4.3 项目组

项目组由 `project_groups` 维护，用于目标、人员、项目归属等场景。

需求口径：

- 项目组由管理员维护。
- 用户可归属多个项目组。
- 目标管理中需要按项目组、部门、小组、个人形成层级口径。

## 5. 认证与会话

当前实现：

- 登录接口签发 JWT。
- token 有效期为 8 小时。
- 修改密码后通过 `password_version` 让旧 token 失效。
- 前端有 2 小时无操作自动退出。
- 除 `/api/auth/login` 外，所有 `/api` 路由都需要 JWT 鉴权。

## 6. 菜单与模块权限

### 6.1 菜单权限

菜单权限由 `user_menu_perms` 控制。

当前前端口径：

- 前端 `isAdmin(user)` 同时识别 `role` 与 `executive_role` 中的 `admin/ceo/coo/cto/cmo`，命中后默认看到所有菜单。
- 非管理员、非老板身份菜单展示依赖 `/api/auth/login` 或 `/api/auth/me` 返回的 `menuPerms`。
- `menuPerms` 存储的是菜单 key，当前主要为页面路径，例如 `/goals`、`/weekly-reports`、`/documents`、`/menu-perms`。
- 菜单权限只控制前端入口是否展示，不等同于后端数据权限；具体页面路由仍受登录保护，后端接口仍按模块自身规则校验。
- 菜单权限管理接口为 `/api/admin/menu-perms/:userId`，保存时全量替换该用户的菜单 key。

### 6.2 模块读写权限

模块权限由 `user_module_perms` 控制，主要用于访客账号。

当前前端口径：

- `admin` / 老板角色 / `member` / `leader` / `sales_director` / `readonly` 默认可读。
- `guest` 根据 `modulePerms.can_read` 判断是否可读。
- `member` / `leader` / `sales_director` / 管理员默认可写。
- `readonly` 不可写。
- `guest` 前端会参考 `modulePerms.can_write`。

当前后端口径：

- `canWrite` 中间件会阻止 `readonly` 和 `guest` 写入。
- 部分较新的业务接口没有统一挂 `canWrite`，依赖各自模块规则。
- 用户管理中仅 `guest` 角色会保存 `modulePerms` 明细；其他角色主要依赖角色和业务规则。

待统一点：

- 如果要支持访客可写，应让后端 `canWrite` 与前端 `modulePerms.can_write` 对齐。
- 如果访客永远不可写，应调整前端 `canWrite`，避免出现前端可写但后端拒绝。

## 7. 系统菜单与模块范围

### 7.1 工作台

路径：`/`

主要能力：

- 查看统计卡片。
- 查看“我指派给别人的任务”。
- 查看“需我执行的任务”。
- 查看“需我关注的任务”。
- 查看“团队任务”。
- 创建、编辑、更新、删除普通任务。
- 通过共享人让跨组成员看到任务。

### 7.2 目标计划

包含：

- 目标管理：`/goals`
- 周报管理：`/weekly-reports`

### 7.3 业务流转

包含：

- 商机/线索：`/leads`
- 策略：`/strategies`
- 需求/研发任务：`/dev-tasks`

核心业务链路：

**线索 -> 策略 -> 需求**

### 7.4 资产管理

包含：

- 主体管理：`/company-subjects`
- 产品资产：`/product-assets`
- 产品模版：`/product-templates`
- 媒体管理：`/media-management`

媒体管理权限口径：

- 管理员及老板身份可直接访问；其他用户需要 `/media-management` 菜单权限。
- `guest` 还必须具备 `product_assets.can_read=1`；`readonly` 和 `guest` 不可写。
- 通过上述入口校验的用户统一读取全部未删除媒体记录，以及每条媒体绑定的关联文档、正文、附件和
  历史；文档创建人和共享范围不得再次缩减媒体列表或详情可见范围。媒体及关联文档的编辑仍按
  文档创建人、共享人和管理员权限判断。
- 负责人会自动加入关联文档共享人；删除媒体仅允许 CEO、COO、CTO、CMO，以及系统小组中实际
  担任“流量商务”商务小组组长的 `leader`。普通管理员、创建人、共享协作者、预算商务组长和
  其他部门组长均不展示删除入口，直接调用删除接口返回 `403`。

### 7.5 商务协作

包含：

- 公司研究：`/companies`
- 人脉管理：`/persons`
- 互动记录：`/interactions`
- 提醒事项：`/reminders`
- 待跟进任务：`/follow-up-tasks`

### 7.6 团队管理

包含：

- 任务看板：`/task-board`
- 送礼计划：`/gift-plans`
- 审核与记录：`/gift-review`
- 礼品库：`/gifts`
- 出差申请：`/trips`
- 费用统计：`/trip-stats`
- 预算管理：`/budgets`

### 7.7 常用工具

包含：

- DAU查询助手：`/dau-query-assistant`，在系统内容区直接嵌入
  `https://ngwlcg9gyg3i.space.mcode.cn`
- 网络抓包：`/network-capture`
- 手机采集：`/mobile-task-center`

DAU查询助手遵循通用菜单权限：管理员及老板身份默认可见，其他用户需配置
`/dau-query-assistant` 菜单权限；页面不代理或存储第三方页面数据。

### 7.8 公司经营

包含：

- 经营概览：`/executive`
- 高级人才：`/executive/talents`
- 竞品动态：`/executive/dynamics`
- 重点客户：`/executive/customers`
- 战略月会：`/executive/strategic`
- 经营周会：`/executive/operational`
- 招聘雷达：`/executive/recruit-radar`
- 雷达配置：`/executive/recruit-radar/config`

### 7.9 系统管理

前端系统管理菜单对前端 `isAdmin(user)` 可见，即 `role` 或 `executive_role` 命中 `admin/ceo/coo/cto/cmo` 均可显示。

后端系统管理接口不是完全同一口径：

- 用户、小组、项目组、菜单权限等接口多数使用 `adminOnly`，按 `role` 是否属于 `admin/ceo/coo/cto/cmo` 判断。
- 跨团队权限接口当前仅允许 `role = admin` 访问和配置。
- 操作日志入口当前仅 `role = admin` 显示。

包含：

- 用户管理
- 小组管理
- 项目组管理
- 菜单权限管理
- 跨团队权限

## 8. 通用数据可见性规则

### 8.1 管理员与老板

管理员和老板角色默认具备全量可见能力。

老板身份判断口径：

- `role` 为 `ceo/coo/cto/cmo`
- 或 `executive_role` 为 `ceo/coo/cto/cmo`

注意：

- 任务和经营模块已同时兼容 `role` 与 `executive_role`。
- 部分 `adminOnly` 后端接口当前只判断 `role`，若老板只配置为 `executive_role`，可能出现前端菜单可见但后端拒绝的情况。
- 目标、周报、跨团队权限等接口当前主要按 `role` 做数据边界判断，`executive_role` 不应被默认视为这些接口的全量可见依据，除非后续统一改造。

### 8.2 小组组长

组长可见范围来自：

- 自己所属小组。
- 自己作为 `teams.leader_id` 负责的小组。
- 直接下属。
- 部分模块可叠加跨团队权限。

### 8.3 商务总监

总监可见范围来自：

- `director_teams` 配置的管辖小组。
- 自己作为组长负责的小组。

### 8.4 普通成员

普通成员默认可见：

- 自己创建的数据。
- 指派给自己的数据。
- 所属小组范围内的数据，具体以模块实现为准。
- 显式共享给自己的数据。

### 8.5 跨团队权限

跨团队权限由 `cross_team_access` 管理。

当前支持模块：

- `strategies`
- `dev_tasks`
- `leads`
- `product_assets`
- `goals`
- `weekly_reports`

需求口径：

- 跨团队权限用于模块级查看目标团队数据。
- 配置维度为“用户 + 目标小组 + 模块”，同一组合唯一。
- 当前只有 `role = admin` 可进入跨团队权限接口进行配置。
- 对任务模块，跨组任务建议通过“任务共享人”实现，而不是使用 `cross_team_access`。

## 9. 任务权限设计

### 9.1 普通任务

普通任务存储在 `tasks`。

关键字段：

- 创建人：`created_by`
- 执行人：`assigned_to`
- 小组：`team_id`
- 父任务：`parent_id`
- 共享人：`task_shared_users`

### 9.2 任务可见性

当前统一口径：

- 老板和管理员可看全部任务。
- 组长、总监、普通成员可看自己所属或管辖小组范围内任务。
- 任何用户可看自己创建的任务。
- 任何用户可看指派给自己的任务。
- 被加入任务共享人的用户可看该任务。

跨组任务口径：

- 跨组成员默认不能看到其他小组任务。
- 如果需要跨组成员看到任务，需要在任务“共享给”中添加该成员。

### 9.3 工作台任务 tab

工作台包含：

- 我指派给别人的任务：我创建且执行人不是我的任务。
- 需我执行的任务：指派给我的任务。
- 需我关注的任务：我作为关注人的商机跟进任务。
- 团队任务：后端返回的同组/管辖/共享任务集合。

团队任务列表需要展示：

- 任务来源
- 优先级
- 指派人
- 跟进人/执行人
- 共享人
- 计划日期、开始日期、完成日期
- 状态
- 任务进度/结果

### 9.4 任务写入权限

当前实现：

- 创建任务：登录用户均可创建。
- 更新任务：执行人、创建人、管理员、商务总监可修改。
- 更新共享人：创建人、管理员、商务总监可修改。
- 删除任务：创建人或管理员可删除；非管理员只能删除待处理任务。
- 状态流转：待处理 -> 进行中 -> 已完成。

### 9.5 任务看板

任务看板按成员聚合某日任务。

当前口径：

- 老板和管理员可看全员。
- 组长和总监可看其可见小组成员。
- 普通成员若有小组，可看同组成员看板。

## 10. 待跟进任务与商机

待跟进任务存储在 `follow_up_tasks`，来源包括：

- 人脉互动中的商机字段。
- 竞品研究中的商机字段。

### 10.1 待跟进任务可见性

当前口径：

- 自己是执行人或指派人时可见。
- 老板和管理员使用全量参数时可见全部。
- 组长和总监使用全量参数时可见可见成员范围内的任务。
- 关注人通过 `lead_watchers` 进入“需我关注的任务”。

### 10.2 待跟进任务写入

当前口径：

- 只有执行人或管理员可更新跟进任务状态、备注和截止日期。

## 11. 人脉与互动

### 11.1 人脉管理

人脉存储在 `persons`，支持商务圈、人才圈、创业圈、社交圈等分类。

可见性：

- 管理员/老板：全量。
- 组长/总监：按 `getVisibleUserIds` 范围查看创建人或指派人在可见成员范围内的数据。
- 普通成员：默认查看自己创建或指派给自己的数据。
- 共享给自己的数据可见。

写入权限：

- `canWrite` 通过的用户可创建。
- 普通成员/只读账号编辑时仅能修改自己创建或指派给自己的人脉。
- 普通成员删除时只能删除自己录入的人脉。
- 组长、总监、管理员可进行人脉指派。

### 11.2 互动记录

互动记录关联人脉。

可见性：

- 通过关联人脉的可见范围控制。

业务自动化：

- 录入下一步行动和日期时，自动生成提前 3 天提醒。
- 录入商机标题和负责人时，自动生成待跟进任务。

### 11.3 提醒事项

提醒用于跟进行动提醒。

当前实现：

- 支持按完成状态和人脉筛选。
- 支持创建、标记完成、删除。

## 12. 公司研究与竞品动态

### 12.1 公司研究

公司研究存储在 `companies` 及其子表。

可见性：

- 管理员/老板可见全部。
- 非管理员可见自己创建或 `shared_with` 包含自己的公司。

能力：

- 公司基础信息。
- 公司实体。
- 人员。
- 产品。
- 动态。
- 竞品研究。

### 12.2 竞品研究

竞品研究可产生商机和待跟进任务。

能力：

- 记录竞品动态。
- 设置商机标题、负责人、备注。
- 设置关注人。
- 生成/同步待跟进任务。

## 13. 目标管理

目标存储在 `goals`。

核心字段：

- 目标标题
- 目标类型：季度/月度/周等
- 周期
- 负责人
- 归属颗粒度：项目组、部门、小组、个人
- 进度、状态、结果
- 父目标

可见性：

- `role` 为 `admin/ceo/coo/cto/cmo`：全量。
- `sales_director`：`director_teams` 管辖小组成员目标，以及自己作为组长负责的小组目标。
- `leader`：自己所属小组、自己作为 `teams.leader_id` 负责的小组成员目标，可叠加 `cross_team_access(module='goals')`。
- `member`：自己、上级组长、所属小组 leader，以及 `cross_team_access(module='goals')` 授权目标小组成员目标。
- 商务部门 `leader` 的特殊可见规则：除上述范围外，可查看 `scope_type = 'team'` 且部门口径为 `operation` 的产运部门小组目标。部门判断按 `goals.department -> teams.department -> owner.department` 兜底。
- 目标列表和目标详情必须使用同一套可见性判断，避免列表可见但详情返回 403。
- 部门筛选同样按 `goals.department -> teams.department -> owner.department` 兜底，确保小组目标没有显式填写 `goals.department` 时仍可被正确筛选。

写入权限：

- `role` 为 `admin/ceo/coo/cto/cmo` 可为任何负责人创建、编辑、删除目标。
- 目标负责人本人可管理自己的目标。
- 组长/总监可管理可见小组成员目标。
- 商务部门 `leader` 对产运部门小组目标的特殊可见规则仅用于查看，不自动赋予创建、编辑、删除权限。

目标校验：

- 目标必须包含标题、负责人、目标类型、周期。
- 当归属颗粒度为项目组、部门、小组、个人时，需要满足对应字段必填规则。

## 14. 周报管理

周报存储在 `weekly_reports`。

可见性：

- `role` 为 `admin/ceo/coo/cto/cmo`：全量。
- `sales_director`：管辖小组成员周报。
- `leader`：负责或所属小组成员周报，可叠加 `cross_team_access(module='weekly_reports')`。
- `member`：自己、上级组长、所属小组 leader，以及授权目标小组成员周报。

写入权限：

- 普通用户只能写自己的周报。
- 管理员可代写或管理他人周报。
- 仅管理员可删除周报。
- 仅管理员可维护需要写周报的用户名单。

## 15. 线索、策略、需求链路

### 15.1 线索

线索存储在 `leads`。

核心字段：

- 标题
- 来源
- 联系人/公司/联系方式
- 描述
- 跟进结果
- 负责人
- 状态
- 优先级

可见性：

- 管理员/老板：全量。
- 总监：管辖小组成员创建或负责的线索。
- 组长：所属/负责小组成员创建或负责的线索，可叠加跨团队权限。
- 普通成员：自己创建或负责的线索，可叠加跨团队权限。

### 15.2 策略

策略存储在 `strategies`。

核心字段：

- 策略 ID
- 标题
- 维度
- 角色类型
- 预算组类型
- 负责人
- 来源类型和来源 ID
- 状态
- 执行记录
- 复盘结果

可见性：

- 管理员/老板：全量。
- 总监：管辖小组负责人策略。
- 组长：所属/负责小组负责人策略，可叠加跨团队权限。
- 普通成员：自己负责的策略，可叠加跨团队权限。
- 运营部门成员当前可按同组策略查看。

### 15.3 需求/研发任务

需求存储在 `dev_tasks`。

核心字段：

- 需求 ID
- 标题
- 描述
- 来源线索或来源策略
- 负责人
- 状态
- 优先级
- 预计工时、实际工时
- 完成备注

可见性：

- 管理员/老板：全量。
- 总监：管辖小组创建或负责的需求。
- 组长：所属/负责小组创建或负责的需求，可叠加跨团队权限。
- 普通成员：自己创建或负责的需求，可叠加跨团队权限。
- 运营部门成员当前可按同组需求查看。

写入权限：

- 普通成员创建需求时只能指派给自己。
- 负责人变更和完成会产生通知。

## 16. 产品资产与主体管理

### 16.1 主体管理

主体管理存储在 `company_subjects`。

能力：

- 维护集团名、公司主体、法人、电话、邮箱、备注、状态。
- 上传和删除主体附件。
- 已关联产品资产的主体不能删除。
- 更新主体会同步关联产品资产中的主体字段。

### 16.2 产品资产

产品资产存储在 `product_assets`。

核心字段：

- 集团名
- 公司主体
- 应用名称
- AppID
- 预算类型
- 平台
- 上线状态
- 运营负责人
- 备注

可见性：

- 管理员/老板：全量。
- 总监：管辖小组负责人或创建人资产。
- 组长：所属/负责小组负责人或创建人资产，可叠加跨团队权限。
- 普通成员：自己负责或创建的资产，同组或跨团队授权范围内的资产。

### 16.3 核减记录

产品资产可记录核减事件。

能力：

- 记录核减日期、上下游、前后预算、核减金额、比例、处罚对象、原因分类、原因分析、影响范围、状态、负责人。
- 核减记录可作为策略来源。

## 17. 送礼管理

### 17.1 礼品库

礼品库维护礼品名称、类别、价格、库存、单位、备注。

当前菜单口径：

- 礼品库菜单仅管理员/老板可见。

### 17.2 送礼计划

送礼计划用于组织节日、拜访、客户维护等计划。

### 17.3 送礼申请与审核

流程：

1. 用户提交送礼申请。
2. 系统校验库存。
3. 组长、总监、管理员/老板审核。
4. 审核通过后扣减库存并生成送礼记录。

可见性：

- 使用 `getVisibleUserIds` 按组织范围查看申请和记录。

### 17.4 送礼记录

送礼记录可更新状态、反馈和评分。

当状态变为“已接收”时，系统自动生成一条礼品互动记录。

## 18. 出差与报销

### 18.1 出差申请

流程：

1. 用户创建草稿。
2. 用户提交审批。
3. 组长、总监、管理员/老板审批通过或驳回。
4. 审批通过后可录入费用。
5. 出差完成后可创建报销单。

可见性：

- 使用 `getVisibleUserIds` 按组织范围查看。

写入权限：

- 草稿或驳回状态可由本人或管理员编辑。
- 只有本人可提交。
- 本人或管理员可标记完成。
- 已审批申请不可删除。

### 18.2 报销单

流程：

1. 出差完成后创建报销单。
2. 提交报销审批。
3. 管理员或组长审批。
4. 审批通过后状态变为已支付。

## 19. 预算管理

预算存储在 `budgets`。

可见性：

- 管理员/老板：全量。
- 总监：管辖小组创建的预算。
- 组长：同组成员创建的预算。
- 普通成员：自己创建的预算。

写入权限：

- 可写用户可创建预算。
- 创建人、管理员、总监可修改。
- 创建人或管理员可删除。

## 20. 公司经营模块

公司经营模块仅老板角色可访问。

老板身份：

- `role` 为 `ceo/coo/cto/cmo`
- 或 `executive_role` 为 `ceo/coo/cto/cmo`

能力：

- 查看经营概览。
- 查看高级人才。
- 查看竞品动态。
- 查看重点客户。
- 维护经营周报/战略月会/经营周会报告。
- 使用招聘雷达和雷达配置。

招聘雷达要求：

- 仅 CEO/COO/CTO/CMO 可访问。
- 用于 Boss 相关岗位监控和通知配置。

## 21. 系统管理

系统管理由管理员/老板菜单进入，主要能力包括：

- 用户管理：创建、编辑、删除用户，重置密码。
- 小组管理：创建、编辑、删除小组。
- 项目组管理：创建、编辑、删除项目组。
- 菜单权限管理：配置非管理员可见菜单。
- 跨团队权限：配置指定用户可查看指定团队在指定模块的数据。

当前实现注意：

- 多数系统管理后端接口使用 `adminOnly`，即只判断 `role` 是否在 `admin/ceo/coo/cto/cmo` 中。
- 如果某用户仅通过 `executive_role` 标记为老板，但 `role` 不是老板角色，可能无法访问部分系统管理接口。

## 22. 通知与附件

### 22.1 通知

通知存储在 `notifications`。

触发场景包括：

- 线索分配。
- 研发任务分配。
- 研发任务完成。

用户只能查看、标记和删除自己的通知。

### 22.2 附件

附件支持上传、查询、下载和删除。

删除权限：

- 创建人或管理员可删除附件。

## 23. 权限矩阵摘要

| 场景 | admin/老板 | sales_director | leader | member | readonly | guest |
| --- | --- | --- | --- | --- | --- | --- |
| 登录系统 | 是 | 是 | 是 | 是 | 是 | 是 |
| 菜单可见 | 全部 | 按菜单权限 | 按菜单权限 | 按菜单权限 | 按菜单权限 | 按菜单权限 |
| 普通模块读取 | 全量或模块规则 | 管辖小组 | 所属/负责小组 | 自己/同组/共享，视模块而定 | 可读 | 按模块读权限 |
| 普通模块写入 | 是 | 是 | 是 | 是 | 否 | 当前后端多为否 |
| 系统管理 | 是 | 否 | 否 | 否 | 否 | 否 |
| 任务全量可见 | 是 | 否 | 否 | 否 | 否 | 否 |
| 小组任务可见 | 全部 | 管辖小组 | 所属/负责小组 | 所属小组 | 受限 | 受限 |
| 跨组任务可见 | 全部 | 共享或管辖 | 共享或授权 | 共享 | 共享 | 共享 |
| 目标小组可见 | 全部 | 管辖小组 | 所属/负责小组；商务 leader 额外可看产运部门小组目标 | 自己/上级/授权 | 受限 | 按模块读权限 |
| 周报小组可见 | 全部 | 管辖小组 | 所属/负责小组/授权 | 自己/上级/授权 | 受限 | 按模块读权限 |
| 送礼/出差审批 | 是 | 是 | 是 | 否 | 否 | 否 |
| 经营模块 | 是 | 否，除非老板身份 | 否 | 否 | 否 | 否 |

## 24. 当前需要重点保持一致的权限口径

### 24.1 团队任务

统一规则：

- 老板看所有人的任务。
- 小组组长和小组成员看本小组任务。
- 不看其他小组成员任务。
- 跨组任务通过任务共享人授权。

### 24.2 跨团队授权与任务共享的区别

跨团队授权：

- 用于线索、策略、需求、资产、目标、周报等模块。
- 配置对象是“某用户可看某团队某模块数据”。

任务共享：

- 用于普通任务的跨组可见。
- 配置对象是“某任务共享给某用户”。

### 24.3 老板身份

建议统一：

- 所有老板权限判断都同时支持 `role` 和 `executive_role`。
- 避免前端识别为老板、后端不认的情况。

### 24.4 只读和访客

建议统一：

- 明确 `guest` 是否允许写入。
- 若允许，应后端按模块权限放行。
- 若不允许，应前端隐藏写入口。

## 25. 验收建议

后续权限相关改动建议至少覆盖以下测试场景：

- CEO/CTO/COO/CMO 可查看全量任务、经营模块和关键业务数据。
- 普通成员 A 可看到同组成员 B 的团队任务。
- 普通成员 A 看不到其他小组成员 C 的任务。
- 任务共享给 C 后，C 可以在团队任务中看到该任务。
- 取消共享后，C 不再看到该任务。
- 组长可查看所属/负责小组数据。
- 商务总监可查看管辖小组数据。
- 跨团队权限只影响配置模块，不影响普通任务。
- 商务部门 leader 可在目标管理中看到产运部门 `scope_type=team` 的小组目标，并可打开详情。
- 商务部门 leader 对产运部门小组目标的特殊规则只增加查看权限，不增加编辑、删除权限。
- 产运部门小组目标未显式填写 `goals.department` 时，系统应能通过 `teams.department` 识别为产运目标。
- readonly 不可执行写入操作。
- guest 的前后端读写口径保持一致。

## 26. 待确认事项

- 是否要让所有老板账号都使用 `role=ceo/coo/cto/cmo`，还是继续支持 `executive_role` 补充身份。
- `guest` 是否需要真实写权限。
- 普通成员在人脉、预算、线索、策略、需求、资产等模块是否都要统一为“同组可见”，还是保留各模块差异。
- 任务看板是否要允许无小组普通成员访问自己的看板。
- 经营模块是否允许 `admin` 访问，还是严格限制为 CEO/COO/CTO/CMO。
