# 文档中心在线表格文档 PRD

## 1. 背景

文档中心当前承担类 Office Word 的知识文档能力，已经具备标题、编号、文件夹位置、共享人、
收藏、改动历史、页面编辑记录、自动保存和多人更新等文档治理底座。用户希望在同一文档中心内
新增类 Office Excel 的在线表格文档，交互参考石墨表格：顶部有表格菜单和公式栏，中间是可编辑
网格，底部可新增和切换工作表。

本需求不是“在普通文档中插入一个表格块”，而是新增一种独立的文档形态。普通文档继续服务
方案、纪要、PRD、SOP 等写作场景；在线表格服务预算、台账、名单、排期、经营数据和批量数据
维护场景。

2026-07-30 最新决策：在线表格统一使用 Relation 原生表格内核，作为正式入口和后续完整能力建设的
唯一实现路线。删除 Univer 第三方依赖、编辑器、适配器、预览开关及相关运行时代码，不再新增
引擎专属工作簿格式或双编辑器分支。Relation 原生表格负责网格渲染、公式、选区、快捷键、排序筛选、
冻结、格式、复制粘贴、Sheet 管理、保存、历史和协作。

删除第三方代码不得损坏存量数据。若数据库中已经存在 `relation_univer_spreadsheet_workbook_v1`
快照，必须先盘点、备份并无损迁移为原生 `relation_spreadsheet_workbook_v1`；迁移失败时保留原始
内容并明确阻断写入，不得用空白工作簿、计算缓存值或不完整转换结果覆盖。UI 目标仍为像素级复刻
石墨表格体验，但不使用石墨品牌 Logo、商标、水印或受保护素材。

## 2. 产品定位

文档中心统一管理两类文件：

| 文档形态 | 类比 | 数据入口 | 主要用途 |
| --- | --- | --- | --- |
| 普通文档 `rich_text` | Word / wolai 文档 | 块编辑器正文 | 方案、纪要、SOP、知识沉淀 |
| 在线表格 `spreadsheet` | Excel / 石墨表格 | 工作簿网格 | 预算、清单、台账、排期、数据管理 |

现有 `doc_type` 继续表示业务分类，例如 `SOP`、`PRD`、`IMP`、`TMP`。新增 `document_kind`
表示文件形态，避免业务分类和编辑器类型混用。

## 3. 新建交互

### 3.1 PC 端入口

文档中心左侧工具区的 `新建` 按钮调整为下拉按钮：

- 主按钮点击：默认新建普通文档，保持旧用户习惯。
- 下拉菜单：
  - `普通文档`：用于方案、纪要、PRD、知识内容。
  - `在线表格`：用于预算、名单、数据清单、排期和台账。

空状态中的 `新建文档` 也改为同样的下拉入口。

### 3.2 移动端入口

移动端点击 `新建` 后以弹层展示文档形态：

- 普通文档
- 在线表格

移动端第一期可以只支持创建和基础查看，复杂网格编辑能力以 PC 端为主。

### 3.3 新建弹窗

新建弹窗新增 `文件形态` 单选项，默认 `普通文档`：

- 普通文档
- 在线表格

选择在线表格后：

- 标题默认值或占位文案为 `未命名表格`。
- 图标默认使用表格图标。
- 仍需填写或沿用文件夹、归属域、项目组、部门、业务文档类型、安全等级和默认共享人。
- 创建后进入在线表格编辑页。

编辑文档属性时，`文件形态` 只读展示，不允许直接修改。普通文档和在线表格的数据结构不同，
直接切换可能造成内容丢失；后续如有需要，通过“转换为在线表格”或“导出为普通表格”承载。

## 4. 页面布局

在线表格打开后复用文档中心顶部信息区：

- 多文档 Tab
- 标题
- 文档编号
- 当前版本
- 访问人数
- 归属域、项目组、部门、业务文档类型
- 文件夹位置
- 页面菜单
- 分享入口
- 改动历史
- 收藏/取消收藏

桌面非嵌入在线表格将上述能力收敛为单行紧凑治理栏：直接编辑标题，并展示自动保存状态与省略显示的
文件夹路径；页面、分享、改动历史和收藏使用图标入口。文档编号、版本、访问范围、归属域等详细属性
继续由文档列表的属性编辑能力承载，不再以多行标签组重复占据表格首屏。普通文档、嵌入式场景和
移动端不应用该紧凑布局。

标题信息区下方切换为在线表格工作区：

- 菜单栏：编辑、插入、格式、公式、数据、查看、表单、工具、帮助。
- 工具栏：撤销、重做、字体、字号、加粗、斜体、下划线、边框、填充色、对齐、格式刷、百分比、
  千分位、增加/减少小数位、冻结、排序、筛选、求和、公式、链接、图表、图片等。
- 公式栏：名称框、`fx`、当前单元格输入内容。
- 网格区：列头、行号、单元格选区。
- 底部 Sheet 区：Sheet 列表、新增工作表、工作表标签、选区统计、视图、全屏和缩放。

工作区按剩余视口高度连续布局，网格作为唯一可伸缩滚动区；Sheet 区必须始终保留在视口底部，不能
随工作表声明行数扩展页面高度。表格外壳不使用卡片式圆角和聚焦描边。

桌面非嵌入在线表格支持两级纵向空间回收：网格内容向上滚动后，自动收起多文档标签和标题治理栏，
回到网格顶部时自动展开；工具栏最右侧固定提供专注模式箭头，开启后额外收起表格菜单栏，使工具栏
直接位于全局页面标签下方。工具栏、公式栏、网格和 Sheet 栏始终保留，专注箭头不得随工具栏横向
滚动消失。自动收起只响应具有足够滚动范围的纵向网格滚动，短内容、横向滚动、移动端、嵌入式文档
和普通文档不应用该行为。

页面选项中的普通文档专属能力，例如标题目录、页面宽度、小字体，对在线表格不直接生效；
在线表格改由表格菜单承载冻结、缩放、视图和格式设置。

### 4.1 石墨像素级 UI 目标

在线表格工作区以石墨表格截图为像素级参考对象。实现时需在原生编辑器中抽取独立 skin/theme 层，
不得直接套用 Ant Design 的按钮、卡片、输入框视觉。验收维度包括：

- 菜单栏：文案、字号、行高、顶部留白、左右间距和分隔线密度贴近石墨。
- 工具栏：图标尺寸、按钮点击热区、分组分隔线、禁用态、悬浮态和整体高度贴近石墨。
- 公式栏：名称框宽度、高度、边框圆角、`fx` 标识、输入区占位、字号和上下边距贴近石墨。
- 网格区：默认列宽、默认行高、列头高度、行号宽度、字体大小、数字对齐、网格线颜色、冻结分隔线、
  空白区域背景和滚动条位置贴近石墨。
- 选区：活动单元格边框、选区填充、引用公式彩色虚线、填充柄、行列头高亮和远端选区不能出现
  AntD 蓝色主按钮感。
- 公式可视化：选中公式单元格时公式栏展示原始公式而非计算值；同 Sheet 的单格和区域引用使用与
  公式文本一致的彩色虚线框，字符串中的伪坐标和不可见跨 Sheet 引用不得错误高亮。
- Sheet 区固定为 `44px` 浅灰工具带；活动标签为 `38px` 白底灰色描边，非活动标签使用平面文字和
  竖向分隔线。左侧依次为 Sheet 列表与新增，右侧依次为选区统计、视图、全屏、缩小、百分比和放大，
  尺寸、悬浮态、底部分隔线和标签内部横向滚动贴近石墨，不使用蓝色下划线或 AntD 主按钮态。
- 小于 `900px` 时可隐藏视图，小于 `720px` 时继续隐藏选区统计和全屏；Sheet 标签区必须优先保留并
  独立横向滚动，底栏高度仍为 `44px`，不得换行、重叠或造成页面横向溢出。
- 右键菜单、筛选面板、条件筛选面板、颜色选择、字体/字号菜单、插入附件/图片入口均需保持
  表格软件质感，不能出现后台表单弹窗风格。

像素级验收需使用 Playwright 桌面截图，并至少覆盖 1440px、1920px 宽度和一个移动宽度。允许因
Relation 外层标题区与石墨产品壳不同而存在差异，但表格工作区内部必须按石墨截图对齐。

### 4.2 原生内核边界

- `SpreadsheetDocumentEditor.js` 负责原生网格与交互，`spreadsheetWorkbook.js` 负责工作簿、公式、
  排序、结构迁移和格式等纯逻辑，`spreadsheetWorkbookFile.js` 负责 Excel 文件适配；复杂能力继续按
  清晰模块拆分，不能堆入 `Documents.js`。
- `Documents.js` 只提供文档、权限、保存、历史、协作、附件和布局上下文，不直接实现公式解析、
  工作簿转换或单元格级规则。
- 公式必须经过词法分析、语法解析、依赖图和安全求值器处理，禁止 `eval`、`Function` 或拼接后执行
  JavaScript；字符串、Sheet 名、引用和运算符必须在语法层区分，不能依赖正则替换后直接求值。
- 不再接入 Univer 或其他完整表格运行时，不保留双编辑器、预览参数、预览开关或动态加载入口。
  可复用经过评估的轻量通用工具，但工作簿模型、权限、保存、历史和交互状态只能有一套权威实现。
- 删除第三方依赖时必须同步删除对应组件、样式、适配器、测试、开关、动态 import 和 lockfile 依赖；
  普通文档、原生在线表格以及其他一级页面的生产构建和功能不得受到影响。

### 4.3 原生内核发布准入

原生表格增强或第三方代码清理进入正式入口前必须补齐以下条件：

- 使用隔离 MySQL 和登录测试账号完成真实文档端到端回归：新建/打开在线表格、直接输入、公式编辑、
  石墨复制粘贴、自动保存、刷新回读、排序、筛选、冻结、Sheet 切换、Excel 导入导出和只读拒绝写入。
- 生产构建通过 `npm run performance:frontend`，确认删除第三方运行时后首屏及最大异步 chunk 均满足
  既有预算，且构建产物中不再包含被删除表格引擎的独立大 chunk。
- 发布前盘点引擎专属旧快照；迁移须先备份、可重复执行、可校验公式和样式数量，并提供失败清单。
  迁移失败不得覆盖原始内容，成功后统一写回原生版本化工作簿格式。
- 保留代码发布回滚和数据迁移回滚方案；回滚不能要求把已写入的原生工作簿再次转换为第三方格式。
- 发布记录需包含删除依赖清单、包体变化、迁移结果、未覆盖能力、测试账号/环境和失败回退路径。

## 5. 数据模型

### 5.1 文档主表

`documents` 新增：

```text
document_kind TEXT DEFAULT 'rich_text'
```

合法值：

- `rich_text`
- `spreadsheet`

旧数据统一视为 `rich_text`。

### 5.2 工作簿内容

原生工作簿内容存储于文档 `content`，当前正式格式为：

```json
{
  "format": "relation_spreadsheet_workbook_v1",
  "activeSheetId": "sheet_1",
  "sheets": [
    {
      "id": "sheet_1",
      "name": "工作表1",
      "rowCount": 1000,
      "columnCount": 26,
      "cells": {},
      "rowHeights": {},
      "columnWidths": {},
      "mergedCells": [],
      "filters": [],
      "frozen": null
    }
  ],
  "styles": {},
  "definedNames": {}
}
```

`sheets[].id` 是选区、公式迁移和协作合并的稳定标识，在工作簿内必须唯一且不得因重命名变化；
连续新增工作表时必须基于已有 ID 去重，不能仅依赖可能重复的毫秒时间戳。

约束：

- `relation_spreadsheet_workbook_v1` 是当前唯一可新增和写回的正式格式；后续升级必须继续使用
  引擎无关的版本化 Relation 格式，不能新增以某个第三方表格引擎命名的持久化结构。
- 公式原文保存在单元格原始值中，计算结果仅作为派生展示和检索数据；任何保存、历史、协作或导出
  路径都不得用计算结果覆盖公式。
- 若曾通过预览能力写入 `relation_univer_spreadsheet_workbook_v1`，上线清理前必须扫描实际数量并
  逐份迁移。迁移前后 Sheet ID/名称、活动 Sheet、单元格原值、公式、基础样式、合并、冻结、筛选、
  行高列宽、附件引用和 `content_text` 均不得丢失。
- 旧快照迁移失败时必须保留原文和备份并返回明确错误；不得自动创建空白原生工作簿，也不得允许用户
  在不完整转换结果上继续保存。迁移工具是受控的一次性兼容能力，不得重新引入第三方运行时入口。

后续当表格规模、协同粒度或导入导出复杂度提升时，再迁移到独立表：

```text
document_spreadsheets
  id
  document_id
  schema_version
  workbook_json LONGTEXT
  active_sheet_id
  created_at
  updated_at
```

迁移时仍以 `documents.id` 为文档治理入口，文档中心现有分享、收藏、历史和权限不变。

## 6. 保存、历史和协作

- 普通文档继续使用块编辑器自动保存。
- 在线表格使用原生版本化工作簿 JSON 自动保存，不能走普通文档 blocks 覆盖逻辑。
- 单元格、公式栏、Sheet 和格式等本地工作簿回调发生时，客户端必须同步标记文档 dirty 并更新
  活动编辑器快照，不能等 React effect 才记录本地草稿。
- 多人更新使用带 Bearer Token 的 SSE 接收轻量 `document_updated` 和 presence 事件；现有 5 秒
  轮询继续作为断线兜底。无本地脏数据时直接同步远端工作簿，有本地脏数据时仍进入三方合并。
- 存在本地脏数据时，客户端按远端基线、本地工作簿和远端最新工作簿做三方合并：不同单元格
  自动合并，同一单元格冲突保留本地并提示，本地清空同样作为有效修改；Sheet 删除与另一端
  修改冲突时保留有内容的版本；合并后基于远端最新 `updated_at` 继续自动保存。
- 每个浏览器标签生成独立 `session_id`，约 150ms 合并上报选区并每 8 秒续期。服务端 presence
  只存内存，20 秒无心跳自动失效；不写文档正文、页面历史或操作日志。只读共享用户可展示在线
  状态与选区，但仍不能提交任何表格写操作。
- SSE 只发送文档 ID、更新时间、编辑人以及协作者名称、颜色、Sheet ID 和选区，不发送工作簿
  正文、Token、角色或其他用户字段；每 10 秒心跳重新检查订阅者的文档可见权限。断线自动重连，
  即使实时通道不可用也不得影响现有自动保存和 5 秒轮询。
- 同一单元格冲突提示必须显示具体协作者、明确说明保留本地内容，并至少保留 8 秒；合并结果后续
  自动保存成功不得立即清空提示。服务端返回 `409 DOCUMENT_CONFLICT + latest` 时客户端直接使用
  `latest` 完成三方合并，不再依赖下一次轮询才能反馈。提示状态必须绑定当前文档 ID，并在表格
  工作区稳定层展示，不能因 workbook 重建或切换文档而丢失、串页。
- `content_text` 是工作簿的派生检索文本，包含 Sheet 名及按行列排序的有效单元格坐标和内容，
  最多保留 20,000 字。请求提交新 `content` 但省略 `content_text` 时，服务端必须从新工作簿重建，
  不得复用数据库旧值；恢复页面历史时同样按目标工作簿重建，避免已删除数据继续命中搜索、摘要
  或 AI 输入。客户端显式提交的当前公式计算文本仍保留现有兼容能力。
- `content_revisions` 继续存版本快照，`entity_type=document`、`scope_key=content`。
- 页面编辑记录展示为可读描述，不向前端暴露原始 workbook JSON，例如：
  - 修改了 `B2`：空 -> 1000
  - 新增工作表：预算明细
  - 重命名工作表：工作表1 -> 客户台账
  - 调整列宽：C 列
- 页面编辑记录的生成条件必须比较实际工作簿内容，不能只比较 `content_text`。加粗、填充、列宽、
  行高、冻结、合并等不改变检索文本的操作仍需生成可恢复记录；暂时无法生成细粒度描述时，至少
  展示“表格格式或结构已更新”。
- 用户打开“改动历史”时，客户端先等待当前自动保存或手动保存完成，再读取最新文档详情和
  `edit_records`；新记录无需刷新整页即可查看和恢复。
- 在线表格维护工作簿级撤销/重做历史。公式栏和单元格内连续输入以 Enter、Tab 或失焦为一次
  提交；Sheet 管理、排序、筛选、合并、格式和行列调整按离散操作记录。撤销不得落入普通文档
  blocks 历史，也不得只恢复 Sheet 名而遗漏关联公式；仅切换 Sheet、移动选区和缩放视图不
  产生撤销步骤。历史使用序列化快照并同时限制条数和总容量，避免大型导入工作簿因多份完整
  对象快照耗尽浏览器内存。
- 字体、字号、粗体、斜体、下划线、文字色、填充色、边框、对齐、换行和数字格式均须在修改前记录
  `before`，一次确认形成一个工作簿事务。`Ctrl/Cmd+Z`、`Ctrl/Cmd+Shift+Z`、`Ctrl+Y` 与工具栏按钮
  共用同一历史，自动保存和焦点切换不得导致格式操作提示“没有可撤回的操作”。
- 原生编辑器只维护一套工作簿撤销/重做历史；Relation 保存、历史恢复、协作合并和 operation API
  均以当前原生工作簿为准，不得再维护并行的第三方快照或命令历史。
- 系统操作日志、性能日志和通知不得记录完整工作簿 JSON、单元格批量 before/after、附件临时
  路径或文件正文，只允许记录文档 ID、操作类型、数量、耗时和摘要。

## 7. 权限和安全

- 在线表格复用文档中心菜单权限、文档可见性、共享人、收藏和删除权限。
- `readonly`、`guest` 可查看在线表格，但不能编辑单元格、工作表、格式或共享范围。
- Excel 导入要求文档可编辑权限，Excel 导出只要求文档可见权限；未登录返回 `401`，只读用户
  即使通过共享可见，也只能导出，不能通过导入接口修改工作簿。
- 原生编辑器必须在只读态下禁用编辑、粘贴、拖拽填充、格式修改、附件插入、Sheet 操作和任何会
  改变工作簿的命令；前端禁用只负责体验，服务端保存和 operation 接口仍需独立拒绝越权。
- 插入图片、附件、链接文件或其他对象时，必须先走 Relation 附件上传和权限校验。工作簿中只能保存
  受控附件 ID/URL、文件名、尺寸、mime 类型和展示位置，不得保存本地临时路径、
  未授权 OSS key 或完整文件内容。
- 新建在线表格默认安全等级仍为 `SL1`；用户可在文档属性中调整安全等级。
- 当文档安全等级为 `SL3` 或 `SL4` 时，在线表格工作簿内容和历史快照必须走统一 AES-GCM
  加密逻辑，不得出现未加密的表格正文旁路。
- 产品文案不得声称端到端加密或运维不可见。

## 8. 兼容策略

- 历史文档没有 `document_kind` 时视为普通文档。
- 现有 `table-simple` 和 `database-embed` 仍作为普通文档内部块存在，不迁移、不破坏。
- Excel/石墨复制粘贴到普通文档中，仍可作为普通表格块；复制粘贴到在线表格中，按单元格区域导入。
- 从石墨、Excel、WPS 或飞书复制到在线表格时，若浏览器剪贴板暴露公式元数据，必须保留公式原文、
  相对/绝对/混合引用、跨 Sheet 引用和基础样式，并按粘贴目标正确迁移相对坐标；全表
  `Ctrl/Cmd+A` 复制粘贴与普通区域使用同一保真规则，不得只粘贴计算值。
- 若来源只提供计算值，只能粘贴结果值并明确提示“源内容未提供公式，仅粘贴结果值”，不得根据数值
  反推公式。跨 Sheet 公式引用的目标 Sheet 未随剪贴板进入当前工作簿时，仍保留公式原文并显示
  `#REF!`；目标 Sheet 后续存在后自动重算。需要保证多 Sheet 完整迁移时继续支持通过 `.xlsx` 导入。
- 普通文档中的表格块后续可提供“转为在线表格文档”。
- 在线表格支持导出为 `.xlsx`；复制选区同时输出 TSV 和 HTML table，可粘贴到 Excel、石墨或
  普通文档表格场景。

## 9. 分期

### 第零期：原生内核收敛与石墨 UI 基线

- 删除 Univer 依赖、编辑器、样式、适配器、测试、预览开关、查询参数和动态加载入口，正式及隐藏
  路径均只保留原生编辑器；同步清理 package lock 和生产构建产物中的相关大 chunk。
- 盘点数据库中是否存在引擎专属旧快照；存在时先备份并无损迁移到原生格式，迁移失败必须阻断覆盖并
  形成可定位清单。无存量时也要保留扫描结果作为发布证据。
- 建立石墨 UI token：菜单栏高度、工具栏高度、公式栏高度、默认行高、默认列宽、行号宽度、
  列头高度、字号、网格线、选区颜色、Sheet 标签高度和缩放控件尺寸。
- 建立截图回归：空表、有数据表、公式引用、冻结行列、筛选面板、条件筛选面板、右键菜单、
  图片/附件对象、多个 Sheet 和只读态。
- 跑 `npm run performance:frontend`，确认首屏和最大异步 chunk 满足门禁；记录依赖清理前后的 gzip
  变化，并确认生产构建不再包含第三方表格运行时。
- 输出原生工作簿兼容评估：旧格式字段映射、不可迁移能力、失败保护、回滚方案和测试矩阵。

### 第一期：文档形态和骨架

- 新增 `document_kind`。
- 新建入口支持普通文档/在线表格。
- 在线表格打开后展示文档中心头部和表格工作区骨架。
- 初始化 `工作表1`。
- 保护普通文档保存逻辑，避免在线表格被 blocks 覆盖。
- 列表、Tab、标题区域用表格图标或标签区分在线表格。

### 第二期：可编辑网格

- 单元格编辑。
- 行列增删。
- Sheet 新增、重命名、删除。
- Sheet 名称不能为空、最多 31 个字符，不得包含 `\ / ? * [ ] :`，不得以英文单引号开头或
  结尾，并在同一工作簿内不区分大小写唯一。新增 Sheet 自动使用未占用的“工作表N”。
- 重命名 Sheet 时同步迁移所有精确匹配的跨 Sheet 公式引用；支持中文、空格、单引号和区域引用，
  不修改公式字符串字面量或名称前缀相似的其他 Sheet。
- 基础格式、列宽、行高。
- 复制粘贴 TSV/HTML。
- 自动保存工作簿。

### 第三期：Excel 体验增强

- 公式。
- 排序筛选。
- 冻结行列。
- 合并单元格。
- `.xlsx` 导入导出。
- 大表虚拟滚动。

第三期实现口径：

- 公式原文以 `=` 开头保存，支持单元格/区域引用、跨 Sheet 引用、四则运算、幂、字符串连接、
  比较，以及 `SUM`、`AVERAGE`、`MIN`、`MAX`、`COUNT`、`COUNTA`、`IF`、`ROUND`、`ABS`。
  循环引用和常见错误显示 `#CYCLE!`、`#DIV/0!`、`#NAME?`、`#REF!`、`#VALUE!`。
- 插入或删除行列时，结构引用变化同步覆盖整个工作簿中指向该 Sheet 的公式，包含绝对引用和
  跨 Sheet 区域引用；公式字符串、Sheet 名中的数字和前缀相似名称不参与坐标迁移。
- 排序以当前列为关键列；选区只有一个单元格时使用当前已用区域，首行含文本时保留为标题行。
  筛选首期支持“筛选为当前值”和清除筛选，筛选条件保存在 Sheet `filters`。
- 工具栏通过石墨式下拉菜单支持冻结至当前行、当前列或同时冻结，并可取消冻结；菜单动态显示当前
  行列数量，单独冻结行或列只更新对应维度并保留另一维，支持先后操作组合冻结，未冻结时取消项
  置灰。冻结状态保存在 Sheet `frozen`，
  进入自动保存、页面历史和撤销重做；为保持虚拟滚动性能，最多冻结前 100 行、50 列。
- 点击行号选择当前 Sheet 整行，点击列字母选择整列；表头和可见选区使用连续浅蓝背景与蓝色边界，
  调整行高列宽手柄不得误触发选择。整行整列选择本身不产生保存记录，复制、清空、格式和 presence
  继续复用统一范围模型。
- 原生工具栏格式刷单击使用一次，双击连续使用，再次点击或按 `Esc` 退出；只复制已有基础 `style`，
  不复制值、公式、合并、行高或列宽。多格来源按二维图案平铺，每次目标应用进入一个撤销步骤，只读
  用户不可启用。完整条件格式、数字格式和高级样式继续由第五期原生增强路线承载。
- 支持鼠标拖拽选区并合并/取消合并；合并仅保留左上角内容，有其他内容时必须二次确认。
- 支持 `.xlsx`、`.xlsm` 导入和 `.xlsx` 导出。导入保留多 Sheet、普通/共享公式、基础加粗与填充色、
  行高列宽、冻结和合并；`.xlsm` 仅导入工作表数据，不保留 VBA 宏。导出要求先保存当前文档。
- 网格采用行列双向虚拟渲染，只创建视口及少量缓冲区内的单元格 DOM；导入上限为 10 万行、
  2000 列、30 万个非空单元格和解压后 200MB，单个公式最多展开 10 万个单元格，避免单个
  工作簿或公式占满浏览器、服务内存。
- 当前原生实现尚不是完整 Excel 计算引擎，暂不支持数组公式、数据透视表、图表和全部函数集；未支持
  语法必须返回明确错误或降级提示，不能显示旧缓存值。后续在同一原生工作簿、解析器、依赖图和安全
  求值器上分批扩展，不再通过替换编辑器内核实现。单元格锁定、条件格式和数据验证按第 11 节使用
  引擎无关的版本化模型，不得改变 `document_kind` 和文档治理接口。

### 第四期：实时协作增强

- 单元格级 operation API。
- 单元格级冲突合并。
- Sheet 增删、重排和属性 operation。
- 在线光标和选区展示。
- WebSocket 或 SSE 实时同步。

### 第五期：原生内核完整石墨能力

- 在同一原生编辑器和 Relation 版本化工作簿上持续增强，不再引入第二套表格编辑器或引擎专属格式。
- 支持石墨级直接输入、单元格/公式栏编辑、填充柄、拖拽选区、复制粘贴、查找替换、撤销重做、
  右键菜单、快捷键和多 Sheet 管理。
- 支持完整公式能力：常用数学、文本、日期、查找引用、逻辑、统计、财务等函数，错误码、循环引用、
  跨 Sheet 引用和 Excel 导入公式兼容统一由原生公式解析器、依赖图和文件适配层负责。
- 支持排序、普通筛选、条件筛选、多条件组合、清除筛选、筛选状态保存和历史恢复。
- 支持条件格式、字体、字号、颜色、填充、边框、对齐、数字格式、百分比、货币、日期格式、
  格式刷、合并单元格、行高列宽、隐藏行列、冻结行列和视图缩放。
- 支持单元格锁定、受保护范围和数据验证，并在服务端执行权限校验；锁定、条件格式和验证规则均使用
  稳定 ID，可撤销、可保存、可历史恢复，并随行列结构变化迁移。
- 支持插入图片、附件、链接、批注/评论、图表等对象；对象文件由 Relation 附件体系存储和鉴权，
  工作簿只保存受控引用。
- Excel 导入导出尽量保留 Sheet、公式、样式、筛选、冻结、合并、条件格式、图片和附件引用；
  不支持项必须在导入结果中给出明确提示。
- 历史、协作和 operation API 围绕原生工作簿差异工作，避免高频写入整本大快照；超限、未知结构或
  operation 不可用时仍可回退到文档级保存。

第四期 operation 服务端能力：

- `POST /api/documents/:id/spreadsheet/operations` 接受原子混合批次，每项包含唯一操作 ID、
  `before` 前置快照和 `after` 目标快照。支持以下类型：
  - `set_cell`：稳定 `sheet_id`、A1 单元格坐标；`after=null` 表示清空。
  - `set_sheet_property`：稳定 `sheet_id`，属性限于 `name`、`rowCount`、`columnCount`、
    `rowHeights`、`columnWidths`、`mergedCells`、`filters`、`filterRange`、`frozen`。
  - `set_workbook_property`：属性限于 `activeSheetId`、`styles`、`definedNames`。
  - `add_sheet`：`before=null`、`after` 为完整 Sheet 快照，使用稳定 `sheet_id`、目标位置及前后
    相邻 Sheet ID 定位；相邻项因并发变化而无法确定位置时返回冲突。
  - `delete_sheet`：`before` 为完整旧 Sheet 快照、`after=null`；目标 Sheet 已被远端修改时整批拒绝。
  - `reorder_sheets`：`before/after` 为同一组稳定 Sheet ID 的顺序；只重排这些 ID 在当前工作簿中的
    投影，并发新增的其他 Sheet 原位置保留。
- 请求必须携带 `base_updated_at`。即使文档基线已经更新，只要所有目标位置或属性仍等于各自
  `before`，服务端就直接合并并返回 `merged_remote_update=true`；任一目标已变化则整批不落库，
  返回 `409 SPREADSHEET_OPERATION_CONFLICT`、冲突类型、位置或属性、当前快照和最新文档。
- 网络超时重试时，如果当前目标已经等于 `after`，视为该操作已应用并成功返回，不重复更新时间
  或历史；单元格、属性和 Sheet 结构操作均具备基于最终状态的幂等性。
- Sheet 名称操作在服务端当前工作簿上同步迁移跨 Sheet 公式；即使远端在旧基线之后新增了引用公式，
  重命名也不会留下旧 Sheet 名引用。客户端同批提交的已迁移公式按最终状态幂等处理。
- 单次最多 500 个操作，Sheet ID 和操作 ID 最多 128 字符，坐标限制在 10 万行、2000 列内；
  单元格单个 `before/after` 最多 16KB，属性快照最多 256KB。操作正文不得进入普通操作日志，只记录摘要。
- `add_sheet.after` 省略行高列宽、合并、筛选或冻结时由服务端补齐默认值；出现未知 Sheet 字段、
  非规范坐标、坐标超出声明行列或任一单元格超过 16KB 时返回 `400`，工作簿和 `updated_at` 不变。
- 操作成功后必须同步更新工作簿、`content_text`、摘要、页面编辑记录、编辑人和单调递增的
  `updated_at`。现有文档级自动保存和客户端三方合并继续作为兼容路径，前端接入 operation API
  后再逐步缩小整本工作簿写入频率。

第四期 operation 前端接入：

- 自动保存触发时，客户端比较已确认的远端工作簿基线与当前本地工作簿。标题未变、差异仅涉及
  上述支持属性、单元格或 Sheet 结构，且总数不超过 500 项时，生成原子混合批次并调用 operation API。
- 普通输入、公式栏、粘贴、清空、单元格样式、Sheet 重命名、行列数量、列宽行高、冻结、筛选、
  合并、活动 Sheet、工作簿样式或定义名称，以及对应撤销/重做均可进入 operation 路径。
- Sheet 新增、删除和重排及对应撤销/重做进入 operation 路径；右键 Sheet 标签可向左或向右移动。
  新增、删除所需完整 Sheet 快照或顺序快照超过 256KB 时不拆分结构操作。
- 标题、未知字段变化、非法单元格坐标、单项快照超限或超过 500 项的批次继续使用现有
  `PUT /documents/:id/content` 整本保存，确保新旧工作簿兼容。
- operation 成功响应可能包含其他协作者在不同单元格的远端更新。客户端只校验本批 `after` 均已
  应用，然后以响应工作簿和 `updated_at` 替换远端基线与当前页面；不得要求响应工作簿与提交前本地
  快照逐字相同，也不得丢弃响应中的远端单元格。
- `409 SPREADSHEET_OPERATION_CONFLICT` 继续复用现有工作簿三方合并：同格保留本地并显示绑定文档
  的 10 秒提示，不同位置合并后按最新基线重试。operation API 不可用、工作簿无可靠基线或结构
  发生变化时，文档级自动保存仍是完整兼容路径。

第四期实时 presence 与 SSE：

- `GET /api/documents/:id/spreadsheet/events` 建立 SSE；无认证返回 `401`，不可见文档返回 `404`，
  非在线表格返回 `400`。连接后发送当前 presence，文档保存、operation 和历史恢复成功后广播
  `document_updated`，客户端复用既有同步与冲突合并逻辑读取最新工作簿。
- `POST /api/documents/:id/spreadsheet/presence` 更新当前标签的 Sheet 和完整选区；
  `DELETE /api/documents/:id/spreadsheet/presence/:sessionId` 退出。会话 ID、Sheet ID、10 万行、
  2000 列边界均需校验，Sheet 必须存在于当前工作簿。
- 工具栏最多展示 3 个协作者头像和额外人数；当前 Sheet 内的远端选区使用该协作者颜色描边与浅色
  背景，虚拟滚动只渲染当前视口内的远端高亮。移动端不得遮挡菜单、公式栏、Sheet 标签或内容。

## 10. 验收标准

1. 用户点击 `新建` 主按钮仍默认创建普通文档。
2. 用户可通过新建下拉选择 `在线表格`。
3. 新建在线表格后，文档列表、Tab 和详情页能识别其为表格文档。
4. 在线表格复用标题、编号、文件夹位置、分享人、收藏、历史和权限入口。
5. 打开在线表格时不渲染普通文档块编辑器。
6. 修改在线表格标题不会把工作簿内容覆盖成普通文档 blocks。
7. 旧文档不受影响，仍按普通文档打开。
8. 只读用户不能编辑在线表格内容。
9. 后续扩展原生表格能力时，不需要重做文档中心外围能力或新增第二套工作簿状态。
10. 输入公式后，单元格显示计算值，公式栏保留公式原文；循环引用显示明确错误。
11. 用户可以对当前区域排序、按当前值筛选并清除筛选。
12. 用户可以冻结至当前行/列、拖拽多单元格选区并合并或取消合并。
13. 用户可以导入 `.xlsx/.xlsm` 并在文档自动保存后继续编辑，也可以导出当前在线表格为 `.xlsx`。
14. 10 万行、500 列的工作簿打开时只渲染视口附近单元格，不把全部单元格放入 DOM。
15. 新增和重命名 Sheet 时名称规则在前端与服务端一致，重复或非法名称不能保存。
16. 重命名被公式引用的 Sheet 后，公式原文同步迁移且计算结果不变；一次撤销可同时恢复名称和公式。
17. 公式栏、单元格内编辑和离散表格操作均可撤销、重做，循环引用参与运算时仍显示 `#CYCLE!`。
18. 删除单元格后仅提交工作簿 `content`，服务端回读、搜索、摘要和历史恢复均不得残留已删除文本。
19. 单元格 operation 使用旧基线修改不同位置时可服务端合并；修改已被远端更新的同一位置时整批
    返回带坐标的 `409` 且工作簿不发生部分写入；相同请求重试不会重复更新时间。
20. 前端普通单元格、受支持结构属性及 Sheet 增删/重排自动保存使用 operation API；超限批次仍走
    文档级保存；服务端合并其他位置后返回的工作簿在页面和刷新后均完整保留。
21. Sheet 重命名 operation 同步迁移当前服务端工作簿全部跨 Sheet 公式；属性冲突时与同批单元格
    变更一起原子拒绝，不产生部分保存。
22. 两个有权用户打开同一在线表格时，可以看到在线头像和远端选区；远端保存后当前页在 SSE
    通知下立即同步，断开 SSE 后仍可由 5 秒轮询恢复。
23. presence 超时或退出后头像与选区消失；presence 心跳不产生页面历史、业务操作日志或数据库
    持久化数据，只读共享用户可展示 presence 但 operation 仍返回 `403`。
24. Sheet 新增、删除和重排均生成可恢复的 `spreadsheet_operations` 页面编辑记录；不同 Sheet 的
    并发新增可合并，旧基线重排不得覆盖并发新增 Sheet，删除已被远端修改的 Sheet 时整批 `409`。
25. 直接 API 新增精简 Sheet 时缺失的可选字段被规范化补齐；未知字段、非法/越界坐标和超大单格
    返回 `400` 且不落库。并发默认重名合并后，同批改名释放名称的重试可一次原子成功。
26. Univer 依赖、编辑器、适配器、样式、预览开关、动态 import 和 lockfile 依赖已删除；生产构建中
    不再包含对应运行时，普通文档和原生在线表格回归通过。
27. 在线表格工作区截图与石墨参考图在菜单栏、工具栏、公式栏、行列头、默认单元格、选区和 Sheet
    标签维度达到像素级验收阈值；不得出现 AntD 卡片式工具面板。
28. 若存在旧 `relation_univer_spreadsheet_workbook_v1` 快照，可无损迁移到
    `relation_spreadsheet_workbook_v1`；迁移失败时保留原始内容并阻断覆盖，不生成空白工作簿。
29. 只读用户在原生编辑器中无法触发任何工作簿修改命令，直接调用保存或 operation 接口仍返回
    `403`。
30. 插入图片/附件对象后，文件权限、预览、下载和删除均服从 Relation 文档附件接口；历史恢复和
    Excel 导出不会暴露未授权文件地址。

## 11. 2026-07 编辑、公式与右键菜单增强专项

本专项纳入在线表格正式需求，且全部由唯一的原生编辑器实现。先修复阻断性正确性问题并补齐核心右键能力；复杂
条件格式、数据验证和受保护范围必须使用可迁移的版本化模型，不能把规则只写在 React 状态或 DOM
属性中。原生基础版的详细交互、函数矩阵和字段口径以《文档中心在线表格原生基础版 PRD》第 12 节
为准，本节作为文档中心在线表格长期统一约束。

### 11.1 交付顺序

1. 批次 A：单元格输入可靠性、格式撤销/重做、降序空白和数据丢失、石墨公式粘贴、公式交互、复杂
   跨 Sheet 引用、公式引用彩色虚线、数字格式快捷按钮和工具栏异常焦点框。
2. 批次 B：剪切、复制、粘贴、插入复制的单元格、清除、插入、删除、排序、合并、设置单元格格式。
3. 批次 C：锁定单元格、条件格式、数据验证。菜单入口必须与真实数据模型、保存、权限、历史和 API
   校验同批交付，不允许先上线无效果占位项。

### 11.2 撤销、排序和视觉一致性

- 填充色、文字色、粗体、斜体、下划线、字体字号、边框、对齐、换行和数字格式的每次确认均生成
  一个工作簿事务；单格、连续区域、整行、整列均可通过 `Ctrl/Cmd+Z` 撤销并通过
  `Ctrl/Cmd+Shift+Z` 或 `Ctrl+Y` 重做。值、公式和未选中范围不得被误改。
- 升序和降序必须复用同一稳定排序和范围解析逻辑。标题保持原位，空白在两个方向都位于有效数据
  之后，公式按计算值排序但原文和元数据随单元格正确移动；排序失败不得部分落库。
- 选择单列或列头排序时，该列是排序键而不是唯一移动范围；排序必须横向扩展到当前 Sheet 已用数据区
  的全部列，并以整行为最小移动单位，确保日期、名称及其他业务字段不会与排序列串行。
- 连续多格选区使用单一蓝色范围外框，活动格保持原始背景、其他格显示浅蓝选区底色，右下角显示
  填充柄；不得在活动格上再叠一层独立粗框。底部状态栏提供总和、平均、最大、最小、计数和数值计数，
  统计只读取当前计算结果，不写入工作簿或历史。
- 鼠标点击排序、筛选、冻结、合并、颜色和字体等按钮后不得残留紫色焦点框。鼠标使用石墨式轻量
  hover/active，键盘仅通过 `:focus-visible` 展示可访问的蓝色焦点，弹层关闭后焦点回到网格。

### 11.3 输入、公式原文、引用高亮和兼容

- 直接键盘输入、双击单元格编辑和公式栏编辑共用同一份权威草稿与提交逻辑。虚拟滚动、React 重绘、
  自动保存、远端轮询和选区更新不得让编辑输入失焦、变成不可输入或丢失；中文输入法组合输入期间
  不得提前提交。`Enter`/`Tab` 提交，`Esc` 取消，只有真实工作簿变更成功持久化后才能显示保存成功。
  只读、锁定或数据验证拒绝输入时必须显示具体原因，不能表现为“输入无效但保存成功”。
- 在单元格或公式栏输入 `=` 后进入公式编辑态。点击同 Sheet 单元格或拖选区域时，在当前光标处插入
  引用而不是切走编辑态；继续输入 `+ - * / ^ &` 或函数后可再次点选引用。按 `Enter` 提交并显示
  计算值，按 `Esc` 恢复编辑前内容。切换 Sheet 后点选单元格时插入跨 Sheet 引用，名称含空格、中文、
  连字符或其他需转义字符时自动使用英文单引号包裹。
- 选中公式单元格时，单元格显示计算值，`fx` 公式栏显示以 `=` 开头的完整原文。公式编辑态中的引用
  token 按出现顺序使用稳定颜色；当前可见 Sheet 的单格/区域使用同色虚线框，活动公式格继续使用
  实线主选区。切换到被引用 Sheet 时显示该 Sheet 的引用框，不得在当前 Sheet 错框同坐标单元格。
- 公式语法必须覆盖绝对/混合引用、区域引用、中文/空格/连字符/单引号 Sheet 名和多个跨 Sheet 引用。
  单引号包裹的 Sheet 名是一个完整词法单元，其中的 `-` 不能解析为减号；Sheet 名本身含英文单引号时，
  按 Excel 规则使用两个连续单引号转义。双引号字符串中的伪坐标、Sheet 名数字及相似名称不得被识别
  为引用或错误高亮。
- 必须明确支持并正确计算以下公式，三个引用均进入依赖图：

  ```text
  ='飞猪-海韵'!Q18+'小德果园-海韵'!R18+'酷狗音乐-学成'!R18
  ```

  任一被引用单元格变化后结果立即重算；自动保存、刷新、历史恢复、协作合并、排序及 Excel 导入导出
  后公式仍保持公式而非静态结果。任一目标 Sheet 不存在时保留公式原文并显示 `#REF!`，不得回退到旧
  缓存值；目标 Sheet 恢复后重新解析并计算。
- 精确重命名上述任一被引用 Sheet 时，工作簿内全部对应引用同步迁移，其他两个引用、相似名称、字符串
  字面量和坐标不受影响；名称迁移与公式迁移是一个原子事务，一次撤销同时恢复 Sheet 名和完整公式。
- 当前声明函数、运算符、错误码、循环引用、连续依赖、结构迁移、Sheet 重命名、排序、撤销、历史、
  保存回读和 Excel 往返必须形成自动化矩阵；未声明函数明确返回错误或降级，不得显示旧缓存值。
- 从石墨整表 `Ctrl/Cmd+A` 或区域复制时，按结构化自定义 MIME、HTML 公式属性、HTML 表格、纯文本的
  优先级读取来源；只要浏览器暴露公式元数据，就必须保留同 Sheet、跨 Sheet、相对、绝对和混合引用，
  并按目标左上角迁移相对坐标。不能因为同时存在显示值而优先写入显示值。
- 若浏览器剪贴板只有计算值，明确提示“源内容未提供公式，仅粘贴结果值”且不得猜公式；需完整迁移
  多个 Sheet 时以 `.xlsx` 导入作为保真兜底。存量在线表格文档 `480` 作为真实回归样本之一，不在日志
  或测试快照中记录其完整业务数据。

### 11.4 数字格式快捷操作

- 在格式刷右侧依次提供 `百分比`、`千分位分隔符`、`增加小数位数`、`减少小数位数` 四个图标按钮，
  使用 Tooltip 显示完整名称，不使用带文字的胶囊按钮。四项作用于完整选区，只读或锁定范围置灰。
- 百分比仅修改显示格式，原始数值 `2` 显示为 `200%`；千分位格式下原始数值 `2` 显示为 `2.00`，
  `12345.6` 显示为 `12,345.60`。增加小数位将 `2.00` 显示为 `2.000`，减少小数位将 `2.000` 恢复为
  `2.00`；小数位不得低于 0，并需正确处理负数、零、空白和大数。
- 数字格式只改变展示，不修改数值、公式原文或公式依赖；公式计算结果同样可应用。每次按钮点击形成
  一个可撤销/重做的工作簿事务，并进入自动保存、页面编辑记录、历史恢复、协作合并和 Excel 往返。

### 11.5 右键菜单

- 选区内右键保持当前选区，选区外右键先选择目标格。一级和二级菜单均做视口碰撞检测并锚定点击
  位置，横纵滚动、短屏和屏幕边缘场景不能使菜单跑出可视区。
- 菜单按以下顺序提供并根据权限、选区和剪贴板状态禁用：
  - 剪切、复制、粘贴。
  - 插入复制的单元格：右移、下移。
  - 清除：内容、格式、全部。
  - 插入：单元格右移/下移、整行、整列。
  - 删除：删除行、删除列、单元格左移、单元格上移。
  - 排序：升序、降序、自定义排序。
  - 合并单元格或取消合并。
  - 设置单元格格式。
  - 锁定单元格。
  - 条件格式。
  - 数据验证。
- 剪切仅在目标粘贴成功后清空源范围；复制同时提供 Relation 结构、HTML 和 TSV；粘贴优先级为内部
  结构、HTML、TSV。插入、删除、移动和粘贴必须同步迁移公式、合并、样式和规则，并作为单一原子
  事务进入撤销、自动保存和页面历史。
- 设置单元格格式至少包含常规、文本、数值、百分比、货币、会计、日期、时间、分数、科学计数、
  特殊和自定义；数字格式只改变显示，不覆盖原始值或公式。

### 11.6 锁定、条件格式和数据验证

- `protectedRanges` 使用稳定规则 ID 保存范围、创建人、可编辑成员和状态。无权限编辑者可查看和复制，
  但输入、粘贴、格式、排序、合并、行列操作、导入及直接 API 均不可修改；服务端必须与前端执行同一
  权限规则，结构操作与锁定范围相交时整批拒绝。
- `conditionalFormats` 使用稳定规则 ID、范围、条件、样式、优先级和启用状态。首批覆盖数值比较、
  文本包含、日期、空白/非空、重复/唯一及文字色/填充色；条件样式是派生显示层，不覆盖原始值、
  公式和手工样式。
- `dataValidations` 使用稳定规则 ID、范围、类型、条件、空值策略、输入提示和无效数据策略。首批覆盖
  列表、数字、日期时间、文本内容/长度、复选框、评分、进度、身份证、手机号、固定电话、邮箱、
  温度和自定义公式；输入、公式栏、粘贴、批量导入和 API 使用同一“拒绝或警告”规则。
- 三类规则都必须支持创建、编辑、停用或删除、撤销重做、自动保存、页面历史、多人合并、历史恢复、
  行列迁移和 Excel 能力范围内的往返；只读用户不得创建或修改。

### 11.7 专项验收

1. 对 `6780` 所在单元格设置填充、粗体和斜体后，连续撤销逐步恢复，连续重做逐步应用；自动保存后
   未离开页面仍可撤销。
2. 对含标题和空白尾部的整列分别升序、降序，两个方向均不出现空白覆盖有效数据，数据数量、公式、
   样式和历史保持一致；一次撤销恢复排序前顺序。
3. 选中 `=P4/T4` 的公式格时公式栏显示原文，`P4`、`T4` 出现不同颜色的虚线框；修改任一引用格后
   计算值即时更新，刷新后仍正确。
4. 当前 PRD 函数矩阵全部通过单元、组件、Excel 往返及真实文档回归，无 `eval`，循环引用显示
   `#CYCLE!`。
5. 右键菜单全部动作可用或有明确禁用原因，短屏和四个视口边缘都不溢出，鼠标操作无紫色持久焦点框。
6. 锁定、条件格式和数据验证在保存、刷新、历史恢复及两人并发后仍正确；越权 API 不落库，规则变更
   不泄露单元格正文到操作日志。
7. 连续选区只有一个完整外框，活动格不显示第二个粗框，其他选中格浅蓝填充且右下角填充柄可见。
8. 选择数值列升降序后，同一已用数据区的日期、名称及其他列按整行同步移动，标题、空白顺序和撤销
   结果正确。
9. 多格选区底栏可查看总和、平均、最大、最小、计数和数值计数，切换指标不触发保存或页面历史。
10. 直接输入、双击编辑、公式栏编辑和中文输入法连续操作均可稳定提交；虚拟滚动、自动保存和远端更新
    不会造成偶发无法输入，拒绝输入时有明确原因，不出现无变更却提示保存成功。
11. 在 `G7` 输入 `=`、点击 `F6`、输入 `+`、点击 `F7` 并按 Enter 后，网格显示正确结果，公式栏显示
    `=F6+F7`；编辑过程中两个引用使用不同颜色 token 和虚线框，Esc 可完整取消本轮编辑。
12. 建立 `飞猪-海韵`、`小德果园-海韵`、`酷狗音乐-学成` 三个 Sheet 并填充对应引用值后，公式
    `='飞猪-海韵'!Q18+'小德果园-海韵'!R18+'酷狗音乐-学成'!R18` 计算正确；逐个修改三个上游值均
    立即重算，保存刷新、历史恢复、协作合并和 Excel 往返后仍保留公式。逐个重命名被引用 Sheet 时
    公式精确迁移且结果不变，一次撤销同时恢复名称和公式；删除引用 Sheet 后显示 `#REF!` 且不丢原文。
13. 从石墨整表复制一个同时包含同 Sheet 公式、绝对/混合引用和上述多 Sheet 公式的区域，来源提供公式
    元数据时粘贴后全部保真；来源只提供显示值时仅粘贴值并出现明确降级提示，不静默伪装为公式保真。
14. 对原始数值 `2` 分别应用百分比和千分位后显示 `200%`、`2.00`；增加/减少小数位可在 `2.00`、
    `2.000` 间切换。四项均不改变原始数值或公式，可撤销、保存、刷新、恢复历史和 Excel 往返。
