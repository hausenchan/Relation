# 文档中心模块 PRD

## 1. 背景

Relation 目前已经有目标、策略、需求、产品资产、主体管理、公司研究等业务模块，但长期资料沉淀仍然分散在各模块字段、附件、备注或外部文档中。运营侧需要一个统一的内部文档中心，用来沉淀产品资料、主体资料、平台规则、SOP、会议纪要、项目复盘和策略说明。

文档中心参考 wolai 类产品的文档编辑体验，但第一版不做完整知识协作平台，只做能稳定落地的 MVP：可创建、可编辑、可保存、可检索、可按共享对象控制可见范围的内部文档管理模块。

## 2. 模块定位

文档中心用于沉淀运营和项目资料，是资产管理下的知识资产入口。

第一版目标：

- 支持轻量块编辑体验，满足日常 SOP、方案、复盘、会议纪要等写作需求。
- 支持文档分类、搜索、收藏和最近编辑，方便资料找回。
- 支持按项目组、部门、小组、个人共享文档，保证内部资料只对相关人员可见。
- 为后续 AI 总结、评论协作、版本历史、文档关联等能力预留数据结构。

## 3. 菜单与路由

菜单位置：

- 一级菜单：资产管理
- 子菜单：文档中心

建议菜单结构：

- 资产管理
  - 主体管理
  - 产品资产
  - 文档中心
  - 预算管理

建议路由：

- `/documents`

建议模块 key：

- `documents`

菜单权限：

- `文档中心` 需要纳入菜单权限管理。
- 未配置菜单权限的非管理员用户不显示入口。
- 页面内数据可见性由文档共享规则进一步控制。

## 4. MVP 范围

第一版包含：

- 文档列表
- 新建文档
- 编辑文档
- 删除文档
- 文档标题编辑
- 块式富文本正文编辑
- 块选中、块高亮颜色、块格式转换
- 标题目录开关
- 页面宽度自定义
- 自动保存和手动保存
- 文件夹或分类管理
- 归属域、项目组、部门目录树
- 各部门 SOP 文件夹模板和 SOP 总库视图
- 全量文档统一编号管理
- 新建文档自动生成编号
- 文档信息展示：编号、创建人、最后编辑人、当前版本
- 改动历史维护
- 右上角页面编辑记录入口：点击后以抽屉展示文档编辑记录
- 标签管理
- 文档搜索
- 收藏文档
- 最近编辑
- 多文档 Tab：支持同时打开多个文档、自由切换、关闭前自动保存
- 图片上传
- 附件上传
- 表格、待办、引用、代码块等基础富文本能力
- 右上角共享入口：项目组、部门、小组、个人
- 访问权限人数展示

第一版暂不做：

- 实时多人协同编辑
- 光标同步
- 块级拖拽排序
- 双向链接
- 块引用或同步块
- 公开链接分享
- 访问申请审批
- 文档评论线程
- 完整版本对比
- 数据库、看板、日历视图
- AI 总结和 AI 写作

## 5. 核心使用场景

### 5.1 运营 SOP

运营人员创建一篇 SOP 文档，记录平台规则、操作步骤、注意事项和附件截图，并共享给对应部门或小组。

### 5.2 产品资料沉淀

产品资产或主体管理相关资料可以整理为文档，包含产品说明、上线流程、主体证照说明、投放口径和历史问题复盘。

### 5.3 项目资料协作

项目负责人创建项目方案或复盘文档，共享给指定项目组。项目组成员可以查看，非项目组成员不可见。

### 5.4 个人临时草稿

用户新建文档但不设置共享对象时，文档仅创建人、管理员和高管角色可见。

### 5.5 部门 SOP 沉淀

产运部门会产生较多 SOP，但 SOP 不应只服务产运。国内项目、海外项目、经营管理等归属域下的每个部门目录，都需要有固定的 SOP 文件夹，便于产研、商务、产运、投放等部门持续沉淀流程规范。

建议每个部门默认目录模板：

- `01_SOP流程规范`
- `02_规则制度`
- `03_模板表单`
- `04_项目资料`
- `05_复盘案例`
- `临时文档`

文档中心提供 `SOP总库` 虚拟视图，按 `归属域 / 项目组 / 部门` 聚合展示所有 `文档类型 = SOP` 的文档。`SOP总库` 不是实际文件夹，文档仍保存在原目录中。

### 5.6 文档编号治理

所有文档都需要统一编号。新建文档时，系统根据归属域、项目组、部门、文档类型和年份自动生成编号，避免依赖人工命名维护规范。

文档编号需要在文档标题下方或文档信息面板中展示，并与创建人、最后编辑人、当前版本、改动历史一起作为文档治理信息沉淀。

## 6. 页面结构

第一版建议采用“左侧文档列表 + 顶部多文档 Tab + 中间最大化编辑区 + 右上角菜单”的结构。共享、页面选项和更多操作都从右上角进入，避免长期占用编辑区域。

### 6.1 左侧文档区

左侧用于快速定位文档：

- 归属域切换：全部、国内项目、海外项目、经营管理、通用文档、跨区域
- 搜索框
- 新建文档按钮
- SOP 总库入口
- 全部文档
- 最近编辑
- 我的收藏
- 我创建的
- 项目组 / 部门 / 文件夹目录树

列表项展示：

- 文档标题
- 文档编号
- 更新时间
- 创建人或最后编辑人
- 标签
- 收藏状态

目录树建议按 `归属域 -> 项目组 -> 部门 -> 文件夹 -> 文档` 展开。用户没有权限的文档不出现在目录树、列表和搜索结果中。

### 6.2 中间编辑区

中间为核心编辑区：

- 顶部多文档 Tab 区
- 文档标题输入框
- 文档治理信息摘要：编号、版本、创建人、最后编辑人
- 保存状态：保存中、已保存、保存失败
- 块式富文本工具栏
- 正文块编辑区域
- 标题目录入口，可从右上角页面选项中打开或关闭目录抽屉
- 页面宽度，可选择自适应、标准、宽屏等宽度
- 小字体开关

编辑区默认保持满屏书写区域。标题目录不做常驻侧栏，而是通过右上角页面选项打开抽屉；抽屉展开时临时覆盖或贴边展示，收起后回到满屏编辑状态。页面宽度调整只影响当前文档展示，不影响其他文档。

标题即文档名，标题修改后自动保存。

#### 6.2.1 多文档 Tab 区

文档编辑区顶部需要支持多文档 Tab，放置在文档治理信息和右上角工具区上方，用于承载当前用户已打开的多个文档，交互参考浏览器标签页。

展示规则：

- 每个已打开文档对应一个 Tab。
- Tab 展示文档标题，标题过长时单行截断，不撑开整体布局。
- 当前正在编辑或查看的文档 Tab 需要有明确激活态。
- 每个 Tab 右侧展示关闭按钮 `x`。
- Tab 区横向排列，空间不足时允许横向滚动或折叠为可滚动标签栏。
- 右上角工具区、文档标题、治理信息、正文内容均跟随当前激活 Tab 切换。

打开和切换规则：

- 用户从左侧目录、最近编辑、搜索结果或收藏列表点击文档时，如果该文档未打开，则新增一个 Tab 并切换为激活状态。
- 如果该文档已经打开，则不重复创建 Tab，直接切换到已有 Tab。
- 用户点击任意 Tab，可以切换到对应文档。
- 切换 Tab 时保留各文档的本地编辑状态、保存状态和页面设置，例如目录开关、页面宽度、小字体等。
- 切换 Tab 不强制立即保存当前文档，仍沿用自动保存防抖机制；如果当前文档已有未保存内容，需要继续显示对应保存状态。

关闭规则：

- 用户点击某个 Tab 的 `x` 时，系统需要先自动保存该 Tab 对应文档的标题、正文和页面设置。
- 自动保存成功后，再关闭对应 Tab。
- 如果自动保存失败，不关闭该 Tab，并提示用户保存失败，可重试或手动保存，避免内容丢失。
- 如果关闭的是当前激活 Tab，关闭成功后优先切换到右侧相邻 Tab；右侧没有时切换到左侧相邻 Tab。
- 如果关闭后没有任何打开的 Tab，编辑区展示空状态，引导用户从左侧列表选择文档或新建文档。
- 关闭非激活 Tab 时，当前激活文档不受影响。

异常和权限规则：

- 如果用户对某个 Tab 文档的访问权限已失效，切换到该 Tab 时需要提示无权限并关闭或禁用该 Tab。
- 如果文档已被删除，切换或保存时需要提示文档不存在，并从 Tab 区移除该文档。
- 仅有查看权限的文档允许打开和切换 Tab，但不展示可编辑保存状态；关闭时不触发内容保存。

### 6.3 右上角操作区

右上角固定展示文档级操作，参考截图中的紧凑工具区：

- 访问人数，例如“共 4 人”
- 收藏
- 页面选项
- 共享
- 页面编辑记录
- 文档信息
- 历史或更多操作
- 更多菜单

“共享”入口从右上角打开弹层或抽屉，不在正文区域常驻展示。

页面编辑记录入口建议使用 `历史/时钟` 类图标，也可以使用信息类图标，但语义应明确表达“页面修改记录”。鼠标悬浮时展示文案：`页面编辑记录`。点击后从右侧打开抽屉，用于查看当前文档的改动轨迹。

### 6.4 页面选项菜单

页面选项用于控制当前文档的阅读和编辑体验：

- 标题目录：打开或关闭
- 自适应宽度：打开后正文跟随容器宽度，关闭后使用固定阅读宽度
- 小字体：打开后正文使用更紧凑字号
- 标题自动编号：可选，MVP 可先作为开关预留
- 自定义页面宽度：支持标准、宽屏、全宽或输入宽度
- 编辑保护：MVP 可先预留，不作为第一版必做
- 删除页面：仅创建人、管理员和高管可见

### 6.5 共享菜单

- 创建人
- 最后编辑人
- 创建时间
- 更新时间
- 共享项目组
- 共享部门
- 共享小组
- 共享个人
- 当前可访问人数
- 我的权限

共享菜单需要显示当前文档有多少人具备访问权限。人数按共享项目组、部门、小组、个人展开后去重统计，并包含创建人、管理员和高管默认可见人员。

### 6.6 文档信息与改动历史

文档信息从右上角入口打开，不常驻占用编辑区。

文档信息展示：

- 文档编号
- 文档类型
- 归属域
- 项目组
- 部门
- 所属文件夹
- 当前版本
- 创建人
- 创建时间
- 最后编辑人
- 最后更新时间
- 改动历史

改动历史支持由更新文档的人随时新增或编辑记录。系统自动带出更新人和更新时间，用户需要填写本次改动摘要。

### 6.7 页面编辑记录抽屉

页面编辑记录是文档阅读和协作时的高频辅助信息，目标是让阅读者快速理解“谁在什么时候改了什么”，不需要翻正文或询问编辑人。

入口：

- 位置：文档编辑页右上角工具区。
- 图标：推荐使用历史/时钟图标，样式与收藏、页面选项、共享等工具保持一致。
- Tooltip：`页面编辑记录`。
- 点击行为：从右侧打开抽屉。

抽屉结构：

- 标题：`页面编辑记录`
- 内容分为左右两个 Tab：`版本记录`、`页面编辑记录`。
- 顶部操作：
  - `接收页面变更通知` 开关，表示当前用户是否订阅该文档更新提醒。
  - `版本记录` Tab 内展示 `新增记录` 按钮，仅有编辑权限的用户可见。
  - `版本记录` Tab 内展示 `展开全部 / 收起全部`，用于快速查看或折叠全部版本记录。
- 主体：两个 Tab 均按更新时间倒序展示记录，最新记录在最上方。
- 抽屉宽度：桌面端建议 420-520px；小屏时可占满宽度。

记录列表采用“时间线 + 卡片”形式，类似目录一样可以逐条展开或收起：

- 收起态展示：
  - 更新人头像
  - 更新人姓名
  - 动作文案，例如 `编辑了页面`
  - 文档标题
  - 更新时间，例如 `2026-05-24 晚上 20:15`
  - 版本号，例如 `V1.1`
  - 展开/收起图标
- 展开态展示：
  - 本次改动摘要
  - 影响范围或模块
  - 详细改动内容，支持段落、编号列表、普通列表和高亮文本
  - 可选备注
  - 有权限时展示 `编辑记录`、`删除记录`

展示样式建议：

- 更新时间、版本号使用弱化灰色。
- 更新人和动作文案加粗，方便扫读。
- 改动内容中的关键新增点可以使用浅绿色背景或绿色文字突出。
- 卡片之间保持清晰间距，左侧头像和竖向时间线帮助用户理解修改顺序。
- 记录内容较长时默认只展示前 3-5 行，点击展开后展示完整内容。

交互规则：

- 点击右上角图标打开抽屉，再次点击图标或点击遮罩关闭抽屉。
- 点击某条记录的卡片标题区域展开/收起该记录。
- 点击 `新增记录` 打开记录编辑表单。
- 点击 `编辑记录` 可以修改版本记录的改动摘要、详细内容、影响范围、版本号和备注。
- `历史记录` 在界面展示时统一改名为 `版本记录`。
- 保存文档正文不自动强制生成记录，但保存成功后可提示编辑人补充本次改动说明。
- 如果当前文档没有编辑记录，抽屉展示空状态文案：`暂无编辑记录，保存重要修改后可以补充改动说明。`
- 页面编辑记录每条记录右侧需要展示 `恢复到此版本` 图标按钮。
- 鼠标悬浮恢复按钮时展示 Tooltip：`恢复到此版本`。
- 点击恢复按钮后先弹出二次确认，确认后将当前文档标题、正文恢复为该页面编辑记录保存后的版本快照。
- 恢复成功后，该历史版本成为当前文档内容，并自动新增一条页面编辑记录，记录本次恢复动作。
- 如果该页面编辑记录缺少可恢复快照，系统需要提示无法恢复，不允许静默覆盖当前内容。

权限规则：

- 有文档编辑权限的用户可以新增编辑记录。
- 记录创建人、文档创建人、管理员和高管角色可以编辑或删除该条记录。
- 只有有文档编辑权限的用户可以执行 `恢复到此版本`。
- 仅有查看权限的用户只能查看记录，不能新增、编辑或删除。
- 管理员和高管角色可以查看全部记录。

## 7. 块式富文本编辑体验

第一版重点保证写作顺手，并具备类似 wolai 的轻量块操作能力。MVP 需要有“块”的概念，但不要求第一版支持块拖拽、实时协同和复杂块引用。

### 7.1 块基础能力

正文由多个块组成，每个块独立保存类型、内容、顺序和样式属性。

每个块需要支持：

- 独立选中
- 块操作菜单
- 块类型转换
- 块颜色高亮
- 块折叠，适用于折叠标题、折叠列表、折叠待办
- 块级删除
- 块级复制链接，MVP 可先复制文档链接加块锚点

块选中状态需要有明确视觉反馈。鼠标进入或选中块时，可显示块操作按钮或左侧块手柄，但第一版不要求支持拖拽排序。

### 7.2 块颜色和高亮

参考截图中的分块高亮体验，MVP 需要支持给选中块设置背景色。

建议第一版颜色：

- 默认
- 浅黄
- 浅红
- 浅绿
- 浅蓝
- 浅紫
- 灰色

颜色应用规则：

- 可以对单个块设置颜色。
- 可以对连续选中的多个块批量设置颜色。
- 嵌套块或子块继承父级高亮背景时，需要保持层级边界清晰。
- 刷新页面后颜色保留。

### 7.3 块格式转换

选中某个块后，可以通过块菜单“转换为”切换格式。

MVP 支持转换为：

- 文本
- 一级标题
- 二级标题
- 三级标题
- 四级标题
- 无序列表
- 有序列表
- 待办列表
- 折叠列表
- 折叠标题
- 引用文字
- 着重文字或高亮块
- 代码片段
- 表格
- 页面，表示创建子文档或转为文档链接，MVP 可先作为后续能力预留入口

转换规则：

- 转换后保留原文字内容。
- 列表、待办、标题之间转换时尽量保留层级。
- 无法无损转换的复杂块，需要二次确认或降级为普通文本。
- 转换菜单中可以展示快捷键，但第一版不强制实现全部快捷键。

### 7.4 基础文字格式

块内文字需要支持：

- 加粗
- 斜体
- 下划线
- 删除线
- 链接
- 行内代码

选中文字后展示悬浮工具条，允许快速设置常用样式。

### 7.5 列表与待办

需要支持：

- 无序列表
- 有序列表
- 待办清单
- 折叠列表

待办清单需要可勾选，刷新后状态保留。折叠列表需要支持展开、收起，并保留展开状态。

列表块需要支持多级层级编辑，适用于“列表”“数字列表”“折叠列表”三类块：

- 在列表项中按 `Enter` 新增下一行，新行默认继承当前块类型和当前层级。
- 在列表项中按 `Tab` 当前行右缩进一级，成为上一行的子级；按 `Shift + Tab` 当前行退回上一级。
- 缩进不是在文本中插入空格，而是写入块属性，例如 `meta.indent`，刷新后仍保持层级。
- 同一级的标识、正文起点和左侧层级线需要上下对齐；每一级左侧有浅色竖向引导线，便于识别父子层级。
- 无序列表按层级切换标识，例如第一级实心圆点、第二级空心圆点、第三级菱形，后续层级可循环使用。
- 数字列表按层级切换编号格式，例如第一级 `1.`、第二级 `a.`、第三级 `i.`、第四级回到 `1.`，同级自动递增，缩进或退级后自动重算。
- 折叠列表使用三角标识；展开时显示子级块，收起时隐藏直到下一个同级或上级块，折叠状态写入块属性并持久化。
- `Backspace` 在空列表项上优先退一级；已在最外层且为空时再执行删除当前块。
- 列表、数字列表、折叠列表之间转换时尽量保留层级属性。

### 7.6 图片和附件

图片：

- 支持上传图片
- 支持粘贴图片
- 图片在文档正文中展示

附件：

- 支持上传文件
- 展示文件名、大小、上传人、上传时间
- 支持下载

### 7.7 表格

第一版支持简单表格：

- 插入表格
- 编辑单元格文字
- 增加行
- 删除行
- 增加列
- 删除列

不要求第一版支持复杂公式、筛选、排序和多视图。

### 7.8 标题目录

文档需要支持标题目录：

- 根据一级、二级、三级、四级标题自动生成目录。
- 点击目录项可以跳转到对应标题块。
- 目录从右上角页面选项中打开或关闭。
- 目录以抽屉形式展开和收起，不作为常驻侧栏。
- 编辑区默认就是满屏区域，目录关闭后只是隐藏抽屉，不需要重新扩大正文布局。
- 目录抽屉展开时不能和右上角菜单、正文操作浮层产生重叠冲突。

### 7.9 快捷编辑

建议支持：

- 输入 `/` 弹出快捷插入菜单
- 支持 Markdown 快捷输入：
  - `#` 转一级标题
  - `##` 转二级标题
  - `###` 转三级标题
  - `-` 转无序列表
  - `1.` 转有序列表
  - `[]` 或 `[ ]` 转待办
  - `>` 转引用块

## 8. 保存体验

文档编辑需要以自动保存为主，手动保存为辅。

保存规则：

- 正文或标题变更后，1-2 秒防抖自动保存。
- 自动保存中显示“保存中”。
- 保存成功显示“已保存 HH:mm”。
- 保存失败显示“保存失败”，并提供重试入口。
- 页面离开前如果存在未保存内容，需要提示用户确认。
- 关闭文档 Tab 前需要自动保存对应文档；保存成功后关闭，保存失败则保留 Tab 并提示失败原因。

手动保存：

- 顶部保留“保存”按钮。
- 用户点击后立即提交当前激活 Tab 对应文档的标题、正文和页面设置。

## 9. 文档管理

### 9.1 新建文档

用户点击“新建文档”后：

- 默认创建空白文档。
- 标题默认为“未命名文档”。
- 不设置共享对象时，仅创建人、管理员和高管角色可见。

### 9.2 编辑文档

可编辑内容：

- 标题
- 块式正文
- 标签
- 所属文件夹
- 页面选项：目录开关、页面宽度、小字体、标题自动编号
- 共享对象

MVP 中只有创建人、管理员和高管角色可以编辑文档。

### 9.3 删除文档

删除规则：

- 创建人、管理员和高管角色可以删除。
- 第一版建议软删除，避免误删后无法恢复。
- 删除前需要二次确认。

### 9.4 搜索

搜索范围：

- 标题
- 正文纯文本
- 文档编号
- 文档类型
- 标签

搜索结果必须遵守文档可见性规则，不能返回当前用户不可见的文档。

### 9.5 归属域与目录模板

文档需要有归属域字段，用于区分文档业务归属和列表筛选。

归属域枚举：

| 展示名 | 枚举值 | 说明 |
| --- | --- | --- |
| 国内项目 | `domestic_project` | 国内业务、国内项目组资料 |
| 海外项目 | `overseas_project` | 海外业务、海外项目组资料 |
| 经营管理 | `executive_management` | 公司经营、CXO 决策、管理会议、战略复盘资料 |
| 通用文档 | `general` | 制度、流程、模板等跨团队通用资料 |
| 跨区域项目 | `cross_region` | 同时涉及国内和海外协作的项目资料 |

归属域用于目录分组、筛选和共享对象推荐，不直接作为唯一权限判断。文档可见性仍由共享给项目组、部门、小组、个人的规则决定。

目录树建议结构：

```text
归属域
  项目组
    部门
      01_SOP流程规范
      02_规则制度
      03_模板表单
      04_项目资料
      05_复盘案例
      临时文档
```

部门目录建议包括：

| 展示名 | 部门编码 |
| --- | --- |
| 0_项目 | `PM` |
| 1_产研 | `PD` |
| 2_商务 | `BD` |
| 3_产运 | `OPS` |
| 4_投放 | `ADS` |
| 经营管理 | `MGT` |
| 通用 | `ALL` |

### 9.6 文档类型

文档类型用于编号、筛选和 SOP 总库聚合。

建议文档类型：

| 展示名 | 类型编码 | 说明 |
| --- | --- | --- |
| SOP | `SOP` | 流程规范、操作步骤 |
| 规则制度 | `RULE` | 平台规则、制度说明 |
| 模板表单 | `TPL` | 表单、模板、固定格式资料 |
| 技术/需求说明 | `SPEC` | 技术文档、需求说明 |
| 计划 | `PLAN` | 排期、发版、行动计划 |
| 报告 | `RPT` | 月报、发布报告、数据报告 |
| 会议纪要 | `MEET` | 会议记录和决议 |
| 复盘 | `REVIEW` | 项目复盘、问题复盘 |
| 临时文档 | `TMP` | 尚未归档的临时资料 |

从 `01_SOP流程规范` 新建文档时，默认文档类型为 `SOP`。

### 9.7 文档编号

文档编号采用统一规范：

```text
D{全局累计序号}-{项目编码}-{部门编码}-{文档类型}-{年份}
```

示例：

```text
D000001-CNAD-OPS-SOP-2026
D000002-CNAD-PD-SOP-2026
D000003-OSAD-BD-SOP-2026
D000004-MGT-CXO-MEET-2026
```

字段含义：

| 字段 | 示例 | 含义 |
| --- | --- | --- |
| `D` | 固定 | Document 的缩写，表示文档中心文档 |
| 全局累计序号 | `000001` | 系统内第几个生成的文档，全局递增，不按归属域或年份重置 |
| 项目编码 | `CNAD` / `OSAD` / `MGT` / `GEN` / `CR` | 项目组编码或特殊归属编码；项目编码本身承载国内、海外、经营管理等归属信息 |
| 部门编码 | `PD` / `BD` / `OPS` / `ADS` | 产研、商务、产运、投放等 |
| 文档类型 | `SOP` / `RULE` / `TPL` | 文档类型 |
| 年份 | `2026` | 创建年份 |

说明：

- 归属域仍保存在文档字段中，用于筛选、目录和权限推荐。
- 编号中不再单独放归属域编码，避免 `CN-CNAD`、`OS-OSAD` 这类重复表达。
- 国内/海外等含义通过项目编码体现，例如 `CNAD` 表示国内广告，`OSAD` 表示海外广告。

全局累计序号位数：

- 第一版建议使用 6 位数字，格式为 `D000001`。
- 6 位可覆盖 999,999 篇文档，内部知识库长期使用也足够直观。
- 当累计编号超过 999,999 时，不截断、不重置，自动扩展为 `D1000000`。
- 用户可以通过编号第一段直接判断该文档是系统第几个生成的文档。

编号生成规则：

- 新建文档时自动生成编号。
- 从目录新建时，系统根据当前目录自动带出归属域、项目组、部门和文档类型。
- 全局累计序号在所有文档范围内统一递增，不按 `归属域 / 项目组 / 部门 / 文档类型 / 年份` 分段递增。
- 编号生成后默认固定不变，移动目录不自动改编号，避免外部引用失效。
- 如确需刷新编号中的业务字段，仅管理员或高管角色可操作，并需要记录变更原因；全局累计序号不回收、不复用。

### 9.8 文档信息展示

文档标题下方展示轻量治理信息：

```text
编号：D000001-CNAD-OPS-SOP-2026 | V1.0 | 创建人：张三 | 最后更新：李四 2026-05-26
```

右上角文档信息面板展示完整治理信息：

- 文档编号
- 当前版本
- 文档类型
- 归属域
- 项目组
- 部门
- 文件夹
- 创建人
- 创建时间
- 最后编辑人
- 最后更新时间
- 改动历史

### 9.9 改动历史

改动历史用于记录人为确认的重要变更，不替代自动保存。

改动历史既可以在文档信息面板中作为治理信息展示，也需要在右上角“页面编辑记录”抽屉中以时间线卡片形式展示。抽屉承担阅读场景下的快速理解作用，文档信息面板承担完整元信息查看作用。

每条改动历史字段：

| 字段 | 说明 |
| --- | --- |
| 版本号 | 如 `V1.0`、`V1.1`、`V2.0` |
| 更新时间 | 默认当前时间，可手动调整 |
| 更新人 | 默认当前用户 |
| 改动摘要 | 本次改了什么，必填 |
| 详细改动内容 | 可选，支持多行文本、列表和高亮片段，用于抽屉展开态展示 |
| 影响范围 | 可选，如“投放流程”“开户 SOP” |
| 备注 | 可选 |

权限规则：

- 创建人、管理员、高管可以新增、编辑、删除改动历史。
- 记录创建人可以编辑自己创建的改动历史。
- 被共享查看用户默认只能查看改动历史。
- 文档内容保存不会自动生成改动历史，更新人需要在需要留痕时手动添加。
- 保存文档后，如果本次距离上次改动记录时间较近，可提示“是否补充本次改动说明”，但不强制阻断保存。
- SOP 模板可默认插入“变更记录”块，用于在正文中展示关键历史；结构化改动历史仍以文档信息面板为准。

## 10. 共享权限

文档中心沿用“共享人”思路，但扩展为共享对象。每篇文档可以同时共享给项目组、部门、小组和个人。

### 10.1 支持的共享对象

| 共享对象 | 规则 |
| --- | --- |
| 项目组 | 属于该项目组的所有用户可见 |
| 部门 | 所属部门匹配的所有用户可见 |
| 小组 | 属于该小组的所有用户可见 |
| 个人 | 指定用户本人可见 |

用户命中任意一个共享对象，即可查看该文档。

### 10.2 默认可见范围

| 身份 | 权限 |
| --- | --- |
| 创建人 | 可查看、可编辑、可删除、可调整共享对象 |
| admin | 默认可查看全部，可编辑、可删除、可调整共享对象 |
| CEO/COO/CTO/CMO 等高管角色 | 默认可查看全部，可编辑、可删除、可调整共享对象 |
| 被共享对象命中的用户 | 可查看 |
| 未命中共享对象的用户 | 不可查看，列表、搜索、详情均不可见 |

### 10.3 共享设置交互

共享设置从文档右上角“共享”入口进入，打开弹层或抽屉，不在编辑区常驻展示。

右上角需要展示当前可访问人数，例如：

- `共 4 人`
- `仅自己`
- `共 18 人`

共享设置区域包含：

- 共享项目组：多选
- 共享部门：多选
- 共享小组：多选
- 共享个人：多选

提示文案：

> 未添加共享对象时，仅创建人、管理员和高管角色可见。

可访问人数统计规则：

- 包含创建人。
- 包含 admin 和高管角色默认可见人员。
- 展开项目组、部门、小组、个人后按用户 ID 去重。
- 已停用用户不计入人数。
- 人数变化需要在保存共享对象后刷新展示。

### 10.4 MVP 编辑权限

第一版共享仅表示“可查看”，不表示“可编辑”。

被共享用户：

- 可以打开文档
- 可以搜索到文档
- 可以查看附件
- 不可以修改正文
- 不可以删除文档
- 不可以调整共享对象

后续如需多人协作，再新增“可编辑协作者”或共享权限级别。

## 11. 数据模型建议

### 11.1 documents

文档主表。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | INTEGER | 主键 |
| document_no | TEXT | 文档编号，唯一 |
| global_seq | INTEGER | 全局累计序号，唯一，用于生成 `D000001` |
| title | TEXT | 标题 |
| content | TEXT | 块编辑器 JSON，包含块顺序、类型、内容和属性 |
| content_text | TEXT | 正文纯文本，用于搜索 |
| summary | TEXT | 摘要，自动截取正文前 100 字 |
| domain | TEXT | 归属域 |
| project_group_id | INTEGER | 关联项目组 |
| project_code | TEXT | 项目编码，用于编号 |
| department_key | TEXT | 部门编码，如 `PD` / `BD` / `OPS` |
| doc_type | TEXT | 文档类型，如 `SOP` / `RULE` / `TPL` |
| current_version | TEXT | 当前版本号，如 `V1.0` |
| folder_id | INTEGER | 所属文件夹 |
| tags | TEXT | 标签，第一版可用 JSON 字符串 |
| toc_enabled | INTEGER | 是否打开标题目录 |
| width_mode | TEXT | 页面宽度：`auto` / `standard` / `wide` / `full` / `custom` |
| custom_width | INTEGER | 自定义页面宽度，单位 px |
| small_font_enabled | INTEGER | 是否启用小字体 |
| title_numbering_enabled | INTEGER | 是否启用标题自动编号 |
| created_by | INTEGER | 创建人 |
| updated_by | INTEGER | 最后编辑人 |
| is_deleted | INTEGER | 是否删除 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### 11.2 document_blocks

块表。MVP 可以先把块完整存入 `documents.content` 的 JSON；如果需要更稳定的块级锚点、块链接和局部保存，建议独立建表。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | TEXT | 块 ID，建议前端生成稳定 UUID |
| document_id | INTEGER | 文档 ID |
| parent_block_id | TEXT | 父块 ID，用于嵌套块 |
| type | TEXT | 块类型，如 `paragraph` / `heading_1` / `todo` / `quote` / `code` |
| content | TEXT | 块内容 JSON |
| attrs | TEXT | 块属性 JSON，如颜色、折叠状态、标题级别 |
| sort_order | INTEGER | 排序 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

块颜色、高亮、折叠状态、块格式转换后的类型都需要持久化，刷新页面后保持一致。

### 11.3 document_folders

文档文件夹或分类表。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | INTEGER | 主键 |
| name | TEXT | 文件夹名称 |
| parent_id | INTEGER | 父级文件夹，MVP 可先不使用 |
| domain | TEXT | 所属归属域 |
| project_group_id | INTEGER | 所属项目组 |
| department_key | TEXT | 所属部门编码 |
| default_doc_type | TEXT | 该文件夹下新建文档时默认文档类型 |
| sort_order | INTEGER | 排序 |
| created_by | INTEGER | 创建人 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### 11.4 document_sequence_state

文档编号全局序号状态表，用于保证全局累计号自动生成且不重复。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | INTEGER | 主键 |
| scope_key | TEXT | 序号范围，第一版固定为 `global` |
| next_seq | INTEGER | 下一个全局累计序号 |
| updated_at | DATETIME | 更新时间 |

唯一约束建议：

```text
UNIQUE(scope_key)
```

### 11.5 document_change_logs

文档改动历史表。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | INTEGER | 主键 |
| document_id | INTEGER | 文档 ID |
| version | TEXT | 版本号 |
| changed_at | DATETIME | 更新时间 |
| changed_by | INTEGER | 更新人 |
| summary | TEXT | 改动摘要 |
| detail | TEXT | 详细改动内容，支持纯文本或富文本 JSON |
| detail_text | TEXT | 详细改动纯文本，用于搜索和摘要展示 |
| impact_scope | TEXT | 影响范围 |
| remark | TEXT | 备注 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

建议索引：

```text
INDEX(document_id, changed_at)
```

前端展示规则：

- 收起态优先展示 `summary`。
- 展开态展示 `detail`，没有 `detail` 时展示 `summary`。
- `version` 为空时前端可展示 `未标版本`，但建议新增记录时默认带出当前文档版本。

### 11.6 document_shares

文档共享对象表。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | INTEGER | 主键 |
| document_id | INTEGER | 文档 ID |
| target_type | TEXT | `project_group` / `department` / `team` / `user` |
| target_id | INTEGER | 项目组 ID、小组 ID、用户 ID |
| target_key | TEXT | 部门编码或部门名称 |
| created_by | INTEGER | 设置共享的人 |
| created_at | DATETIME | 创建时间 |

说明：

- 项目组、小组、个人使用 `target_id`。
- 部门使用 `target_key`。
- 用户加入项目组或小组后，应动态获得对应文档可见权；离开后自动失去可见权。

### 11.7 document_favorites

用户收藏表。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| user_id | INTEGER | 用户 ID |
| document_id | INTEGER | 文档 ID |
| created_at | DATETIME | 收藏时间 |

### 11.8 document_attachments

文档附件表。也可以复用现有附件能力，但需要能关联到文档。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | INTEGER | 主键 |
| document_id | INTEGER | 文档 ID |
| filename | TEXT | 原始文件名 |
| filepath | TEXT | 存储路径 |
| mimetype | TEXT | 文件类型 |
| size | INTEGER | 文件大小 |
| created_by | INTEGER | 上传人 |
| created_at | DATETIME | 上传时间 |

## 12. API 建议

### 12.1 文档

- `GET /api/documents`：获取当前用户可见文档列表
- `GET /api/documents/:id`：获取文档详情
- `POST /api/documents`：新建文档，自动生成文档编号
- `PUT /api/documents/:id`：更新文档标题、正文、标签、文件夹、归属域、项目组、部门、文档类型
- `PUT /api/documents/:id/page-options`：更新目录开关、页面宽度、小字体、标题自动编号等页面选项
- `DELETE /api/documents/:id`：删除文档
- `POST /api/documents/:id/renumber`：刷新编号中的业务字段，仅管理员或高管可用，需要填写原因；不改变全局累计序号

### 12.2 目录

- `GET /api/document-folders`：获取当前用户可见目录树
- `POST /api/document-folders`：新建目录
- `PUT /api/document-folders/:id`：更新目录
- `DELETE /api/document-folders/:id`：删除目录
- `POST /api/document-folders/apply-template`：为项目组或归属域初始化部门 SOP 目录模板

### 12.3 块编辑

- `PUT /api/documents/:id/content`：保存完整块内容
- `PATCH /api/documents/:id/blocks/:blockId`：更新单个块类型、内容、颜色、折叠状态等，MVP 可选

### 12.4 共享

- `GET /api/documents/:id/shares`：获取文档共享对象
- `PUT /api/documents/:id/shares`：全量保存共享对象
- `GET /api/documents/:id/access-summary`：获取当前可访问人数和权限摘要

### 12.5 改动历史

- `GET /api/documents/:id/change-logs`：获取文档改动历史
- `POST /api/documents/:id/change-logs`：新增改动历史
- `PUT /api/document-change-logs/:logId`：编辑改动历史
- `DELETE /api/document-change-logs/:logId`：删除改动历史
- `POST /api/document-edit-records/:recordId/restore`：恢复到某条页面编辑记录对应的版本快照

### 12.6 收藏

- `POST /api/documents/:id/favorite`：收藏
- `DELETE /api/documents/:id/favorite`：取消收藏

### 12.7 附件

- `POST /api/documents/:id/attachments`：上传附件
- `GET /api/documents/:id/attachments`：获取附件列表
- `GET /api/document-attachments/:id/download`：下载附件
- `DELETE /api/document-attachments/:id`：删除附件

## 13. 权限过滤逻辑

获取文档列表和文档详情时，后端必须统一判断可见性。

当前用户可见条件：

- 用户是 admin
- 用户是高管角色
- 用户是文档创建人
- 文档共享给该用户
- 文档共享给用户所属部门
- 文档共享给用户所属小组
- 文档共享给用户所属项目组

列表、详情、搜索、附件下载都必须复用同一套可见性判断。

## 14. 验收标准

第一版验收标准：

- 可以从 `资产管理 > 文档中心` 进入模块。
- 可以新建一篇文档。
- 从目录中新建文档时，可以自动带出归属域、项目组、部门和文档类型。
- 新建文档时可以自动生成唯一文档编号。
- 每个部门目录下默认有 `01_SOP流程规范`，并支持 SOP 总库聚合查看。
- 文档标题下方或文档信息面板可以展示编号、版本、创建人、最后编辑人。
- 可以编辑标题和块式富文本正文。
- 可以插入标题、列表、待办、引用、代码块、表格、图片和附件。
- 可以选中块，并对单个或多个块设置不同背景高亮颜色。
- 可以将某个块转换为文本、标题、列表、待办、折叠列表、引用、代码片段等格式。
- 可以打开或关闭标题目录，目录项点击后能跳转到对应标题。
- 可以设置页面宽度，支持自适应、标准、宽屏等模式。
- 自动保存状态清晰，刷新页面后内容不丢失。
- 可以同时打开多个文档，并在顶部 Tab 区自由切换。
- 点击已打开文档时不会重复打开 Tab，而是切换到已有 Tab。
- 每个文档 Tab 带关闭按钮，关闭前会自动保存对应文档。
- Tab 自动保存失败时不会关闭对应文档，并能提示用户重试或手动保存。
- 可以删除文档，删除前有确认。
- 可以按标题、正文、标签搜索文档。
- 可以收藏和取消收藏文档。
- 可以维护改动历史，包含版本号、更新人、更新时间、改动摘要、影响范围和备注。
- 文档编辑页右上角可以通过历史/时钟类图标打开页面编辑记录抽屉。
- 页面编辑记录抽屉可以通过 `版本记录` 和 `页面编辑记录` 两个 Tab 切换查看。
- `版本记录` Tab 展示人工维护的版本记录，`页面编辑记录` Tab 展示系统自动保存的页面编辑记录。
- 页面编辑记录抽屉可以按时间倒序展示记录，并支持单条记录展开和收起。
- 展开后的页面编辑记录可以看到更新人、更新时间、版本号、改动摘要和详细改动内容。
- 页面编辑记录右侧可以点击 `恢复到此版本` 图标，确认后恢复文档到对应快照。
- 有编辑权限的用户可以新增或编辑页面编辑记录，仅查看用户只能查看。
- 页面编辑记录抽屉支持接收页面变更通知开关的交互预留。
- 可以通过右上角共享入口设置共享项目组、共享部门、共享小组和共享个人。
- 右上角可以显示当前文档可访问人数。
- 被共享命中的用户可以看到文档。
- 未命中共享规则的用户看不到文档列表项、搜索结果和详情。
- 被共享用户第一版只能查看，不能编辑或删除。
- 管理员和高管角色可以查看全部文档。

## 15. 后续规划

二期可考虑：

- 评论和批注
- 自动版本快照和版本对比
- 文档关联产品资产、主体、策略、需求
- AI 总结、润色、生成大纲
- 可编辑协作者
- 访问申请
- 文档模板库
- 双向链接
- 文档导出 PDF / Word / Markdown
- 更接近 wolai 的块级编辑和拖拽排序
