# 出差协同模块 PRD

## 1. 背景

当前系统已有“出差申请”模块，核心偏出差审批、费用和报销管理。本次新增的“出差协同”模块定位为团队出差期间的行程与日程协同看板，用于让商务部门和高管快速查看某次出差每天、每个时段的安排、参与人员和地图地址。

模块归属：

- 一级菜单：团队管理
- 二级菜单：出差协同
- 建议路由：`/trip-collaboration`

## 2. 目标

- 支持创建一个出差行程，包含行程名称、开始日期、结束日期
- 支持按行程名称、开始时间、结束时间筛选行程列表，其中行程名称为模糊匹配
- 支持在某个行程下创建、编辑、删除日程
- 支持按行程日期范围生成逐日、分时段的日程展示视图
- 支持按人员姓名筛选，只展示该人员相关的行程和日程
- 支持地图地址跳转：PC 打开高德地图网页端，手机打开高德地图 App，失败时降级到网页端
- 按部门和高管角色控制可见权限

## 3. 范围

本次范围包括：

- 出差协同行程列表
- 新增/编辑行程
- 行程下的新建/编辑/删除日程
- 行程日程网格视图
- 人员筛选
- 高德地图跳转
- 商务部门和 CXO 默认可见权限

本次暂不包括：

- 出差审批流
- 费用报销
- 日程冲突检测
- 高德地图地址自动补全和经纬度反查
- 日程提醒、消息推送、日历同步
- 跨部门临时授权

## 4. 角色与权限

### 4.1 可见权限

- 商务部门：所有成员都能看到“出差协同”里的全部行程和日程信息
- CXO 高管：CEO、COO、CTO、CMO 默认可以看到全部行程和日程信息
- 管理员：建议默认可见，用于系统配置和问题排查
- 其他部门：默认看不到该模块菜单，接口层也不返回行程和日程数据

### 4.2 操作权限建议

- 商务部门成员：可以新建行程；可以编辑/删除自己创建的行程和日程
- 商务负责人、管理员、CXO：可以管理全部行程和日程
- 其他部门成员：无查看、创建、编辑、删除权限

说明：操作权限属于待确认项。如果希望商务部门所有人都能编辑全部日程，可在开发前调整。

## 5. 功能需求

### 5.1 出差协同入口

在“团队管理”下新增菜单项“出差协同”。

页面默认展示：

- 左侧行程列表
- 行程筛选区
- 当前选中行程的日程网格视图
- 新增行程入口

布局要求：

- 页面采用类似“文档中心”的左右分栏结构
- 左侧为行程列表面板，右侧为所选行程的日程视图
- 左侧行程列表支持收起和展开
- 左侧面板展开时展示筛选项、行程列表和新增入口
- 左侧面板收起时只保留一个箭头按钮，用于重新展开行程列表
- 手机端默认不启用窄栏收起，行程列表和日程视图上下展示

### 5.2 新增行程

新增行程入口位于页面顶部或行程列表顶部。

新增行程页面字段：

- 行程名称，必填，文本
- 开始日期，必填，日期
- 结束日期，必填，日期

校验规则：

- 行程名称不能为空
- 开始日期不能晚于结束日期
- 同名行程允许创建，但列表需可通过日期区分

保存后：

- 行程进入行程列表
- 默认选中新创建的行程
- 日程网格按照开始日期到结束日期逐天生成

### 5.3 行程列表

行程列表以“列表项”形式展示，不使用大卡片式看板。列表项字段：

- 行程名称
- 开始日期
- 结束日期
- 日程数量
- 相关人员
- 操作

操作：

- 新建日程
- 查看日程
- 编辑行程
- 删除行程

列表交互：

- 点击行程列表项，右侧切换为该行程的日程网格
- 当前选中行程需有高亮状态
- 行程列表左上角提供收起/展开箭头，交互参考文档中心左侧文档目录
- 收起后不展示筛选项和列表内容，仅保留展开箭头

筛选项：

- 行程名称：支持模糊筛选
- 开始时间：筛选开始日期大于等于指定日期的行程
- 结束时间：筛选结束日期小于等于指定日期的行程
- 人员姓名：筛选与该人员相关的行程

人员筛选口径：

- 行程相关人员 = 该行程下所有日程参与人员的并集
- 若行程尚无日程，则创建人可作为默认相关人员
- 选择某个人后，行程列表只展示包含该人员的行程；日程网格只展示该人员参与的日程

### 5.4 新建日程

入口：

- 行程列表行最右侧提供“新建日程”
- 日程网格每个日期/时段格子右上角提供 `+` 入口

日程新建页面字段：

- 日期，必填，只能选择当前行程开始日期到结束日期之间的日期
- 时段，必填，固定值：上午、中午、下午、晚上
- 时间，必填，支持输入具体时间，如 `10:00`、`10:15-13:15`
- 日程名称，必填
- 地图地址，非必填，文本
- 参与人员，必填，可按系统员工勾选，多选

校验规则：

- 日期必须在当前行程范围内
- 时段必须为固定枚举值
- 日程名称不能为空
- 至少选择一名参与人员

保存后：

- 日程出现在对应日期、对应时段的格子中
- 同一格子允许多个日程，按时间升序展示

### 5.5 日程展示视图

展示规则：

- 第一行横向展示日期列
- 日期列包含中文日期和英文周几，例如 `6月2日 Tue`
- 日期范围由行程开始日期和结束日期决定，逐天展示
- 左侧纵向展示时段：上午、中午、下午、晚上
- 每个日期/时段交叉格子展示该时段日程

日程卡片展示字段：

- 删除入口：`X`
- 时间
- 日程名称
- 参与人员
- 查看地图按钮
- 编辑按钮

空状态：

- 无日程的格子展示“暂无安排”
- 每个格子保留 `+` 新增入口

交互：

- 日程网格横向滚动时，左侧“上午/中午/下午/晚上”时段列固定可见
- 日程网格纵向滚动时，顶部日期表头固定可见
- 左上角日期与时段的交叉单元格同时固定，冻结区域不得被日程内容遮挡
- 点击 `+`：打开新建日程页面，并自动带入该格子的日期和时段
- 点击 `编辑`：打开编辑日程页面
- 点击 `X`：二次确认后删除日程
- 点击 `查看地图`：按设备类型跳转到高德地图

手机端适配：

- 小于 `md` 断点时不渲染桌面的多日期双向网格，默认进入“按日”模式，一次只展示一个日期。
- “按日”模式顶部展示前一天、后一天按钮和可横向惯性滚动的日期条；日期按钮显示日期、周几和
  当日日程数量，选中日期有明确高亮。
- 默认选中今天；今天不在当前行程日期范围内时选中第一天。切换行程后重新按该规则定位，人员筛选
  只更新日程内容，不主动跳走当前有效日期。
- 选中日期下按上午、中午、下午、晚上纵向展示四个时段。时段标题保留新增入口；空时段压缩为一行
  “暂无安排”，有内容时日程卡片使用手机可用宽度完整展示。
- 提供“全部”模式，按日期、时段纵向展示当前筛选下所有非空日程，不展示横向日期列；无日程时显示
  统一空状态。
- 日期条只承担原生横向滚动和日期选择，日程正文只承担页面原生纵向滚动，不使用 `preventDefault`
  或全页指针捕获模拟拖拽；从时段标题、空白区域和卡片之间起滑都必须顺畅滚动。
- 手机端日程卡片继续支持新增、编辑、删除、查看地图和参与人员展示；桌面端冻结网格交互保持不变。

### 5.6 高德地图跳转

跳转规则：

- PC 端：打开高德地图网页端搜索页，使用地图地址作为 query
- 手机端：优先尝试高德地图 App URI Scheme
- 手机端未安装高德地图或打开失败：降级到高德地图网页端

建议封装统一方法：

- 输入：地图地址、日程名称
- 输出：PC URL、移动端 App URL、fallback URL

PC 示例：

```text
https://ditu.amap.com/search?query={encodeURIComponent(地图地址)}&src=team-board&callnative=0&innersrc=uriapi
```

### 5.7 人员筛选

页面提供全局人员筛选：

- 默认：全部人员
- 选择人员后：只展示该人员相关的行程和日程

筛选规则：

- 对行程列表：展示该人员参与过至少一个日程的行程
- 对日程网格：只展示该人员参与的日程
- 不突破权限范围，用户只能在自己可见的数据内筛选

## 6. 数据模型建议

### 6.1 行程表

建议表名：`trip_collaboration_trips`

字段：

- `id`
- `name`
- `start_date`
- `end_date`
- `created_by`
- `created_at`
- `updated_at`
- `deleted_at`

### 6.2 日程表

建议表名：`trip_collaboration_schedules`

字段：

- `id`
- `trip_id`
- `schedule_date`
- `period`
- `time_text`
- `name`
- `map_address`
- `map_lng`
- `map_lat`
- `created_by`
- `created_at`
- `updated_at`
- `deleted_at`

### 6.3 日程参与人员表

建议表名：`trip_collaboration_schedule_participants`

字段：

- `id`
- `schedule_id`
- `user_id`
- `created_at`

## 7. 接口建议

- `GET /api/trip-collaboration/trips`
- `POST /api/trip-collaboration/trips`
- `PUT /api/trip-collaboration/trips/:id`
- `DELETE /api/trip-collaboration/trips/:id`
- `GET /api/trip-collaboration/trips/:id/schedules`
- `POST /api/trip-collaboration/trips/:id/schedules`
- `PUT /api/trip-collaboration/schedules/:id`
- `DELETE /api/trip-collaboration/schedules/:id`

行程列表筛选参数：

- `name`
- `start_date_from`
- `end_date_to`
- `person_id`

## 8. 页面原型说明

已提供 HTML 原型文件：

- `出差协同原型.html`

原型包含：

- 团队管理下的“出差协同”菜单位置
- 左侧可收起/展开的行程列表与筛选
- 新增行程页面
- 行程行右侧“新建日程”
- 日程网格视图
- 网格内 `+` 新建、`X` 删除、编辑、查看地图
- 人员筛选
- 权限角色切换示意

## 9. 验收标准

- 商务部门成员进入“团队管理 > 出差协同”后，可以看到全部行程和日程
- CXO 角色进入后，可以看到全部行程和日程
- 其他部门成员无法看到菜单，直接访问接口也不返回数据
- 新增行程时，仅需填写行程名称、开始日期、结束日期即可保存
- 页面左侧为类似文档中心的行程列表面板，并支持通过箭头收起和展开
- 行程列表支持按开始时间、结束时间、行程名称模糊筛选
- 行程列表支持按人员筛选，只展示相关行程
- 某个行程行最右侧有“新建日程”入口
- 新建日程支持日期、时段、时间、日程名称、地图地址、参与人员
- 参与人员来自系统员工，并支持勾选多选
- 日程视图按行程日期范围逐天展示，表头包含日期和英文周几
- 日程网格支持冻结顶部日期行和左侧时段列，双向滚动时日期、时段持续可见
- 手机端默认显示单日纵向议程，可通过日期条或前后按钮切换日期，并可切换“全部”纵向总览；页面
  不出现多日期大网格，也不需要持续右滑才能浏览日程
- 手机端从上午、下午等时段标题区域起滑时可正常纵向滚动，日期条横向滑动与正文纵向滚动互不抢占
- 每个格子支持添加日程
- 日程卡片支持删除、编辑、查看地图
- 人员筛选后，日程视图只展示该人员参与的日程
- PC 点击查看地图打开高德地图网页端
- 手机点击查看地图优先打开高德地图 App，失败后打开网页端

## 10. 待确认问题

- 商务部门成员是否可以编辑/删除其他商务成员创建的日程
- 行程是否需要负责人字段，还是只记录创建人即可
- 行程人员筛选是否仅按日程参与人员计算，还是需要在行程层面也维护参与人员
- 日程时间是否需要结构化开始/结束时间，还是 MVP 先用文本输入
- 地图地址是否需要接入高德地点选择器和经纬度存储
- 是否需要把“出差协同”和现有“出差申请”做数据关联
