interface:
  display_name: "TAPD Relation Doc Import"
  short_description: "导入 TAPD 需求并保留截图格式"
  default_prompt: "Use $tapd-relation-doc-import to import a TAPD requirement page into a Relation document while preserving rich text and screenshots."
policy:
  allow_implicit_invocation: true
